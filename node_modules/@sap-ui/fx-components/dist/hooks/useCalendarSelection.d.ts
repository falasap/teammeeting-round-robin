import type { CalendarSelectionMode, CalendarLegendItemType, DisabledDateRange, SpecialCalendarDate } from "../types/calendar";
/**
 * Hook for managing calendar date selection
 */
export declare function useCalendarSelection(mode: CalendarSelectionMode, initialDates?: string[], rangeStart?: string, rangeEnd?: string, minDate?: string, maxDate?: string, disabledDates?: DisabledDateRange[]): {
    selectedDates: Date[];
    rangeStartDate: Date | null;
    rangeEndDate: Date | null;
    hoveredDate: Date | null;
    setHoveredDate: import("react").Dispatch<import("react").SetStateAction<Date | null>>;
    isRangeSelectionInProgress: boolean;
    isDateDisabled: (date: Date) => boolean;
    isDateSelected: (date: Date) => boolean;
    isRangeStart: (date: Date) => boolean;
    isRangeEnd: (date: Date) => boolean;
    isDateBetweenRange: (date: Date) => boolean;
    selectDate: (date: Date) => {
        selectedValues: string[];
        selectedTimestamps: number[];
    } | null;
    getSelectedValuesAsISO: () => string[];
    getSelectedTimestamps: () => number[];
};
/**
 * Hook for managing special dates lookup
 */
export declare function useSpecialDates(specialDates?: SpecialCalendarDate[]): {
    getSpecialDate: (date: Date) => {
        type: CalendarLegendItemType;
        tooltip?: string;
    } | null;
};
//# sourceMappingURL=useCalendarSelection.d.ts.map