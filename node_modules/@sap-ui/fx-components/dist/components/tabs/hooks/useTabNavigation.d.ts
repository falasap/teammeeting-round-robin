import { UseTabNavigationOptions } from "../../../types/tabs";
/**
 * Hook for managing tab keyboard navigation
 *
 * Implements ARIA Tabs pattern:
 * - Left/Right: Move focus between tabs
 * - Home: Move focus to first tab
 * - End: Move focus to last tab
 * - Enter/Space: Select focused tab
 * - Tab: Move focus out of tab list
 */
export declare function useTabNavigation(options: UseTabNavigationOptions): {
    handleKeyDown: (e: React.KeyboardEvent) => void;
    findNextEnabledTab: (currentIndex: number, direction: 1 | -1) => number;
    findFirstEnabledTab: () => number;
    findLastEnabledTab: () => number;
};
//# sourceMappingURL=useTabNavigation.d.ts.map