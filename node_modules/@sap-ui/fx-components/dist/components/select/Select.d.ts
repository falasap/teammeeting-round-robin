import { SelectProps } from "../../types/select";
/**
 * Select component
 *
 * A native React implementation of the UI5 Select component.
 * Similar to a native <select> but with custom styling and features:
 * - No text input (display only)
 * - Click to open dropdown
 * - Keyboard navigation
 * - Type-ahead search
 * - Value states
 * - Full accessibility
 */
export declare function Select({ value: controlledValue, defaultValue, size, disabled, readonly, required, name, valueState, valueStateMessage, icon, tooltip, textSeparator, accessibleName, accessibleNameRef, accessibleDescription, accessibleDescriptionRef, open: controlledOpen, defaultOpen, onChange, onLiveChange, onOpen, onClose, children, className, label, accessibilityAttributes, ref, }: SelectProps): import("react/jsx-runtime").JSX.Element;
//# sourceMappingURL=Select.d.ts.map