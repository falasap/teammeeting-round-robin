import { FxPromptInputProps } from "../../types/fx";
/**
 * FxPromptInput - AI prompt input with typeahead and actions
 *
 * Matches the fx-components FxPromptInput web component API and design.
 * Supports oneline mode (single-line with ghost text) and multiline mode (textarea with toolbar).
 *
 * @example
 * ```tsx
 * <FxPromptInput
 *   placeholder="Ask me anything..."
 *   onSubmit={({ value }) => sendMessage(value)}
 *   onTypeaheadRequest={async ({ value, requestId }) => fetchSuggestions(value)}
 * />
 * ```
 */
export declare function FxPromptInput({ value: controlledValue, placeholder, disabled, statusMessage, disclaimerMessage, showDictate, showVoice, mode: explicitMode, typeaheadEnabled, maxSuggestions, debounceMs, actions, contextItems, toolbarButtons, onSubmit, onLiveChange, onPlusPress, onDictatePress, onVoicePress, onTypeaheadRequest, onActionSelect: _onActionSelect, onContextItemRemove, className, style, ref, }: FxPromptInputProps): import("react/jsx-runtime").JSX.Element;
//# sourceMappingURL=FxPromptInput.d.ts.map