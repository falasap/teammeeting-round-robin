'use strict';

var React45 = require('react');
var tailwindMerge = require('tailwind-merge');
var jsxRuntime = require('react/jsx-runtime');
var reactI18next = require('react-i18next');
var lucideReact = require('lucide-react');
var reactDom = require('react-dom');

function _interopNamespace(e) {
  if (e && e.__esModule) return e;
  var n = Object.create(null);
  if (e) {
    Object.keys(e).forEach(function (k) {
      if (k !== 'default') {
        var d = Object.getOwnPropertyDescriptor(e, k);
        Object.defineProperty(n, k, d.get ? d : {
          enumerable: true,
          get: function () { return e[k]; }
        });
      }
    });
  }
  n.default = e;
  return Object.freeze(n);
}

var React45__namespace = /*#__PURE__*/_interopNamespace(React45);

var __defProp = Object.defineProperty;
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __publicField = (obj, key, value) => __defNormalProp(obj, typeof key !== "symbol" ? key + "" : key, value);
function toVal(mix) {
  let str = "";
  if (typeof mix === "string" || typeof mix === "number") {
    str += mix;
  } else if (typeof mix === "object") {
    if (Array.isArray(mix)) {
      for (let i = 0; i < mix.length; i++) {
        if (mix[i]) {
          const y = toVal(mix[i]);
          if (y) {
            if (str) str += " ";
            str += y;
          }
        }
      }
    } else {
      for (const k in mix) {
        if (mix[k]) {
          if (str) str += " ";
          str += k;
        }
      }
    }
  }
  return str;
}
function clsx(...inputs) {
  let str = "";
  for (let i = 0; i < inputs.length; i++) {
    if (inputs[i]) {
      const y = toVal(inputs[i]);
      if (y) {
        if (str) str += " ";
        str += y;
      }
    }
  }
  return str;
}
var falsyToString = (value) => typeof value === "boolean" ? `${value}` : value === 0 ? "0" : value;
function cva(base, config) {
  return (props) => {
    if (config?.variants == null) {
      return clsx(base, props?.class, props?.className);
    }
    const { variants, defaultVariants } = config;
    const variantClassNames = Object.keys(variants).map((variant) => {
      const variantProp = props?.[variant];
      const defaultVariantProp = defaultVariants?.[variant];
      if (variantProp === null) return null;
      const variantKey = falsyToString(variantProp) || falsyToString(defaultVariantProp);
      return variants[variant]?.[variantKey];
    });
    const propsWithoutUndefined = props ? Object.entries(props).reduce((acc, [key, value]) => {
      if (value !== void 0) acc[key] = value;
      return acc;
    }, {}) : {};
    const compoundVariantClassNames = config.compoundVariants?.reduce(
      (acc, { class: cvClass, className: cvClassName, ...compoundVariantOptions }) => {
        return Object.entries(compoundVariantOptions).every(([key, value]) => {
          const merged = { ...defaultVariants, ...propsWithoutUndefined };
          return Array.isArray(value) ? value.includes(merged[key]) : merged[key] === value;
        }) ? [...acc, cvClass, cvClassName] : acc;
      },
      []
    );
    return clsx(base, variantClassNames, compoundVariantClassNames, props?.class, props?.className);
  };
}
function cn(...inputs) {
  return tailwindMerge.twMerge(clsx(inputs));
}
var iconVariants = cva(
  [
    "inline-block flex-shrink-0",
    "transition-colors",
    "fill-current"
    // Use fill-current to inherit parent's text color
  ],
  {
    variants: {
      design: {
        ["Default" /* Default */]: "",
        // No color set - inherits from parent
        ["Contrast" /* Contrast */]: "text-foreground-contrast",
        ["Critical" /* Critical */]: "text-sapphire-warning",
        ["Information" /* Information */]: "text-sapphire-info",
        ["Positive" /* Positive */]: "text-sapphire-positive",
        ["Negative" /* Negative */]: "text-sapphire-negative",
        ["Neutral" /* Neutral */]: "text-sapphire-text-tertiary",
        ["NonInteractive" /* NonInteractive */]: "text-sapphire-text-tertiary opacity-60"
      },
      mode: {
        ["Decorative" /* Decorative */]: "",
        ["Image" /* Image */]: "",
        ["Interactive" /* Interactive */]: "cursor-pointer hover:opacity-80 focus:outline-none focus:ring-2 focus:ring-ring focus:ring-offset-2"
      }
    },
    defaultVariants: {
      design: "Default" /* Default */,
      mode: "Decorative" /* Decorative */
    }
  }
);
function Icon({
  pathData: pathData31,
  design = "Default" /* Default */,
  mode = "Decorative" /* Decorative */,
  accessibleName,
  showTooltip = false,
  onClick,
  className,
  style,
  id,
  "data-testid": dataTestId,
  ref
}) {
  const iconRef = React45.useRef(null);
  React45.useImperativeHandle(
    ref,
    () => ({
      focus() {
        iconRef.current?.focus();
      },
      blur() {
        iconRef.current?.blur();
      },
      isFocused() {
        return document.activeElement === iconRef.current;
      },
      get nativeElement() {
        return iconRef.current;
      }
    }),
    []
  );
  const handleClick2 = React45.useCallback(
    (e) => {
      if (mode !== "Interactive" /* Interactive */) return;
      onClick?.({
        originalEvent: e,
        isKeyboard: false
      });
    },
    [mode, onClick]
  );
  const handleKeyDown = React45.useCallback(
    (e) => {
      if (mode !== "Interactive" /* Interactive */) return;
      if (e.key === "Enter" || e.key === " ") {
        e.preventDefault();
        onClick?.({
          originalEvent: e,
          isKeyboard: true
        });
      }
    },
    [mode, onClick]
  );
  if (!pathData31 || pathData31.length === 0) {
    return null;
  }
  const isInteractive = mode === "Interactive" /* Interactive */;
  const isImage = mode === "Image" /* Image */;
  const role = isInteractive ? "button" : isImage ? "img" : "presentation";
  const ariaLabel = accessibleName || void 0;
  const ariaHidden = !accessibleName && !isInteractive ? true : void 0;
  const tabIndex = isInteractive ? 0 : void 0;
  return /* @__PURE__ */ jsxRuntime.jsxs(
    "svg",
    {
      ref: iconRef,
      id,
      role,
      "aria-label": ariaLabel,
      "aria-hidden": ariaHidden,
      tabIndex,
      viewBox: "0 0 512 512",
      xmlns: "http://www.w3.org/2000/svg",
      focusable: isInteractive ? "true" : "false",
      preserveAspectRatio: "xMidYMid meet",
      onClick: handleClick2,
      onKeyDown: handleKeyDown,
      className: cn(
        iconVariants({
          design,
          mode
        }),
        // Default size (can be overridden by className)
        "h-4 w-4",
        className
      ),
      style,
      "data-testid": dataTestId,
      children: [
        showTooltip && accessibleName && /* @__PURE__ */ jsxRuntime.jsx("title", { children: accessibleName }),
        /* @__PURE__ */ jsxRuntime.jsx("g", { role: "presentation", children: pathData31.map((path, index) => /* @__PURE__ */ jsxRuntime.jsx("path", { d: path }, index)) })
      ]
    }
  );
}
var pathData = [
  "M292 256l117 116q7 7 7 18 0 12-7.5 19t-18.5 7q-10 0-18-8L256 292 140 408q-8 8-18 8-11 0-18.5-7.5T96 390q0-10 8-18l116-116-116-116q-8-8-8-18 0-11 7.5-18.5T122 96t18 7l116 117 116-117q7-7 18-7t18.5 7.5T416 122t-7 18z"
];
var DeclineIcon = Object.assign(
  React45__namespace.default.forwardRef(
    (props, ref) => /* @__PURE__ */ jsxRuntime.jsx(Icon, { ref, pathData, ...props })
  ),
  { displayName: "DeclineIcon", iconName: "decline" }
);
var createIcon = (pathData31, fillRule) => {
  const Icon2 = ({ size = 20, className, ...props }) => /* @__PURE__ */ jsxRuntime.jsx(
    "svg",
    {
      viewBox: "0 0 20 20",
      width: size,
      height: size,
      fill: "currentColor",
      className,
      ...props,
      children: /* @__PURE__ */ jsxRuntime.jsx("path", { d: pathData31, fillRule })
    }
  );
  return Icon2;
};
var createSapIcon = (pathData31, fillRule) => {
  const Icon2 = ({ size = 20, className, ...props }) => /* @__PURE__ */ jsxRuntime.jsx(
    "svg",
    {
      viewBox: "0 0 512 512",
      width: size,
      height: size,
      fill: "currentColor",
      className,
      ...props,
      children: /* @__PURE__ */ jsxRuntime.jsx("path", { d: pathData31, fillRule })
    }
  );
  return Icon2;
};
var ConversationsIcon = createIcon(
  "M0 3.6119C7.53742e-05 1.61714 1.59895 7.6231e-05 3.57129 0H10C10.5523 0 11 0.452806 11 1.01137C11 1.56994 10.5523 2.02274 10 2.02274H3.57129C2.70352 2.02282 2.00008 2.73427 2 3.6119V16.7745L5.43555 13.2999L5.50879 13.2328C5.68672 13.0852 5.91045 13.0037 6.14258 13.0036H16.4287C17.2964 13.0036 17.9998 12.292 18 11.4145V10.0594C18.0003 9.50105 18.4479 9.04802 19 9.04802C19.5521 9.04802 19.9997 9.50105 20 10.0594V11.4145C19.9998 13.4091 18.401 15.0263 16.4287 15.0264H6.55664L1.70703 19.9311C1.42103 20.2204 0.990862 20.3069 0.617188 20.1504C0.243586 19.9938 0 19.6251 0 19.2161V3.6119Z M19.043 4.16217C19.9834 4.45547 19.9834 5.80197 19.043 6.0953L17.2435 6.6568C16.9275 6.75535 16.6802 7.00564 16.5826 7.32516L16.0268 9.14585C15.7367 10.0969 14.4054 10.097 14.1154 9.14585L13.5602 7.32586C13.4628 7.00643 13.2158 6.75551 12.9 6.6568L11.0991 6.0953L10.9334 6.02825C10.162 5.63756 10.2175 4.43713 11.0991 4.16217L12.9 3.60066C13.1764 3.51428 13.3998 3.31157 13.5167 3.04824L13.5609 2.93231L14.1154 1.11162C14.4054 0.160524 15.7368 0.160522 16.0268 1.11162L16.5826 2.93231C16.668 3.21197 16.8677 3.43844 17.1282 3.55667L17.2435 3.60066L19.043 4.16217ZM16.8009 5.04982C16.0109 4.80336 15.3928 4.17823 15.1491 3.37928L15.0711 3.12297L14.993 3.37928C14.7494 4.17826 14.1313 4.8034 13.3413 5.04982L13.0885 5.12804L13.3413 5.20765L13.4877 5.25793C14.1593 5.51431 14.6898 6.05088 14.9433 6.73013L14.993 6.87819L15.0711 7.1331L15.1491 6.87819C15.3927 6.07923 16.0109 5.45411 16.8009 5.20765L17.0536 5.12804L16.8009 5.04982Z M5 11.1251C4.44772 11.1251 4 10.6723 4 10.1137C4 9.55515 4.44772 9.10234 5 9.10234L10 9.10234C10.5523 9.10234 11 9.55515 11 10.1137C11 10.6723 10.5523 11.1251 10 11.1251L5 11.1251Z M5 7.0796C4.44772 7.0796 4 6.62679 4 6.06823C4 5.50966 4.44772 5.05686 5 5.05686L8 5.05686C8.55229 5.05686 9 5.50966 9 6.06823C9 6.62679 8.55229 7.0796 8 7.0796L5 7.0796Z"
);
var DiscoverIcon = createIcon(
  "M0 10.0002C1.7068e-05 5.13124 3.36325 1.05876 7.86743 0.0259049C8.41208 -0.0989723 8.95365 0.24708 9.07712 0.797925C9.20042 1.34867 8.85933 1.89651 8.31477 2.02137C4.71172 2.8476 2.02242 6.10737 2.0224 10.0002C2.0224 13.8935 4.71212 17.1543 8.31576 17.9801C8.86042 18.1049 9.2025 18.6527 9.0791 19.2036C8.95559 19.7541 8.41386 20.0991 7.86941 19.9746C3.36445 18.9423 0 14.8698 0 10.0002ZM18.0009 11.8209C18.1247 11.2704 18.6662 10.9251 19.2106 11.0499C19.7552 11.175 20.0967 11.7236 19.973 12.2744C19.1141 16.098 16.1402 19.1055 12.3593 19.9736C11.8147 20.0986 11.2723 19.7534 11.1487 19.2026C11.0251 18.6519 11.3666 18.1043 11.911 17.9791C14.9329 17.2854 17.3145 14.877 18.0009 11.8209ZM11.1447 0.797425C11.2681 0.246651 11.8098 -0.0991971 12.3544 0.0254056C16.1399 0.892653 19.1179 3.90483 19.975 7.73362C20.0981 8.28443 19.7562 8.83237 19.2116 8.95706C18.667 9.08166 18.1252 8.73587 18.0019 8.18504C17.317 5.12515 14.9334 2.71406 11.908 2.02087C11.3635 1.89611 11.0215 1.3482 11.1447 0.797425ZM10.9143 14.909L11.771 12.1C11.8325 11.8982 11.9887 11.7402 12.1883 11.6779L15.3775 10.6831C15.9714 10.4979 15.9713 9.64778 15.3775 9.46257L12.1883 8.46789C12.1639 8.46027 12.1401 8.45121 12.117 8.44081C11.9762 8.37676 11.8616 8.26184 11.7984 8.11935C11.7878 8.09559 11.7787 8.07105 11.771 8.04582L10.7874 4.82033C10.6043 4.21975 9.76373 4.21975 9.58059 4.82032L8.59702 8.04583C8.5579 8.17415 8.48049 8.28476 8.37801 8.3643C8.36337 8.37566 8.34822 8.38639 8.3326 8.39644C8.31782 8.40595 8.30262 8.41486 8.28703 8.42314C8.253 8.4412 8.21711 8.45623 8.17969 8.46791L4.99034 9.46262C4.68288 9.55861 4.53458 9.83299 4.54554 10.1009C4.55459 10.3191 4.66924 10.5329 4.88949 10.6428L4.99033 10.6832L8.17967 11.6779C8.37924 11.7402 8.53546 11.8982 8.597 12.1L9.45351 14.909L9.5805 15.3256L9.6205 15.4276C9.73742 15.6674 9.97346 15.7834 10.2057 15.7757C10.4542 15.7673 10.6983 15.6172 10.7873 15.3256L10.9143 14.909Z"
);
var JobsIcon = createIcon(
  "M6.77487 6.94464C6.28776 6.2744 6 5.44677 6 4.55116C6 2.3169 7.79086 0.505676 10 0.505676C12.2091 0.505676 14 2.3169 14 4.55116C14 5.54361 13.6466 6.4526 13.0602 7.15644L15.5926 11.6515C15.7265 11.6378 15.8625 11.6308 16 11.6308C18.2091 11.6308 20 13.442 20 15.6762C20 17.9105 18.2091 19.7217 16 19.7217C14.1362 19.7217 12.5701 18.4325 12.126 16.6876H7.87398C7.42994 18.4325 5.86384 19.7217 4 19.7217C1.79086 19.7217 0 17.9105 0 15.6762C0 13.4626 1.75795 11.6642 3.93893 11.6312L6.77487 6.94464ZM5.96837 12.1537L8.34069 8.23323C8.84616 8.46661 9.40806 8.59665 10 8.59665C10.5007 8.59665 10.9798 8.50362 11.4216 8.33372L13.6951 12.3695C12.9318 12.9147 12.3656 13.7235 12.126 14.6649L7.87398 14.6649C7.60042 13.5899 6.90096 12.6878 5.96837 12.1537ZM10 6.5739C8.89543 6.5739 8 5.66829 8 4.55116C8 3.43403 8.89543 2.52842 10 2.52842C11.1046 2.52842 12 3.43403 12 4.55116C12 5.66829 11.1046 6.5739 10 6.5739ZM4 17.699C2.89543 17.699 2 16.7934 2 15.6762C2 14.5591 2.89543 13.6535 4 13.6535C5.10457 13.6535 6 14.5591 6 15.6762C6 16.7934 5.10457 17.699 4 17.699ZM14 15.6762C14 14.5591 14.8954 13.6535 16 13.6535C17.1046 13.6535 18 14.5591 18 15.6762C18 16.7934 17.1046 17.699 16 17.699C14.8954 17.699 14 16.7934 14 15.6762Z",
  "evenodd"
);
var SpacesIcon = createIcon(
  "M12.5722 1.39355C10.989 0.432945 9.01096 0.432946 7.42771 1.39355L2.34372 4.47902C0.40189 5.6575 0.401894 8.50357 2.34372 9.68205L3.0551 10.1138L2.34372 10.5453C0.401701 11.7237 0.401704 14.5708 2.34372 15.7493L7.42771 18.8338C9.01107 19.7945 10.9889 19.7945 12.5722 18.8338L17.6562 15.7493C19.5982 14.5708 19.5982 11.7237 17.6562 10.5453L16.9447 10.1139L17.6562 9.68205C19.5981 8.50357 19.598 5.6575 17.6562 4.47902L12.5722 1.39355ZM3.37302 12.2796L4.9985 11.2932L7.42771 12.7675C9.01096 13.7281 10.989 13.7281 12.5722 12.7675L15.0012 11.2934L16.6269 12.2796C17.2743 12.6724 17.2743 13.6221 16.6269 14.0149L11.5439 17.0994C10.5939 17.6759 9.40607 17.6759 8.45603 17.0994L3.37302 14.0149C2.72568 13.6221 2.72568 12.6724 3.37302 12.2796ZM8.45603 3.12789C9.40607 2.55138 10.5939 2.55138 11.5439 3.12789L16.6269 6.21336C17.2743 6.60618 17.2743 7.55489 16.6269 7.94771L11.5439 11.0332C10.5939 11.6097 9.40607 11.6097 8.45603 11.0332L3.37302 7.94771C2.72568 7.55489 2.72568 6.60618 3.37302 6.21336L8.45603 3.12789Z"
);
var VSCodeIcon = createIcon(
  "M14.5 0.5L18.5 2.5C19.1 2.8 19.5 3.4 19.5 4.1V15.9C19.5 16.6 19.1 17.2 18.5 17.5L14.5 19.5C13.8 19.8 13 19.7 12.4 19.2L6.5 13.5L3.3 16C2.7 16.5 1.8 16.5 1.2 16L0.3 15.2C-0.1 14.7 -0.1 14 0.3 13.5L3 10.5L0.3 7.5C-0.1 7 -0.1 6.3 0.3 5.8L1.2 5C1.8 4.5 2.7 4.5 3.3 5L6.5 7.5L12.4 1.8C13 1.3 13.8 1.2 14.5 0.5ZM14.5 5.5V15.5L9.5 10.5L14.5 5.5Z",
  "evenodd"
);
var CodeIcon = ({ size = 20, className, ...props }) => /* @__PURE__ */ jsxRuntime.jsx(
  "svg",
  {
    viewBox: "0 0 20 20",
    width: size,
    height: size,
    fill: "currentColor",
    className,
    ...props,
    children: /* @__PURE__ */ jsxRuntime.jsx("path", { d: "M6.5 4.5L1 10l5.5 5.5 1.4-1.4L3.8 10l4.1-4.1L6.5 4.5zm7 0l-1.4 1.4L16.2 10l-4.1 4.1 1.4 1.4L19 10l-5.5-5.5z" })
  }
);
CodeIcon.displayName = "CodeIcon";
var MenuIcon = ({ size = 20, className, ...props }) => /* @__PURE__ */ jsxRuntime.jsx(
  "svg",
  {
    viewBox: "0 0 20 16",
    width: size,
    height: size,
    fill: "none",
    className,
    ...props,
    children: /* @__PURE__ */ jsxRuntime.jsx("path", { d: "M0.777441 0C0.558944 0 0.373476 0.111789 0.221037 0.335366C0.073679 0.566397 0 0.838417 0 1.15142C0 1.46443 0.073679 1.73273 0.221037 1.95631C0.373476 2.17988 0.558944 2.29167 0.777441 2.29167H19.215C19.4335 2.29167 19.6189 2.17988 19.7714 1.95631C19.9238 1.73273 20 1.46443 20 1.15142C20 0.830963 19.9213 0.558944 19.7637 0.335366C19.6113 0.111789 19.4284 0 19.215 0H0.777441ZM0.777441 6.57319C0.558944 6.57319 0.373476 6.68498 0.221037 6.90855C0.073679 7.13958 0 7.4116 0 7.72461C0 8.03762 0.073679 8.30591 0.221037 8.5295C0.373476 8.75307 0.558944 8.86486 0.777441 8.86486H19.215C19.4335 8.86486 19.6189 8.75307 19.7714 8.5295C19.9238 8.30591 20 8.03762 20 7.72461C20 7.40414 19.9213 7.13213 19.7637 6.90855C19.6113 6.68498 19.4284 6.57319 19.215 6.57319H0.777441ZM0.777441 13.1464C0.558944 13.1464 0.373476 13.2582 0.221037 13.4817C0.073679 13.7128 0 13.981 0 14.2866C0 14.6071 0.073679 14.879 0.221037 15.1027C0.373476 15.3263 0.558944 15.4381 0.777441 15.4381H14.6239C14.8423 15.4381 15.0278 15.3263 15.1802 15.1027C15.3327 14.879 15.4089 14.6071 15.4089 14.2866C15.4089 13.9736 15.3302 13.7054 15.1726 13.4817C15.0202 13.2582 14.8373 13.1464 14.6239 13.1464H0.777441Z", fill: "currentColor" })
  }
);
MenuIcon.displayName = "MenuIcon";
var WaveformIcon = createIcon(
  "M3 8 A 1 1 0 0 1 4 9 V 11 A 1 1 0 0 1 3 12 A 1 1 0 0 1 2 11 V 9 A 1 1 0 0 1 3 8 Z M7 5 A 1 1 0 0 1 8 6 V 14 A 1 1 0 0 1 7 15 A 1 1 0 0 1 6 14 V 6 A 1 1 0 0 1 7 5 Z M11 3 A 1 1 0 0 1 12 4 V 16 A 1 1 0 0 1 11 17 A 1 1 0 0 1 10 16 V 4 A 1 1 0 0 1 11 3 Z M15 6 A 1 1 0 0 1 16 7 V 13 A 1 1 0 0 1 15 14 A 1 1 0 0 1 14 13 V 7 A 1 1 0 0 1 15 6 Z"
);
var PanelCloseIcon = createIcon(
  "M18.2002 3.81201C18.2 3.2311 17.6967 2.69971 17 2.69971H6.90039V17.3003H17C17.6968 17.3003 18.2002 16.7681 18.2002 16.187V3.81201ZM13.4473 7.00342C13.7665 6.69814 14.2727 6.70876 14.5781 7.02783C14.8835 7.34716 14.8721 7.85431 14.5527 8.15967L12.1572 10.4497L14.5527 12.7407C14.872 13.0461 14.8835 13.5523 14.5781 13.8716C14.2728 14.1908 13.7666 14.2022 13.4473 13.897L10.4473 11.0278C10.2895 10.8769 10.2002 10.6681 10.2002 10.4497C10.2002 10.2314 10.2895 10.0225 10.4473 9.87158L13.4473 7.00342ZM1.7998 16.187C1.7998 16.7319 2.24242 17.2331 2.87207 17.2935L3 17.3003H5.2998V2.69971H3C2.30331 2.69971 1.80003 3.2311 1.7998 3.81201V16.187ZM19.7998 16.187C19.7998 17.7184 18.5123 18.8999 17 18.8999H3C1.48771 18.8999 0.200195 17.7184 0.200195 16.187V3.81201C0.200414 2.28078 1.48784 1.1001 3 1.1001H17C18.5122 1.1001 19.7996 2.28078 19.7998 3.81201V16.187Z"
);
var PanelOpenIcon = createIcon(
  "M18.2012 3.81207C18.2009 3.23037 17.6974 2.69879 17 2.69879H6.89844V17.3013H17C17.6975 17.3013 18.2012 16.7689 18.2012 16.1871V3.81207ZM10.4229 7.02887C10.7277 6.7103 11.233 6.69968 11.5518 7.00446L14.5518 9.87262C14.7093 10.0232 14.7988 10.2318 14.7988 10.4498C14.7988 10.6678 14.7093 10.8762 14.5518 11.0269L11.5518 13.8961C11.233 14.2007 10.7276 14.1894 10.4229 13.8707C10.118 13.5519 10.1295 13.0466 10.4482 12.7418L12.8438 10.4498L10.4482 8.15875C10.1294 7.85391 10.118 7.34766 10.4229 7.02887ZM1.79883 16.1871C1.79883 16.7324 2.24123 17.2347 2.87109 17.2955L3 17.3013H5.30176V2.69879H3C2.30263 2.69879 1.79905 3.23037 1.79883 3.81207V16.1871ZM19.7988 16.1871C19.7988 17.7177 18.5116 18.899 17 18.899H3C1.48839 18.899 0.201172 17.7177 0.201172 16.1871V3.81207C0.201377 2.3772 1.3328 1.24998 2.71973 1.11481L3 1.10114H17C18.5115 1.10114 19.7986 2.28163 19.7988 3.81207V16.1871Z"
);
var CursorIcon = createIcon("M4 1L4 15L7.5 11.5L10.5 18L13 17L10 10H15L4 1Z");
var BellIcon = createSapIcon(
  "M475 374q5 7 5 16 0 11-7 18.5t-18 7.5H334q-5 27-27 45.5T256 480t-51-18.5-27-45.5H58q-11 0-18.5-7.5T32 390q0-10 6-16 1-1 8-9.5T61 341t14.5-35.5T82 260v-20q0-100 45-154t129-54 129.5 54T431 240v20q0 25 6.5 45.5T452 341t15 23.5 8 9.5zm-69-9q-11-20-19-46.5t-8-58.5v-20q0-32-5.5-60t-20-50-38-34.5T256 83t-59.5 12.5-38 34.5-20 50-5.5 60v20q0 32-8 58.5T106 365h300z"
);
BellIcon.displayName = "BellIcon";
var OpenCommandFieldIcon = createSapIcon(
  "M90 416q-11 0-18.5-7.5T64 390q0-10 8-18l121-116L72 140q-8-8-8-18 0-11 7.5-18.5T90 96q10 0 17 7l141 134q8 8 8 19 0 12-8 18L107 409q-7 7-17 7zm192 0q-11 0-18.5-7.5T256 390q0-10 8-18l121-116-121-116q-8-8-8-18 0-11 7.5-18.5T282 96q10 0 17 7l141 134q8 8 8 19 0 12-8 18L299 409q-7 7-17 7z"
);
OpenCommandFieldIcon.displayName = "OpenCommandFieldIcon";
var CloseCommandFieldIcon = createSapIcon(
  "M230 416q-10 0-17-7L72 274q-8-6-8-18 0-11 8-19l141-134q7-7 17-7 11 0 18.5 7.5T256 122q0 10-8 18L127 256l121 116q8 8 8 18 0 11-7.5 18.5T230 416zm192 0q-10 0-17-7L264 274q-8-6-8-18 0-11 8-19l141-134q7-7 17-7 11 0 18.5 7.5T448 122q0 10-8 18L319 256l121 116q8 8 8 18 0 11-7.5 18.5T422 416z"
);
CloseCommandFieldIcon.displayName = "CloseCommandFieldIcon";
var JouleIcon = ({ size = 20, className, isHovered: isHoveredProp, ...props }) => {
  const [selfHovered, setSelfHovered] = React45.useState(false);
  const hovered = isHoveredProp ?? selfHovered;
  const uid = React45.useId();
  const id = (n) => `joule-${uid}-${n}`;
  const t = { transition: "fill 0.3s ease" };
  return /* @__PURE__ */ jsxRuntime.jsxs(
    "svg",
    {
      viewBox: "0 0 39 35",
      width: size,
      height: size,
      fill: "none",
      className,
      onMouseEnter: isHoveredProp === void 0 ? () => setSelfHovered(true) : void 0,
      onMouseLeave: isHoveredProp === void 0 ? () => setSelfHovered(false) : void 0,
      ...props,
      children: [
        /* @__PURE__ */ jsxRuntime.jsxs("mask", { id: id("mask"), style: { maskType: "alpha" }, maskUnits: "userSpaceOnUse", x: "2", y: "1", width: "34", height: "33", children: [
          /* @__PURE__ */ jsxRuntime.jsx("path", { d: "M26.5889 1.00195H11.4111C9.90477 1.00195 8.49812 1.75477 7.66257 3.00809L2.76809 10.3498C1.70855 11.9391 1.76641 14.0238 2.91248 15.5519L15.3959 32.1965C17.1979 34.5992 20.8021 34.5992 22.6041 32.1965L35.0875 15.5519C36.2336 14.0238 36.2915 11.9391 35.2319 10.3498L30.3374 3.0081C29.5019 1.75477 28.0952 1.00195 26.5889 1.00195Z", fill: "white" }),
          /* @__PURE__ */ jsxRuntime.jsx("path", { d: "M26.5889 1.00195H11.4111C9.90477 1.00195 8.49812 1.75477 7.66257 3.00809L2.76809 10.3498C1.70855 11.9391 1.76641 14.0238 2.91248 15.5519L15.3959 32.1965C17.1979 34.5992 20.8021 34.5992 22.6041 32.1965L35.0875 15.5519C36.2336 14.0238 36.2915 11.9391 35.2319 10.3498L30.3374 3.0081C29.5019 1.75477 28.0952 1.00195 26.5889 1.00195Z", fill: "black", fillOpacity: "0.2" })
        ] }),
        /* @__PURE__ */ jsxRuntime.jsxs("g", { mask: `url(#${id("mask")})`, children: [
          /* @__PURE__ */ jsxRuntime.jsx("path", { d: "M25.6691 13.002H12.3359L19.0023 37.002L25.6691 13.002Z", fill: `url(#${id("g0")})`, style: t }),
          /* @__PURE__ */ jsxRuntime.jsx("path", { d: "M1 13.002L19 37.002L12.3336 13.002H1Z", fill: `url(#${id("g1")})`, style: t }),
          /* @__PURE__ */ jsxRuntime.jsx("path", { d: "M37 13.002L19 37.002L25.6664 13.002H37Z", fill: `url(#${id("g2")})`, style: t }),
          /* @__PURE__ */ jsxRuntime.jsx("path", { d: "M9 1.00195L12.3334 13.002H25.6667L29 1.00195H9Z", fill: `url(#${id("g3")})`, style: t }),
          /* @__PURE__ */ jsxRuntime.jsx("path", { d: "M1 13.002H12.3336L9 1.00195L1 13.002Z", fill: `url(#${id("g4")})`, style: t }),
          /* @__PURE__ */ jsxRuntime.jsx("path", { d: "M37 13.002H25.6664L29 1.00195L37 13.002Z", fill: `url(#${id("g5")})`, style: t }),
          /* @__PURE__ */ jsxRuntime.jsx("path", { d: "M26.6284 7.74902C26.5172 7.30423 26.1175 6.99219 25.6591 6.99219C25.2006 6.99219 24.8009 7.30423 24.6897 7.74902L24.563 8.25609C24.1155 10.046 22.7179 11.4436 20.928 11.8911L20.4209 12.0179C19.9761 12.1291 19.6641 12.5287 19.6641 12.9872C19.6641 13.4457 19.9761 13.8453 20.4209 13.9565L20.928 14.0833C22.7179 14.5308 24.1155 15.9284 24.563 17.7183L24.6897 18.2254C24.8009 18.6702 25.2006 18.9822 25.6591 18.9822C26.1175 18.9822 26.5172 18.6702 26.6284 18.2254L26.7552 17.7183C27.2026 15.9284 28.6002 14.5308 30.3902 14.0833L30.8972 13.9565C31.342 13.8453 31.6541 13.4457 31.6541 12.9872C31.6541 12.5287 31.342 12.1291 30.8972 12.0179L30.3902 11.8911C28.6002 11.4436 27.2026 10.046 26.7552 8.25609L26.6284 7.74902Z", fill: "white" })
        ] }),
        /* @__PURE__ */ jsxRuntime.jsxs("defs", { children: [
          /* @__PURE__ */ jsxRuntime.jsxs("linearGradient", { id: id("g0"), x1: "15.2204", y1: "28.942", x2: "30.3063", y2: "19.5615", gradientUnits: "userSpaceOnUse", children: [
            /* @__PURE__ */ jsxRuntime.jsx("stop", { offset: "0.0785293", stopColor: hovered ? "#5a2aff" : "#4013E3" }),
            /* @__PURE__ */ jsxRuntime.jsx("stop", { offset: "1", stopColor: hovered ? "#ffa8f5" : "#F090EB" })
          ] }),
          /* @__PURE__ */ jsxRuntime.jsxs("linearGradient", { id: id("g1"), x1: "15.44", y1: "19.962", x2: "7.32", y2: "23.762", gradientUnits: "userSpaceOnUse", children: [
            /* @__PURE__ */ jsxRuntime.jsx("stop", { stopColor: hovered ? "#6e4dff" : "#5739F5" }),
            /* @__PURE__ */ jsxRuntime.jsx("stop", { offset: "1", stopColor: hovered ? "#3b20c9" : "#2815A6" })
          ] }),
          /* @__PURE__ */ jsxRuntime.jsxs("linearGradient", { id: id("g2"), x1: "26.28", y1: "11.462", x2: "32.36", y2: "25.002", gradientUnits: "userSpaceOnUse", children: [
            /* @__PURE__ */ jsxRuntime.jsx("stop", { stopColor: hovered ? "#ffc8f2" : "#F3B2E9" }),
            /* @__PURE__ */ jsxRuntime.jsx("stop", { offset: "1", stopColor: hovered ? "#5a2aff" : "#4013E3" })
          ] }),
          /* @__PURE__ */ jsxRuntime.jsxs("linearGradient", { id: id("g3"), x1: "26.28", y1: "16.6615", x2: "10.34", y2: "5.44149", gradientUnits: "userSpaceOnUse", children: [
            /* @__PURE__ */ jsxRuntime.jsx("stop", { stopColor: hovered ? "#ffc8f2" : "#F3B2E9" }),
            /* @__PURE__ */ jsxRuntime.jsx("stop", { offset: "0.995192", stopColor: hovered ? "#5a2aff" : "#4013E3" })
          ] }),
          /* @__PURE__ */ jsxRuntime.jsxs("linearGradient", { id: id("g4"), x1: "11.68", y1: "11.402", x2: "0.26", y2: "7.88195", gradientUnits: "userSpaceOnUse", children: [
            /* @__PURE__ */ jsxRuntime.jsx("stop", { stopColor: hovered ? "#6e4dff" : "#5739F5" }),
            /* @__PURE__ */ jsxRuntime.jsx("stop", { offset: "1", stopColor: hovered ? "#3b20c9" : "#2815A6" })
          ] }),
          /* @__PURE__ */ jsxRuntime.jsxs("linearGradient", { id: id("g5"), x1: "42.1", y1: "3.44195", x2: "28.7342", y2: "16.5549", gradientUnits: "userSpaceOnUse", children: [
            /* @__PURE__ */ jsxRuntime.jsx("stop", { stopColor: hovered ? "#8a6fff" : "#735AF6" }),
            /* @__PURE__ */ jsxRuntime.jsx("stop", { offset: "1", stopColor: hovered ? "#ffa8f5" : "#F090EB" })
          ] })
        ] })
      ]
    }
  );
};
JouleIcon.displayName = "JouleIcon";
var MoreIcon = ({ size = 20, className, ...props }) => /* @__PURE__ */ jsxRuntime.jsx(
  "svg",
  {
    viewBox: "0 0 20 20",
    width: size,
    height: size,
    fill: "currentColor",
    className,
    ...props,
    children: /* @__PURE__ */ jsxRuntime.jsx("path", { d: "M2.5 7.5C3.88071 7.5 5 8.61929 5 10C5 11.3807 3.88071 12.5 2.5 12.5C1.11929 12.5 0 11.3807 0 10C0 8.61929 1.11929 7.5 2.5 7.5ZM10 7.5C11.3807 7.5 12.5 8.61929 12.5 10C12.5 11.3807 11.3807 12.5 10 12.5C8.61929 12.5 7.5 11.3807 7.5 10C7.5 8.61929 8.61929 7.5 10 7.5ZM17.5 7.5C18.8807 7.5 20 8.61929 20 10C20 11.3807 18.8807 12.5 17.5 12.5C16.1193 12.5 15 11.3807 15 10C15 8.61929 16.1193 7.5 17.5 7.5Z" })
  }
);
MoreIcon.displayName = "MoreIcon";
var NewConversationIcon = ({ size = 20, className, ...props }) => /* @__PURE__ */ jsxRuntime.jsxs(
  "svg",
  {
    viewBox: "0 0 20 20",
    width: size,
    height: size,
    fill: "currentColor",
    className,
    ...props,
    children: [
      /* @__PURE__ */ jsxRuntime.jsx("path", { d: "M0 3.57129C7.53742e-05 1.59896 1.59895 7.5374e-05 3.57129 0H10C10.5523 0 11 0.447716 11 1C11 1.55229 10.5523 2 10 2H3.57129C2.70352 2.00008 2.00008 2.70353 2 3.57129V16.586L5.43555 13.1504L5.50879 13.084C5.68672 12.9381 5.91045 12.8575 6.14258 12.8574H16.4287C17.2964 12.8574 17.9998 12.1538 18 11.2861V9.9463C18.0003 9.39424 18.4479 8.9463 19 8.9463C19.5521 8.9463 19.9997 9.39424 20 9.9463V11.2861C19.9998 13.2584 18.401 14.8574 16.4287 14.8574H6.55664L1.70703 19.7071C1.42103 19.9931 0.990862 20.0786 0.617188 19.9239C0.243586 19.769 0 19.4044 0 19V3.57129Z" }),
      /* @__PURE__ */ jsxRuntime.jsx("path", { d: "M5 11C4.44772 11 4 10.5523 4 10C4 9.44773 4.44772 9.00001 5 9.00001H10C10.5523 9.00001 11 9.44773 11 10C11 10.5523 10.5523 11 10 11H5Z" }),
      /* @__PURE__ */ jsxRuntime.jsx("path", { d: "M5 7.00001C4.44772 7.00001 4 6.55229 4 6.00001C4 5.44772 4.44772 5.00001 5 5.00001L8 5.00001C8.55229 5.00001 9 5.44772 9 6.00001C9 6.55229 8.55229 7.00001 8 7.00001H5Z" }),
      /* @__PURE__ */ jsxRuntime.jsx("path", { d: "M16.8571 0.847507C16.8571 0.379442 16.4734 0 16 0C15.5266 0 15.1429 0.379442 15.1429 0.847507V3.10752H12.8571C12.3838 3.10752 12 3.48697 12 3.95503C12 4.42307 12.3838 4.80254 12.8571 4.80254H15.1429V7.06256C15.1429 7.53059 15.5266 7.91006 16 7.91006C16.4734 7.91006 16.8571 7.53059 16.8571 7.06256V4.80254L19.231 4.79813C19.663 4.75448 20 4.39364 20 3.95503C20 3.5164 19.663 3.15558 19.231 3.11194L16.8571 3.10752V0.847507Z" })
    ]
  }
);
NewConversationIcon.displayName = "NewConversationIcon";
var FxSideNavigationContext = React45.createContext(null);
var useFxSideNavigationContext = () => {
  const context = React45.useContext(FxSideNavigationContext);
  return context;
};
function countNavigationItems(children) {
  let count = 0;
  React45.Children.forEach(children, (child) => {
    if (React45.isValidElement(child)) {
      count++;
    }
  });
  return count;
}
function enrichChildrenWithAria(children, totalCount, startIndex = 1) {
  let currentIndex = startIndex;
  return React45.Children.map(children, (child) => {
    if (React45.isValidElement(child)) {
      const ariaRoleDescription = `navigation item, ${currentIndex} of ${totalCount}`;
      currentIndex++;
      return React45.cloneElement(child, { ariaRoleDescription });
    }
    return child;
  });
}
function FxSideNavigation({
  collapsed = false,
  isTransitioning = false,
  transitionDirection = null,
  forceMerged = false,
  dialogMode = false,
  children,
  fixedItems,
  header,
  onSelectionChange,
  onMergeChange: _onMergeChange,
  // Deprecated - merge is now controlled by parent
  className,
  style,
  ref
}) {
  const { t } = reactI18next.useTranslation("fx");
  const containerRef = React45.useRef(null);
  const flexibleRef = React45.useRef(null);
  const fixedRef = React45.useRef(null);
  const flyoutCloseHandlersRef = React45.useRef(/* @__PURE__ */ new Map());
  const itemRefsRef = React45.useRef(/* @__PURE__ */ new Map());
  const itemOrderRef = React45.useRef([]);
  const registerFlyoutClose = React45.useCallback((name, handler) => {
    flyoutCloseHandlersRef.current.set(name, handler);
  }, []);
  const unregisterFlyoutClose = React45.useCallback((name) => {
    flyoutCloseHandlersRef.current.delete(name);
  }, []);
  const registerItemRef = React45.useCallback((name, ref2) => {
    if (ref2) {
      itemRefsRef.current.set(name, ref2);
      const allItems = Array.from(itemRefsRef.current.entries());
      allItems.sort((a, b) => {
        const aRect = a[1].getBoundingClientRect();
        const bRect = b[1].getBoundingClientRect();
        return aRect.top - bRect.top;
      });
      itemOrderRef.current = allItems.map(([name2]) => name2);
    } else {
      itemRefsRef.current.delete(name);
      itemOrderRef.current = itemOrderRef.current.filter((n) => n !== name);
    }
  }, []);
  const navigateToItem = React45.useCallback((currentName, direction) => {
    const order = itemOrderRef.current;
    const currentIndex = order.indexOf(currentName);
    if (currentIndex === -1) return;
    let targetIndex;
    if (direction === "next") {
      targetIndex = currentIndex + 1;
      if (targetIndex >= order.length) targetIndex = 0;
    } else {
      targetIndex = currentIndex - 1;
      if (targetIndex < 0) targetIndex = order.length - 1;
    }
    const targetName = order[targetIndex];
    const targetRef = itemRefsRef.current.get(targetName);
    targetRef?.focus();
  }, []);
  const closeFlyout = React45.useCallback((itemName) => {
    const handler = flyoutCloseHandlersRef.current.get(itemName);
    handler?.();
  }, []);
  const closeOtherFlyouts = React45.useCallback((exceptName) => {
    flyoutCloseHandlersRef.current.forEach((handler, name) => {
      if (name !== exceptName) {
        handler();
      }
    });
  }, []);
  React45.useImperativeHandle(ref, () => ({
    closeFlyout,
    get isMerged() {
      return forceMerged;
    },
    get nativeElement() {
      return containerRef.current;
    },
    getNativeElement: () => containerRef.current
  }));
  const handleItemClick = React45.useCallback(
    (name) => {
      onSelectionChange?.({ name });
    },
    [onSelectionChange]
  );
  const totalCount = React45.useMemo(() => countNavigationItems(children), [children]);
  const enrichedChildren = React45.useMemo(() => enrichChildrenWithAria(children, totalCount, 1), [children, totalCount]);
  const flexibleContextValue = React45.useMemo(
    () => ({
      collapsed,
      isTransitioning,
      transitionDirection,
      isFixedSection: false,
      onItemClick: handleItemClick,
      registerFlyoutClose,
      unregisterFlyoutClose,
      closeOtherFlyouts,
      registerItemRef,
      navigateToItem
    }),
    [collapsed, isTransitioning, transitionDirection, handleItemClick, registerFlyoutClose, unregisterFlyoutClose, closeOtherFlyouts, registerItemRef, navigateToItem]
  );
  const fixedContextValue = React45.useMemo(
    () => ({
      collapsed,
      isTransitioning,
      transitionDirection,
      isFixedSection: true,
      onItemClick: handleItemClick,
      registerFlyoutClose,
      unregisterFlyoutClose,
      closeOtherFlyouts,
      registerItemRef,
      navigateToItem
    }),
    [collapsed, isTransitioning, transitionDirection, handleItemClick, registerFlyoutClose, unregisterFlyoutClose, closeOtherFlyouts, registerItemRef, navigateToItem]
  );
  return /* @__PURE__ */ jsxRuntime.jsxs(
    "nav",
    {
      ref: containerRef,
      className: cn(
        "fx-side-navigation flex flex-col bg-sapphire-shell-bg-primary pb-6",
        !dialogMode && "h-full",
        collapsed && "collapsed",
        forceMerged && "merged",
        className
      ),
      style: {
        overflow: "visible",
        // Allow flyouts to extend beyond nav
        paddingLeft: "1.25rem",
        paddingRight: "1.25rem",
        ...style
      },
      children: [
        header && /* @__PURE__ */ jsxRuntime.jsx(
          "div",
          {
            className: "flex items-center shrink-0 py-6 overflow-hidden",
            style: { paddingLeft: "0.25rem" },
            children: header
          }
        ),
        !dialogMode && /* @__PURE__ */ jsxRuntime.jsx(
          "div",
          {
            className: forceMerged ? "flex-1" : "shrink-0",
            style: {
              height: forceMerged ? void 0 : "10rem",
              minHeight: 0,
              transition: "height 200ms ease-out"
            }
          }
        ),
        forceMerged ? (
          // Merged: single list with divider (no flex spacer - items flow naturally after top spacer)
          /* @__PURE__ */ jsxRuntime.jsx(
            "div",
            {
              ref: flexibleRef,
              className: "relative",
              style: { overflow: "visible" },
              children: /* @__PURE__ */ jsxRuntime.jsxs(
                "ul",
                {
                  role: "list",
                  "aria-label": t("FX_MAIN_NAVIGATION"),
                  className: "list-none p-0 m-0 flex flex-col gap-6",
                  children: [
                    /* @__PURE__ */ jsxRuntime.jsx(FxSideNavigationContext.Provider, { value: flexibleContextValue, children: enrichedChildren }),
                    fixedItems && /* @__PURE__ */ jsxRuntime.jsxs(jsxRuntime.Fragment, { children: [
                      /* @__PURE__ */ jsxRuntime.jsx("li", { role: "separator", className: "my-1.5 h-px bg-sapphire-border-primary" }),
                      /* @__PURE__ */ jsxRuntime.jsx(FxSideNavigationContext.Provider, { value: fixedContextValue, children: fixedItems })
                    ] })
                  ]
                }
              )
            }
          )
        ) : (
          // Separate: flexible list + spacer + fixed list
          /* @__PURE__ */ jsxRuntime.jsxs(jsxRuntime.Fragment, { children: [
            /* @__PURE__ */ jsxRuntime.jsx(
              "div",
              {
                ref: flexibleRef,
                className: "shrink-0 relative",
                style: { overflow: "visible" },
                children: /* @__PURE__ */ jsxRuntime.jsx(
                  "ul",
                  {
                    role: "list",
                    "aria-label": t("FX_MAIN_NAVIGATION"),
                    className: "list-none p-0 m-0 flex flex-col gap-6",
                    children: /* @__PURE__ */ jsxRuntime.jsx(FxSideNavigationContext.Provider, { value: flexibleContextValue, children: enrichedChildren })
                  }
                )
              }
            ),
            /* @__PURE__ */ jsxRuntime.jsx("div", { className: "flex-1" }),
            fixedItems && /* @__PURE__ */ jsxRuntime.jsx(
              "div",
              {
                ref: fixedRef,
                className: "shrink-0 relative",
                style: { overflow: "visible" },
                children: /* @__PURE__ */ jsxRuntime.jsx("div", { role: "toolbar", "aria-label": t("FX_USER_ACTIONS"), className: "flex flex-col gap-6", children: /* @__PURE__ */ jsxRuntime.jsx(FxSideNavigationContext.Provider, { value: fixedContextValue, children: fixedItems }) })
              }
            )
          ] })
        )
      ]
    }
  );
}
var pathData2 = [
  "M454 230q11 0 18.5 7.5T480 256t-7.5 18.5T454 282H282v172q0 11-7.5 18.5T256 480t-18.5-7.5T230 454V282H58q-11 0-18.5-7.5T32 256t7.5-18.5T58 230h172V58q0-11 7.5-18.5T256 32t18.5 7.5T282 58v172h172z"
];
var AddIcon = Object.assign(
  React45__namespace.default.forwardRef(
    (props, ref) => /* @__PURE__ */ jsxRuntime.jsx(Icon, { ref, pathData: pathData2, ...props })
  ),
  { displayName: "AddIcon", iconName: "add" }
);
var dotVariants = cva("rounded-full bg-current inline-block", {
  variants: {
    size: {
      ["S" /* S */]: "w-2 h-2",
      ["M" /* M */]: "w-4 h-4",
      ["L" /* L */]: "w-8 h-8"
    }
  },
  defaultVariants: { size: "M" /* M */ }
});
var dotGapClasses = {
  ["S" /* S */]: "gap-px",
  ["M" /* M */]: "gap-[0.1875rem]",
  ["L" /* L */]: "gap-1"
};
function BusyIndicator({
  size = "M" /* M */,
  textPlacement = "Bottom" /* Bottom */,
  active = false,
  delay = 1e3,
  text,
  children,
  accessibleName,
  className,
  style,
  id,
  "data-testid": dataTestId,
  ref
}) {
  const rootRef = React45.useRef(null);
  const overlayRef = React45.useRef(null);
  const [isBusy, setIsBusy] = React45.useState(false);
  const labelId = React45.useId();
  const hasChildren = React45__namespace.default.Children.count(children) > 0;
  const normalizedSize = size || "M" /* M */;
  const isTopPlacement = textPlacement === "Top";
  React45.useEffect(() => {
    if (!active) {
      setIsBusy(false);
      return;
    }
    if (delay <= 0) {
      setIsBusy(true);
      return;
    }
    const timer = setTimeout(() => {
      setIsBusy(true);
    }, delay);
    return () => clearTimeout(timer);
  }, [active, delay]);
  React45.useImperativeHandle(
    ref,
    () => ({
      focus() {
        if (isBusy && overlayRef.current) {
          overlayRef.current.focus();
        } else {
          rootRef.current?.focus();
        }
      },
      blur() {
        if (isBusy && overlayRef.current) {
          overlayRef.current.blur();
        } else {
          rootRef.current?.blur();
        }
      },
      isFocused() {
        return document.activeElement === overlayRef.current || document.activeElement === rootRef.current;
      },
      get nativeElement() {
        return rootRef.current;
      }
    }),
    [isBusy]
  );
  const textLabel = text ? /* @__PURE__ */ jsxRuntime.jsx(
    "span",
    {
      id: labelId,
      className: cn(
        "w-full text-center text-sapphire-text-tertiary text-sm font-medium",
        isTopPlacement ? "mb-2" : "mt-2"
      ),
      children: text
    }
  ) : null;
  const dots = /* @__PURE__ */ jsxRuntime.jsx(
    "div",
    {
      className: cn(
        "inline-flex items-center leading-none",
        dotGapClasses[normalizedSize]
      ),
      children: [0, 1, 2].map((i) => /* @__PURE__ */ jsxRuntime.jsx(
        "span",
        {
          className: dotVariants({ size: normalizedSize }),
          style: {
            animation: `busy-grow 1.6s cubic-bezier(0.32, 0.06, 0.85, 1.11) ${i * 200}ms infinite`
          }
        },
        i
      ))
    }
  );
  return /* @__PURE__ */ jsxRuntime.jsxs(
    "div",
    {
      ref: rootRef,
      id,
      className: cn(
        "inline-block",
        hasChildren && "relative",
        isBusy && "text-ring",
        className
      ),
      style: { ...style, borderRadius: "inherit", height: "inherit" },
      "data-testid": dataTestId,
      "data-part": "root",
      children: [
        /* @__PURE__ */ jsxRuntime.jsx("style", { children: `@keyframes busy-grow{0%,50%,100%{transform:scale(.5)}25%{transform:scale(1)}}` }),
        isBusy && /* @__PURE__ */ jsxRuntime.jsxs(
          "div",
          {
            ref: overlayRef,
            role: "progressbar",
            "aria-valuemin": 0,
            "aria-valuemax": 100,
            "aria-valuetext": "Busy",
            "aria-labelledby": text ? labelId : void 0,
            "aria-label": !text ? accessibleName ?? "Please wait" : void 0,
            title: !text ? accessibleName ?? "Please wait" : void 0,
            tabIndex: 0,
            "data-sap-focus-ref": true,
            className: cn(
              "flex flex-col items-center justify-center",
              "bg-inherit rounded-[inherit]",
              "focus-visible:outline focus-visible:outline-2 focus-visible:outline-ring focus-visible:outline-offset-[-2px]",
              hasChildren && "absolute inset-0 z-[99]"
            ),
            children: [
              isTopPlacement && textLabel,
              dots,
              !isTopPlacement && textLabel
            ]
          }
        ),
        children && /* @__PURE__ */ jsxRuntime.jsx(
          "div",
          {
            className: cn(isBusy && "opacity-40"),
            ...isBusy ? { inert: true } : {},
            children
          }
        )
      ]
    }
  );
}
var filledBrandStyles = [
  "bg-sapphire-brand-background text-sapphire-neutral-foreground-white font-semibold",
  "hover:bg-sapphire-brand-hover-background",
  "active:bg-sapphire-brand-pressed-background [&.kb-active]:bg-sapphire-brand-pressed-background",
  "focus:[outline-style:solid] focus:outline-2 focus:-outline-offset-2 focus:outline-sapphire-border-focus btn-filled-focus"
];
var filledJouleStyles = [
  "bg-sapphire-joule-background text-sapphire-neutral-foreground-white font-semibold",
  "hover:bg-sapphire-joule-hover-background",
  "active:bg-sapphire-joule-pressed-background [&.kb-active]:bg-sapphire-joule-pressed-background",
  "focus:[outline-style:solid] focus:outline-2 focus:-outline-offset-2 focus:outline-sapphire-border-joule btn-filled-focus"
];
var outlineBrandStyles = [
  "bg-transparent text-sapphire-brand-foreground border border-sapphire-brand-foreground",
  "hover:bg-sapphire-neutral-hover-background",
  "active:bg-sapphire-neutral-pressed-background active:border-sapphire-brand-pressed-foreground active:text-sapphire-brand-pressed-foreground [&.kb-active]:bg-sapphire-neutral-pressed-background [&.kb-active]:border-sapphire-brand-pressed-foreground [&.kb-active]:text-sapphire-brand-pressed-foreground",
  "focus:[outline-style:solid] focus:outline-2 focus:-outline-offset-2 focus:outline-ring"
];
var outlineJouleStyles = [
  "bg-transparent text-sapphire-joule-foreground border border-sapphire-joule-foreground",
  "hover:bg-sapphire-neutral-hover-background hover:text-sapphire-joule-hover-background hover:border-sapphire-joule-hover-background",
  "active:bg-sapphire-neutral-pressed-background active:border-sapphire-joule-pressed-foreground active:text-sapphire-joule-pressed-foreground [&.kb-active]:bg-sapphire-neutral-pressed-background [&.kb-active]:border-sapphire-joule-pressed-foreground [&.kb-active]:text-sapphire-joule-pressed-foreground",
  "focus:[outline-style:solid] focus:outline-2 focus:-outline-offset-2 focus:outline-sapphire-border-joule"
];
var buttonVariants = cva(
  [
    // Base styles
    "inline-flex items-center justify-center gap-2",
    "font-semibold whitespace-nowrap",
    "transition-[color,background-color,border-color,text-decoration-color,fill,stroke] cursor-pointer",
    "outline-none",
    "disabled:pointer-events-none disabled:opacity-40"
  ],
  {
    variants: {
      design: {
        // --- Primary (filled) ---
        ["Primary" /* Primary */]: filledBrandStyles,
        ["PrimaryJoule" /* PrimaryJoule */]: filledJouleStyles,
        // --- Secondary (outlined) ---
        ["Secondary" /* Secondary */]: outlineBrandStyles,
        ["SecondaryJoule" /* SecondaryJoule */]: outlineJouleStyles,
        // --- Tertiary (ghost, colored text) ---
        ["Tertiary" /* Tertiary */]: [
          "bg-transparent text-sapphire-brand-foreground border border-transparent",
          "hover:bg-sapphire-neutral-hover-background",
          "active:bg-sapphire-neutral-pressed-background active:text-sapphire-brand-pressed-foreground [&.kb-active]:bg-sapphire-neutral-pressed-background [&.kb-active]:text-sapphire-brand-pressed-foreground",
          "focus:[outline-style:solid] focus:outline-2 focus:-outline-offset-2 focus:outline-ring"
        ],
        ["TertiaryJoule" /* TertiaryJoule */]: [
          "bg-transparent text-sapphire-joule-foreground border border-transparent",
          "hover:bg-sapphire-neutral-hover-background hover:text-sapphire-joule-hover-background",
          "active:bg-sapphire-neutral-pressed-background active:text-sapphire-joule-pressed-foreground [&.kb-active]:bg-sapphire-neutral-pressed-background [&.kb-active]:text-sapphire-joule-pressed-foreground",
          "focus:[outline-style:solid] focus:outline-2 focus:-outline-offset-2 focus:outline-sapphire-border-joule"
        ],
        // --- Neutral (ghost, dark text) ---
        ["Neutral" /* Neutral */]: [
          "bg-transparent text-sapphire-neutral-foreground-black border border-sapphire-neutral-foreground-muted",
          "hover:bg-sapphire-neutral-hover-background hover:text-sapphire-neutral-foreground-black",
          "active:bg-sapphire-neutral-pressed-background active:text-sapphire-neutral-foreground-black [&.kb-active]:bg-sapphire-neutral-pressed-background [&.kb-active]:text-sapphire-neutral-foreground-black",
          "focus:[outline-style:solid] focus:outline-2 focus:-outline-offset-2 focus:outline-foreground"
        ],
        // --- SecondaryNeutral (ghost, neutral-black text) ---
        ["SecondaryNeutral" /* SecondaryNeutral */]: [
          "bg-transparent text-sapphire-neutral-foreground-black border border-transparent",
          "hover:bg-sapphire-neutral-hover-background",
          "active:bg-sapphire-neutral-pressed-background [&.kb-active]:bg-sapphire-neutral-pressed-background",
          "focus:[outline-style:solid] focus:outline-2 focus:-outline-offset-2 focus:outline-foreground"
        ],
        // --- TertiaryNeutral (ghost, muted neutral text, no background) ---
        ["TertiaryNeutral" /* TertiaryNeutral */]: [
          "bg-transparent text-sapphire-neutral-foreground-muted border border-transparent",
          "hover:text-sapphire-neutral-foreground-black",
          "active:text-sapphire-neutral-foreground-black [&.kb-active]:text-sapphire-neutral-foreground-black",
          "focus:[outline-style:solid] focus:outline-2 focus:-outline-offset-2 focus:outline-foreground"
        ]
      }
    },
    defaultVariants: {
      design: "Secondary" /* Secondary */
    }
  }
);
var largeSizeClasses = {
  default: "h-10 px-4 py-3 text-base gap-2 rounded-lg",
  iconOnly: "h-10 w-10 p-0 text-base rounded-lg",
  icon: "h-5 w-5 [&>svg]:h-[17.5px] [&>svg]:w-[17.5px]"
};
var mediumSizeClasses = {
  default: "h-8 px-3 py-2 text-sm gap-1.5 rounded",
  iconOnly: "h-8 w-8 p-0 text-sm rounded",
  icon: "h-4 w-4 [&>svg]:h-3.5 [&>svg]:w-3.5"
};
var smallSizeClasses = {
  default: "h-6 px-2 py-1 text-xs font-normal gap-1 rounded",
  iconOnly: "h-6 w-6 p-0 text-xs rounded",
  icon: "h-3 w-3 [&>svg]:h-[10.5px] [&>svg]:w-[10.5px]"
};
var sizeClasses = {
  ["Large" /* Large */]: largeSizeClasses,
  ["Medium" /* Medium */]: mediumSizeClasses,
  ["Small" /* Small */]: smallSizeClasses
};
function getButtonType(type) {
  switch (type) {
    case "Submit" /* Submit */:
    case "Submit":
      return "submit";
    case "Reset" /* Reset */:
    case "Reset":
      return "reset";
    default:
      return "button";
  }
}
function Button({
  design = "Secondary" /* Secondary */,
  size = "Large" /* Large */,
  type = "Button" /* Button */,
  disabled = false,
  loading = false,
  icon,
  endIcon,
  iconOnly = false,
  children,
  tooltip,
  states,
  state,
  stateTransition = "slide-up",
  accessibleName,
  accessibleNameRef,
  accessibleRole = "Button" /* Button */,
  accessibilityAttributes,
  name,
  value,
  form,
  onClick,
  onFocus,
  onBlur,
  className,
  style,
  id,
  tabIndex,
  "data-testid": dataTestId,
  "data-growing-button": dataGrowingButton,
  ref
}) {
  const buttonRef = React45.useRef(null);
  const hiddenButtonRef = React45.useRef(null);
  const [animationPhase, setAnimationPhase] = React45.useState("idle");
  const [displayedState, setDisplayedState] = React45.useState(
    null
  );
  const [buttonWidth, setButtonWidth] = React45.useState(void 0);
  const currentStateObject = states?.find((s) => s.name === state) ?? states?.[0];
  const isMultiState = states && states.length > 0;
  React45.useEffect(() => {
    if (isMultiState && !displayedState) {
      setDisplayedState(currentStateObject ?? null);
    }
  }, [isMultiState, currentStateObject, displayedState]);
  React45.useEffect(() => {
    if (!isMultiState || !currentStateObject || animationPhase !== "idle") {
      return;
    }
    if (displayedState && displayedState.name !== currentStateObject.name) {
      if (buttonRef.current) {
        setButtonWidth(buttonRef.current.offsetWidth);
      }
      setAnimationPhase("fade-out");
    }
  }, [currentStateObject, displayedState, isMultiState, animationPhase]);
  React45.useEffect(() => {
    if (animationPhase === "fade-out") {
      const timer = setTimeout(() => {
        setAnimationPhase("fade-mid");
      }, 180);
      return () => clearTimeout(timer);
    }
    if (animationPhase === "fade-mid") {
      setDisplayedState(currentStateObject ?? null);
      if (hiddenButtonRef.current) {
        setButtonWidth(hiddenButtonRef.current.offsetWidth);
      }
      const timer = setTimeout(() => {
        setAnimationPhase("fade-in");
      }, 20);
      return () => clearTimeout(timer);
    }
    if (animationPhase === "fade-in") {
      const timer = setTimeout(() => {
        setAnimationPhase("idle");
        setButtonWidth(void 0);
      }, 160);
      return () => clearTimeout(timer);
    }
  }, [animationPhase, currentStateObject]);
  React45.useImperativeHandle(
    ref,
    () => ({
      focus() {
        buttonRef.current?.focus();
      },
      blur() {
        buttonRef.current?.blur();
      },
      isFocused() {
        return document.activeElement === buttonRef.current;
      },
      get nativeElement() {
        return buttonRef.current;
      }
    }),
    []
  );
  const handleClick2 = React45.useCallback(
    (e) => {
      if (disabled || loading) {
        e.preventDefault();
        return;
      }
      onClick?.({
        originalEvent: e,
        isKeyboard: false
      });
    },
    [disabled, loading, onClick]
  );
  const handleKeyDown = React45.useCallback(
    (e) => {
      if (disabled || loading) return;
      if (e.key === "Enter" || e.key === " ") {
        if (e.key === " ") {
          e.preventDefault();
        }
        buttonRef.current?.classList.add("kb-active");
        if (e.key === " ") {
          onClick?.({
            originalEvent: e,
            isKeyboard: true
          });
        }
      }
    },
    [disabled, loading, onClick]
  );
  const handleKeyUp = React45.useCallback(
    (e) => {
      if (e.key === "Enter" || e.key === " ") {
        buttonRef.current?.classList.remove("kb-active");
      }
    },
    []
  );
  const ariaExpanded = accessibilityAttributes?.expanded;
  const ariaHasPopup = accessibilityAttributes?.hasPopup;
  const ariaControls = accessibilityAttributes?.controls;
  const ariaPressed = accessibilityAttributes?.pressed;
  const role = accessibleRole === "Link" /* Link */ || accessibleRole === "Link" ? "link" : void 0;
  const isDisabled = disabled;
  const effectiveIcon = isMultiState ? displayedState?.icon : icon;
  const effectiveEndIcon = isMultiState ? displayedState?.endIcon : endIcon;
  const effectiveChildren = isMultiState ? displayedState?.text : children;
  const effectiveIconOnly = isMultiState ? !displayedState?.text && !!displayedState?.icon : iconOnly;
  const effectivePulsing = isMultiState ? displayedState?.pulsing : false;
  const PulsingText = ({ children: text }) => /* @__PURE__ */ jsxRuntime.jsxs("span", { className: "relative inline-block", children: [
    /* @__PURE__ */ jsxRuntime.jsx("span", { className: "opacity-50", children: text }),
    /* @__PURE__ */ jsxRuntime.jsx(
      "span",
      {
        className: "absolute inset-0 overflow-hidden",
        style: {
          background: "linear-gradient(90deg, transparent 0%, currentColor 50%, transparent 100%)",
          backgroundSize: "200% 100%",
          WebkitBackgroundClip: "text",
          backgroundClip: "text",
          WebkitTextFillColor: "transparent",
          animation: "pulse-sweep 1.5s ease-in-out infinite"
        },
        children: text
      }
    ),
    /* @__PURE__ */ jsxRuntime.jsx("style", { children: `
          @keyframes pulse-sweep {
            0% { background-position: 200% 0; }
            100% { background-position: -200% 0; }
          }
        ` })
  ] });
  const getTransitionStyles = (phase, transition) => {
    if (transition === "none" || phase === "idle") {
      return {};
    }
    const baseTransition = "all 100ms ease-out";
    switch (transition) {
      case "slide-up":
        if (phase === "fade-out") return { opacity: 0, transform: "translateY(-8px)", transition: baseTransition };
        if (phase === "fade-mid") return { opacity: 0, transform: "translateY(8px)" };
        if (phase === "fade-in") return { opacity: 1, transform: "translateY(0)", transition: baseTransition };
        break;
      case "slide-down":
        if (phase === "fade-out") return { opacity: 0, transform: "translateY(8px)", transition: baseTransition };
        if (phase === "fade-mid") return { opacity: 0, transform: "translateY(-8px)" };
        if (phase === "fade-in") return { opacity: 1, transform: "translateY(0)", transition: baseTransition };
        break;
      case "slide-left":
        if (phase === "fade-out") return { opacity: 0, transform: "translateX(-12px)", transition: baseTransition };
        if (phase === "fade-mid") return { opacity: 0, transform: "translateX(12px)" };
        if (phase === "fade-in") return { opacity: 1, transform: "translateX(0)", transition: baseTransition };
        break;
      case "slide-right":
        if (phase === "fade-out") return { opacity: 0, transform: "translateX(12px)", transition: baseTransition };
        if (phase === "fade-mid") return { opacity: 0, transform: "translateX(-12px)" };
        if (phase === "fade-in") return { opacity: 1, transform: "translateX(0)", transition: baseTransition };
        break;
      case "fade":
        if (phase === "fade-out") return { opacity: 0, transition: baseTransition };
        if (phase === "fade-mid") return { opacity: 0 };
        if (phase === "fade-in") return { opacity: 1, transition: baseTransition };
        break;
      case "scale":
        if (phase === "fade-out") return { opacity: 0, transform: "scale(0.75)", transition: baseTransition };
        if (phase === "fade-mid") return { opacity: 0, transform: "scale(0.75)" };
        if (phase === "fade-in") return { opacity: 1, transform: "scale(1)", transition: baseTransition };
        break;
      case "flip":
        if (phase === "fade-out") return { opacity: 0, transform: "rotateX(90deg)", transition: baseTransition, transformStyle: "preserve-3d" };
        if (phase === "fade-mid") return { opacity: 0, transform: "rotateX(-90deg)", transformStyle: "preserve-3d" };
        if (phase === "fade-in") return { opacity: 1, transform: "rotateX(0)", transition: baseTransition, transformStyle: "preserve-3d" };
        break;
    }
    return {};
  };
  const contentAnimationStyles = getTransitionStyles(animationPhase, stateTransition);
  const iconAnimationStyles = getTransitionStyles(animationPhase, stateTransition);
  return /* @__PURE__ */ jsxRuntime.jsxs(jsxRuntime.Fragment, { children: [
    /* @__PURE__ */ jsxRuntime.jsxs(
      "button",
      {
        ref: buttonRef,
        type: getButtonType(type),
        disabled: isDisabled,
        "aria-disabled": loading || void 0,
        id,
        name,
        value,
        form,
        role,
        "aria-label": accessibleName,
        "aria-labelledby": accessibleNameRef,
        "aria-expanded": ariaExpanded,
        "aria-haspopup": ariaHasPopup,
        "aria-controls": ariaControls,
        "aria-pressed": ariaPressed,
        "aria-busy": loading || void 0,
        title: tooltip,
        tabIndex,
        onClick: handleClick2,
        onKeyDown: handleKeyDown,
        onKeyUp: handleKeyUp,
        onFocus,
        onBlur: (e) => {
          buttonRef.current?.classList.remove("kb-active");
          onBlur?.(e);
        },
        className: cn(
          buttonVariants({
            design
          }),
          sizeClasses[size][effectiveIconOnly ? "iconOnly" : "default"],
          isMultiState && !effectiveIconOnly && "transition-[width] duration-300 ease-out overflow-hidden",
          loading && "relative pointer-events-none",
          className
        ),
        style: {
          ...style,
          ...buttonWidth !== void 0 && !effectiveIconOnly ? { width: buttonWidth } : {}
        },
        "data-testid": dataTestId,
        "data-growing-button": dataGrowingButton,
        children: [
          loading && /* @__PURE__ */ jsxRuntime.jsx("span", { className: "absolute inset-0 flex items-center justify-center z-[1] [&_*]:!text-inherit", inert: true, children: /* @__PURE__ */ jsxRuntime.jsx(BusyIndicator, { size: "S", active: true, delay: 0 }) }),
          effectiveIcon ? /* @__PURE__ */ jsxRuntime.jsx("span", { className: cn("inline-flex items-center justify-center shrink-0", sizeClasses[size].icon, loading && "opacity-40"), style: isMultiState ? iconAnimationStyles : void 0, children: effectiveIcon }) : null,
          !effectiveIconOnly && effectiveChildren && /* @__PURE__ */ jsxRuntime.jsx("span", { className: loading ? "opacity-40" : void 0, style: isMultiState ? contentAnimationStyles : void 0, children: effectivePulsing ? /* @__PURE__ */ jsxRuntime.jsx(PulsingText, { children: effectiveChildren }) : effectiveChildren }),
          effectiveEndIcon && /* @__PURE__ */ jsxRuntime.jsx("span", { className: cn("inline-flex items-center", sizeClasses[size].icon, loading && "opacity-40"), style: isMultiState ? contentAnimationStyles : void 0, children: effectiveEndIcon })
        ]
      }
    ),
    isMultiState && /* @__PURE__ */ jsxRuntime.jsxs(
      "button",
      {
        ref: hiddenButtonRef,
        className: cn(
          buttonVariants({
            design
          }),
          sizeClasses[size][!currentStateObject?.text && currentStateObject?.icon ? "iconOnly" : "default"],
          "absolute -top-[10000px] -left-[10000px] invisible w-fit"
        ),
        tabIndex: -1,
        "aria-hidden": "true",
        children: [
          currentStateObject?.icon && /* @__PURE__ */ jsxRuntime.jsx("span", { children: currentStateObject.icon }),
          currentStateObject?.text && /* @__PURE__ */ jsxRuntime.jsx("span", { children: currentStateObject.text }),
          currentStateObject?.endIcon && /* @__PURE__ */ jsxRuntime.jsx("span", { children: currentStateObject.endIcon })
        ]
      }
    )
  ] });
}
function useFlyoutState(name, locked, disabled, skipFlyoutOnFocusRef) {
  const [isHovered, setIsHovered] = React45.useState(false);
  const [isFocused, setIsFocused] = React45.useState(false);
  const [isAddButtonFocused, setIsAddButtonFocused] = React45.useState(false);
  const isMounted = typeof window !== "undefined";
  const hoverTimeoutRef = React45.useRef(null);
  const isMouseInsideRef = React45.useRef(false);
  const prevLockedRef = React45.useRef(locked);
  const context = useFxSideNavigationContext();
  React45.useEffect(() => {
    if (context?.registerFlyoutClose) {
      const closeHandler = () => {
        setIsHovered(false);
        setIsFocused(false);
      };
      context.registerFlyoutClose(name, closeHandler);
      return () => context.unregisterFlyoutClose(name);
    }
  }, [context, name]);
  React45.useEffect(() => {
    if (locked && !prevLockedRef.current) {
      setTimeout(() => {
        setIsHovered(false);
        setIsFocused(false);
      }, 0);
    } else if (!locked && prevLockedRef.current) {
      if (isMouseInsideRef.current) {
        setTimeout(() => {
          setIsHovered(true);
        }, 0);
      }
    }
    prevLockedRef.current = locked;
  }, [locked]);
  const clearHoverTimeout = React45.useCallback(() => {
    if (hoverTimeoutRef.current) {
      clearTimeout(hoverTimeoutRef.current);
      hoverTimeoutRef.current = null;
    }
  }, []);
  React45.useEffect(() => {
    return () => {
      clearHoverTimeout();
    };
  }, [clearHoverTimeout]);
  const handleMouseEnter = React45.useCallback(() => {
    isMouseInsideRef.current = true;
    if (locked || disabled) return;
    clearHoverTimeout();
    context?.closeOtherFlyouts(name);
    setIsHovered(true);
  }, [locked, disabled, clearHoverTimeout, context, name]);
  const handleMouseLeave = React45.useCallback(() => {
    isMouseInsideRef.current = false;
    if (!isFocused && !isAddButtonFocused) {
      hoverTimeoutRef.current = setTimeout(() => {
        setIsHovered(false);
      }, 100);
    }
  }, [isFocused, isAddButtonFocused]);
  const handleFocus = React45.useCallback(() => {
    if (locked || disabled) return;
    if (skipFlyoutOnFocusRef?.current) return;
    clearHoverTimeout();
    context?.closeOtherFlyouts(name);
    setIsFocused(true);
    setIsHovered(true);
  }, [locked, disabled, clearHoverTimeout, context, name, skipFlyoutOnFocusRef]);
  const handleBlur = React45.useCallback((e) => {
    const relatedTarget = e.relatedTarget;
    const container = e.currentTarget.closest(".relative");
    if (relatedTarget) {
      if (container?.contains(relatedTarget)) {
        return;
      }
      if (relatedTarget.closest('[data-flyout-child="true"]')) {
        return;
      }
    }
    setIsFocused(false);
    if (!isMouseInsideRef.current) {
      hoverTimeoutRef.current = setTimeout(() => {
        setIsHovered(false);
      }, 100);
    }
  }, []);
  const handleFlyoutMouseEnter = React45.useCallback(() => {
    isMouseInsideRef.current = true;
    clearHoverTimeout();
  }, [clearHoverTimeout]);
  const handleFlyoutMouseLeave = React45.useCallback(() => {
    isMouseInsideRef.current = false;
    if (!isFocused && !isAddButtonFocused) {
      hoverTimeoutRef.current = setTimeout(() => {
        setIsHovered(false);
      }, 100);
    }
  }, [isFocused, isAddButtonFocused]);
  return {
    isHovered,
    isFocused,
    isAddButtonFocused,
    isMounted,
    setIsHovered,
    setIsFocused,
    setIsAddButtonFocused,
    handleMouseEnter,
    handleMouseLeave,
    handleFocus,
    handleBlur,
    handleFlyoutMouseEnter,
    handleFlyoutMouseLeave
  };
}
function useItemInteractions(name, disabled, onClick, setIsHovered, setIsFocused, itemRef) {
  const context = useFxSideNavigationContext();
  const handleClick2 = React45.useCallback(
    (e) => {
      e.preventDefault();
      if (disabled) return;
      setIsHovered?.(false);
      setIsFocused?.(false);
      if (onClick) {
        onClick(e);
      } else {
        context?.onItemClick(name);
      }
      setTimeout(() => {
        itemRef?.current?.focus();
      }, 0);
    },
    [disabled, onClick, context, name, setIsHovered, setIsFocused, itemRef]
  );
  const handleKeyDown = React45.useCallback(
    (e) => {
      if (e.key === "ArrowDown") {
        e.preventDefault();
        setIsHovered?.(false);
        setIsFocused?.(false);
        context?.navigateToItem(name, "next");
      } else if (e.key === "ArrowUp") {
        e.preventDefault();
        setIsHovered?.(false);
        setIsFocused?.(false);
        context?.navigateToItem(name, "prev");
      } else if (e.key === "Enter" || e.key === " ") {
        e.preventDefault();
        if (disabled) return;
        setIsHovered?.(false);
        setIsFocused?.(false);
        if (onClick) {
          onClick(e);
        } else {
          context?.onItemClick(name);
        }
        setTimeout(() => {
          itemRef?.current?.focus();
        }, 0);
      } else if (e.key === "Escape") {
        setIsHovered?.(false);
        setIsFocused?.(false);
        itemRef?.current?.blur();
      }
    },
    [context, name, disabled, onClick, setIsHovered, setIsFocused, itemRef]
  );
  return {
    handleClick: handleClick2,
    handleKeyDown
  };
}
function FxSideNavigationItem({
  name,
  icon,
  text,
  badge,
  showAddButton = false,
  addButtonTooltip,
  selected = false,
  disabled = false,
  locked = false,
  avatar,
  onClick,
  onAddClick,
  className,
  ariaRoleDescription,
  ref
}) {
  const itemRef = React45.useRef(null);
  const flyoutRef = React45.useRef(null);
  const skipFlyoutOnFocusRef = React45.useRef(false);
  const focusTimerRef = React45.useRef(null);
  const rafRef = React45.useRef(null);
  const flagResetTimerRef = React45.useRef(null);
  const [showForcedFocusRing, setShowForcedFocusRing] = React45.useState(false);
  const context = useFxSideNavigationContext();
  const collapsed = context?.collapsed ?? false;
  const isTransitioning = context?.isTransitioning ?? false;
  const isFixedSection = context?.isFixedSection ?? false;
  const flyoutState = useFlyoutState(name, locked, disabled, skipFlyoutOnFocusRef);
  React45.useEffect(() => {
    if (context?.registerItemRef && itemRef.current) {
      context.registerItemRef(name, itemRef.current);
      return () => context.registerItemRef(name, null);
    }
  }, [context, name]);
  React45.useEffect(() => {
    return () => {
      if (focusTimerRef.current !== null) clearTimeout(focusTimerRef.current);
      if (rafRef.current !== null) cancelAnimationFrame(rafRef.current);
      if (flagResetTimerRef.current !== null) clearTimeout(flagResetTimerRef.current);
    };
  }, []);
  const { handleClick: handleClick2, handleKeyDown } = useItemInteractions(
    name,
    disabled,
    onClick,
    flyoutState.setIsHovered,
    flyoutState.setIsFocused,
    itemRef
  );
  const { setIsAddButtonFocused, setIsHovered, setIsFocused } = flyoutState;
  const handleAddClick = React45.useCallback(
    (detail) => {
      detail.originalEvent.stopPropagation();
      onAddClick?.();
      if (collapsed) {
        skipFlyoutOnFocusRef.current = true;
        setIsAddButtonFocused(false);
        setIsHovered(false);
        setIsFocused(false);
        focusTimerRef.current = setTimeout(() => {
          itemRef.current?.blur();
          rafRef.current = requestAnimationFrame(() => {
            if (itemRef.current) {
              setShowForcedFocusRing(true);
              itemRef.current.focus();
            }
            flagResetTimerRef.current = setTimeout(() => {
              skipFlyoutOnFocusRef.current = false;
            }, 50);
          });
        }, 100);
      }
    },
    [onAddClick, setIsAddButtonFocused, setIsHovered, setIsFocused, collapsed]
  );
  React45.useImperativeHandle(ref, () => ({
    closeFlyout: () => {
      flyoutState.setIsHovered(false);
      flyoutState.setIsFocused(false);
    },
    get nativeElement() {
      return itemRef.current;
    },
    getNativeElement: () => itemRef.current
  }));
  const settledCollapsed = collapsed && !isTransitioning;
  const showFlyout = flyoutState.isMounted && settledCollapsed && (flyoutState.isHovered || flyoutState.isFocused || flyoutState.isAddButtonFocused) && !locked;
  const canShowAddButton = showAddButton && !isFixedSection;
  const showBadgeDot = settledCollapsed && !showFlyout && badge;
  const Element2 = isFixedSection ? "button" : "a";
  const elementProps = isFixedSection ? { type: "button" } : { href: "#" };
  const content = /* @__PURE__ */ jsxRuntime.jsxs(
    "div",
    {
      className: cn(
        "relative flex items-center",
        !settledCollapsed && canShowAddButton && "gap-2 rounded pr-3",
        !settledCollapsed && canShowAddButton && selected && "bg-sapphire-chrome-button-bg-selected",
        !settledCollapsed && canShowAddButton && !disabled && !selected && "hover:bg-sapphire-chrome-button-bg-selected",
        !settledCollapsed && canShowAddButton && !flyoutState.isAddButtonFocused && "focus-within:ring-2 focus-within:ring-ring focus-within:ring-offset-0"
      ),
      style: { zIndex: showFlyout ? 50 : "auto" },
      onMouseEnter: flyoutState.handleMouseEnter,
      onMouseLeave: flyoutState.handleMouseLeave,
      children: [
        /* @__PURE__ */ jsxRuntime.jsxs(
          Element2,
          {
            ref: itemRef,
            ...elementProps,
            "data-name": name,
            className: cn(
              "fx-side-nav-item relative",
              "flex items-center text-sm rounded cursor-pointer no-underline overflow-hidden min-w-10",
              !settledCollapsed && "gap-3",
              !settledCollapsed && (canShowAddButton ? "flex-1" : "w-full"),
              selected ? "text-sapphire-text-accent" : "text-sapphire-chrome-button-fg-default",
              "focus-visible:outline-none",
              !canShowAddButton && "focus-visible:ring-2 focus-visible:ring-ring",
              showForcedFocusRing && settledCollapsed && !showFlyout && "ring-2 ring-ring",
              settledCollapsed && !showFlyout && !disabled && "hover:bg-sapphire-chrome-button-bg-selected",
              settledCollapsed && selected && !showFlyout && "bg-sapphire-chrome-button-bg-selected",
              !settledCollapsed && !canShowAddButton && selected && "bg-sapphire-chrome-button-bg-selected",
              !settledCollapsed && !canShowAddButton && !disabled && !selected && "hover:bg-sapphire-chrome-button-bg-selected",
              settledCollapsed && showFlyout && "opacity-0",
              disabled && "opacity-50 cursor-not-allowed pointer-events-none",
              isFixedSection && "border-0 bg-transparent text-left",
              className
            ),
            style: {
              padding: "0.625rem"
            },
            onClick: handleClick2,
            onKeyDown: handleKeyDown,
            onFocus: flyoutState.handleFocus,
            onBlur: (e) => {
              flyoutState.handleBlur(e);
              setShowForcedFocusRing(false);
            },
            onMouseEnter: !settledCollapsed ? flyoutState.handleMouseEnter : void 0,
            onMouseLeave: !settledCollapsed ? flyoutState.handleMouseLeave : void 0,
            title: settledCollapsed && !showFlyout ? text : void 0,
            "aria-expanded": settledCollapsed ? showFlyout : void 0,
            "aria-current": selected ? "page" : void 0,
            "aria-label": settledCollapsed ? text : badge ? `${text} (${badge})` : text,
            "aria-roledescription": !isFixedSection ? ariaRoleDescription : void 0,
            "data-label": text,
            children: [
              /* @__PURE__ */ jsxRuntime.jsx("span", { className: "shrink-0 flex items-center justify-center h-5 w-5", "aria-hidden": "true", children: avatar && isFixedSection ? avatar : icon }),
              !settledCollapsed && /* @__PURE__ */ jsxRuntime.jsxs(jsxRuntime.Fragment, { children: [
                /* @__PURE__ */ jsxRuntime.jsx(
                  "span",
                  {
                    className: "flex-1 truncate text-left font-semibold whitespace-nowrap overflow-hidden",
                    style: {
                      opacity: collapsed ? 0 : 1,
                      transition: isTransitioning ? "opacity 150ms ease" : "none"
                    },
                    children: text
                  }
                ),
                badge && /* @__PURE__ */ jsxRuntime.jsx(
                  "span",
                  {
                    className: "flex items-center justify-center min-w-4 h-4 px-1 text-xs font-semibold rounded-full bg-sapphire-negative text-sapphire-text-on-surface border border-sapphire-text-on-surface shrink-0",
                    style: {
                      fontSize: "0.625rem",
                      opacity: collapsed ? 0 : 1,
                      transition: isTransitioning ? "opacity 150ms ease" : "none"
                    },
                    "aria-hidden": "true",
                    children: badge
                  }
                )
              ] })
            ]
          }
        ),
        canShowAddButton && !collapsed && /* @__PURE__ */ jsxRuntime.jsx(
          Button,
          {
            design: "Tertiary",
            iconOnly: true,
            icon: /* @__PURE__ */ jsxRuntime.jsx(AddIcon, { className: "h-4 w-4" }),
            onClick: handleAddClick,
            disabled,
            tooltip: addButtonTooltip,
            "aria-label": addButtonTooltip || `Add new ${text?.toLowerCase()}`,
            tabIndex: 0,
            onFocus: () => setIsAddButtonFocused(true),
            onBlur: () => setIsAddButtonFocused(false),
            className: "h-5 w-5 text-sapphire-chrome-button-fg-default shrink-0",
            style: {
              minWidth: "1.25rem",
              minHeight: "1.25rem",
              borderRadius: "0.25rem"
            }
          }
        ),
        showBadgeDot && /* @__PURE__ */ jsxRuntime.jsx(
          "span",
          {
            className: "absolute w-2 h-2 rounded-full pointer-events-none bg-sapphire-negative",
            style: { top: "2px", right: collapsed ? "9px" : "2px" },
            "aria-hidden": "true"
          }
        ),
        showFlyout && /* @__PURE__ */ jsxRuntime.jsxs(
          "div",
          {
            ref: flyoutRef,
            "data-name": name,
            "data-flyout-child": "true",
            onMouseEnter: flyoutState.handleFlyoutMouseEnter,
            onMouseLeave: flyoutState.handleFlyoutMouseLeave,
            onClick: handleClick2,
            className: cn(
              "absolute top-0 left-0 flex items-center gap-3 text-sm rounded p-2.5 pr-3 cursor-pointer",
              "shadow-[0_4px_12px_rgba(0,0,0,0.15)]",
              flyoutState.isFocused && !flyoutState.isAddButtonFocused && "ring-2 ring-ring",
              disabled && "opacity-50 cursor-not-allowed",
              selected ? "bg-sapphire-chrome-button-bg-selected text-sapphire-text-accent" : "bg-sapphire-chrome-button-bg-default text-sapphire-chrome-button-fg-default"
            ),
            style: {
              whiteSpace: "nowrap",
              position: "absolute",
              zIndex: 10
            },
            children: [
              /* @__PURE__ */ jsxRuntime.jsx("span", { className: "shrink-0 flex items-center justify-center h-5 w-5", "aria-hidden": "true", children: avatar && isFixedSection ? avatar : icon }),
              /* @__PURE__ */ jsxRuntime.jsx("span", { className: "whitespace-nowrap font-semibold", "aria-hidden": "true", children: text }),
              (badge || canShowAddButton) && /* @__PURE__ */ jsxRuntime.jsxs("span", { className: "flex items-center gap-2 shrink-0", children: [
                badge && /* @__PURE__ */ jsxRuntime.jsx(
                  "span",
                  {
                    className: "flex items-center justify-center min-w-4 h-4 px-1 text-xs font-semibold rounded-full bg-sapphire-negative text-sapphire-text-on-surface border border-sapphire-text-on-surface",
                    style: { fontSize: "0.625rem" },
                    "aria-hidden": "true",
                    children: badge
                  }
                ),
                canShowAddButton && /* @__PURE__ */ jsxRuntime.jsx(
                  Button,
                  {
                    design: "Tertiary",
                    iconOnly: true,
                    icon: /* @__PURE__ */ jsxRuntime.jsx(AddIcon, { className: "h-4 w-4" }),
                    onClick: handleAddClick,
                    disabled,
                    tooltip: addButtonTooltip,
                    "aria-label": addButtonTooltip || `Add new ${text?.toLowerCase()}`,
                    tabIndex: 0,
                    onFocus: () => setIsAddButtonFocused(true),
                    onBlur: (e) => {
                      setIsAddButtonFocused(false);
                      const relatedTarget = e.relatedTarget;
                      const container = e.currentTarget.closest(".relative");
                      if (!relatedTarget || !container?.contains(relatedTarget)) {
                        setIsHovered(false);
                      }
                    },
                    className: "h-5 w-5 text-sapphire-chrome-button-fg-default",
                    style: {
                      minWidth: "1.25rem",
                      minHeight: "1.25rem",
                      borderRadius: "0.25rem"
                    }
                  }
                )
              ] })
            ]
          }
        )
      ]
    }
  );
  if (isFixedSection) {
    return /* @__PURE__ */ jsxRuntime.jsx("div", { children: content });
  }
  return /* @__PURE__ */ jsxRuntime.jsx("li", { className: "list-none", children: content });
}
var DATA_ATTR = "data-sap-ui-fastnavgroup";
var CONTAINER_ATTR = "data-sap-ui-fastnavgroup-container";
var isF6Next = (e) => e.key === "F6" && !e.shiftKey && !e.ctrlKey && !e.altKey && !e.metaKey || e.key === "ArrowDown" && e.ctrlKey && e.altKey && !e.shiftKey && !e.metaKey;
var isF6Previous = (e) => e.key === "F6" && e.shiftKey && !e.ctrlKey && !e.altKey && !e.metaKey || e.key === "ArrowUp" && e.ctrlKey && e.altKey && !e.shiftKey && !e.metaKey;
var isElementVisible = (el) => {
  let current = el;
  while (current && current !== document.documentElement) {
    const style = window.getComputedStyle(current);
    if (style.display === "none" || style.visibility === "hidden" || style.opacity === "0" || style.width === "0px" && style.height === "0px") {
      return false;
    }
    current = current.parentElement;
  }
  return true;
};
var FOCUSABLE_SELECTOR = [
  "a[href]",
  "button:not([disabled])",
  "input:not([disabled])",
  "select:not([disabled])",
  "textarea:not([disabled])",
  '[tabindex]:not([tabindex="-1"])',
  '[contenteditable="true"]'
].join(",");
var getFirstFocusableElement = (container) => {
  if (container.matches(FOCUSABLE_SELECTOR) && isElementVisible(container)) {
    return container;
  }
  const candidates = container.querySelectorAll(FOCUSABLE_SELECTOR);
  for (const el of candidates) {
    if (isElementVisible(el)) return el;
  }
  return null;
};
var collectGroups = (root) => {
  const groups = [];
  const walk = (el) => {
    if (el.getAttribute(DATA_ATTR) === "true") {
      if (isElementVisible(el)) {
        groups.push(el);
      }
    }
    let child = el.firstElementChild;
    while (child) {
      if (child.nodeType === 1) walk(child);
      child = child.nextElementSibling;
    }
  };
  walk(root);
  return groups;
};
var findContainer = () => {
  const el = document.activeElement;
  const scope = el?.closest(`[${CONTAINER_ATTR}="true"]`);
  return scope || document.body;
};
var findSelectedGroup = (groups) => {
  let el = document.activeElement;
  while (el && el !== document.documentElement) {
    if (el.getAttribute(DATA_ATTR) === "true") {
      const idx = groups.indexOf(el);
      if (idx !== -1) return idx;
    }
    el = el.parentElement;
  }
  return -1;
};
var focusNextGroup = (groups, currentIndex) => {
  for (const _group of groups) {
    currentIndex = currentIndex + 1 >= groups.length ? 0 : currentIndex + 1;
    const target = getFirstFocusableElement(groups[currentIndex]);
    if (target) {
      target.focus();
      return;
    }
  }
};
var focusPreviousGroup = (groups, currentIndex) => {
  let startIndex = currentIndex;
  if (startIndex < 0) {
    startIndex = groups.length;
  }
  const currentFocusable = currentIndex >= 0 ? getFirstFocusableElement(groups[currentIndex]) : null;
  let distance = 1;
  if (currentIndex > 0 && currentFocusable) {
    for (let d = 1; d < groups.length; d++) {
      const prevIdx = currentIndex - d;
      if (prevIdx < 0) break;
      const prevFocusable = getFirstFocusableElement(groups[prevIdx]);
      if (prevFocusable === currentFocusable) {
        distance++;
      } else {
        break;
      }
    }
  }
  let targetIndex = startIndex - distance;
  if (targetIndex < 0) targetIndex = groups.length - 1;
  for (const _group of groups) {
    const target = getFirstFocusableElement(groups[targetIndex]);
    if (target) {
      target.focus();
      return;
    }
    targetIndex = targetIndex - 1 < 0 ? groups.length - 1 : targetIndex - 1;
  }
};
var handleKeydown = (event) => {
  const forward = isF6Next(event);
  const backward = isF6Previous(event);
  if (!forward && !backward) return;
  const container = findContainer();
  const groups = collectGroups(container);
  if (groups.length === 0) return;
  event.preventDefault();
  const currentIndex = findSelectedGroup(groups);
  if (forward) {
    focusNextGroup(groups, currentIndex);
  } else {
    focusPreviousGroup(groups, currentIndex);
  }
};
function useF6Navigation() {
  React45.useEffect(() => {
    document.addEventListener("keydown", handleKeydown);
    return () => document.removeEventListener("keydown", handleKeydown);
  }, []);
}
var pathData3 = [
  "M342 255q48 23 77 67.5t29 99.5v32q0 11-7.5 18.5T422 480H90q-11 0-18.5-7.5T64 454v-32q0-56 29-100t77-67q-20-18-31-42.5T128 160q0-27 10-50t27.5-40.5 41-27.5T256 32t49.5 10.5 41 28T374 111t10 49q0 27-11 52t-31 43zM256 83q-32 0-54.5 22.5T179 160t22.5 54.5T256 237t54.5-22.5T333 160t-22.5-54.5T256 83zm141 339q0-28-10.5-52.5t-29-42.5-43-28.5T262 288h-12q-28 0-52.5 10.5t-43 28.5-29 42.5T115 422v7h282v-7z"
];
var PersonPlaceholderIcon = Object.assign(
  React45__namespace.default.forwardRef(
    (props, ref) => /* @__PURE__ */ jsxRuntime.jsx(Icon, { ref, pathData: pathData3, ...props })
  ),
  { displayName: "PersonPlaceholderIcon", iconName: "person-placeholder" }
);
var avatarVariants = cva(
  [
    "inline-flex items-center justify-center",
    "font-medium overflow-hidden",
    "select-none"
  ],
  {
    variants: {
      shape: {
        ["Circle" /* Circle */]: "rounded-full",
        ["Square" /* Square */]: ""
      },
      size: {
        ["XS" /* XS */]: "h-[2rem] w-[2rem] text-[1rem]",
        ["S" /* S */]: "h-[3rem] w-[3rem] text-[1.125rem]",
        ["M" /* M */]: "h-[4rem] w-[4rem] text-[1.5rem]",
        ["L" /* L */]: "h-[5rem] w-[5rem] text-[2.25rem]",
        ["XL" /* XL */]: "h-[7rem] w-[7rem] text-[3rem]"
      },
      colorScheme: {
        ["Accent1" /* Accent1 */]: "bg-[var(--avatar-accent-bg-1)] text-[var(--avatar-accent-color-1)]",
        ["Accent2" /* Accent2 */]: "bg-[var(--avatar-accent-bg-2)] text-[var(--avatar-accent-color-2)]",
        ["Accent3" /* Accent3 */]: "bg-[var(--avatar-accent-bg-3)] text-[var(--avatar-accent-color-3)]",
        ["Accent4" /* Accent4 */]: "bg-[var(--avatar-accent-bg-4)] text-[var(--avatar-accent-color-4)]",
        ["Accent5" /* Accent5 */]: "bg-[var(--avatar-accent-bg-5)] text-[var(--avatar-accent-color-5)]",
        ["Accent6" /* Accent6 */]: "bg-[var(--avatar-accent-bg-6)] text-[var(--avatar-accent-color-6)]",
        ["Accent7" /* Accent7 */]: "bg-[var(--avatar-accent-bg-7)] text-[var(--avatar-accent-color-7)]",
        ["Accent8" /* Accent8 */]: "bg-[var(--avatar-accent-bg-8)] text-[var(--avatar-accent-color-8)]",
        ["Accent9" /* Accent9 */]: "bg-[var(--avatar-accent-bg-9)] text-[var(--avatar-accent-color-9)]",
        ["Accent10" /* Accent10 */]: "bg-[var(--avatar-accent-bg-10)] text-[var(--avatar-accent-color-10)]",
        ["Placeholder" /* Placeholder */]: "bg-muted text-sapphire-text-tertiary"
      },
      interactive: {
        true: "cursor-pointer hover:opacity-80 focus:outline-none focus:ring-2 focus:ring-ring focus:ring-offset-2",
        false: ""
      },
      disabled: {
        true: "opacity-50 cursor-not-allowed",
        false: ""
      }
    },
    compoundVariants: [
      {
        shape: "Square" /* Square */,
        size: "XS" /* XS */,
        className: "rounded-sapphire-s"
        // 4px — Figma token --radius-s
      },
      {
        shape: "Square" /* Square */,
        size: "S" /* S */,
        className: "rounded-[6px]"
        // 6px — hardcoded in Figma
      },
      {
        shape: "Square" /* Square */,
        size: "M" /* M */,
        className: "rounded-sapphire-m"
        // 8px — Figma token --radius-m
      },
      {
        shape: "Square" /* Square */,
        size: "L" /* L */,
        className: "rounded-[12px]"
        // 12px — hardcoded in Figma
      },
      {
        shape: "Square" /* Square */,
        size: "XL" /* XL */,
        className: "rounded-sapphire-l"
        // 16px — Figma token --radius-l
      },
      // Neutralize interactive hover/focus/cursor when disabled
      {
        interactive: true,
        disabled: true,
        className: "cursor-not-allowed hover:opacity-50 focus:ring-0 focus:ring-offset-0"
      }
    ],
    defaultVariants: {
      shape: "Circle" /* Circle */,
      size: "M" /* M */,
      colorScheme: "Accent6" /* Accent6 */,
      interactive: false,
      disabled: false
    }
  }
);
var iconSizeMap = {
  ["XS" /* XS */]: "h-[1rem] w-[1rem]",
  ["S" /* S */]: "h-[1.125rem] w-[1.125rem]",
  ["M" /* M */]: "h-[1.5rem] w-[1.5rem]",
  ["L" /* L */]: "h-[2.25rem] w-[2.25rem]",
  ["XL" /* XL */]: "h-[3rem] w-[3rem]"
};
function Avatar({
  shape = "Circle" /* Circle */,
  size = "M" /* M */,
  colorScheme = "Accent6" /* Accent6 */,
  icon,
  image,
  fallbackImage,
  initials,
  interactive = false,
  disabled = false,
  accessibleName = "Avatar",
  badge,
  onClick,
  className,
  style,
  ref
}) {
  const avatarRef = React45.useRef(null);
  const [imageError, setImageError] = React45.useState(false);
  const [fallbackError, setFallbackError] = React45.useState(false);
  React45.useImperativeHandle(ref, () => ({
    focus: () => avatarRef.current?.focus(),
    isFocused: () => document.activeElement === avatarRef.current,
    get nativeElement() {
      return avatarRef.current;
    },
    getNativeElement: () => avatarRef.current
  }));
  const handleImageError = () => {
    if (!imageError) {
      setImageError(true);
    } else if (fallbackImage && !fallbackError) {
      setFallbackError(true);
    }
  };
  const handleClick2 = (e) => {
    if (!disabled && interactive && onClick) {
      onClick(e);
    }
  };
  const handleKeyDown = (e) => {
    if (!disabled && interactive && (e.key === "Enter" || e.key === " ")) {
      e.preventDefault();
      onClick?.(e);
    }
  };
  const showImage = image && !imageError;
  const showFallbackImage = !showImage && fallbackImage && !fallbackError;
  const showInitials = !showImage && !showFallbackImage && initials;
  const showIcon = !showImage && !showFallbackImage && !showInitials;
  const iconSize = iconSizeMap[size] || iconSizeMap["M" /* M */];
  return /* @__PURE__ */ jsxRuntime.jsxs("div", { className: "relative inline-block", children: [
    /* @__PURE__ */ jsxRuntime.jsxs(
      "div",
      {
        ref: avatarRef,
        role: interactive ? "button" : "img",
        tabIndex: interactive && !disabled ? 0 : void 0,
        "aria-label": showInitials ? `${accessibleName} ${initials}` : accessibleName,
        "aria-disabled": disabled,
        onClick: handleClick2,
        onKeyDown: handleKeyDown,
        className: cn(
          avatarVariants({
            shape,
            size,
            colorScheme,
            interactive,
            disabled
          }),
          className
        ),
        style,
        children: [
          showImage && /* @__PURE__ */ jsxRuntime.jsx(
            "img",
            {
              src: image,
              alt: accessibleName || "",
              className: "h-full w-full object-cover",
              onError: handleImageError
            }
          ),
          showFallbackImage && /* @__PURE__ */ jsxRuntime.jsx(
            "img",
            {
              src: fallbackImage,
              alt: accessibleName || "",
              className: "h-full w-full object-cover",
              onError: handleImageError
            }
          ),
          showInitials && /* @__PURE__ */ jsxRuntime.jsx("span", { className: "uppercase", children: initials.slice(0, 3) }),
          showIcon && (icon ? /* @__PURE__ */ jsxRuntime.jsx("span", { className: cn(iconSize, "flex items-center justify-center"), children: icon }) : /* @__PURE__ */ jsxRuntime.jsx(PersonPlaceholderIcon, { className: iconSize }))
        ]
      }
    ),
    badge && /* @__PURE__ */ jsxRuntime.jsx("div", { className: "absolute top-0 right-0", children: badge })
  ] });
}
var scrollbarStyle = {
  overflowY: "auto",
  overflowX: "hidden",
  scrollbarWidth: "thin",
  scrollbarColor: "var(--color-neutral-400) transparent"
};
var PANE_MIN_WIDTH = 320;
var NAVIGATION_COMPACT_THRESHOLD = 600;
var SIDEBAR_COLLAPSED_WIDTH = 80;
var SIDEBAR_EXPANDED_WIDTH = 256;
var NAV_ITEM_HEIGHT = 40;
var NAV_ITEM_GAP = 24;
var NAV_HEADER_HEIGHT = 80;
var NAV_NORMAL_TOP_SPACER = 160;
var NAV_MERGED_MIN_SPACER = 0;
var NAV_BOTTOM_PADDING = 24;
var NAV_MERGED_DIVIDER_HEIGHT = 13;
var BREAKPOINT_HYSTERESIS = 24;
function calculateItemsHeight(count) {
  if (count === 0) return 0;
  return count * NAV_ITEM_HEIGHT + (count - 1) * NAV_ITEM_GAP;
}
function calculateVerticalBreakpoints(flexibleItemCount, fixedItemCount) {
  const mergeBreakpoint = NAV_HEADER_HEIGHT + NAV_NORMAL_TOP_SPACER + calculateItemsHeight(flexibleItemCount) + calculateItemsHeight(fixedItemCount) + NAV_BOTTOM_PADDING + 16;
  const totalItems = flexibleItemCount + fixedItemCount;
  const compactBreakpoint = NAV_HEADER_HEIGHT + NAV_MERGED_MIN_SPACER + calculateItemsHeight(totalItems) + NAV_MERGED_DIVIDER_HEIGHT + NAV_BOTTOM_PADDING;
  return { mergeBreakpoint, compactBreakpoint };
}
var FxLayoutContext = React45.createContext(null);
var useFxLayoutContext = () => {
  const context = React45.useContext(FxLayoutContext);
  return context;
};
var KEYBOARD_RESIZE_STEP = 50;
var ResizeHandle = ({
  separator,
  startPaneRef,
  centerPaneRef,
  endPaneRef,
  onResizeEnd,
  onResetToDefault,
  ariaLabel
}) => {
  const handleRef = React45.useRef(null);
  const [isDragging, setIsDragging] = React45.useState(false);
  const [isFocused, setIsFocused] = React45.useState(false);
  const [ariaValueNow, setAriaValueNow] = React45.useState(50);
  const computeAriaValue = React45.useCallback(() => {
    if (separator === "start") {
      const startW = startPaneRef.current?.offsetWidth || 0;
      const centerW = centerPaneRef.current?.offsetWidth || 0;
      const total = startW + centerW;
      if (total > 0) setAriaValueNow(Math.round(startW / total * 100));
    } else {
      const centerW = centerPaneRef.current?.offsetWidth || 0;
      const endW = endPaneRef.current?.offsetWidth || 0;
      const total = centerW + endW;
      if (total > 0) setAriaValueNow(Math.round(centerW / total * 100));
    }
  }, [separator, startPaneRef, centerPaneRef, endPaneRef]);
  React45.useEffect(() => {
    computeAriaValue();
  }, [computeAriaValue]);
  const handleKeyDown = (e) => {
    if (e.key === "Home") {
      e.preventDefault();
      onResetToDefault();
      return;
    }
    if (e.key !== "ArrowLeft" && e.key !== "ArrowRight") return;
    e.preventDefault();
    const startPane = startPaneRef.current;
    const centerPane = centerPaneRef.current;
    const endPane = endPaneRef.current;
    if (!startPane || !centerPane) return;
    const delta = e.key === "ArrowRight" ? KEYBOARD_RESIZE_STEP : -KEYBOARD_RESIZE_STEP;
    if (separator === "start") {
      let newStartWidth = startPane.offsetWidth + delta;
      let newCenterWidth = centerPane.offsetWidth - delta;
      if (newStartWidth < PANE_MIN_WIDTH) {
        newStartWidth = PANE_MIN_WIDTH;
        newCenterWidth = startPane.offsetWidth + centerPane.offsetWidth - PANE_MIN_WIDTH;
      }
      if (newCenterWidth < PANE_MIN_WIDTH) {
        newCenterWidth = PANE_MIN_WIDTH;
        newStartWidth = startPane.offsetWidth + centerPane.offsetWidth - PANE_MIN_WIDTH;
      }
      onResizeEnd(newStartWidth, newCenterWidth, endPane?.offsetWidth || 0);
      requestAnimationFrame(computeAriaValue);
    } else {
      if (!endPane) return;
      let newCenterWidth = centerPane.offsetWidth + delta;
      let newEndWidth = endPane.offsetWidth - delta;
      if (newEndWidth < PANE_MIN_WIDTH) {
        newEndWidth = PANE_MIN_WIDTH;
        newCenterWidth = centerPane.offsetWidth + endPane.offsetWidth - PANE_MIN_WIDTH;
      }
      if (newCenterWidth < PANE_MIN_WIDTH) {
        newCenterWidth = PANE_MIN_WIDTH;
        newEndWidth = centerPane.offsetWidth + endPane.offsetWidth - PANE_MIN_WIDTH;
      }
      onResizeEnd(startPane.offsetWidth, newCenterWidth, newEndWidth);
      requestAnimationFrame(computeAriaValue);
    }
  };
  const handleMouseDown = (e) => {
    e.preventDefault();
    const startPane = startPaneRef.current;
    const centerPane = centerPaneRef.current;
    const endPane = endPaneRef.current;
    if (!startPane || !centerPane) return;
    const initialStartWidth = startPane.offsetWidth;
    const initialCenterWidth = centerPane.offsetWidth;
    const initialEndWidth = endPane?.offsetWidth || 0;
    const startX = e.clientX;
    const originalStartTransition = startPane.style.transition;
    const originalCenterTransition = centerPane.style.transition;
    const originalEndTransition = endPane?.style.transition || "";
    startPane.style.transition = "none";
    centerPane.style.transition = "none";
    if (endPane) endPane.style.transition = "none";
    setIsDragging(true);
    document.body.style.cursor = "col-resize";
    document.body.style.userSelect = "none";
    const handleMouseMove = (ev) => {
      const delta = ev.clientX - startX;
      if (separator === "start") {
        let newStartWidth = initialStartWidth + delta;
        let newCenterWidth = initialCenterWidth - delta;
        if (newStartWidth < PANE_MIN_WIDTH) {
          newStartWidth = PANE_MIN_WIDTH;
          newCenterWidth = initialStartWidth + initialCenterWidth - PANE_MIN_WIDTH;
        }
        if (newCenterWidth < PANE_MIN_WIDTH) {
          newCenterWidth = PANE_MIN_WIDTH;
          newStartWidth = initialStartWidth + initialCenterWidth - PANE_MIN_WIDTH;
        }
        startPane.style.width = `${newStartWidth}px`;
        centerPane.style.width = `${newCenterWidth}px`;
      } else {
        if (!endPane) return;
        let newCenterWidth = initialCenterWidth + delta;
        let newEndWidth = initialEndWidth - delta;
        if (newEndWidth < PANE_MIN_WIDTH) {
          newEndWidth = PANE_MIN_WIDTH;
          newCenterWidth = initialCenterWidth + initialEndWidth - PANE_MIN_WIDTH;
        }
        if (newCenterWidth < PANE_MIN_WIDTH) {
          newCenterWidth = PANE_MIN_WIDTH;
          newEndWidth = initialCenterWidth + initialEndWidth - PANE_MIN_WIDTH;
        }
        centerPane.style.width = `${newCenterWidth}px`;
        endPane.style.width = `${newEndWidth}px`;
      }
    };
    const handleMouseUp = () => {
      document.body.style.cursor = "";
      document.body.style.userSelect = "";
      document.removeEventListener("mousemove", handleMouseMove);
      document.removeEventListener("mouseup", handleMouseUp);
      startPane.style.transition = originalStartTransition;
      centerPane.style.transition = originalCenterTransition;
      if (endPane) endPane.style.transition = originalEndTransition;
      setIsDragging(false);
      handleRef.current?.focus();
      const finalStartWidth = startPane.offsetWidth;
      const finalCenterWidth = centerPane.offsetWidth;
      const finalEndWidth = endPane?.offsetWidth || 0;
      onResizeEnd(finalStartWidth, finalCenterWidth, finalEndWidth);
      requestAnimationFrame(computeAriaValue);
    };
    document.addEventListener("mousemove", handleMouseMove);
    document.addEventListener("mouseup", handleMouseUp);
  };
  return /* @__PURE__ */ jsxRuntime.jsx(
    "div",
    {
      ref: handleRef,
      role: "separator",
      "aria-orientation": "vertical",
      "aria-label": ariaLabel,
      "aria-valuenow": ariaValueNow,
      "aria-valuemin": 0,
      "aria-valuemax": 100,
      tabIndex: 0,
      className: cn(
        "absolute inset-0 cursor-col-resize z-10",
        "after:content-[''] after:absolute after:top-0 after:bottom-0 after:left-1/2 after:-translate-x-1/2",
        "after:w-px after:bg-border after:transition-all",
        "hover:after:w-0.5 hover:after:bg-sapphire-chrome-button-bg-selected",
        isDragging && "after:w-0.5 after:bg-sapphire-chrome-button-bg-selected",
        isFocused && "after:w-0.5 after:bg-sapphire-border-accent"
      ),
      style: { outline: "none" },
      onMouseDown: handleMouseDown,
      onDoubleClick: onResetToDefault,
      onKeyDown: handleKeyDown,
      onFocus: () => setIsFocused(true),
      onBlur: () => setIsFocused(false)
    }
  );
};
function calculateMaxPanes(width) {
  if (width >= 1965) {
    return 3;
  }
  if (width >= 1020) {
    return 2;
  }
  return 1;
}
var InputContainer = ({
  input,
  inputInEndPane,
  mobileLayout,
  effectiveShowStart,
  effectiveShowEnd,
  finalStartWidth,
  finalEndWidth,
  paneAnimationsEnabled
}) => {
  let inputLeft;
  let inputRight;
  if (mobileLayout) {
    inputLeft = "1rem";
    inputRight = "1rem";
  } else if (inputInEndPane) {
    inputLeft = `calc(100% - ${finalEndWidth} + 1.25rem)`;
    inputRight = "1.25rem";
  } else {
    inputLeft = effectiveShowStart ? `calc(${finalStartWidth} + 1.25rem)` : "1.25rem";
    inputRight = effectiveShowEnd ? `calc(${finalEndWidth} + 1.25rem)` : "1.25rem";
  }
  return /* @__PURE__ */ jsxRuntime.jsx(
    "div",
    {
      className: "pointer-events-none flex justify-center",
      style: {
        position: "absolute",
        bottom: "1rem",
        left: inputLeft,
        right: inputRight,
        zIndex: 2,
        transition: paneAnimationsEnabled ? "left 350ms cubic-bezier(0.4, 0, 0.2, 1), right 350ms cubic-bezier(0.4, 0, 0.2, 1)" : "none",
        padding: "0 0.5rem 0.5rem 0.5rem",
        margin: "0 -0.5rem -0.5rem -0.5rem"
      },
      children: /* @__PURE__ */ jsxRuntime.jsx(
        "div",
        {
          className: "pointer-events-auto w-full",
          style: { maxWidth: inputInEndPane ? void 0 : "600px" },
          children: input
        }
      )
    }
  );
};
function FxLayout({
  id,
  hideStart: hideStartProp = false,
  hideEnd: hideEndProp = false,
  suppressEnd = false,
  priorityPane: priorityPaneProp = "Center",
  mode = "",
  notificationsBadge,
  notificationsLocked = false,
  navItems = [],
  startHeader,
  centerHeader,
  endHeader,
  startContent,
  centerContent,
  endContent,
  userMenu,
  input,
  onVisibilityChange,
  onModeChange,
  onLayoutChange,
  onAddClick,
  onNotificationsClick,
  className,
  style,
  children,
  ref
}) {
  const layoutRef = React45.useRef(null);
  const sideNavRef = React45.useRef(null);
  const notificationsItemRef = React45.useRef(null);
  const userItemRef = React45.useRef(null);
  const navDialogCloseRef = React45.useRef(null);
  const startPaneRef = React45.useRef(null);
  const centerPaneRef = React45.useRef(null);
  const endPaneRef = React45.useRef(null);
  useF6Navigation();
  const { t } = reactI18next.useTranslation("fx");
  const startContentRef = React45.useRef(null);
  const centerContentRef = React45.useRef(null);
  const endContentRef = React45.useRef(null);
  const [startScrolled, setStartScrolled] = React45.useState(false);
  const [centerScrolled, setCenterScrolled] = React45.useState(false);
  const [endScrolled, setEndScrolled] = React45.useState(false);
  const [navCollapsed, setNavCollapsed] = React45.useState(true);
  const [navTransitioning, setNavTransitioning] = React45.useState(false);
  const [navTransitionDirection, setNavTransitionDirection] = React45.useState(null);
  const navTransitionTimeoutRef = React45.useRef(null);
  const [horizontalCompactMode, setHorizontalCompactMode] = React45.useState(false);
  const [verticalCompactMode, setVerticalCompactMode] = React45.useState(false);
  const [navMerged, setNavMerged] = React45.useState(false);
  const [navigationOpen, setNavigationOpen] = React45.useState(false);
  const [navigationClosing, setNavigationClosing] = React45.useState(false);
  const [maxPanes, setMaxPanes] = React45.useState(3);
  const [totalWidth, setTotalWidth] = React45.useState(0);
  const compactMode = horizontalCompactMode || verticalCompactMode;
  const mobileLayout = horizontalCompactMode;
  const [animationsEnabled, setAnimationsEnabled] = React45.useState(false);
  const [internalHideStart, setInternalHideStart] = React45.useState(hideStartProp);
  const [internalHideEnd, setInternalHideEnd] = React45.useState(hideEndProp);
  const [internalPriorityPane, setInternalPriorityPane] = React45.useState(priorityPaneProp);
  const [userMenuOpen, setUserMenuOpen] = React45.useState(false);
  const [userMenuOpener, setUserMenuOpener] = React45.useState(null);
  const [userMenuFromHamburger, setUserMenuFromHamburger] = React45.useState(false);
  const _userMenuAccounts = userMenu?.props?.accounts;
  const _selectedUserAccount = _userMenuAccounts?.find((a) => a.selected) || _userMenuAccounts?.[0];
  React45.useEffect(() => {
    const timer = setTimeout(() => {
      setAnimationsEnabled(true);
    }, 100);
    return () => clearTimeout(timer);
  }, []);
  const paneAnimationsEnabled = animationsEnabled && maxPanes > 1;
  const toggleNavCollapsed = React45.useCallback(() => {
    const willCollapse = !navCollapsed;
    if (animationsEnabled) {
      setNavTransitioning(true);
      setNavTransitionDirection(willCollapse ? "collapsing" : "expanding");
      if (navTransitionTimeoutRef.current) clearTimeout(navTransitionTimeoutRef.current);
      navTransitionTimeoutRef.current = setTimeout(() => {
        setNavTransitioning(false);
        setNavTransitionDirection(null);
      }, 400);
    }
    setNavCollapsed(willCollapse);
  }, [navCollapsed, animationsEnabled]);
  const handleNavTransitionEnd = React45.useCallback((e) => {
    if (e.propertyName === "width" && e.target === e.currentTarget) {
      setNavTransitioning(false);
      setNavTransitionDirection(null);
      if (navTransitionTimeoutRef.current) {
        clearTimeout(navTransitionTimeoutRef.current);
        navTransitionTimeoutRef.current = null;
      }
    }
  }, []);
  React45.useEffect(() => {
    return () => {
      if (navTransitionTimeoutRef.current) clearTimeout(navTransitionTimeoutRef.current);
    };
  }, []);
  const previousTotalWidthRef = React45.useRef(0);
  const lastMaxPanesRef = React45.useRef(3);
  const isInternalStartChangeRef = React45.useRef(false);
  const isInternalEndChangeRef = React45.useRef(false);
  const isInternalPriorityChangeRef = React45.useRef(false);
  React45.useEffect(() => {
    if (!isInternalStartChangeRef.current) {
      setInternalHideStart(hideStartProp);
    }
    isInternalStartChangeRef.current = false;
  }, [hideStartProp]);
  React45.useEffect(() => {
    if (!isInternalEndChangeRef.current) {
      setInternalHideEnd(hideEndProp);
    }
    isInternalEndChangeRef.current = false;
  }, [hideEndProp]);
  React45.useEffect(() => {
    if (!isInternalPriorityChangeRef.current) {
      setInternalPriorityPane(priorityPaneProp);
    }
    isInternalPriorityChangeRef.current = false;
  }, [priorityPaneProp]);
  const currentNavItem = React45.useMemo(
    () => navItems.find((item) => item.name === mode),
    [navItems, mode]
  );
  const isSettingsMode = mode === "settings";
  const allowStart = isSettingsMode || !currentNavItem?.noStartPane;
  const allowEnd = !suppressEnd && !isSettingsMode;
  const utilityEndPane = currentNavItem?.utilityEndPane ?? false;
  const externalEndPaneInput = currentNavItem?.externalEndPaneInput ?? false;
  const internalHideCenter = false;
  const requestedPaneCount = React45.useMemo(() => {
    return (internalHideStart || !allowStart ? 0 : 1) + 1 + (internalHideEnd || !allowEnd ? 0 : 1);
  }, [internalHideStart, internalHideEnd, allowStart, allowEnd]);
  const effectivePriority = React45.useMemo(() => {
    if (requestedPaneCount <= maxPanes) {
      return "Center";
    }
    return internalPriorityPane;
  }, [requestedPaneCount, maxPanes, internalPriorityPane]);
  React45.useEffect(() => {
    if (requestedPaneCount <= maxPanes && internalPriorityPane !== "Center") {
      setInternalPriorityPane("Center");
      onLayoutChange?.({ priorityPane: "Center" });
    }
  }, [requestedPaneCount, maxPanes, internalPriorityPane, onLayoutChange]);
  const effectiveShowStart = React45.useMemo(() => {
    if (!allowStart) {
      return false;
    }
    if (internalHideStart) {
      return false;
    }
    if (effectivePriority === "Start") {
      return true;
    }
    let centerPriority = 0;
    if (effectivePriority === "Center") {
      centerPriority = 2;
    } else if (effectivePriority === "End") {
      centerPriority = 1;
    }
    const endPriority = effectivePriority === "End" ? 2 : 1;
    let higherPriorityCount = 0;
    if (centerPriority > 0) {
      higherPriorityCount++;
    }
    if (!internalHideEnd && allowEnd && !suppressEnd && !internalHideCenter && endPriority > 0) {
      higherPriorityCount++;
    }
    if (higherPriorityCount >= maxPanes) {
      return false;
    }
    return true;
  }, [allowStart, internalHideStart, effectivePriority, internalHideCenter, internalHideEnd, allowEnd, suppressEnd, maxPanes]);
  const effectiveShowCenter = React45.useMemo(() => {
    if (effectivePriority === "Center") {
      return true;
    }
    let higherPriorityCount = 0;
    if (effectivePriority === "Start" && !internalHideStart && allowStart) {
      higherPriorityCount++;
    }
    if (effectivePriority === "End" && !internalHideEnd && allowEnd) {
      higherPriorityCount++;
    }
    if (higherPriorityCount >= maxPanes) {
      return false;
    }
    return true;
  }, [internalHideCenter, effectivePriority, internalHideStart, allowStart, internalHideEnd, allowEnd, maxPanes]);
  const effectiveShowEnd = React45.useMemo(() => {
    if (!allowEnd) {
      return false;
    }
    if (suppressEnd) {
      return false;
    }
    if (internalHideEnd) {
      return false;
    }
    if (effectivePriority === "End") {
      return true;
    }
    let higherPriorityCount = 0;
    if (effectivePriority === "Start" && !internalHideStart && allowStart) {
      higherPriorityCount++;
    }
    {
      higherPriorityCount++;
    }
    if (higherPriorityCount >= maxPanes) {
      return false;
    }
    return true;
  }, [allowEnd, suppressEnd, internalHideEnd, effectivePriority, internalHideCenter, internalHideStart, allowStart, maxPanes]);
  const hideInputEntirely = externalEndPaneInput && effectiveShowEnd;
  const inputInCenterPane = !hideInputEntirely && (!effectiveShowEnd || utilityEndPane);
  const inputInEndPane = !hideInputEntirely && effectiveShowEnd && !utilityEndPane;
  const inputOverlayPadding = mobileLayout ? "8rem" : "7.5rem";
  const inputFadeMask = `linear-gradient(180deg, #000 0%, #000 calc(100% - ${mobileLayout ? "4.5rem" : "4rem"}), transparent calc(100% - ${mobileLayout ? "2.5rem" : "2rem"}))`;
  const fixedItemCount = 3;
  const { mergeBreakpoint, compactBreakpoint } = React45.useMemo(
    () => calculateVerticalBreakpoints(navItems.length, fixedItemCount),
    [navItems.length]
  );
  const prevVerticalCompactRef = React45.useRef(false);
  React45.useEffect(() => {
    if (!layoutRef.current) return;
    const handleResize = (width, height) => {
      const previousTotalWidth = previousTotalWidthRef.current;
      const browserShrinking = previousTotalWidth > 0 && width < previousTotalWidth;
      previousTotalWidthRef.current = width;
      setTotalWidth(width);
      const isHorizontalCompact = width < NAVIGATION_COMPACT_THRESHOLD;
      setHorizontalCompactMode((prev) => {
        if (prev && !isHorizontalCompact) {
          setUserMenuOpen(false);
        }
        return isHorizontalCompact;
      });
      if (!isHorizontalCompact && height > 0) {
        const shouldMerge = height < mergeBreakpoint;
        setNavMerged(shouldMerge);
        let shouldVerticalCompact;
        if (prevVerticalCompactRef.current) {
          shouldVerticalCompact = height < compactBreakpoint + BREAKPOINT_HYSTERESIS;
        } else {
          shouldVerticalCompact = height < compactBreakpoint;
        }
        prevVerticalCompactRef.current = shouldVerticalCompact;
        setVerticalCompactMode(shouldVerticalCompact);
      } else {
        prevVerticalCompactRef.current = false;
        setNavMerged(false);
        setVerticalCompactMode(false);
      }
      const currentNavWidth = isHorizontalCompact ? 0 : navCollapsed ? SIDEBAR_COLLAPSED_WIDTH : SIDEBAR_EXPANDED_WIDTH;
      const mainWidth = width - currentNavWidth;
      const newMaxPanes = calculateMaxPanes(mainWidth);
      if (newMaxPanes === lastMaxPanesRef.current) {
        return;
      }
      if (browserShrinking && !isHorizontalCompact && !navCollapsed) {
        const expandedNavMainWidth = width - SIDEBAR_EXPANDED_WIDTH;
        const collapsedNavMainWidth = width - SIDEBAR_COLLAPSED_WIDTH;
        const maxPanesWithExpanded = calculateMaxPanes(expandedNavMainWidth);
        const maxPanesWithCollapsed = calculateMaxPanes(collapsedNavMainWidth);
        if (maxPanesWithCollapsed > maxPanesWithExpanded) {
          setNavCollapsed(true);
          if (animationsEnabled) {
            setNavTransitioning(true);
            setNavTransitionDirection("collapsing");
            if (navTransitionTimeoutRef.current) clearTimeout(navTransitionTimeoutRef.current);
            navTransitionTimeoutRef.current = setTimeout(() => {
              setNavTransitioning(false);
              setNavTransitionDirection(null);
            }, 400);
          }
          const collapsedMaxPanes = calculateMaxPanes(collapsedNavMainWidth);
          setMaxPanes(collapsedMaxPanes);
          lastMaxPanesRef.current = collapsedMaxPanes;
          return;
        }
      }
      setMaxPanes(newMaxPanes);
      lastMaxPanesRef.current = newMaxPanes;
    };
    const observer = new ResizeObserver((entries) => {
      const entry = entries[0];
      if (entry) {
        handleResize(entry.contentRect.width, entry.contentRect.height);
      }
    });
    observer.observe(layoutRef.current);
    return () => {
      observer.disconnect();
    };
  }, [navCollapsed, mergeBreakpoint, compactBreakpoint]);
  const prevStartVisibleRef = React45.useRef(null);
  const prevEndVisibleRef = React45.useRef(null);
  const onVisibilityChangeRef = React45.useRef(onVisibilityChange);
  onVisibilityChangeRef.current = onVisibilityChange;
  React45.useEffect(() => {
    if (prevStartVisibleRef.current === null || prevStartVisibleRef.current !== effectiveShowStart) {
      onVisibilityChangeRef.current?.({
        pane: "start",
        visible: effectiveShowStart
      });
    }
    prevStartVisibleRef.current = effectiveShowStart;
  }, [effectiveShowStart]);
  React45.useEffect(() => {
    if (prevEndVisibleRef.current === null || prevEndVisibleRef.current !== effectiveShowEnd) {
      onVisibilityChangeRef.current?.({
        pane: "end",
        visible: effectiveShowEnd
      });
    }
    prevEndVisibleRef.current = effectiveShowEnd;
  }, [effectiveShowEnd]);
  React45.useEffect(() => {
    if (!mobileLayout) return;
    startContentRef.current?.scrollTo({ top: 0 });
    centerContentRef.current?.scrollTo({ top: 0 });
    endContentRef.current?.scrollTo({ top: 0 });
  }, [mobileLayout, effectiveShowStart, effectiveShowCenter, effectiveShowEnd, internalPriorityPane]);
  React45.useImperativeHandle(ref, () => ({
    focusPane: () => {
    },
    closeProfileFlyout: () => {
      setUserMenuOpen(false);
      setUserMenuFromHamburger(false);
    },
    get nativeElement() {
      return layoutRef.current;
    },
    getNativeElement: () => layoutRef.current
  }));
  const handleNavSelect = React45.useCallback(
    (key) => {
      const previousMode = mode;
      if (key !== previousMode) {
        onModeChange?.({ mode: key, previousMode });
      }
      if (navigationOpen) {
        setNavigationOpen(false);
      }
    },
    [mode, onModeChange, navigationOpen]
  );
  const toggleStartPane = React45.useCallback(() => {
    const shouldHide = effectiveShowStart;
    let newPriority;
    if (shouldHide) {
      newPriority = "Center";
    } else {
      const requestedAfter = (!shouldHide ? 1 : 0) + 1 + (internalHideEnd ? 0 : 1);
      newPriority = requestedAfter <= maxPanes ? "Center" : "Start";
    }
    isInternalStartChangeRef.current = true;
    isInternalPriorityChangeRef.current = true;
    setInternalHideStart(shouldHide);
    setInternalPriorityPane(newPriority);
    onLayoutChange?.({ priorityPane: newPriority, hideStart: shouldHide });
  }, [effectiveShowStart, internalHideEnd, maxPanes, onLayoutChange]);
  const toggleEndPane = React45.useCallback(() => {
    const currentlyVisible = effectiveShowEnd;
    const newHideEnd = currentlyVisible;
    let newPriority;
    if (currentlyVisible) {
      newPriority = "Center";
    } else {
      const requestedPanes = (internalHideStart ? 0 : 1) + 1 + 1;
      newPriority = requestedPanes <= maxPanes ? "Center" : "End";
    }
    isInternalEndChangeRef.current = true;
    isInternalPriorityChangeRef.current = true;
    setInternalHideEnd(newHideEnd);
    setInternalPriorityPane(newPriority);
    onLayoutChange?.({ priorityPane: newPriority, hideEnd: newHideEnd });
  }, [effectiveShowEnd, internalHideStart, maxPanes, onLayoutChange]);
  const openNavigation = React45.useCallback(() => {
    setNavigationOpen(true);
  }, []);
  const closeNavigation = React45.useCallback(() => {
    setNavigationClosing(true);
    setTimeout(() => {
      setNavigationOpen(false);
      setNavigationClosing(false);
    }, 300);
  }, []);
  React45.useEffect(() => {
    if (!compactMode && navigationOpen) {
      setNavigationOpen(false);
      setNavigationClosing(false);
    }
  }, [compactMode, navigationOpen]);
  React45.useEffect(() => {
    if (navigationOpen && navDialogCloseRef.current) {
      navDialogCloseRef.current.focus();
    }
  }, [navigationOpen]);
  const navWidth = compactMode ? 0 : navCollapsed ? SIDEBAR_COLLAPSED_WIDTH : SIDEBAR_EXPANDED_WIDTH;
  const mainAreaWidth = totalWidth - navWidth;
  const defaultWidths = React45.useMemo(() => {
    const visibleCount = (effectiveShowStart ? 1 : 0) + (effectiveShowCenter ? 1 : 0) + (effectiveShowEnd ? 1 : 0);
    if (visibleCount === 1) {
      if (effectiveShowStart) return { startWidth: "100%", endWidth: "0%" };
      if (effectiveShowEnd) return { startWidth: "0%", endWidth: "100%" };
      return { startWidth: "0%", endWidth: "0%" };
    }
    if (visibleCount === 2) {
      if (effectiveShowStart && effectiveShowCenter && !effectiveShowEnd) {
        return { startWidth: "33%", endWidth: "0%" };
      } else if (!effectiveShowStart && effectiveShowCenter && effectiveShowEnd) {
        return { startWidth: "0%", endWidth: "33%" };
      } else if (effectiveShowStart && !effectiveShowCenter && effectiveShowEnd) {
        return { startWidth: "33%", endWidth: "67%" };
      }
    }
    return { startWidth: "25%", endWidth: "25%" };
  }, [effectiveShowStart, effectiveShowCenter, effectiveShowEnd]);
  const [userStartWidth, setUserStartWidth] = React45.useState(null);
  const [userEndWidth, setUserEndWidth] = React45.useState(null);
  React45.useEffect(() => {
    setUserStartWidth(null);
    setUserEndWidth(null);
  }, [effectiveShowStart, effectiveShowCenter, effectiveShowEnd]);
  const toPercent = React45.useCallback((px) => {
    if (mainAreaWidth <= 0) return "0%";
    return `${(px / mainAreaWidth * 100).toFixed(2)}%`;
  }, [mainAreaWidth]);
  const finalStartWidth = effectiveShowStart ? userStartWidth ?? defaultWidths.startWidth : "0%";
  const finalEndWidth = effectiveShowEnd ? userEndWidth ?? defaultWidths.endWidth : "0%";
  const handleResizeEnd = React45.useCallback((startWidthPx, _centerWidthPx, endWidthPx) => {
    setUserStartWidth(toPercent(startWidthPx));
    setUserEndWidth(toPercent(endWidthPx));
  }, [toPercent]);
  const handleResetToDefault = React45.useCallback(() => {
    setUserStartWidth(null);
    setUserEndWidth(null);
  }, []);
  const inputMode = React45.useMemo(() => {
    const isInEndPane = effectiveShowEnd && !utilityEndPane;
    return isInEndPane ? "oneline" : "multiline";
  }, [effectiveShowEnd, utilityEndPane]);
  const leftmostVisiblePane = React45.useMemo(() => {
    if (effectiveShowStart) return "start";
    if (effectiveShowCenter) return "center";
    return "end";
  }, [effectiveShowStart, effectiveShowCenter]);
  const contextValue = React45.useMemo(
    () => ({
      startVisible: effectiveShowStart,
      endVisible: effectiveShowEnd,
      maxPanes,
      compactMode,
      leftmostVisiblePane,
      allowStart,
      allowEnd,
      suppressEnd,
      utilityEndPane,
      isSettingsMode,
      inputMode,
      toggleStartPane,
      toggleEndPane,
      openNavigation
    }),
    [effectiveShowStart, effectiveShowEnd, maxPanes, compactMode, leftmostVisiblePane, allowStart, allowEnd, suppressEnd, utilityEndPane, isSettingsMode, inputMode, toggleStartPane, toggleEndPane, openNavigation]
  );
  return /* @__PURE__ */ jsxRuntime.jsx(FxLayoutContext.Provider, { value: contextValue, children: /* @__PURE__ */ jsxRuntime.jsxs(
    "div",
    {
      ref: layoutRef,
      id,
      "data-mode": mode,
      "data-hide-start": !effectiveShowStart ? "" : void 0,
      "data-hide-end": !effectiveShowEnd ? "" : void 0,
      className: cn(
        "flex h-full w-full overflow-hidden relative bg-sapphire-canvas-primary",
        className
      ),
      style: {
        // Use dvh on mobile for accurate viewport height (accounts for browser chrome)
        ...mobileLayout ? { height: "100dvh" } : {},
        ...style
      },
      children: [
        !compactMode && /* @__PURE__ */ jsxRuntime.jsx(
          "div",
          {
            className: cn(
              "flex flex-col h-full shrink-0 relative",
              effectiveShowStart && "border-r border-border"
            ),
            "data-sap-ui-fastnavgroup": "true",
            style: {
              width: navWidth,
              transition: animationsEnabled ? "width 350ms cubic-bezier(0.4, 0, 0.2, 1)" : "none",
              overflow: "visible"
              // Allow flyouts to extend beyond nav column
            },
            onTransitionEnd: handleNavTransitionEnd,
            children: /* @__PURE__ */ jsxRuntime.jsx(
              FxSideNavigation,
              {
                ref: sideNavRef,
                collapsed: navCollapsed,
                isTransitioning: navTransitioning,
                transitionDirection: navTransitionDirection,
                forceMerged: navMerged,
                onSelectionChange: ({ name }) => handleNavSelect(name),
                header: /* @__PURE__ */ jsxRuntime.jsxs(
                  "button",
                  {
                    onClick: () => handleNavSelect(navItems[0]?.name || ""),
                    className: "cursor-pointer focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring rounded flex items-center gap-2",
                    children: [
                      /* @__PURE__ */ jsxRuntime.jsx(JouleIcon, { size: 32 }),
                      !navCollapsed && /* @__PURE__ */ jsxRuntime.jsxs("span", { className: "text-lg font-semibold whitespace-nowrap", children: [
                        /* @__PURE__ */ jsxRuntime.jsx("span", { className: "text-purple-600", children: "Joule" }),
                        /* @__PURE__ */ jsxRuntime.jsx("span", { className: "text-foreground", children: "Work" })
                      ] })
                    ]
                  }
                ),
                fixedItems: /* @__PURE__ */ jsxRuntime.jsxs(jsxRuntime.Fragment, { children: [
                  /* @__PURE__ */ jsxRuntime.jsx(
                    FxSideNavigationItem,
                    {
                      ref: notificationsItemRef,
                      name: "notifications",
                      icon: /* @__PURE__ */ jsxRuntime.jsx(BellIcon, { className: "h-4 w-4" }),
                      text: t("FX_NOTIFICATIONS"),
                      badge: notificationsBadge?.toString(),
                      selected: notificationsLocked,
                      locked: notificationsLocked,
                      onClick: () => {
                        const opener = notificationsItemRef.current?.getNativeElement();
                        if (opener) onNotificationsClick?.(opener);
                      }
                    }
                  ),
                  /* @__PURE__ */ jsxRuntime.jsx(
                    FxSideNavigationItem,
                    {
                      ref: userItemRef,
                      name: "profile",
                      text: _selectedUserAccount?.titleText || "User",
                      avatar: /* @__PURE__ */ jsxRuntime.jsx(
                        Avatar,
                        {
                          size: "XS",
                          shape: "Square",
                          image: _selectedUserAccount?.avatarSrc,
                          initials: _selectedUserAccount?.avatarInitials || "U",
                          colorScheme: _selectedUserAccount?.avatarColorScheme || "Accent1"
                        }
                      ),
                      selected: userMenuOpen,
                      locked: userMenuOpen,
                      onClick: () => {
                        if (userMenu) {
                          if (userMenuOpen) {
                            setUserMenuOpen(false);
                          } else {
                            const opener = userItemRef.current?.getNativeElement();
                            if (opener) setUserMenuOpener(opener);
                            setUserMenuOpen(true);
                          }
                        }
                      }
                    }
                  ),
                  /* @__PURE__ */ jsxRuntime.jsx(
                    FxSideNavigationItem,
                    {
                      name: "toggle",
                      icon: navCollapsed ? /* @__PURE__ */ jsxRuntime.jsx(OpenCommandFieldIcon, { className: "h-4 w-4" }) : /* @__PURE__ */ jsxRuntime.jsx(CloseCommandFieldIcon, { className: "h-4 w-4" }),
                      text: navCollapsed ? t("NAVIGATION_EXPAND") : t("NAVIGATION_COLLAPSE"),
                      onClick: toggleNavCollapsed
                    }
                  )
                ] }),
                children: navItems.map((item) => /* @__PURE__ */ jsxRuntime.jsx(
                  FxSideNavigationItem,
                  {
                    name: item.name,
                    icon: item.icon ? /* @__PURE__ */ jsxRuntime.jsx("span", { className: "h-4 w-4 flex items-center justify-center", children: item.icon }) : void 0,
                    text: item.text,
                    selected: mode === item.name,
                    showAddButton: !!item.createActionTooltip,
                    addButtonTooltip: item.createActionTooltip,
                    onAddClick: () => onAddClick?.({ mode: item.name })
                  },
                  item.name
                ))
              }
            )
          }
        ),
        /* @__PURE__ */ jsxRuntime.jsxs(
          "main",
          {
            className: "flex flex-1 h-full min-w-0 relative",
            style: {
              // In mobile layout, clip all overflow to prevent any child from making page wider
              overflow: mobileLayout ? "hidden" : "visible",
              overflowX: mobileLayout ? "clip" : void 0
            },
            children: [
              allowStart && /* @__PURE__ */ jsxRuntime.jsxs(
                "aside",
                {
                  ref: startPaneRef,
                  "aria-label": t("FX_START_PANE"),
                  className: cn(
                    "flex flex-col shrink-0 relative bg-sapphire-shell-bg-primary"
                  ),
                  style: {
                    width: effectiveShowStart ? mobileLayout ? "100%" : finalStartWidth : 0,
                    minWidth: effectiveShowStart ? mobileLayout ? 0 : PANE_MIN_WIDTH : 0,
                    height: "100%",
                    paddingTop: effectiveShowStart ? "1.5rem" : 0,
                    paddingInline: effectiveShowStart ? "0.25rem" : 0,
                    overflow: "hidden",
                    visibility: effectiveShowStart ? "visible" : "hidden",
                    transition: paneAnimationsEnabled ? `width 350ms cubic-bezier(0.4, 0, 0.2, 1), padding 350ms cubic-bezier(0.4, 0, 0.2, 1), visibility 0s ${effectiveShowStart ? "0s" : "350ms"}` : "none"
                  },
                  children: [
                    /* @__PURE__ */ jsxRuntime.jsx(
                      "div",
                      {
                        className: "shrink-0",
                        "data-sap-ui-fastnavgroup": "true",
                        style: {
                          paddingInline: effectiveShowStart ? mobileLayout ? "0.75rem" : "1rem" : 0,
                          opacity: effectiveShowStart ? 1 : 0,
                          transform: effectiveShowStart ? "translateX(0)" : "translateX(-30%)",
                          transition: paneAnimationsEnabled ? "opacity 350ms cubic-bezier(0.4, 0, 0.2, 1), transform 350ms cubic-bezier(0.4, 0, 0.2, 1)" : "none"
                        },
                        children: React45.isValidElement(startHeader) ? React45.cloneElement(startHeader, { showBorder: startScrolled }) : startHeader
                      }
                    ),
                    /* @__PURE__ */ jsxRuntime.jsx(
                      "div",
                      {
                        ref: startContentRef,
                        className: "flex-1 fx-pane-content min-h-0",
                        "data-sap-ui-fastnavgroup": "true",
                        onScrollCapture: (e) => {
                          const target = e.target;
                          setStartScrolled(target.scrollTop > 0);
                        },
                        style: {
                          ...scrollbarStyle,
                          opacity: effectiveShowStart ? 1 : 0,
                          transform: effectiveShowStart ? "translateX(0)" : "translateX(-30%)",
                          transition: paneAnimationsEnabled ? "opacity 350ms cubic-bezier(0.4, 0, 0.2, 1), transform 350ms cubic-bezier(0.4, 0, 0.2, 1)" : "none"
                        },
                        children: startContent
                      }
                    )
                  ]
                }
              ),
              effectiveShowStart && effectiveShowCenter && /* @__PURE__ */ jsxRuntime.jsx("div", { className: "w-0 shrink-0 relative", style: { marginLeft: "-8px", marginRight: "-8px", width: "16px", zIndex: 10 }, children: /* @__PURE__ */ jsxRuntime.jsx(
                ResizeHandle,
                {
                  separator: "start",
                  startPaneRef,
                  centerPaneRef,
                  endPaneRef,
                  onResizeEnd: handleResizeEnd,
                  onResetToDefault: handleResetToDefault,
                  ariaLabel: t("FX_RESIZE_START_CENTER")
                }
              ) }),
              /* @__PURE__ */ jsxRuntime.jsxs(
                "div",
                {
                  ref: centerPaneRef,
                  className: "flex flex-col relative bg-background border-l border-border",
                  style: {
                    boxShadow: "0 22px 14px 0 rgba(0,0,0,0.06), 0 182px 111px 0 rgba(0,0,0,0.05)",
                    flex: effectiveShowCenter ? "1 1 auto" : "0 0 0%",
                    minWidth: effectiveShowCenter ? mobileLayout ? 0 : PANE_MIN_WIDTH : 0,
                    height: "100%",
                    paddingTop: effectiveShowCenter ? "1.5rem" : 0,
                    paddingInline: effectiveShowCenter ? "0.25rem" : 0,
                    overflow: "hidden",
                    visibility: effectiveShowCenter ? "visible" : "hidden",
                    zIndex: 1,
                    transition: paneAnimationsEnabled ? `flex 350ms cubic-bezier(0.4, 0, 0.2, 1), padding 350ms cubic-bezier(0.4, 0, 0.2, 1), visibility 0s ${effectiveShowCenter ? "0s" : "350ms"}` : "none"
                  },
                  children: [
                    /* @__PURE__ */ jsxRuntime.jsx(
                      "div",
                      {
                        className: "shrink-0",
                        "data-sap-ui-fastnavgroup": "true",
                        style: {
                          paddingInline: effectiveShowCenter ? mobileLayout ? "0.75rem" : "1rem" : 0,
                          opacity: effectiveShowCenter ? 1 : 0,
                          transition: paneAnimationsEnabled ? "opacity 350ms cubic-bezier(0.4, 0, 0.2, 1)" : "none"
                        },
                        children: React45.isValidElement(centerHeader) ? React45.cloneElement(centerHeader, { showBorder: centerScrolled }) : centerHeader
                      }
                    ),
                    /* @__PURE__ */ jsxRuntime.jsxs(
                      "div",
                      {
                        ref: centerContentRef,
                        className: "flex-1 fx-pane-content min-h-0",
                        "data-sap-ui-fastnavgroup": "true",
                        onScrollCapture: (e) => {
                          const target = e.target;
                          setCenterScrolled(target.scrollTop > 0);
                        },
                        style: {
                          ...scrollbarStyle,
                          // Add padding at bottom when input is shown in center pane so content isn't covered
                          paddingBottom: input && inputInCenterPane ? inputOverlayPadding : 0,
                          opacity: effectiveShowCenter ? 1 : 0,
                          transition: paneAnimationsEnabled ? "opacity 350ms cubic-bezier(0.4, 0, 0.2, 1)" : "none",
                          // Fade content at bottom edge when input is shown in center pane
                          ...input && inputInCenterPane ? {
                            WebkitMaskImage: inputFadeMask,
                            maskImage: inputFadeMask
                          } : {}
                        },
                        children: [
                          centerContent,
                          children
                        ]
                      }
                    )
                  ]
                }
              ),
              effectiveShowCenter && effectiveShowEnd && /* @__PURE__ */ jsxRuntime.jsx("div", { className: "w-0 shrink-0 relative", style: { marginLeft: "-8px", marginRight: "-8px", width: "16px", zIndex: 10 }, children: /* @__PURE__ */ jsxRuntime.jsx(
                ResizeHandle,
                {
                  separator: "end",
                  startPaneRef,
                  centerPaneRef,
                  endPaneRef,
                  onResizeEnd: handleResizeEnd,
                  onResetToDefault: handleResetToDefault,
                  ariaLabel: t("FX_RESIZE_CENTER_END")
                }
              ) }),
              allowEnd && /* @__PURE__ */ jsxRuntime.jsxs(
                "aside",
                {
                  ref: endPaneRef,
                  "aria-label": t("FX_END_PANE"),
                  className: cn(
                    "flex flex-col shrink-0 relative bg-sapphire-shell-bg-primary",
                    !mobileLayout && "border-l border-border"
                  ),
                  style: {
                    width: effectiveShowEnd ? mobileLayout ? "100%" : finalEndWidth : 0,
                    minWidth: effectiveShowEnd ? mobileLayout ? 0 : PANE_MIN_WIDTH : 0,
                    height: "100%",
                    paddingTop: effectiveShowEnd ? "1.5rem" : 0,
                    paddingInline: effectiveShowEnd ? "0.25rem" : 0,
                    overflow: "hidden",
                    visibility: effectiveShowEnd ? "visible" : "hidden",
                    transition: paneAnimationsEnabled ? `width 350ms cubic-bezier(0.4, 0, 0.2, 1), padding 350ms cubic-bezier(0.4, 0, 0.2, 1), visibility 0s ${effectiveShowEnd ? "0s" : "350ms"}` : "none"
                  },
                  children: [
                    /* @__PURE__ */ jsxRuntime.jsx(
                      "div",
                      {
                        className: "shrink-0",
                        "data-sap-ui-fastnavgroup": "true",
                        style: {
                          paddingInline: effectiveShowEnd ? mobileLayout ? "0.75rem" : "1rem" : 0,
                          opacity: effectiveShowEnd ? 1 : 0,
                          transform: effectiveShowEnd ? "translateX(0)" : "translateX(30%)",
                          transition: paneAnimationsEnabled ? "opacity 350ms cubic-bezier(0.4, 0, 0.2, 1), transform 350ms cubic-bezier(0.4, 0, 0.2, 1)" : "none"
                        },
                        children: React45.isValidElement(endHeader) ? React45.cloneElement(endHeader, { showBorder: endScrolled }) : endHeader
                      }
                    ),
                    /* @__PURE__ */ jsxRuntime.jsx(
                      "div",
                      {
                        ref: endContentRef,
                        className: "flex-1 fx-pane-content min-h-0",
                        "data-sap-ui-fastnavgroup": "true",
                        onScrollCapture: (e) => {
                          const target = e.target;
                          setEndScrolled(target.scrollTop > 0);
                        },
                        style: {
                          ...scrollbarStyle,
                          // Add padding at bottom when input is shown in end pane so content isn't covered
                          paddingBottom: input && inputInEndPane ? inputOverlayPadding : 0,
                          opacity: effectiveShowEnd ? 1 : 0,
                          transform: effectiveShowEnd ? "translateX(0)" : "translateX(30%)",
                          transition: paneAnimationsEnabled ? "opacity 350ms cubic-bezier(0.4, 0, 0.2, 1), transform 350ms cubic-bezier(0.4, 0, 0.2, 1)" : "none",
                          // Fade content at bottom edge when input is shown in end pane
                          ...input && inputInEndPane ? {
                            WebkitMaskImage: inputFadeMask,
                            maskImage: inputFadeMask
                          } : {}
                        },
                        children: endContent
                      }
                    )
                  ]
                }
              ),
              input && !isSettingsMode && !hideInputEntirely && (effectiveShowCenter || inputInEndPane) && /* @__PURE__ */ jsxRuntime.jsx(
                InputContainer,
                {
                  input,
                  inputInEndPane,
                  mobileLayout,
                  effectiveShowStart,
                  effectiveShowEnd,
                  finalStartWidth,
                  finalEndWidth,
                  paneAnimationsEnabled
                }
              )
            ]
          }
        ),
        userMenu && React45__namespace.default.cloneElement(userMenu, {
          open: userMenuOpen,
          opener: userMenuOpener,
          useDialog: mobileLayout || userMenuFromHamburger,
          placement: "End",
          verticalAlign: "Top",
          offset: 16,
          onClose: () => {
            setUserMenuOpen(false);
            setUserMenuFromHamburger(false);
          },
          onBackClick: () => {
            setNavigationOpen(true);
          }
        }),
        compactMode && navigationOpen && /* @__PURE__ */ jsxRuntime.jsxs(
          "div",
          {
            className: "fixed inset-0 z-50 flex",
            onClick: closeNavigation,
            children: [
              /* @__PURE__ */ jsxRuntime.jsx(
                "div",
                {
                  className: "absolute inset-0",
                  style: {
                    backgroundColor: "color-mix(in srgb, var(--foreground) 50%, transparent)",
                    animation: navigationClosing ? "fadeOut 300ms cubic-bezier(0.4, 0, 0.2, 1) forwards" : "fadeIn 300ms cubic-bezier(0.4, 0, 0.2, 1)"
                  }
                }
              ),
              /* @__PURE__ */ jsxRuntime.jsxs(
                "div",
                {
                  className: "relative w-64 h-full border-r border-border flex flex-col shadow-xl bg-sapphire-shell-bg-primary",
                  style: {
                    animation: navigationClosing ? "slideOutToLeft 300ms cubic-bezier(0.4, 0, 0.2, 1) forwards" : "slideInFromLeft 300ms cubic-bezier(0.4, 0, 0.2, 1)"
                  },
                  onClick: (e) => e.stopPropagation(),
                  children: [
                    /* @__PURE__ */ jsxRuntime.jsxs("div", { className: "flex items-center h-14 px-2 border-b border-border shrink-0", children: [
                      /* @__PURE__ */ jsxRuntime.jsx(
                        Button,
                        {
                          ref: navDialogCloseRef,
                          design: "Tertiary",
                          iconOnly: true,
                          icon: /* @__PURE__ */ jsxRuntime.jsx(DeclineIcon, { className: "h-4 w-4" }),
                          onClick: closeNavigation,
                          className: "text-sapphire-text-primary"
                        }
                      ),
                      /* @__PURE__ */ jsxRuntime.jsxs(
                        "button",
                        {
                          onClick: () => handleNavSelect(navItems[0]?.name || ""),
                          className: "cursor-pointer flex items-center gap-2 ml-2 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring rounded",
                          children: [
                            /* @__PURE__ */ jsxRuntime.jsx(JouleIcon, { size: 28 }),
                            /* @__PURE__ */ jsxRuntime.jsxs("span", { className: "text-lg font-semibold", children: [
                              /* @__PURE__ */ jsxRuntime.jsx("span", { className: "text-purple-600", children: "Joule" }),
                              /* @__PURE__ */ jsxRuntime.jsx("span", { className: "text-foreground", children: "Work" })
                            ] })
                          ]
                        }
                      )
                    ] }),
                    /* @__PURE__ */ jsxRuntime.jsx("div", { className: "flex-1 overflow-y-auto", children: /* @__PURE__ */ jsxRuntime.jsx(
                      FxSideNavigation,
                      {
                        collapsed: false,
                        forceMerged: true,
                        dialogMode: true,
                        onSelectionChange: ({ name }) => handleNavSelect(name),
                        fixedItems: /* @__PURE__ */ jsxRuntime.jsxs(jsxRuntime.Fragment, { children: [
                          /* @__PURE__ */ jsxRuntime.jsx(
                            FxSideNavigationItem,
                            {
                              name: "notifications",
                              icon: /* @__PURE__ */ jsxRuntime.jsx(BellIcon, { className: "h-4 w-4" }),
                              text: t("FX_NOTIFICATIONS"),
                              badge: notificationsBadge?.toString(),
                              onClick: (e) => {
                                const opener = e.target.closest("button");
                                if (opener) onNotificationsClick?.(opener);
                              }
                            }
                          ),
                          /* @__PURE__ */ jsxRuntime.jsx(
                            FxSideNavigationItem,
                            {
                              name: "profile",
                              text: _selectedUserAccount?.titleText || "User",
                              avatar: /* @__PURE__ */ jsxRuntime.jsx(
                                Avatar,
                                {
                                  size: "XS",
                                  shape: "Square",
                                  image: _selectedUserAccount?.avatarSrc,
                                  initials: _selectedUserAccount?.avatarInitials || "U",
                                  colorScheme: _selectedUserAccount?.avatarColorScheme || "Accent1"
                                }
                              ),
                              onClick: () => {
                                if (userMenu) {
                                  setNavigationClosing(true);
                                  setTimeout(() => {
                                    setNavigationOpen(false);
                                    setNavigationClosing(false);
                                    setUserMenuFromHamburger(true);
                                    setUserMenuOpen(true);
                                  }, 300);
                                }
                              }
                            }
                          )
                        ] }),
                        children: navItems.map((item) => /* @__PURE__ */ jsxRuntime.jsx(
                          FxSideNavigationItem,
                          {
                            name: item.name,
                            icon: item.icon ? /* @__PURE__ */ jsxRuntime.jsx("span", { className: "h-4 w-4 flex items-center justify-center", children: item.icon }) : void 0,
                            text: item.text,
                            selected: mode === item.name,
                            showAddButton: !!item.createActionTooltip,
                            addButtonTooltip: item.createActionTooltip,
                            onAddClick
                          },
                          item.name
                        ))
                      }
                    ) })
                  ]
                }
              )
            ]
          }
        )
      ]
    }
  ) });
}
var toggleButtonVariants = cva(
  [
    "inline-flex items-center justify-center gap-2",
    "font-semibold whitespace-nowrap",
    "transition-[color,background-color,border-color,text-decoration-color,fill,stroke] cursor-pointer",
    "outline-none",
    "disabled:pointer-events-none disabled:opacity-40"
  ],
  {
    variants: {
      design: {
        ["Secondary" /* Secondary */]: "border focus:[outline-style:solid] focus:outline-2 focus:-outline-offset-2 focus:outline-ring",
        ["Primary" /* Primary */]: "focus:[outline-style:solid] focus:outline-2 focus:-outline-offset-2 focus:outline-sapphire-border-focus btn-filled-focus",
        ["PrimaryJoule" /* PrimaryJoule */]: "focus:[outline-style:solid] focus:outline-2 focus:-outline-offset-2 focus:outline-sapphire-border-joule btn-filled-focus",
        ["SecondaryJoule" /* SecondaryJoule */]: "border focus:[outline-style:solid] focus:outline-2 focus:-outline-offset-2 focus:outline-sapphire-border-joule",
        ["Neutral" /* Neutral */]: "border focus:[outline-style:solid] focus:outline-2 focus:-outline-offset-2 focus:outline-foreground",
        ["SecondaryNeutral" /* SecondaryNeutral */]: "border border-transparent focus:[outline-style:solid] focus:outline-2 focus:-outline-offset-2 focus:outline-foreground",
        ["TertiaryNeutral" /* TertiaryNeutral */]: "border border-transparent focus:[outline-style:solid] focus:outline-2 focus:-outline-offset-2 focus:outline-foreground",
        ["Tertiary" /* Tertiary */]: "border border-transparent focus:[outline-style:solid] focus:outline-2 focus:-outline-offset-2 focus:outline-ring",
        ["TertiaryJoule" /* TertiaryJoule */]: "border border-transparent focus:[outline-style:solid] focus:outline-2 focus:-outline-offset-2 focus:outline-sapphire-border-joule"
      },
      pressed: {
        true: "",
        false: ""
      }
    },
    compoundVariants: [
      // Secondary
      {
        design: "Secondary" /* Secondary */,
        pressed: false,
        className: "bg-transparent text-sapphire-brand-foreground border-sapphire-brand-foreground hover:bg-sapphire-neutral-hover-background active:bg-sapphire-neutral-pressed-background active:border-sapphire-brand-pressed-foreground active:text-sapphire-brand-pressed-foreground"
      },
      {
        design: "Secondary" /* Secondary */,
        pressed: true,
        className: "bg-sapphire-brand-selected-background text-sapphire-brand-foreground border-transparent hover:bg-sapphire-brand-toggle-background hover:border-sapphire-border-toggle"
      },
      // Primary
      {
        design: "Primary" /* Primary */,
        pressed: false,
        className: "bg-sapphire-brand-background/20 text-sapphire-brand-foreground hover:bg-sapphire-brand-background/30"
      },
      {
        design: "Primary" /* Primary */,
        pressed: true,
        className: "bg-sapphire-brand-hover-background text-sapphire-neutral-foreground-white hover:bg-blue-800"
      },
      // PrimaryJoule
      {
        design: "PrimaryJoule" /* PrimaryJoule */,
        pressed: false,
        className: "bg-sapphire-joule-background/20 text-sapphire-joule-foreground hover:bg-sapphire-joule-background/30"
      },
      {
        design: "PrimaryJoule" /* PrimaryJoule */,
        pressed: true,
        className: "bg-sapphire-joule-hover-background text-sapphire-neutral-foreground-white hover:bg-purple-800"
      },
      // SecondaryJoule
      {
        design: "SecondaryJoule" /* SecondaryJoule */,
        pressed: false,
        className: "bg-transparent text-sapphire-joule-foreground border-sapphire-joule-foreground hover:bg-sapphire-neutral-hover-background active:bg-sapphire-neutral-pressed-background active:border-sapphire-joule-pressed-foreground active:text-sapphire-joule-pressed-foreground"
      },
      {
        design: "SecondaryJoule" /* SecondaryJoule */,
        pressed: true,
        className: "bg-sapphire-joule-background-area text-sapphire-joule-foreground border-transparent hover:bg-sapphire-joule-background-light hover:border-sapphire-border-toggle"
      },
      // Neutral
      {
        design: "Neutral" /* Neutral */,
        pressed: false,
        className: "bg-transparent text-sapphire-neutral-foreground-black border-sapphire-neutral-foreground-muted hover:bg-sapphire-neutral-hover-background hover:text-sapphire-neutral-foreground-black"
      },
      {
        design: "Neutral" /* Neutral */,
        pressed: true,
        className: "bg-sapphire-neutral-pressed-background text-sapphire-neutral-foreground-black border-sapphire-neutral-foreground-muted hover:bg-sapphire-neutral-hover-background"
      },
      // SecondaryNeutral
      {
        design: "SecondaryNeutral" /* SecondaryNeutral */,
        pressed: false,
        className: "bg-transparent text-sapphire-neutral-foreground-black border-transparent hover:bg-sapphire-neutral-hover-background active:bg-sapphire-neutral-pressed-background"
      },
      {
        design: "SecondaryNeutral" /* SecondaryNeutral */,
        pressed: true,
        className: "bg-sapphire-brand-selected-background text-sapphire-brand-foreground border-sapphire-border-toggle hover:bg-sapphire-brand-toggle-background hover:border-sapphire-border-toggle"
      },
      // TertiaryNeutral
      {
        design: "TertiaryNeutral" /* TertiaryNeutral */,
        pressed: false,
        className: "bg-transparent text-sapphire-neutral-foreground-muted border-transparent hover:text-sapphire-neutral-foreground-black active:text-sapphire-neutral-foreground-black"
      },
      {
        design: "TertiaryNeutral" /* TertiaryNeutral */,
        pressed: true,
        className: "bg-sapphire-brand-selected-background text-sapphire-brand-foreground border-sapphire-border-toggle hover:bg-sapphire-brand-toggle-background hover:border-sapphire-border-toggle"
      },
      // Tertiary
      {
        design: "Tertiary" /* Tertiary */,
        pressed: false,
        className: "bg-transparent text-sapphire-brand-foreground hover:bg-sapphire-neutral-hover-background active:bg-sapphire-neutral-pressed-background active:text-sapphire-brand-pressed-foreground"
      },
      {
        design: "Tertiary" /* Tertiary */,
        pressed: true,
        className: "bg-sapphire-brand-selected-background text-sapphire-brand-foreground border-transparent hover:bg-sapphire-brand-toggle-background hover:border-sapphire-border-toggle"
      },
      // TertiaryJoule
      {
        design: "TertiaryJoule" /* TertiaryJoule */,
        pressed: false,
        className: "bg-transparent text-sapphire-joule-foreground hover:bg-sapphire-neutral-hover-background active:bg-sapphire-neutral-pressed-background active:text-sapphire-joule-pressed-foreground"
      },
      {
        design: "TertiaryJoule" /* TertiaryJoule */,
        pressed: true,
        className: "bg-sapphire-joule-background-area text-sapphire-joule-foreground border-transparent hover:bg-sapphire-joule-background-light hover:border-sapphire-border-toggle"
      }
    ],
    defaultVariants: {
      design: "Secondary" /* Secondary */,
      pressed: false
    }
  }
);
var sizeClasses2 = {
  ["Large" /* Large */]: {
    default: "h-10 px-4 py-2 rounded-lg",
    iconOnly: "h-10 w-10 p-0 rounded-lg",
    icon: "h-5 w-5 [&>svg]:h-[17.5px] [&>svg]:w-[17.5px]"
  },
  ["Medium" /* Medium */]: {
    default: "h-8 px-3 py-2 text-sm rounded",
    iconOnly: "h-8 w-8 p-0 text-sm rounded",
    icon: "h-4 w-4 [&>svg]:h-3.5 [&>svg]:w-3.5"
  },
  ["Small" /* Small */]: {
    default: "h-6 px-2 py-1 text-xs font-normal gap-1 rounded",
    iconOnly: "h-6 w-6 p-0 text-xs rounded",
    icon: "h-3 w-3 [&>svg]:h-[10.5px] [&>svg]:w-[10.5px]"
  }
};
function ToggleButton({
  pressed: controlledPressed,
  design = "Secondary" /* Secondary */,
  size = "Large" /* Large */,
  icon,
  endIcon,
  children,
  disabled = false,
  iconOnly = false,
  tooltip,
  accessibleName,
  onChange,
  onClick,
  tabIndex,
  className,
  style,
  ref
}) {
  const buttonRef = React45.useRef(null);
  const [internalPressed, setInternalPressed] = React45.useState(false);
  const isControlled = controlledPressed !== void 0;
  const pressed = isControlled ? controlledPressed : internalPressed;
  React45.useImperativeHandle(ref, () => ({
    focus: () => buttonRef.current?.focus(),
    isFocused: () => document.activeElement === buttonRef.current,
    get nativeElement() {
      return buttonRef.current;
    },
    getNativeElement: () => buttonRef.current
  }));
  const handleClick2 = (e) => {
    if (disabled) return;
    const newPressed = !pressed;
    if (!isControlled) {
      setInternalPressed(newPressed);
    }
    onChange?.({ pressed: newPressed });
    onClick?.(e);
  };
  const isIconOnly = iconOnly || icon && !children;
  return /* @__PURE__ */ jsxRuntime.jsxs(
    "button",
    {
      ref: buttonRef,
      type: "button",
      disabled,
      tabIndex,
      title: tooltip,
      "aria-label": accessibleName,
      "aria-pressed": pressed,
      onClick: handleClick2,
      className: cn(
        toggleButtonVariants({
          design,
          pressed
        }),
        isIconOnly ? sizeClasses2[size].iconOnly : sizeClasses2[size].default,
        className
      ),
      style,
      children: [
        icon && /* @__PURE__ */ jsxRuntime.jsx("span", { className: cn("inline-flex items-center justify-center shrink-0", sizeClasses2[size].icon), children: icon }),
        !isIconOnly && children,
        endIcon && /* @__PURE__ */ jsxRuntime.jsx("span", { className: cn("inline-flex items-center justify-center shrink-0", sizeClasses2[size].icon), children: endIcon })
      ]
    }
  );
}
var pathData4 = [
  "M186 416q-11 0-18.5-7.5T160 390q0-10 8-18l121-116-121-116q-8-8-8-18 0-11 7.5-18.5T186 96q10 0 17 7l141 134q8 8 8 19 0 12-8 18L203 409q-7 7-17 7z"
];
var SlimArrowRightIcon = Object.assign(
  React45__namespace.default.forwardRef(
    (props, ref) => /* @__PURE__ */ jsxRuntime.jsx(Icon, { ref, pathData: pathData4, ...props })
  ),
  { displayName: "SlimArrowRightIcon", iconName: "slim-arrow-right" }
);
var pathData5 = [
  "M187 416q-12 0-20-9L71 299q-7-7-7-17 0-11 7.5-18.5T90 256q12 0 19 9l77 87 217-247q8-9 19-9t18.5 7.5T448 122q0 10-6 16L206 407q-7 9-19 9z"
];
var AcceptIcon = Object.assign(
  React45__namespace.default.forwardRef(
    (props, ref) => /* @__PURE__ */ jsxRuntime.jsx(Icon, { ref, pathData: pathData5, ...props })
  ),
  { displayName: "AcceptIcon", iconName: "accept" }
);
var pathData6 = [
  "M282 416q-11 0-18.5-7.5T256 390t7-18l109-109q7-7 18-7t18.5 7.5T416 282t-7 18L300 409q-7 7-18 7zm-160 0q-11 0-18.5-7.5T96 390t7-18l269-269q7-7 18-7t18.5 7.5T416 122t-7 18L140 409q-7 7-18 7z"
];
var ResizeCornerIcon = Object.assign(
  React45__namespace.default.forwardRef(
    (props, ref) => /* @__PURE__ */ jsxRuntime.jsx(Icon, { ref, pathData: pathData6, ...props })
  ),
  { displayName: "ResizeCornerIcon", iconName: "resize-corner" }
);
var titleVariants = cva(
  ["text-foreground"],
  {
    variants: {
      level: {
        ["H1" /* H1 */]: "text-[3.5rem] leading-[4rem] font-light",
        ["H2" /* H2 */]: "text-[2.5rem] leading-[3rem] font-normal",
        ["H3" /* H3 */]: "text-[2rem] leading-[2.5rem] font-normal",
        ["H4" /* H4 */]: "text-xl leading-7 font-normal",
        ["H5" /* H5 */]: "text-base leading-[1.375rem] font-semibold",
        ["H6" /* H6 */]: "text-base leading-[1.375rem] font-normal"
      },
      wrappingType: {
        ["None" /* None */]: "truncate",
        ["Normal" /* Normal */]: ""
      }
    },
    defaultVariants: {
      level: "H2" /* H2 */,
      wrappingType: "None" /* None */
    }
  }
);
var levelToElement = {
  ["H1" /* H1 */]: "h1",
  ["H2" /* H2 */]: "h2",
  ["H3" /* H3 */]: "h3",
  ["H4" /* H4 */]: "h4",
  ["H5" /* H5 */]: "h5",
  ["H6" /* H6 */]: "h6"
};
function Title({
  level = "H2" /* H2 */,
  wrappingType = "None" /* None */,
  children,
  className,
  style,
  id,
  ref
}) {
  const Tag = levelToElement[level] || "h2";
  return /* @__PURE__ */ jsxRuntime.jsx(
    Tag,
    {
      ref,
      id,
      className: cn(
        titleVariants({
          level,
          wrappingType
        }),
        className
      ),
      style,
      children
    }
  );
}

// src/lib/popupRegistry.ts
var openedPopups = [];
function attachListeners() {
  document.addEventListener("keydown", handleKeydown2);
  document.addEventListener("mousedown", handleClick, true);
}
function detachListeners() {
  document.removeEventListener("keydown", handleKeydown2);
  document.removeEventListener("mousedown", handleClick, true);
}
function handleKeydown2(e) {
  if (e.key !== "Escape" || openedPopups.length === 0) return;
  e.preventDefault();
  e.stopImmediatePropagation();
  const topmost = openedPopups[openedPopups.length - 1];
  topmost.close(true);
}
function handleClick(e) {
  if (openedPopups.length === 0) return;
  const target = e.target;
  for (let i = openedPopups.length - 1; i >= 0; i--) {
    const entry = openedPopups[i];
    if (entry.isModal || entry.type === "dialog") return;
    if (entry.getOpenerEl) {
      const openerEl = entry.getOpenerEl();
      if (openerEl?.contains(target)) return;
    }
    if (entry.element.contains(target)) return;
    if (entry.isInsidePopup?.(target)) return;
    entry.close();
  }
}
function addOpenedPopup(entry) {
  openedPopups.push(entry);
  if (openedPopups.length === 1) {
    attachListeners();
  }
}
function removeOpenedPopup(element) {
  const idx = openedPopups.findIndex((p) => p.element === element);
  if (idx !== -1) openedPopups.splice(idx, 1);
  if (openedPopups.length === 0) {
    detachListeners();
  }
}

// src/lib/Device.ts
var isSSR = typeof document === "undefined";
function getUserAgent() {
  return isSSR ? "" : navigator.userAgent;
}
function hasTouch() {
  if (isSSR) return false;
  return "ontouchstart" in window || navigator.maxTouchPoints > 0;
}
function isIPad() {
  if (isSSR) return false;
  const ua = getUserAgent();
  return /ipad/i.test(ua) || /Macintosh/i.test(ua) && "ontouchend" in document;
}
function isWindowsPlatform() {
  if (isSSR) return false;
  return navigator.platform.indexOf("Win") !== -1;
}
function isWindows8OrAbove() {
  if (!isWindowsPlatform()) return false;
  const matches = getUserAgent().match(/Windows NT (\d+)/);
  const winVer = matches ? parseFloat(matches[1]) : 0;
  return winVer >= 8;
}
function isAndroidUA() {
  if (isSSR) return false;
  return !isWindowsPlatform() && /Android/.test(getUserAgent());
}
function isChromeUA() {
  if (isSSR) return false;
  return /(Chrome|CriOS)/.test(getUserAgent());
}
function isWebkitUA() {
  if (isSSR) return false;
  return /webkit/.test(getUserAgent());
}
function isTablet() {
  if (isSSR) return false;
  const touch = hasTouch();
  const ua = getUserAgent();
  if (isIPad()) return true;
  if (touch) {
    if (isWindows8OrAbove()) return true;
    if (isChromeUA() && isAndroidUA()) {
      return !/Mobile Safari\/[.0-9]+/.test(ua);
    }
    let densityFactor = window.devicePixelRatio || 1;
    if (isAndroidUA() && isWebkitUA()) {
      const wkMatches = ua.match(/(webkit)[ /]([\w.]+)/);
      const wkVer = wkMatches ? parseFloat(wkMatches[2]) : 0;
      if (wkVer >= 537.1) densityFactor = 1;
    }
    return Math.min(
      window.screen.width / densityFactor,
      window.screen.height / densityFactor
    ) >= 600;
  }
  return ua.indexOf("Touch") !== -1 || isAndroidUA() && !/(?=android)(?=.*mobile)/i.test(ua);
}
function isPhone() {
  if (isSSR) return false;
  return hasTouch() && !isTablet();
}
function isDesktop() {
  if (isSSR) return false;
  return !isTablet() && !isPhone() || isWindows8OrAbove();
}
var ARROW_DEPTH = 8;
var ARROW_BASE = 16;
var ARROW_SIZE = ARROW_DEPTH;
var VIEWPORT_MARGIN = 10;
function clamp(val, min, max) {
  return Math.min(Math.max(val, min), max);
}
var FOCUSABLE = 'a[href], button:not([disabled]), textarea:not([disabled]), input:not([disabled]), select:not([disabled]), [tabindex]:not([tabindex="-1"])';
function getFirstFocusable(container) {
  for (const el of container.querySelectorAll(FOCUSABLE)) {
    if (el.dataset.focusTrap) continue;
    return el;
  }
  return null;
}
function getLastFocusable(container) {
  const list = container.querySelectorAll(FOCUSABLE);
  for (let i = list.length - 1; i >= 0; i--) {
    if (list[i].dataset.focusTrap) continue;
    return list[i];
  }
  return null;
}
function resolveOpener(opener) {
  if (!opener) return null;
  if (opener instanceof HTMLElement) return opener;
  if ("current" in opener) {
    const val = opener.current;
    if (!val) return null;
    if (val instanceof HTMLElement) return val;
    if (typeof val === "object" && "nativeElement" in val && val.nativeElement instanceof HTMLElement) {
      return val.nativeElement;
    }
  }
  return null;
}
function useIsRtl(ref) {
  const [isRtl, setIsRtl] = React45.useState(false);
  React45.useEffect(() => {
    const el = ref?.current ?? document.documentElement;
    const dir = el.closest("[dir]")?.getAttribute("dir") ?? document.documentElement.getAttribute("dir");
    setIsRtl(dir === "rtl");
  });
  return isRtl;
}
function isOpenerClippedByAncestor(opener) {
  let parent = opener.parentElement;
  while (parent && parent !== document.documentElement) {
    if (parent.hasAttribute("popover")) {
      try {
        if (parent.matches(":popover-open")) return false;
      } catch {
      }
    }
    const { overflow, overflowX, overflowY } = window.getComputedStyle(parent);
    const clips = [overflow, overflowX, overflowY].some(
      (v) => v === "hidden" || v === "scroll" || v === "auto"
    );
    if (clips) {
      const parentRect = parent.getBoundingClientRect();
      const openerRect = opener.getBoundingClientRect();
      if (openerRect.bottom <= parentRect.top || openerRect.top >= parentRect.bottom || openerRect.right <= parentRect.left || openerRect.left >= parentRect.right) {
        return true;
      }
    }
    parent = parent.parentElement;
  }
  return false;
}
function getActualHorizontalAlign(align, isRtl) {
  switch (align) {
    case "Start" /* Start */:
      return isRtl ? "Right" /* Right */ : "Left" /* Left */;
    case "End" /* End */:
      return isRtl ? "Left" /* Left */ : "Right" /* Right */;
    case "Stretch" /* Stretch */:
      return "Stretch" /* Stretch */;
    default:
      return "Center" /* Center */;
  }
}
function fallbackPlacement(clientWidth, clientHeight, targetRect, popoverSize) {
  if (targetRect.left > popoverSize.width) return "Left" /* Left */;
  if (clientWidth - targetRect.right > targetRect.left) return "Right" /* Right */;
  if (clientHeight - targetRect.bottom > popoverSize.height) return "Bottom" /* Bottom */;
  if (clientHeight - targetRect.bottom < targetRect.top) return "Top" /* Top */;
  return void 0;
}
function getActualPlacement(desiredPlacement, isRtl, hideArrow, targetRect, popoverSize) {
  let actual;
  switch (desiredPlacement) {
    case "Start" /* Start */:
      actual = isRtl ? "Right" /* Right */ : "Left" /* Left */;
      break;
    case "End" /* End */:
      actual = isRtl ? "Left" /* Left */ : "Right" /* Right */;
      break;
    case "Top" /* Top */:
      actual = "Top" /* Top */;
      break;
    case "Bottom" /* Bottom */:
      actual = "Bottom" /* Bottom */;
      break;
    default:
      actual = isRtl ? "Left" /* Left */ : "Right" /* Right */;
  }
  const clientWidth = document.documentElement.clientWidth;
  let clientHeight = document.documentElement.clientHeight;
  const isVertical = desiredPlacement === "Top" /* Top */ || desiredPlacement === "Bottom" /* Bottom */;
  let popoverHeight = popoverSize.height;
  if (isVertical) {
    popoverHeight += hideArrow ? 0 : ARROW_SIZE;
    clientHeight -= VIEWPORT_MARGIN;
  }
  switch (actual) {
    case "Top" /* Top */:
      if (targetRect.top < popoverHeight && targetRect.top < clientHeight - targetRect.bottom)
        actual = "Bottom" /* Bottom */;
      break;
    case "Bottom" /* Bottom */:
      if (clientHeight - targetRect.bottom < popoverHeight && clientHeight - targetRect.bottom < targetRect.top)
        actual = "Top" /* Top */;
      break;
    case "Left" /* Left */:
      if (targetRect.left < popoverSize.width)
        actual = fallbackPlacement(clientWidth, clientHeight, targetRect, popoverSize) || actual;
      break;
    case "Right" /* Right */:
      if (clientWidth - targetRect.right < popoverSize.width)
        actual = fallbackPlacement(clientWidth, clientHeight, targetRect, popoverSize) || actual;
      break;
  }
  return actual;
}
function calcPlacement(targetRect, popoverSize, desiredPlacement, horizontalAlign, verticalAlign, isRtl, hideArrow, allowTargetOverlap, borderRadius, offset) {
  const clientWidth = document.documentElement.clientWidth;
  const clientHeight = document.documentElement.clientHeight;
  let maxHeight = clientHeight;
  let maxWidth = clientWidth;
  let stretchWidth;
  let stretchHeight;
  const actualPlacement = getActualPlacement(
    desiredPlacement,
    isRtl,
    hideArrow,
    targetRect,
    popoverSize
  );
  const isVertical = actualPlacement === "Top" /* Top */ || actualPlacement === "Bottom" /* Bottom */;
  if (horizontalAlign === "Stretch" /* Stretch */ && isVertical) {
    popoverSize = { ...popoverSize, width: targetRect.width };
    stretchWidth = `${targetRect.width}px`;
  } else if (verticalAlign === "Stretch" /* Stretch */ && !isVertical) {
    popoverSize = { ...popoverSize, height: targetRect.height };
    stretchHeight = `${targetRect.height}px`;
  }
  const arrowOffset = (hideArrow ? 0 : ARROW_SIZE) + offset;
  const actualHAlign = getActualHorizontalAlign(horizontalAlign, isRtl);
  const getVerticalLeft = () => {
    switch (actualHAlign) {
      case "Center" /* Center */:
      case "Stretch" /* Stretch */:
        return targetRect.left - (popoverSize.width - targetRect.width) / 2;
      case "Left" /* Left */:
        return targetRect.left;
      case "Right" /* Right */:
        return targetRect.right - popoverSize.width;
      default:
        return VIEWPORT_MARGIN;
    }
  };
  const getHorizontalTop = () => {
    switch (verticalAlign) {
      case "Center" /* Center */:
      case "Stretch" /* Stretch */:
        return targetRect.top - (popoverSize.height - targetRect.height) / 2;
      case "Top" /* Top */:
        return targetRect.top;
      case "Bottom" /* Bottom */:
        return targetRect.bottom - popoverSize.height;
      default:
        return 0;
    }
  };
  let left = VIEWPORT_MARGIN;
  let top = 0;
  switch (actualPlacement) {
    case "Top" /* Top */:
      left = getVerticalLeft();
      top = Math.max(targetRect.top - popoverSize.height - arrowOffset, 0);
      if (!allowTargetOverlap) maxHeight = targetRect.top - arrowOffset;
      break;
    case "Bottom" /* Bottom */:
      left = getVerticalLeft();
      top = targetRect.bottom + arrowOffset;
      if (allowTargetOverlap) {
        top = Math.max(Math.min(top, clientHeight - popoverSize.height), 0);
      } else {
        maxHeight = clientHeight - targetRect.bottom - arrowOffset;
      }
      break;
    case "Left" /* Left */:
      left = Math.max(targetRect.left - popoverSize.width - arrowOffset, 0);
      top = getHorizontalTop();
      if (!allowTargetOverlap) maxWidth = targetRect.left - arrowOffset;
      break;
    case "Right" /* Right */:
      left = targetRect.left + targetRect.width + arrowOffset;
      top = getHorizontalTop();
      if (allowTargetOverlap) {
        left = Math.max(Math.min(left, clientWidth - popoverSize.width), 0);
      } else {
        maxWidth = clientWidth - targetRect.right - arrowOffset;
      }
      break;
  }
  if (isVertical) {
    if (popoverSize.width > clientWidth || left < VIEWPORT_MARGIN) {
      left = VIEWPORT_MARGIN;
    } else if (left + popoverSize.width > clientWidth - VIEWPORT_MARGIN) {
      left = clientWidth - VIEWPORT_MARGIN - popoverSize.width;
    }
  } else {
    if (popoverSize.height > clientHeight || top < VIEWPORT_MARGIN) {
      top = VIEWPORT_MARGIN;
    } else if (top + popoverSize.height > clientHeight - VIEWPORT_MARGIN) {
      top = clientHeight - VIEWPORT_MARGIN - popoverSize.height;
    }
  }
  left = Math.round(left);
  top = Math.round(top);
  maxHeight = Math.round(maxHeight - VIEWPORT_MARGIN);
  maxWidth = Math.round(maxWidth - VIEWPORT_MARGIN);
  let arrowXCentered = actualHAlign === "Center" /* Center */ || actualHAlign === "Stretch" /* Stretch */;
  if (actualHAlign === "Right" /* Right */ && left <= targetRect.left)
    arrowXCentered = true;
  if (actualHAlign === "Left" /* Left */ && left + popoverSize.width >= targetRect.left + targetRect.width)
    arrowXCentered = true;
  let arrowTranslateX = 0;
  if (isVertical && arrowXCentered) {
    arrowTranslateX = targetRect.left + targetRect.width / 2 - left - popoverSize.width / 2;
  }
  let arrowTranslateY = 0;
  if (!isVertical) {
    arrowTranslateY = targetRect.top + targetRect.height / 2 - top - popoverSize.height / 2;
  }
  const safeY = popoverSize.height / 2 - borderRadius - ARROW_BASE / 2 - 2;
  arrowTranslateY = clamp(arrowTranslateY, -safeY, safeY);
  const safeX = popoverSize.width / 2 - borderRadius - ARROW_BASE / 2 - 2;
  arrowTranslateX = clamp(arrowTranslateX, -safeX, safeX);
  return {
    arrow: { x: Math.round(arrowTranslateX), y: Math.round(arrowTranslateY) },
    top,
    left,
    maxHeight,
    maxWidth,
    actualPlacement,
    stretchWidth,
    stretchHeight
  };
}
function getResizeHandleCorner(popoverRect, openerRect, side, verticalAlign, actualHAlign, isRtl) {
  const offset = 2;
  let openerCX = Math.floor(openerRect.x + openerRect.width / 2);
  const openerCY = Math.floor(openerRect.y + openerRect.height / 2);
  let popoverCX = Math.floor(popoverRect.x + popoverRect.width / 2);
  const popoverCY = Math.floor(popoverRect.y + popoverRect.height / 2);
  const isPopoverWider = popoverRect.width > openerRect.width;
  const isPopoverTaller = popoverRect.height > openerRect.height;
  if (isRtl) {
    openerCX = -openerCX;
    popoverCX = -popoverCX;
  }
  switch (side) {
    case "Left" /* Left */:
      if (isPopoverTaller) {
        return popoverCY > openerCY + offset ? "bottom-left" : "top-left";
      }
      return verticalAlign === "Top" /* Top */ ? "bottom-left" : "top-left";
    case "Right" /* Right */:
      if (isPopoverTaller) {
        return popoverCY + offset < openerCY ? "top-right" : "bottom-right";
      }
      return verticalAlign === "Bottom" /* Bottom */ ? "top-right" : "bottom-right";
    case "Bottom" /* Bottom */:
      if (isPopoverWider) {
        if (popoverCX + offset < openerCX) {
          return isRtl ? "bottom-right" : "bottom-left";
        }
        return isRtl ? "bottom-left" : "bottom-right";
      }
      if (isRtl) {
        return actualHAlign === "Left" /* Left */ ? "bottom-right" : "bottom-left";
      }
      return actualHAlign === "Right" /* Right */ ? "bottom-left" : "bottom-right";
    case "Top" /* Top */:
    default:
      if (isPopoverWider) {
        if (popoverCX + offset < openerCX) {
          return isRtl ? "top-right" : "top-left";
        }
        return isRtl ? "top-left" : "top-right";
      }
      if (isRtl) {
        return actualHAlign === "Left" /* Left */ ? "top-right" : "top-left";
      }
      return actualHAlign === "Right" /* Right */ ? "top-left" : "top-right";
  }
}
function Popover({
  headerText,
  placement = "End" /* End */,
  horizontalAlign = "Center" /* Center */,
  verticalAlign = "Center" /* Center */,
  hideArrow = false,
  offset: offsetProp,
  allowTargetOverlap = false,
  resizable = false,
  noPadding = false,
  opener,
  open = false,
  initialFocus,
  preventFocusRestore = false,
  preventInitialFocus = false,
  accessibleName,
  accessibleNameRef,
  accessibleRole = "Dialog" /* Dialog */,
  accessibleDescription,
  header,
  footer,
  children,
  onBeforeOpen,
  onOpen,
  onBeforeClose,
  onClose,
  isInsidePopup,
  className,
  style,
  id,
  ref
}) {
  const popoverRef = React45.useRef(null);
  const arrowRef = React45.useRef(null);
  const focusedBeforeOpen = React45.useRef(null);
  const [isOpen, setIsOpen] = React45.useState(false);
  const [pos, setPos] = React45.useState(null);
  const [resizeSize, setResizeSize] = React45.useState(null);
  const resizeState = React45.useRef(null);
  const [isDesktop2] = React45.useState(() => isDesktop());
  const containerRef = React45.useRef(null);
  const isRtl = useIsRtl(containerRef);
  const getOpenerEl = React45.useCallback(() => resolveOpener(opener), [opener]);
  const doCloseRef = React45.useRef(() => {
  });
  const reposition = React45.useCallback(() => {
    const openerEl = getOpenerEl();
    const popEl = popoverRef.current;
    if (!openerEl || !popEl) return;
    const targetRect = openerEl.getBoundingClientRect();
    if (targetRect.bottom < 0 || targetRect.top > window.innerHeight || targetRect.right < 0 || targetRect.left > window.innerWidth) {
      doCloseRef.current();
      return;
    }
    if (targetRect.top === 0 && targetRect.bottom === 0 && targetRect.left === 0 && targetRect.right === 0) {
      doCloseRef.current();
      return;
    }
    if (isOpenerClippedByAncestor(openerEl)) {
      doCloseRef.current();
      return;
    }
    const rect = popEl.getBoundingClientRect();
    const popoverSize = { width: rect.width, height: rect.height };
    if (popoverSize.width === 0 || popoverSize.height === 0) return;
    const borderRadius = Number.parseInt(
      window.getComputedStyle(popEl).getPropertyValue("border-radius") || "0"
    );
    const result = calcPlacement(
      targetRect,
      popoverSize,
      placement,
      horizontalAlign,
      verticalAlign,
      isRtl,
      hideArrow,
      allowTargetOverlap,
      borderRadius,
      offsetProp ?? 0
    );
    setPos(result);
  }, [getOpenerEl, placement, horizontalAlign, verticalAlign, isRtl, hideArrow, allowTargetOverlap, offsetProp]);
  React45.useEffect(() => {
    if (!isOpen) return;
    const raf = requestAnimationFrame(() => {
      reposition();
      requestAnimationFrame(reposition);
    });
    const onScrollResize = () => reposition();
    window.addEventListener("scroll", onScrollResize, true);
    window.addEventListener("resize", onScrollResize);
    const openerEl = getOpenerEl();
    let ro = null;
    if (openerEl instanceof Element) {
      ro = new ResizeObserver(reposition);
      ro.observe(openerEl);
    }
    let roPopover = null;
    if (popoverRef.current instanceof Element) {
      roPopover = new ResizeObserver(reposition);
      roPopover.observe(popoverRef.current);
    }
    return () => {
      cancelAnimationFrame(raf);
      window.removeEventListener("scroll", onScrollResize, true);
      window.removeEventListener("resize", onScrollResize);
      ro?.disconnect();
      roPopover?.disconnect();
    };
  }, [isOpen, reposition, getOpenerEl]);
  const doOpen = React45.useCallback(() => {
    if (isOpen) return;
    if (onBeforeOpen?.() === false) return;
    focusedBeforeOpen.current = document.activeElement;
    setIsOpen(true);
  }, [isOpen, onBeforeOpen]);
  const doClose = React45.useCallback(
    (escPressed = false) => {
      if (!isOpen) return;
      if (onBeforeClose?.({ escPressed }) === false) return;
      const el = popoverRef.current;
      if (el && el.matches(":popover-open")) {
        el.hidePopover();
      }
      setIsOpen(false);
      setResizeSize(null);
      setPos(null);
      if (!preventFocusRestore && focusedBeforeOpen.current) {
        focusedBeforeOpen.current.focus();
        focusedBeforeOpen.current = null;
      }
      onClose?.();
    },
    [isOpen, onBeforeClose, onClose, preventFocusRestore]
  );
  doCloseRef.current = doClose;
  React45.useEffect(() => {
    if (open) doOpen();
    else if (isOpen) doClose();
  }, [open]);
  React45.useEffect(() => {
    const el = popoverRef.current;
    if (!el) return;
    if (isOpen && !el.matches(":popover-open")) {
      el.showPopover();
    }
    return () => {
      if (el.matches(":popover-open")) {
        el.hidePopover();
      }
    };
  }, [isOpen]);
  React45.useEffect(() => {
    if (!isOpen) return;
    requestAnimationFrame(() => {
      if (preventInitialFocus || !popoverRef.current) return;
      if (initialFocus) {
        const el = document.getElementById(initialFocus);
        if (el) {
          el.focus();
          onOpen?.();
          return;
        }
      }
      const first = getFirstFocusable(popoverRef.current);
      if (first) first.focus();
      else popoverRef.current.focus();
      onOpen?.();
    });
  }, [isOpen]);
  React45.useEffect(() => {
    if (!isOpen || !popoverRef.current) return;
    const el = popoverRef.current;
    addOpenedPopup({
      element: el,
      close: (escPressed) => doCloseRef.current(escPressed),
      type: "popover",
      isModal: false,
      getOpenerEl,
      isInsidePopup
    });
    return () => removeOpenedPopup(el);
  }, [isOpen, getOpenerEl, isInsidePopup]);
  const focusFirst = React45.useCallback(() => {
    if (!popoverRef.current) return;
    const el = getFirstFocusable(popoverRef.current);
    (el || popoverRef.current).focus();
  }, []);
  const focusLast = React45.useCallback(() => {
    if (!popoverRef.current) return;
    const el = getLastFocusable(popoverRef.current);
    (el || popoverRef.current).focus();
  }, []);
  const handleResizeMouseDown = React45.useCallback(
    (e) => {
      if (!resizable || !popoverRef.current) return;
      e.preventDefault();
      const rect = popoverRef.current.getBoundingClientRect();
      const openerEl = getOpenerEl();
      const opRect = openerEl?.getBoundingClientRect();
      const corner = opRect && pos ? getResizeHandleCorner(
        rect,
        opRect,
        pos.actualPlacement,
        verticalAlign,
        getActualHorizontalAlign(horizontalAlign, isRtl),
        isRtl
      ) : "bottom-right";
      resizeState.current = {
        startX: e.clientX,
        startY: e.clientY,
        startWidth: rect.width,
        startHeight: rect.height,
        corner
      };
      const onMove = (ev) => {
        if (!resizeState.current) return;
        const { startX, startY, startWidth, startHeight, corner: c } = resizeState.current;
        const dx = ev.clientX - startX;
        const dy = ev.clientY - startY;
        const isL = c === "top-left" || c === "bottom-left";
        const isT = c === "top-left" || c === "top-right";
        const w = clamp(startWidth + (isL ? -dx : dx), 150, window.innerWidth - VIEWPORT_MARGIN * 2);
        const h = clamp(startHeight + (isT ? -dy : dy), 100, window.innerHeight - VIEWPORT_MARGIN * 2);
        setResizeSize({ width: w, height: h });
      };
      const onUp = () => {
        resizeState.current = null;
        window.removeEventListener("mousemove", onMove);
        window.removeEventListener("mouseup", onUp);
      };
      window.addEventListener("mousemove", onMove);
      window.addEventListener("mouseup", onUp);
    },
    [resizable, getOpenerEl, pos]
  );
  React45.useImperativeHandle(ref, () => ({
    open() {
      doOpen();
    },
    close() {
      doClose();
    },
    async applyFocus() {
      if (!popoverRef.current) return;
      const el = getFirstFocusable(popoverRef.current);
      (el || popoverRef.current).focus();
    },
    isOpen() {
      return isOpen;
    },
    get nativeElement() {
      return popoverRef.current;
    }
  }), [isOpen, doOpen, doClose]);
  const resizeHandleCorner = React45.useMemo(() => {
    if (!resizable || !isDesktop2 || !popoverRef.current || !pos) return "bottom-right";
    const openerEl = getOpenerEl();
    if (!openerEl) return "bottom-right";
    return getResizeHandleCorner(
      popoverRef.current.getBoundingClientRect(),
      openerEl.getBoundingClientRect(),
      pos.actualPlacement,
      verticalAlign,
      getActualHorizontalAlign(horizontalAlign, isRtl),
      isRtl
    );
  }, [resizable, isDesktop2, getOpenerEl, pos, verticalAlign, horizontalAlign, isRtl]);
  const role = accessibleRole === "None" /* None */ ? void 0 : accessibleRole === "AlertDialog" /* AlertDialog */ ? "alertdialog" : "dialog";
  const ariaModal = accessibleRole === "None" /* None */ ? void 0 : "true";
  const displayHeader = !!(header || headerText);
  const actualPlacement = pos?.actualPlacement ?? "Right" /* Right */;
  const resizeHandleClasses = {
    "top-left": "-top-2 -left-2 cursor-nw-resize",
    "top-right": "-top-2 -right-2 cursor-ne-resize",
    "bottom-left": "-bottom-2 -left-2 cursor-ne-resize",
    "bottom-right": "-bottom-2 -right-2 cursor-nw-resize"
  };
  const resizeIconRotation = {
    "top-left": "180deg",
    "top-right": "270deg",
    "bottom-left": "90deg",
    "bottom-right": "0deg"
  };
  if (!isOpen) {
    return /* @__PURE__ */ jsxRuntime.jsx("div", { ref: containerRef, className: "hidden" });
  }
  const popoverStyle = {
    position: "fixed",
    top: pos ? pos.top : -1e4,
    left: pos ? pos.left : -1e4,
    maxHeight: pos ? pos.maxHeight : void 0,
    maxWidth: pos ? pos.maxWidth : void 0,
    boxShadow: "0px 10px 15px -5px rgba(10, 10, 10, 0.15), 0px 5px 5px -2px rgba(10, 10, 10, 0.05)",
    ...pos?.stretchWidth ? { width: pos.stretchWidth } : {},
    ...pos?.stretchHeight ? { height: pos.stretchHeight } : {},
    ...resizeSize ? { width: resizeSize.width, height: resizeSize.height } : {},
    ...style
  };
  return /* @__PURE__ */ jsxRuntime.jsxs(jsxRuntime.Fragment, { children: [
    /* @__PURE__ */ jsxRuntime.jsx("div", { ref: containerRef, className: "hidden" }),
    /* @__PURE__ */ jsxRuntime.jsxs(
      "div",
      {
        ref: popoverRef,
        id,
        popover: "manual",
        role,
        "aria-modal": ariaModal,
        "aria-label": accessibleName,
        "aria-labelledby": accessibleNameRef || (!accessibleName && headerText ? `${id || "popover"}-title` : void 0),
        "aria-describedby": accessibleDescription ? `${id || "popover"}-desc` : void 0,
        tabIndex: -1,
        className: cn(
          "rounded-lg border border-border bg-popover text-popover-foreground outline-none",
          className
        ),
        style: popoverStyle,
        children: [
          /* @__PURE__ */ jsxRuntime.jsx("span", { tabIndex: 0, onFocus: focusLast, "data-focus-trap": "true", className: "sr-only", role: "none" }),
          !hideArrow && pos && (() => {
            const arrowBorderColor = "var(--border)";
            const arrowFill = "var(--popover)";
            let svgWidth;
            let svgHeight;
            let fillPoints;
            let strokePath;
            const wrapperStyle = { position: "absolute", lineHeight: 0 };
            switch (actualPlacement) {
              case "Bottom" /* Bottom */:
                svgWidth = ARROW_BASE;
                svgHeight = ARROW_DEPTH;
                fillPoints = `0,${ARROW_DEPTH} ${ARROW_BASE / 2},0 ${ARROW_BASE},${ARROW_DEPTH}`;
                strokePath = `M0,${ARROW_DEPTH} L${ARROW_BASE / 2},0 L${ARROW_BASE},${ARROW_DEPTH}`;
                wrapperStyle.left = "50%";
                wrapperStyle.top = -ARROW_DEPTH;
                wrapperStyle.transform = `translateX(calc(-50% + ${pos.arrow.x}px))`;
                break;
              case "Top" /* Top */:
                svgWidth = ARROW_BASE;
                svgHeight = ARROW_DEPTH;
                fillPoints = `0,0 ${ARROW_BASE / 2},${ARROW_DEPTH} ${ARROW_BASE},0`;
                strokePath = `M0,0 L${ARROW_BASE / 2},${ARROW_DEPTH} L${ARROW_BASE},0`;
                wrapperStyle.left = "50%";
                wrapperStyle.bottom = -ARROW_DEPTH;
                wrapperStyle.transform = `translateX(calc(-50% + ${pos.arrow.x}px))`;
                break;
              case "Right" /* Right */:
                svgWidth = ARROW_DEPTH;
                svgHeight = ARROW_BASE;
                fillPoints = `${ARROW_DEPTH},0 0,${ARROW_BASE / 2} ${ARROW_DEPTH},${ARROW_BASE}`;
                strokePath = `M${ARROW_DEPTH},0 L0,${ARROW_BASE / 2} L${ARROW_DEPTH},${ARROW_BASE}`;
                wrapperStyle.top = "50%";
                wrapperStyle.left = -ARROW_DEPTH;
                wrapperStyle.transform = `translateY(calc(-50% + ${pos.arrow.y}px))`;
                break;
              case "Left" /* Left */:
              default:
                svgWidth = ARROW_DEPTH;
                svgHeight = ARROW_BASE;
                fillPoints = `0,0 ${ARROW_DEPTH},${ARROW_BASE / 2} 0,${ARROW_BASE}`;
                strokePath = `M0,0 L${ARROW_DEPTH},${ARROW_BASE / 2} L0,${ARROW_BASE}`;
                wrapperStyle.top = "50%";
                wrapperStyle.right = -ARROW_DEPTH;
                wrapperStyle.transform = `translateY(calc(-50% + ${pos.arrow.y}px))`;
                break;
            }
            return /* @__PURE__ */ jsxRuntime.jsx("div", { ref: arrowRef, style: wrapperStyle, children: /* @__PURE__ */ jsxRuntime.jsxs("svg", { width: svgWidth, height: svgHeight, viewBox: `0 0 ${svgWidth} ${svgHeight}`, style: { display: "block" }, children: [
              /* @__PURE__ */ jsxRuntime.jsx("polygon", { points: fillPoints, fill: arrowFill, stroke: "none" }),
              /* @__PURE__ */ jsxRuntime.jsx("path", { d: strokePath, fill: "none", stroke: arrowBorderColor, strokeWidth: 1 })
            ] }) });
          })(),
          /* @__PURE__ */ jsxRuntime.jsxs("div", { className: "isolate z-1 flex flex-col gap-sapphire-2xs overflow-hidden rounded-lg", style: { maxHeight: "inherit" }, children: [
            displayHeader && /* @__PURE__ */ jsxRuntime.jsx(
              "header",
              {
                className: cn(
                  "shrink-0",
                  // When custom header is provided, let it control its own styling.
                  // When using headerText, apply default popover header styling.
                  !header && "px-sapphire-s py-sapphire-xs border-b border-sapphire-border-primary"
                ),
                children: header || /* @__PURE__ */ jsxRuntime.jsx(Title, { level: "H1", id: `${id || "popover"}-title`, className: "text-base font-bold", children: headerText })
              }
            ),
            /* @__PURE__ */ jsxRuntime.jsx("div", { className: cn("overflow-y-auto overflow-x-hidden flex-1 min-h-0", !noPadding && "px-sapphire-s py-sapphire-xs"), children }),
            accessibleDescription && /* @__PURE__ */ jsxRuntime.jsx("span", { id: `${id || "popover"}-desc`, className: "sr-only", children: accessibleDescription }),
            footer && /* @__PURE__ */ jsxRuntime.jsx("footer", { className: "px-sapphire-s py-sapphire-xs border-t border-sapphire-border-primary shrink-0", children: footer })
          ] }),
          resizable && isDesktop2 && /* @__PURE__ */ jsxRuntime.jsx(
            "div",
            {
              className: cn(
                "absolute flex items-center justify-center w-6 h-6 rounded-full z-[1]",
                resizeHandleClasses[resizeHandleCorner]
              ),
              onMouseDown: handleResizeMouseDown,
              children: /* @__PURE__ */ jsxRuntime.jsx(
                ResizeCornerIcon,
                {
                  className: "h-4 w-4 text-sapphire-text-tertiary",
                  style: { transform: `rotate(${resizeIconRotation[resizeHandleCorner]})` }
                }
              )
            }
          ),
          /* @__PURE__ */ jsxRuntime.jsx("span", { tabIndex: 0, onFocus: focusFirst, "data-focus-trap": "true", className: "sr-only", role: "none" })
        ]
      }
    )
  ] });
}
var MenuContext = React45.createContext({
  closeMenu: () => {
  },
  closeAllSubmenus: () => {
  },
  registerSubmenu: () => {
  },
  unregisterSubmenu: () => {
  },
  submenuDepth: 0
});
var MenuItemGroupContext = React45.createContext(null);
function calculateSubmenuPosition(parentItem, submenu) {
  const parentRect = parentItem.getBoundingClientRect();
  const submenuRect = submenu.getBoundingClientRect();
  const viewportWidth = document.documentElement.clientWidth;
  const viewportHeight = document.documentElement.clientHeight;
  const gap = 4;
  const margin = 8;
  const spaceRight = viewportWidth - parentRect.right - gap;
  const spaceLeft = parentRect.left - gap;
  let top = parentRect.top;
  let left;
  if (spaceRight >= submenuRect.width) {
    left = parentRect.right + gap;
  } else if (spaceLeft >= submenuRect.width) {
    left = parentRect.left - submenuRect.width - gap;
  } else {
    const parentMenu = parentItem.closest('[role="menu"], [data-submenu="true"]');
    if (parentMenu) {
      const parentMenuRect = parentMenu.getBoundingClientRect();
      left = parentMenuRect.left;
    } else {
      left = parentRect.left;
    }
    left = Math.max(Math.min(left, viewportWidth - submenuRect.width - margin), margin);
  }
  if (top + submenuRect.height > viewportHeight - margin) {
    top = viewportHeight - submenuRect.height - margin;
  }
  if (top < margin) {
    top = margin;
  }
  return { top, left };
}
function getFocusableItems(container) {
  const all = Array.from(
    container.querySelectorAll(
      '[role^="menuitem"]:not([aria-disabled="true"])'
    )
  );
  return all.filter((item) => {
    let parent = item.parentElement;
    while (parent && parent !== container) {
      if (parent.getAttribute("role") === "menu" || parent.getAttribute("data-submenu") === "true") {
        return false;
      }
      parent = parent.parentElement;
    }
    return true;
  });
}
function useMenuNavigation(containerRef, options) {
  const typeAheadBufferRef = React45.useRef("");
  const typeAheadTimeoutRef = React45.useRef(null);
  const handleKeyDown = React45.useCallback(
    (e) => {
      const container = containerRef.current;
      if (!container) return;
      const items = getFocusableItems(container);
      if (items.length === 0) return;
      const currentIndex = items.indexOf(
        document.activeElement
      );
      switch (e.key) {
        case "ArrowDown": {
          e.preventDefault();
          e.stopPropagation();
          if (currentIndex < items.length - 1) {
            items[currentIndex + 1]?.focus();
          }
          break;
        }
        case "ArrowUp": {
          e.preventDefault();
          e.stopPropagation();
          if (currentIndex > 0) {
            items[currentIndex - 1]?.focus();
          }
          break;
        }
        case "Home": {
          e.preventDefault();
          e.stopPropagation();
          items[0]?.focus();
          break;
        }
        case "End": {
          e.preventDefault();
          e.stopPropagation();
          items[items.length - 1]?.focus();
          break;
        }
        case "ArrowLeft": {
          if (options?.isSubmenu) {
            e.preventDefault();
            e.stopPropagation();
            options.onClose?.();
            options.parentItemRef?.current?.focus();
          }
          break;
        }
        case "Escape": {
          if (options?.isSubmenu) {
            e.preventDefault();
            e.stopPropagation();
            e.nativeEvent.stopImmediatePropagation();
            options.onClose?.();
            options.parentItemRef?.current?.focus();
          }
          break;
        }
        case "Tab": {
          e.preventDefault();
          if (options?.isSubmenu) {
            e.stopPropagation();
            e.nativeEvent.stopImmediatePropagation();
            options.onClose?.();
            options.parentItemRef?.current?.focus();
          } else {
            options?.onCloseRoot?.();
          }
          break;
        }
        default: {
          if (e.key.length === 1 && !e.ctrlKey && !e.metaKey && !e.altKey) {
            e.stopPropagation();
            if (typeAheadTimeoutRef.current) {
              clearTimeout(typeAheadTimeoutRef.current);
            }
            typeAheadBufferRef.current += e.key.toLowerCase();
            typeAheadTimeoutRef.current = setTimeout(() => {
              typeAheadBufferRef.current = "";
            }, 500);
            const buffer = typeAheadBufferRef.current;
            const match = items.find((item) => {
              const text = item.textContent?.toLowerCase() || "";
              return text.startsWith(buffer);
            });
            if (match) {
              match.focus();
            }
          }
          break;
        }
      }
    },
    [containerRef, options]
  );
  React45.useEffect(() => {
    return () => {
      if (typeAheadTimeoutRef.current) {
        clearTimeout(typeAheadTimeoutRef.current);
      }
    };
  }, []);
  return handleKeyDown;
}
function LoadingSpinner({ delay = 50 }) {
  const [show, setShow] = React45.useState(delay <= 0);
  React45.useEffect(() => {
    if (delay <= 0) return;
    const timer = setTimeout(() => setShow(true), delay);
    return () => clearTimeout(timer);
  }, [delay]);
  if (!show) return null;
  return /* @__PURE__ */ jsxRuntime.jsx("div", { className: "flex items-center justify-center py-4 px-8", children: /* @__PURE__ */ jsxRuntime.jsx(lucideReact.Loader2, { className: "h-5 w-5 animate-spin text-sapphire-text-tertiary" }) });
}
function mapMenuPlacementToPopover(menuPlacement = "bottom-start") {
  switch (menuPlacement) {
    case "bottom-start":
      return { placement: "Bottom" /* Bottom */, horizontalAlign: "Start" /* Start */ };
    case "bottom-end":
      return { placement: "Bottom" /* Bottom */, horizontalAlign: "End" /* End */ };
    case "top-start":
      return { placement: "Top" /* Top */, horizontalAlign: "Start" /* Start */ };
    case "top-end":
      return { placement: "Top" /* Top */, horizontalAlign: "End" /* End */ };
    case "right-start":
      return { placement: "End" /* End */, verticalAlign: "Top" /* Top */ };
    case "right-end":
      return { placement: "End" /* End */, verticalAlign: "Bottom" /* Bottom */ };
    case "left-start":
      return { placement: "Start" /* Start */, verticalAlign: "Top" /* Top */ };
    case "left-end":
      return { placement: "Start" /* Start */, verticalAlign: "Bottom" /* Bottom */ };
    default:
      return { placement: "Bottom" /* Bottom */, horizontalAlign: "Start" /* Start */ };
  }
}
function Menu({
  open = false,
  opener,
  children,
  placement = "bottom-start",
  onClose,
  onItemClick,
  onCheck,
  className,
  style,
  headerText,
  loading,
  loadingDelay = 50,
  onBeforeOpen,
  onOpen,
  onBeforeClose,
  ref
}) {
  const { t } = reactI18next.useTranslation("fx");
  const menuRef = React45.useRef(null);
  const submenuClosersRef = React45.useRef(/* @__PURE__ */ new Map());
  const closingRef = React45.useRef(false);
  const onBeforeCloseRef = React45.useRef(onBeforeClose);
  const onCloseRef = React45.useRef(onClose);
  const onItemClickRef = React45.useRef(onItemClick);
  const onCheckRef = React45.useRef(onCheck);
  React45.useEffect(() => {
    onBeforeCloseRef.current = onBeforeClose;
    onCloseRef.current = onClose;
    onItemClickRef.current = onItemClick;
    onCheckRef.current = onCheck;
  });
  const resolvedOpener = typeof opener === "string" ? document.getElementById(opener) : opener;
  const openerRef = React45.useRef(null);
  React45.useEffect(() => {
    openerRef.current = resolvedOpener ?? null;
  }, [resolvedOpener]);
  const closeMenu = React45.useCallback((escPressed) => {
    closingRef.current = true;
    onBeforeCloseRef.current?.({ escPressed: !!escPressed });
    onCloseRef.current?.();
  }, []);
  React45.useImperativeHandle(ref, () => ({
    close: () => closeMenu(),
    isOpen: () => open
  }), [closeMenu, open]);
  React45.useEffect(() => {
    if (open) {
      closingRef.current = false;
    }
  }, [open]);
  const closeAllSubmenus = React45.useCallback(() => {
    submenuClosersRef.current.forEach((close) => close());
  }, []);
  const registerSubmenu = React45.useCallback((id, close) => {
    submenuClosersRef.current.set(id, close);
  }, []);
  const unregisterSubmenu = React45.useCallback((id) => {
    submenuClosersRef.current.delete(id);
  }, []);
  const handleKeyDown = useMenuNavigation(menuRef, { onCloseRoot: closeMenu });
  const stableOnItemClick = React45.useCallback(
    (detail) => onItemClickRef.current?.(detail),
    []
  );
  const stableOnCheck = React45.useCallback(
    (detail) => onCheckRef.current?.(detail),
    []
  );
  const prevOpenLoadingRef = React45.useRef({ open, loading });
  React45.useEffect(() => {
    const prev = prevOpenLoadingRef.current;
    prevOpenLoadingRef.current = { open, loading };
    if (prev.open && open && prev.loading && !loading && menuRef.current) {
      requestAnimationFrame(() => {
        if (menuRef.current) {
          const items = getFocusableItems(menuRef.current);
          const selected = items.find((el) => el.hasAttribute("data-menu-selected") || el.getAttribute("aria-checked") === "true");
          (selected ?? items[0])?.focus();
        }
      });
    }
  }, [loading, open]);
  const menuContextValue = React45.useMemo(
    () => ({ closeMenu, onItemClick: stableOnItemClick, onCheck: stableOnCheck, closeAllSubmenus, registerSubmenu, unregisterSubmenu, submenuDepth: 0 }),
    [closeMenu, stableOnItemClick, stableOnCheck, closeAllSubmenus, registerSubmenu, unregisterSubmenu]
  );
  const popoverPlacement = React45.useMemo(
    () => mapMenuPlacementToPopover(placement),
    [placement]
  );
  const handleOpen = React45.useCallback(() => {
    onOpen?.();
    if (!menuRef.current) return;
    const items = getFocusableItems(menuRef.current);
    const selected = items.find((el) => el.hasAttribute("data-menu-selected") || el.getAttribute("aria-checked") === "true");
    (selected ?? items[0])?.focus();
  }, [onOpen]);
  const isInsidePopup = React45.useCallback((target) => {
    return !!target.closest?.('[data-submenu="true"]');
  }, []);
  const handlePopoverBeforeClose = React45.useCallback(({ escPressed }) => {
    if (closingRef.current) return;
    onBeforeCloseRef.current?.({ escPressed });
  }, []);
  const handlePopoverClose = React45.useCallback(() => {
    const wasClosing = closingRef.current;
    closingRef.current = false;
    if (wasClosing) return;
    onCloseRef.current?.();
  }, []);
  return /* @__PURE__ */ jsxRuntime.jsx(
    Popover,
    {
      opener: openerRef,
      open,
      onBeforeOpen,
      onOpen: handleOpen,
      onBeforeClose: handlePopoverBeforeClose,
      onClose: handlePopoverClose,
      hideArrow: true,
      noPadding: true,
      offset: 4,
      placement: popoverPlacement.placement,
      horizontalAlign: popoverPlacement.horizontalAlign,
      verticalAlign: popoverPlacement.verticalAlign,
      accessibleRole: "Dialog" /* Dialog */,
      accessibleName: t("MENU_POPOVER_ACCESSIBLE_NAME"),
      isInsidePopup,
      className: "p-0 border border-sapphire-border-primary shadow-lg [&>div]:overflow-visible",
      style: {
        filter: "none",
        ...style
      },
      children: /* @__PURE__ */ jsxRuntime.jsx(MenuContext.Provider, { value: menuContextValue, children: /* @__PURE__ */ jsxRuntime.jsxs(
        "div",
        {
          ref: menuRef,
          role: "menu",
          tabIndex: -1,
          className: cn("min-w-[18.75rem] py-sapphire-xs outline-none", className),
          onKeyDown: handleKeyDown,
          children: [
            headerText && /* @__PURE__ */ jsxRuntime.jsx(MenuHeader, { text: headerText }),
            loading ? /* @__PURE__ */ jsxRuntime.jsx(LoadingSpinner, { delay: loadingDelay }) : children
          ]
        }
      ) })
    }
  );
}
function SubmenuScope({ closeMenu, onItemClick, onCheck, parentDepth, children }) {
  const submenuClosersRef = React45.useRef(/* @__PURE__ */ new Map());
  const closeAllSubmenus = React45.useCallback(() => {
    submenuClosersRef.current.forEach((close) => close());
  }, []);
  const registerSubmenu = React45.useCallback((id, close) => {
    submenuClosersRef.current.set(id, close);
  }, []);
  const unregisterSubmenu = React45.useCallback((id) => {
    submenuClosersRef.current.delete(id);
  }, []);
  const depth = parentDepth + 1;
  const contextValue = React45.useMemo(
    () => ({ closeMenu, onItemClick, onCheck, closeAllSubmenus, registerSubmenu, unregisterSubmenu, submenuDepth: depth }),
    [closeMenu, onItemClick, onCheck, closeAllSubmenus, registerSubmenu, unregisterSubmenu, depth]
  );
  return /* @__PURE__ */ jsxRuntime.jsx(MenuContext.Provider, { value: contextValue, children });
}
function MenuItem({
  text,
  icon,
  endContent,
  disabled = false,
  startsSection = false,
  children,
  onClick,
  className,
  data,
  checked,
  additionalText,
  loading: itemLoading,
  loadingDelay = 50,
  tooltip,
  accessibleName,
  accessibilityAttributes,
  ref
}) {
  const { closeMenu, onItemClick, onCheck, closeAllSubmenus, registerSubmenu, unregisterSubmenu, submenuDepth } = React45.useContext(MenuContext);
  const groupContext = React45.useContext(MenuItemGroupContext);
  const itemRef = React45.useRef(null);
  const submenuRef = React45.useRef(null);
  const [submenuOpen, setSubmenuOpen] = React45.useState(false);
  const [portalContainer, setPortalContainer] = React45.useState(null);
  const hoverTimeoutRef = React45.useRef(null);
  const submenuId = React45.useId();
  const submenuChildren = [];
  let hasSubmenu = false;
  React45.Children.forEach(children, (child) => {
    if (React45.isValidElement(child)) {
      const displayName = child.type?.displayName;
      if (displayName === "MenuItem" || displayName === "MenuSeparator" || displayName === "MenuHeader" || displayName === "MenuItemGroup") {
        hasSubmenu = true;
        submenuChildren.push(child);
      }
    }
  });
  if (itemLoading && !hasSubmenu) {
    hasSubmenu = true;
  }
  const checkMode = groupContext?.checkMode ?? "None" /* None */;
  const isInCheckGroup = checkMode !== "None" /* None */;
  let role = "menuitem";
  if (checkMode === "Single" /* Single */) {
    role = "menuitemradio";
  } else if (checkMode === "Multiple" /* Multiple */) {
    role = "menuitemcheckbox";
  }
  if (accessibilityAttributes?.role) {
    role = accessibilityAttributes.role;
  }
  const openSubmenu = React45.useCallback(() => {
    if (itemRef.current) {
      const popoverEl = itemRef.current.closest("[popover]");
      setPortalContainer(popoverEl ?? document.body);
    }
    setSubmenuOpen(true);
  }, []);
  React45.useEffect(() => {
    if (hasSubmenu) {
      registerSubmenu(submenuId, () => setSubmenuOpen(false));
      return () => unregisterSubmenu(submenuId);
    }
  }, [hasSubmenu, registerSubmenu, unregisterSubmenu]);
  const updateSubmenuPosition = React45.useCallback(() => {
    if (!itemRef.current || !submenuRef.current) return;
    const pos = calculateSubmenuPosition(itemRef.current, submenuRef.current);
    submenuRef.current.style.top = `${pos.top}px`;
    submenuRef.current.style.left = `${pos.left}px`;
  }, []);
  const setSubmenuRef = React45.useCallback((node) => {
    submenuRef.current = node;
    if (node) {
      updateSubmenuPosition();
    }
  }, [updateSubmenuPosition]);
  React45.useEffect(() => {
    if (!submenuOpen) return;
    window.addEventListener("scroll", updateSubmenuPosition, true);
    window.addEventListener("resize", updateSubmenuPosition);
    return () => {
      window.removeEventListener("scroll", updateSubmenuPosition, true);
      window.removeEventListener("resize", updateSubmenuPosition);
    };
  }, [submenuOpen, updateSubmenuPosition]);
  const prevSubmenuLoadingRef = React45.useRef({ submenuOpen, itemLoading });
  React45.useEffect(() => {
    const prev = prevSubmenuLoadingRef.current;
    prevSubmenuLoadingRef.current = { submenuOpen, itemLoading };
    if (prev.submenuOpen && submenuOpen && prev.itemLoading && !itemLoading && submenuRef.current) {
      requestAnimationFrame(() => {
        if (submenuRef.current) {
          const items = getFocusableItems(submenuRef.current);
          items[0]?.focus();
        }
      });
    }
  }, [itemLoading, submenuOpen]);
  const openedByKeyboardRef = React45.useRef(false);
  React45.useEffect(() => {
    if (submenuOpen && submenuRef.current && openedByKeyboardRef.current) {
      openedByKeyboardRef.current = false;
      requestAnimationFrame(() => {
        if (submenuRef.current) {
          const items = getFocusableItems(submenuRef.current);
          if (items.length > 0) {
            items[0].focus();
          }
        }
      });
    }
  }, [submenuOpen]);
  const activateItem = (e) => {
    onClick?.(e);
    const result = onItemClick?.({
      item: { text, icon, endContent, disabled, startsSection, hasSubmenu, data, checked },
      text: text || ""
    });
    if (result !== false) {
      closeMenu();
    }
  };
  const handleClick2 = (e) => {
    if (disabled) return;
    if (hasSubmenu) {
      e.stopPropagation();
      if (submenuOpen) {
        setSubmenuOpen(false);
      } else {
        openSubmenu();
      }
      return;
    }
    if (isInCheckGroup) {
      const detail = {
        item: { text, icon, endContent, disabled, startsSection, hasSubmenu, data, checked },
        text: text || "",
        checked: !checked
      };
      onCheck?.(detail);
      onClick?.(e);
      onItemClick?.(detail);
      if (!e.shiftKey) {
        closeMenu();
      }
      return;
    }
    activateItem(e);
  };
  const handleMouseEnter = () => {
    if (disabled) return;
    if (hoverTimeoutRef.current) {
      clearTimeout(hoverTimeoutRef.current);
      hoverTimeoutRef.current = null;
    }
    if (hasSubmenu) {
      closeAllSubmenus();
      openSubmenu();
    }
  };
  const handleMouseLeave = () => {
    if (hasSubmenu) {
      hoverTimeoutRef.current = setTimeout(() => {
        setSubmenuOpen(false);
      }, 150);
    }
  };
  const handleSubmenuMouseEnter = () => {
    if (hoverTimeoutRef.current) {
      clearTimeout(hoverTimeoutRef.current);
      hoverTimeoutRef.current = null;
    }
  };
  const handleSubmenuMouseLeave = () => {
    hoverTimeoutRef.current = setTimeout(() => {
      setSubmenuOpen(false);
    }, 150);
  };
  const handleKeyDown = (e) => {
    if (disabled) return;
    if (e.key === "Enter" || e.key === " ") {
      e.preventDefault();
      if (isInCheckGroup) {
        const detail = {
          item: { text, icon, endContent, disabled, startsSection, hasSubmenu, data, checked },
          text: text || "",
          checked: !checked
        };
        onCheck?.(detail);
        onItemClick?.(detail);
        if (!e.shiftKey) {
          closeMenu();
        }
        return;
      }
      if (hasSubmenu) {
        e.stopPropagation();
        openedByKeyboardRef.current = true;
        openSubmenu();
        return;
      }
      activateItem(e);
    } else if (e.key === "ArrowRight" && hasSubmenu) {
      e.preventDefault();
      e.stopPropagation();
      openedByKeyboardRef.current = true;
      openSubmenu();
    }
  };
  const closeSubmenu = React45.useCallback(() => setSubmenuOpen(false), []);
  const submenuKeyDown = useMenuNavigation(submenuRef, {
    isSubmenu: true,
    onClose: closeSubmenu,
    parentItemRef: itemRef
  });
  React45.useEffect(() => {
    return () => {
      if (hoverTimeoutRef.current) {
        clearTimeout(hoverTimeoutRef.current);
      }
    };
  }, []);
  return /* @__PURE__ */ jsxRuntime.jsxs(jsxRuntime.Fragment, { children: [
    startsSection && /* @__PURE__ */ jsxRuntime.jsx(MenuSeparator, {}),
    /* @__PURE__ */ jsxRuntime.jsxs(
      "div",
      {
        ref: (node) => {
          itemRef.current = node;
          if (typeof ref === "function") {
            ref(node);
          } else if (ref) {
            ref.current = node;
          }
        },
        role,
        tabIndex: disabled ? -1 : 0,
        "aria-disabled": disabled,
        "aria-haspopup": hasSubmenu ? "menu" : void 0,
        "aria-expanded": hasSubmenu ? submenuOpen : void 0,
        "aria-checked": isInCheckGroup ? !!checked : void 0,
        "data-menu-selected": checked && !isInCheckGroup ? "" : void 0,
        "aria-label": accessibleName,
        "aria-keyshortcuts": accessibilityAttributes?.ariaKeyShortcuts,
        title: tooltip,
        onClick: handleClick2,
        onMouseEnter: handleMouseEnter,
        onMouseLeave: handleMouseLeave,
        onKeyDown: handleKeyDown,
        className: cn(
          "group/item relative flex min-h-[54px] cursor-pointer select-none items-center gap-4 px-4 py-2 text-base font-semibold text-sapphire-text-secondary outline outline-2 outline-transparent outline-offset-[-4px]",
          "hover:bg-sapphire-neutral-hover-background-2 hover:text-foreground",
          "active:bg-sapphire-brand-selected-background active:text-foreground",
          "focus:outline-ring focus:z-10",
          checked && !isInCheckGroup && "bg-sapphire-brand-selected-background text-foreground border border-sapphire-brand-foreground focus:border-transparent",
          checked && !isInCheckGroup && "hover:bg-sapphire-brand-toggle-background",
          disabled && "pointer-events-none opacity-50",
          className
        ),
        children: [
          isInCheckGroup && /* @__PURE__ */ jsxRuntime.jsx("span", { className: "flex size-5 shrink-0 items-center justify-center", children: checked && /* @__PURE__ */ jsxRuntime.jsx(AcceptIcon, { className: "size-5" }) }),
          icon && /* @__PURE__ */ jsxRuntime.jsx("span", { className: cn("flex size-5 shrink-0 items-center justify-center [&>svg]:size-5 group-hover/item:text-sapphire-brand-foreground group-active/item:text-sapphire-brand-foreground", checked && !isInCheckGroup && "text-sapphire-brand-foreground"), children: icon }),
          /* @__PURE__ */ jsxRuntime.jsx("span", { className: "flex-1 truncate", children: text }),
          hasSubmenu ? /* @__PURE__ */ jsxRuntime.jsx(SlimArrowRightIcon, { className: "ml-auto size-5 shrink-0 text-sapphire-text-tertiary" }) : endContent ? /* @__PURE__ */ jsxRuntime.jsx("span", { className: "ml-auto", children: endContent }) : additionalText ? /* @__PURE__ */ jsxRuntime.jsx("span", { className: "ml-4 shrink-0 text-sm text-sapphire-text-tertiary", children: additionalText }) : null
        ]
      }
    ),
    hasSubmenu && submenuOpen && reactDom.createPortal(
      /* @__PURE__ */ jsxRuntime.jsx(SubmenuScope, { closeMenu, onItemClick, onCheck, parentDepth: submenuDepth, children: /* @__PURE__ */ jsxRuntime.jsx(
        "div",
        {
          ref: setSubmenuRef,
          role: "menu",
          "data-submenu": "true",
          className: "min-w-[18.75rem] overflow-hidden rounded-lg border border-sapphire-border-primary bg-sapphire-card-bg-primary py-3.5 text-popover-foreground shadow-lg",
          style: {
            position: "fixed",
            // submenuDepth is the *parent's* depth from context.
            // +1 yields the z-index for this child submenu, so nested
            // submenus stack correctly: depth 0→z61, depth 1→z62, etc.
            zIndex: 60 + submenuDepth + 1
          },
          onMouseEnter: handleSubmenuMouseEnter,
          onMouseLeave: handleSubmenuMouseLeave,
          onKeyDown: submenuKeyDown,
          children: itemLoading ? /* @__PURE__ */ jsxRuntime.jsx(LoadingSpinner, { delay: loadingDelay }) : submenuChildren
        }
      ) }),
      portalContainer ?? document.body
    )
  ] });
}
MenuItem.displayName = "MenuItem";
function MenuSeparator({ className, ref }) {
  return /* @__PURE__ */ jsxRuntime.jsx(
    "div",
    {
      ref,
      role: "separator",
      className: cn("my-2 h-px bg-border mx-4", className)
    }
  );
}
MenuSeparator.displayName = "MenuSeparator";
function MenuHeader({ text, children, endContent, className, ref }) {
  return /* @__PURE__ */ jsxRuntime.jsxs(
    "div",
    {
      ref,
      className: cn(
        "px-4 py-1.5 text-sm font-semibold text-sapphire-text-tertiary",
        endContent && "flex items-center gap-2",
        className
      ),
      children: [
        /* @__PURE__ */ jsxRuntime.jsx("span", { className: endContent ? "flex-1" : void 0, children: text || children }),
        endContent && /* @__PURE__ */ jsxRuntime.jsx("span", { className: "flex shrink-0 items-center gap-1", children: endContent })
      ]
    }
  );
}
MenuHeader.displayName = "MenuHeader";
var SegmentedButtonContext = React45.createContext({
  selectionMode: "Single",
  size: "Large",
  onItemSelect: () => {
  }
});
function SegmentedButton({
  selectedId,
  selectedIds,
  selectionMode = "Single",
  size = "Large",
  children,
  onSelectionChange,
  accessibleName,
  accessibleNameRef,
  className,
  style,
  ref
}) {
  const { t } = reactI18next.useTranslation("fx");
  const handleItemSelect = React45.useCallback(
    (id, item) => {
      if (selectionMode === "Multiple") {
        const current = selectedIds ?? [];
        const newIds = current.includes(id) ? current.filter((i) => i !== id) : [...current, id];
        onSelectionChange?.({
          selectedItem: item,
          selectedId: id,
          selectedIds: newIds
        });
      } else {
        onSelectionChange?.({
          selectedItem: item,
          selectedId: id,
          selectedIds: [id]
        });
      }
    },
    [onSelectionChange, selectionMode, selectedIds]
  );
  const innerRef = React45.useRef(null);
  const handleKeyDown = React45.useCallback(
    (e) => {
      const container = innerRef.current;
      if (!container) return;
      if (e.key === "ArrowLeft" || e.key === "ArrowRight") {
        const items = Array.from(
          container.querySelectorAll(
            'li[role="option"]:not([aria-disabled="true"])'
          )
        );
        if (items.length === 0) return;
        const currentIndex = items.indexOf(
          document.activeElement?.closest?.("li")
        );
        if (currentIndex === -1) return;
        e.preventDefault();
        const next = e.key === "ArrowRight" ? (currentIndex + 1) % items.length : (currentIndex - 1 + items.length) % items.length;
        items[next].focus();
      } else if (e.key === "Enter" || e.key === " ") {
        const focused = document.activeElement?.closest?.(
          'li[role="option"]'
        );
        if (focused) {
          e.preventDefault();
          focused.click();
        }
      }
    },
    []
  );
  const isLarge = size === "Large";
  return /* @__PURE__ */ jsxRuntime.jsx(
    SegmentedButtonContext.Provider,
    {
      value: {
        selectedId,
        selectedIds,
        selectionMode,
        size,
        onItemSelect: handleItemSelect
      },
      children: /* @__PURE__ */ jsxRuntime.jsx(
        "ul",
        {
          ref: (node) => {
            innerRef.current = node;
            if (typeof ref === "function") ref(node);
            else if (ref) ref.current = node;
          },
          role: "listbox",
          "aria-label": accessibleName || t("SEGMENTEDBUTTON_ARIA_DESCRIPTION"),
          "aria-labelledby": accessibleNameRef,
          "aria-multiselectable": selectionMode === "Multiple" || void 0,
          onKeyDown: handleKeyDown,
          className: cn(
            "inline-flex list-none m-0 p-0 outline outline-1 -outline-offset-1 outline-sapphire-border-primary bg-background",
            isLarge ? "rounded-lg" : "rounded",
            className
          ),
          style,
          children
        }
      )
    }
  );
}
function SegmentedButtonItem({
  id,
  text,
  icon,
  selected: controlledSelected,
  disabled = false,
  tooltip,
  className,
  ref
}) {
  const { selectedId, selectedIds, selectionMode, size, onItemSelect } = React45.useContext(SegmentedButtonContext);
  const { t } = reactI18next.useTranslation("fx");
  const isSelected = controlledSelected ?? (selectionMode === "Multiple" ? selectedIds?.includes(id) ?? false : selectedId === id);
  const itemRef = React45.useRef(null);
  const handleClick2 = () => {
    if (disabled) return;
    itemRef.current?.focus();
    onItemSelect(id, { id, text, icon, selected: true, disabled, tooltip });
  };
  const isIconOnly = icon && !text;
  const isLarge = size === "Large";
  return /* @__PURE__ */ jsxRuntime.jsxs(
    "li",
    {
      ref: (node) => {
        itemRef.current = node;
        if (typeof ref === "function") ref(node);
        else if (ref) ref.current = node;
      },
      role: "option",
      "aria-selected": isSelected,
      "aria-disabled": disabled || void 0,
      "aria-roledescription": t("SEGMENTEDBUTTONITEM_ARIA_DESCRIPTION"),
      tabIndex: isSelected ? 0 : -1,
      title: tooltip,
      onClick: handleClick2,
      className: cn(
        "inline-flex items-center justify-center gap-sapphire-2xs transition-[color,background-color,border-color] cursor-pointer relative",
        isLarge ? "rounded-lg h-10 min-w-[2.5rem] px-4 text-base gap-2 font-semibold" : size === "Medium" ? "rounded h-8 min-w-[2.25rem] px-3 text-sm gap-1.5 font-semibold" : "rounded h-6 min-w-[1.75rem] px-2 text-xs gap-1 font-normal",
        "focus:z-20",
        "border",
        isSelected ? "z-10 border-sapphire-brand-foreground text-sapphire-brand-foreground focus:[outline-style:solid] focus:outline-2 focus:-outline-offset-2 focus:outline-ring" : "border-transparent text-sapphire-neutral-foreground-black hover:bg-sapphire-neutral-hover-background focus:[outline-style:solid] focus:outline-2 focus:-outline-offset-2 focus:outline-ring",
        disabled && "pointer-events-none text-sapphire-button-fg-disabled",
        isIconOnly && (isLarge ? "px-2" : size === "Medium" ? "px-2" : "px-1.5"),
        className
      ),
      children: [
        icon && /* @__PURE__ */ jsxRuntime.jsx("span", { className: cn("inline-flex items-center", isLarge ? "[&>svg]:h-5 [&>svg]:w-5" : size === "Medium" ? "[&>svg]:h-4 [&>svg]:w-4" : "[&>svg]:h-[10.5px] [&>svg]:w-[10.5px]"), children: icon }),
        text && /* @__PURE__ */ jsxRuntime.jsx("span", { children: text })
      ]
    }
  );
}
var pathData7 = [
  "M410.53 200.75C418.95 210.98 417.48 226.11 407.25 234.53L271.34 346.53C262.48 353.83 249.68 353.83 240.81 346.53L104.75 234.53C94.52 226.11 93.05 210.98 101.47 200.75C109.89 190.52 125.02 189.05 135.25 197.47L256.06 296.91L376.75 197.47C386.98 189.05 402.11 190.52 410.53 200.75Z"
];
var SlimArrowDownIcon = Object.assign(
  React45__namespace.default.forwardRef(
    (props, ref) => /* @__PURE__ */ jsxRuntime.jsx(Icon, { ref, pathData: pathData7, ...props })
  ),
  { displayName: "SlimArrowDownIcon", iconName: "slim-arrow-down" }
);
function isSpace(e) {
  return e.key === " " || e.key === "Spacebar";
}
function isEnter(e) {
  return e.key === "Enter";
}
function isDown(e) {
  return e.key === "ArrowDown" && !e.altKey;
}
function isUp(e) {
  return e.key === "ArrowUp" && !e.altKey;
}
function isDownAlt(e) {
  return e.key === "ArrowDown" && e.altKey;
}
function isUpAlt(e) {
  return e.key === "ArrowUp" && e.altKey;
}
function isF4(e) {
  return e.key === "F4";
}
function isShift(e) {
  return e.key === "Shift";
}
function isEscape(e) {
  return e.key === "Escape";
}
function isTab(e) {
  return e.key === "Tab";
}
function isArrowKeyAction(e) {
  return isDown(e) || isUp(e) || isDownAlt(e) || isUpAlt(e) || isF4(e);
}
function isDefaultAction(e) {
  return isSpace(e) || isEnter(e);
}
var containerVariants = cva(
  [
    "inline-flex relative group",
    "border"
  ],
  {
    variants: {
      design: {
        ["Secondary" /* Secondary */]: "bg-transparent border-sapphire-brand-foreground",
        ["Primary" /* Primary */]: "bg-sapphire-brand-background border-sapphire-brand-background",
        ["PrimaryJoule" /* PrimaryJoule */]: "bg-sapphire-joule-background border-sapphire-joule-background",
        ["SecondaryJoule" /* SecondaryJoule */]: "bg-transparent border-sapphire-joule-foreground",
        ["Neutral" /* Neutral */]: "bg-transparent border-sapphire-neutral-foreground-muted",
        ["SecondaryNeutral" /* SecondaryNeutral */]: "bg-transparent border-transparent",
        ["TertiaryNeutral" /* TertiaryNeutral */]: "bg-transparent border-transparent",
        ["Tertiary" /* Tertiary */]: "bg-transparent border-transparent",
        ["TertiaryJoule" /* TertiaryJoule */]: "bg-transparent border-transparent"
      }
    },
    defaultVariants: {
      design: "Secondary" /* Secondary */
    }
  }
);
var textColorClass = {
  ["Secondary" /* Secondary */]: "text-sapphire-brand-foreground",
  ["Primary" /* Primary */]: "text-sapphire-neutral-foreground-white",
  ["PrimaryJoule" /* PrimaryJoule */]: "text-sapphire-neutral-foreground-white",
  ["SecondaryJoule" /* SecondaryJoule */]: "text-sapphire-joule-foreground",
  ["Neutral" /* Neutral */]: "text-sapphire-neutral-foreground-black",
  ["SecondaryNeutral" /* SecondaryNeutral */]: "text-sapphire-neutral-foreground-black",
  ["TertiaryNeutral" /* TertiaryNeutral */]: "text-sapphire-neutral-foreground-muted",
  ["Tertiary" /* Tertiary */]: "text-sapphire-brand-foreground",
  ["TertiaryJoule" /* TertiaryJoule */]: "text-sapphire-joule-foreground"
};
var textBtnHoverClass = {
  ["Secondary" /* Secondary */]: "hover:bg-sapphire-neutral-hover-background hover:z-10",
  ["Primary" /* Primary */]: "hover:bg-sapphire-brand-hover-background hover:z-10",
  ["PrimaryJoule" /* PrimaryJoule */]: "hover:bg-sapphire-joule-hover-background hover:z-10",
  ["SecondaryJoule" /* SecondaryJoule */]: "hover:bg-sapphire-neutral-hover-background hover:z-10",
  ["Neutral" /* Neutral */]: "hover:bg-sapphire-neutral-hover-background hover:text-sapphire-neutral-foreground-black hover:z-10",
  ["SecondaryNeutral" /* SecondaryNeutral */]: "hover:bg-sapphire-neutral-hover-background hover:z-10",
  ["TertiaryNeutral" /* TertiaryNeutral */]: "hover:text-sapphire-neutral-foreground-black hover:z-10",
  ["Tertiary" /* Tertiary */]: "hover:bg-sapphire-neutral-hover-background hover:z-10",
  ["TertiaryJoule" /* TertiaryJoule */]: "hover:bg-sapphire-neutral-hover-background hover:z-10"
};
var textBtnActiveClass = {
  ["Secondary" /* Secondary */]: "bg-sapphire-neutral-pressed-background text-sapphire-brand-pressed-foreground z-10",
  ["Primary" /* Primary */]: "bg-sapphire-brand-pressed-background text-sapphire-neutral-foreground-white z-10",
  ["PrimaryJoule" /* PrimaryJoule */]: "bg-sapphire-joule-pressed-background text-sapphire-neutral-foreground-white z-10",
  ["SecondaryJoule" /* SecondaryJoule */]: "bg-sapphire-neutral-pressed-background text-sapphire-joule-pressed-foreground z-10",
  ["Neutral" /* Neutral */]: "bg-sapphire-neutral-pressed-background text-sapphire-neutral-foreground-black z-10",
  ["SecondaryNeutral" /* SecondaryNeutral */]: "bg-sapphire-neutral-pressed-background text-sapphire-neutral-foreground-black z-10",
  ["TertiaryNeutral" /* TertiaryNeutral */]: "text-sapphire-neutral-foreground-black z-10",
  ["Tertiary" /* Tertiary */]: "bg-sapphire-neutral-pressed-background text-sapphire-brand-pressed-foreground z-10",
  ["TertiaryJoule" /* TertiaryJoule */]: "bg-sapphire-neutral-pressed-background text-sapphire-joule-pressed-foreground z-10"
};
var arrowBtnHoverClass = {
  ["Secondary" /* Secondary */]: "hover:bg-sapphire-neutral-hover-background hover:z-10",
  ["Primary" /* Primary */]: "hover:bg-sapphire-brand-hover-background hover:z-10",
  ["PrimaryJoule" /* PrimaryJoule */]: "hover:bg-sapphire-joule-hover-background hover:z-10",
  ["SecondaryJoule" /* SecondaryJoule */]: "hover:bg-sapphire-neutral-hover-background hover:z-10",
  ["Neutral" /* Neutral */]: "hover:bg-sapphire-neutral-hover-background hover:text-sapphire-neutral-foreground-black hover:z-10",
  ["SecondaryNeutral" /* SecondaryNeutral */]: "hover:bg-sapphire-neutral-hover-background hover:z-10",
  ["TertiaryNeutral" /* TertiaryNeutral */]: "hover:text-sapphire-neutral-foreground-black hover:z-10",
  ["Tertiary" /* Tertiary */]: "hover:bg-sapphire-neutral-hover-background hover:z-10",
  ["TertiaryJoule" /* TertiaryJoule */]: "hover:bg-sapphire-neutral-hover-background hover:z-10"
};
var arrowBtnActiveClass = {
  ["Secondary" /* Secondary */]: "bg-sapphire-brand-toggle-background text-sapphire-brand-foreground z-10",
  ["Primary" /* Primary */]: "bg-sapphire-brand-hover-background text-sapphire-neutral-foreground-white z-10",
  ["PrimaryJoule" /* PrimaryJoule */]: "bg-sapphire-joule-hover-background text-sapphire-neutral-foreground-white z-10",
  ["SecondaryJoule" /* SecondaryJoule */]: "bg-sapphire-brand-toggle-background text-sapphire-joule-foreground z-10",
  ["Neutral" /* Neutral */]: "bg-sapphire-neutral-pressed-background text-sapphire-neutral-foreground-black z-10",
  ["SecondaryNeutral" /* SecondaryNeutral */]: "bg-sapphire-brand-toggle-background text-sapphire-neutral-foreground-black z-10",
  ["TertiaryNeutral" /* TertiaryNeutral */]: "text-sapphire-neutral-foreground-black z-10",
  ["Tertiary" /* Tertiary */]: "bg-sapphire-brand-toggle-background text-sapphire-brand-foreground z-10",
  ["TertiaryJoule" /* TertiaryJoule */]: "bg-sapphire-brand-toggle-background text-sapphire-joule-foreground z-10"
};
var arrowBtnActiveHoverClass = {
  ["Secondary" /* Secondary */]: "hover:bg-sapphire-brand-toggle-hover-background hover:z-10",
  ["Primary" /* Primary */]: "hover:bg-blue-800 hover:z-10",
  ["PrimaryJoule" /* PrimaryJoule */]: "hover:bg-purple-800 hover:z-10",
  ["SecondaryJoule" /* SecondaryJoule */]: "hover:bg-sapphire-brand-toggle-hover-background hover:z-10",
  ["Neutral" /* Neutral */]: "hover:bg-sapphire-neutral-hover-background hover:z-10",
  ["SecondaryNeutral" /* SecondaryNeutral */]: "hover:bg-sapphire-brand-toggle-hover-background hover:z-10",
  ["TertiaryNeutral" /* TertiaryNeutral */]: "hover:text-sapphire-neutral-foreground-black hover:z-10",
  ["Tertiary" /* Tertiary */]: "hover:bg-sapphire-brand-toggle-hover-background hover:z-10",
  ["TertiaryJoule" /* TertiaryJoule */]: "hover:bg-sapphire-brand-toggle-hover-background hover:z-10"
};
var separatorColor = {
  ["Secondary" /* Secondary */]: "bg-sapphire-brand-foreground",
  ["Primary" /* Primary */]: "bg-sapphire-neutral-foreground-white",
  ["PrimaryJoule" /* PrimaryJoule */]: "bg-sapphire-neutral-foreground-white",
  ["SecondaryJoule" /* SecondaryJoule */]: "bg-sapphire-joule-foreground",
  ["Neutral" /* Neutral */]: "bg-sapphire-neutral-foreground-black",
  ["SecondaryNeutral" /* SecondaryNeutral */]: "bg-sapphire-neutral-foreground-black",
  ["TertiaryNeutral" /* TertiaryNeutral */]: "bg-sapphire-neutral-foreground-muted",
  ["Tertiary" /* Tertiary */]: "bg-sapphire-brand-foreground",
  ["TertiaryJoule" /* TertiaryJoule */]: "bg-sapphire-joule-foreground"
};
var separatorDesigns = /* @__PURE__ */ new Set([
  "Secondary" /* Secondary */,
  "SecondaryJoule" /* SecondaryJoule */,
  "Neutral" /* Neutral */
]);
var borderlessDesigns = /* @__PURE__ */ new Set([
  "Tertiary" /* Tertiary */,
  "TertiaryJoule" /* TertiaryJoule */,
  "TertiaryNeutral" /* TertiaryNeutral */,
  "SecondaryNeutral" /* SecondaryNeutral */
]);
var focusSizeClass = {
  Large: "split-btn-focus-lg",
  Medium: "split-btn-focus-md",
  Small: "split-btn-focus-sm"
};
var innerSizeClassMap = {
  Large: "split-btn-inner-lg",
  Medium: "split-btn-inner-md",
  Small: "split-btn-inner-sm"
};
var hoverRoundingMap = {
  Large: "hover:rounded-lg",
  Medium: "hover:rounded",
  Small: "hover:rounded"
};
var activeRoundingMap = {
  Large: "rounded-lg",
  Medium: "rounded",
  Small: "rounded"
};
function SplitButton({
  text,
  icon,
  endIcon,
  design = "Secondary" /* Secondary */,
  size = "Large",
  disabled = false,
  loading = false,
  activeArrowButton: activeArrowButtonProp = false,
  accessibleName,
  accessibilityAttributes,
  onClick,
  onArrowClick,
  className,
  style,
  id: idProp,
  ref
}) {
  const autoId = React45.useId();
  const id = idProp || autoId;
  const { t } = reactI18next.useTranslation("fx");
  const containerRef = React45.useRef(null);
  const textButtonRef = React45.useRef(null);
  const arrowButtonRef = React45.useRef(null);
  const [textButtonActive, setTextButtonActive] = React45.useState(false);
  const [internalArrowActive, setInternalArrowActive] = React45.useState(false);
  const [shiftOrEscapeDuringSpace, setShiftOrEscapeDuringSpace] = React45.useState(false);
  const [tabIndex, setTabIndex] = React45.useState(disabled ? -1 : 0);
  const effectiveActiveArrow = activeArrowButtonProp || internalArrowActive;
  const d = design;
  const sizeClasses3 = {
    Large: { container: "h-10 rounded-lg", arrow: "w-10", text: "text-base px-4 gap-2 font-semibold", icon: "h-5 w-5 [&>svg]:h-[17.5px] [&>svg]:w-[17.5px]", chevron: "h-4 w-4" },
    Medium: { container: "h-8 rounded", arrow: "w-8", text: "text-sm px-3 gap-1.5 font-semibold", icon: "h-4 w-4 [&>svg]:h-3.5 [&>svg]:w-3.5", chevron: "h-3.5 w-3.5" },
    Small: { container: "h-6 rounded", arrow: "w-6", text: "text-xs px-2 gap-1 font-normal", icon: "h-3 w-3 [&>svg]:h-[10.5px] [&>svg]:w-[10.5px]", chevron: "h-3 w-3" }
  }[size];
  React45.useImperativeHandle(ref, () => ({
    focus() {
      containerRef.current?.focus();
    },
    get nativeElement() {
      return containerRef.current;
    },
    get textButton() {
      return textButtonRef.current;
    },
    get arrowButton() {
      return arrowButtonRef.current;
    }
  }));
  const fireClick = React45.useCallback(() => {
    onClick?.();
  }, [onClick]);
  const fireArrowClick = React45.useCallback(() => {
    onArrowClick?.();
  }, [onArrowClick]);
  const resetActionStates = React45.useCallback(() => {
    setInternalArrowActive(false);
    setTextButtonActive(false);
    setShiftOrEscapeDuringSpace(false);
  }, []);
  const setTabIndexValue = React45.useCallback(
    (innerButtonPressed) => {
      let val = disabled ? -1 : 0;
      if (val === -1 && innerButtonPressed) {
        val = 0;
      }
      setTabIndex(val);
    },
    [disabled]
  );
  const handleFocusOut = React45.useCallback(
    (e) => {
      if (disabled) return;
      const related = e.relatedTarget;
      if (containerRef.current?.contains(related)) return;
      resetActionStates();
      setTabIndexValue();
    },
    [disabled, resetActionStates, setTabIndexValue]
  );
  const handleKeyDown = React45.useCallback(
    (e) => {
      if (disabled || loading) return;
      if (isArrowKeyAction(e)) {
        e.preventDefault();
        fireArrowClick();
        setInternalArrowActive(true);
        return;
      }
      if (isDefaultAction(e)) {
        e.preventDefault();
        const target = e.target;
        if (isEnter(e)) {
          if (arrowButtonRef.current && target === arrowButtonRef.current) {
            setInternalArrowActive(true);
            onArrowClick?.();
            return;
          }
          setTextButtonActive(true);
          fireClick();
          return;
        }
        if (isSpace(e)) {
          setTextButtonActive(true);
        }
        if (isTab(e)) {
          resetActionStates();
        }
        return;
      }
      if ((isShift(e) || isEscape(e)) && textButtonActive) {
        e.preventDefault();
        setShiftOrEscapeDuringSpace(true);
      }
      if (isEscape(e) && !textButtonActive) {
        resetActionStates();
      }
      setTabIndex(-1);
    },
    [disabled, loading, fireArrowClick, fireClick, onArrowClick, textButtonActive, resetActionStates]
  );
  const handleKeyUp = React45.useCallback(
    (e) => {
      if (disabled || loading) return;
      if (isArrowKeyAction(e)) {
        e.preventDefault();
        setInternalArrowActive(false);
        return;
      }
      if (isSpace(e)) {
        e.preventDefault();
        e.stopPropagation();
        setTextButtonActive(false);
        if (!shiftOrEscapeDuringSpace) {
          const target = e.target;
          if (arrowButtonRef.current && target === arrowButtonRef.current) {
            onArrowClick?.();
          } else {
            fireClick();
          }
        }
        setShiftOrEscapeDuringSpace(false);
        return;
      }
      if (isEnter(e) || isShift(e) && textButtonActive) {
        setTextButtonActive(false);
      }
    },
    [disabled, loading, shiftOrEscapeDuringSpace, fireClick, onArrowClick, textButtonActive]
  );
  const handleTextMouseDown = React45.useCallback(
    (e) => {
      if (disabled || loading) return;
      e.stopPropagation();
      setTextButtonActive(true);
      setTabIndex(-1);
    },
    [disabled, loading]
  );
  const handleTextMouseUp = React45.useCallback(() => {
    setTextButtonActive(false);
    setTabIndex(-1);
  }, []);
  const handleTextClick = React45.useCallback(
    (e) => {
      if (disabled || loading) return;
      e.stopPropagation();
      fireClick();
    },
    [disabled, loading, fireClick]
  );
  const handleArrowMouseDown = React45.useCallback(
    (e) => {
      if (disabled || loading) return;
      e.stopPropagation();
      setTabIndex(-1);
    },
    [disabled, loading]
  );
  const handleArrowClick = React45.useCallback(
    (e) => {
      if (disabled || loading) return;
      e.preventDefault();
      e.stopPropagation();
      fireArrowClick();
    },
    [disabled, loading, fireArrowClick]
  );
  const handleInnerFocusIn = React45.useCallback(
    (e) => {
      e.stopPropagation();
      setTabIndexValue(true);
      e.target.focus();
    },
    [setTabIndexValue]
  );
  const arrowHasPopup = accessibilityAttributes?.arrowButton?.hasPopup || "menu";
  const arrowExpanded = accessibilityAttributes?.arrowButton?.expanded ?? effectiveActiveArrow;
  const arrowTitle = accessibilityAttributes?.arrowButton?.title || t("SPLIT_BUTTON_ARROW_BUTTON_TOOLTIP");
  const rootHasPopup = accessibilityAttributes?.root?.hasPopup;
  const rootRoleDescription = accessibilityAttributes?.root?.roleDescription || t("SPLIT_BUTTON_DESCRIPTION");
  const rootTitle = accessibilityAttributes?.root?.title;
  const rootAriaKeyShortcuts = accessibilityAttributes?.root?.ariaKeyShortcuts;
  const isDisabled = disabled || loading;
  const isBorderless = borderlessDesigns.has(d);
  const hoverRounding = isBorderless ? hoverRoundingMap[size] : "";
  const activeRounding = isBorderless ? activeRoundingMap[size] : "";
  const isFilled = d === "Primary" /* Primary */ || d === "PrimaryJoule" /* PrimaryJoule */;
  const containerFocusVariant = {
    ["Primary" /* Primary */]: "split-btn-focus split-btn-focus-filled-brand",
    ["PrimaryJoule" /* PrimaryJoule */]: "split-btn-focus split-btn-focus-filled-joule",
    ["Secondary" /* Secondary */]: "split-btn-focus split-btn-focus-brand",
    ["SecondaryJoule" /* SecondaryJoule */]: "split-btn-focus split-btn-focus-joule",
    ["Tertiary" /* Tertiary */]: "split-btn-focus split-btn-focus-brand",
    ["TertiaryJoule" /* TertiaryJoule */]: "split-btn-focus split-btn-focus-joule",
    ["Neutral" /* Neutral */]: "split-btn-focus split-btn-focus-neutral",
    ["SecondaryNeutral" /* SecondaryNeutral */]: "split-btn-focus split-btn-focus-neutral",
    ["TertiaryNeutral" /* TertiaryNeutral */]: "split-btn-focus split-btn-focus-neutral"
  };
  const containerFocusSizeClass = focusSizeClass[size];
  const containerFocusOutlineClass = cn(containerFocusVariant[d], containerFocusSizeClass);
  const innerFocusColorClass = {
    ["Primary" /* Primary */]: "split-btn-inner-white",
    ["PrimaryJoule" /* PrimaryJoule */]: "split-btn-inner-white",
    ["Secondary" /* Secondary */]: "split-btn-inner-brand",
    ["SecondaryJoule" /* SecondaryJoule */]: "split-btn-inner-joule",
    ["Tertiary" /* Tertiary */]: "split-btn-inner-brand",
    ["TertiaryJoule" /* TertiaryJoule */]: "split-btn-inner-joule",
    ["Neutral" /* Neutral */]: "split-btn-inner-neutral",
    ["SecondaryNeutral" /* SecondaryNeutral */]: "split-btn-inner-neutral",
    ["TertiaryNeutral" /* TertiaryNeutral */]: "split-btn-inner-neutral"
  };
  const innerSizeClass = innerSizeClassMap[size];
  const innerFocusClass = cn(
    "split-btn-inner",
    innerSizeClass,
    innerFocusColorClass[d]
  );
  const textFocusRing = innerFocusClass;
  const arrowFocusRing = innerFocusClass;
  return /* @__PURE__ */ jsxRuntime.jsxs(
    "div",
    {
      ref: containerRef,
      id,
      role: "group",
      tabIndex: isDisabled ? -1 : tabIndex,
      "aria-labelledby": `${id}-invisibleTextDefault ${id}-invisibleText`,
      "aria-haspopup": rootHasPopup,
      "aria-roledescription": rootRoleDescription,
      "aria-keyshortcuts": rootAriaKeyShortcuts,
      className: cn(
        containerVariants({ design: d }),
        sizeClasses3.container,
        "shrink-0 w-fit",
        isDisabled && "pointer-events-none opacity-50",
        "outline-none",
        containerFocusOutlineClass,
        className
      ),
      style,
      onBlur: handleFocusOut,
      onKeyDown: handleKeyDown,
      onKeyUp: handleKeyUp,
      children: [
        /* @__PURE__ */ jsxRuntime.jsxs(
          "button",
          {
            ref: textButtonRef,
            type: "button",
            tabIndex: -1,
            disabled: isDisabled,
            title: rootTitle,
            onClick: handleTextClick,
            onMouseDown: handleTextMouseDown,
            onMouseUp: handleTextMouseUp,
            onFocus: handleInnerFocusIn,
            className: cn(
              "inline-flex items-center justify-start cursor-pointer relative",
              sizeClasses3.text,
              "flex-grow",
              "rounded-s-[inherit] rounded-e-none",
              "bg-transparent",
              isFilled && "border-r border-r-sapphire-neutral-foreground-white",
              textColorClass[d],
              "whitespace-nowrap",
              "outline-none transition-colors",
              "disabled:pointer-events-none",
              textFocusRing,
              !textButtonActive && textBtnHoverClass[d],
              !textButtonActive && hoverRounding,
              textButtonActive && textBtnActiveClass[d],
              textButtonActive && activeRounding
            ),
            children: [
              loading ? /* @__PURE__ */ jsxRuntime.jsx(lucideReact.Loader2, { className: cn(sizeClasses3.icon, "animate-spin") }) : icon && /* @__PURE__ */ jsxRuntime.jsx("span", { className: cn("inline-flex items-center justify-center shrink-0", sizeClasses3.icon), children: icon }),
              text && /* @__PURE__ */ jsxRuntime.jsx("span", { children: text }),
              endIcon && !loading && /* @__PURE__ */ jsxRuntime.jsx("span", { className: "inline-flex items-center shrink-0", children: endIcon })
            ]
          }
        ),
        separatorDesigns.has(d) && /* @__PURE__ */ jsxRuntime.jsx(
          "span",
          {
            "aria-hidden": "true",
            className: cn(
              "w-px shrink-0 self-stretch z-[1] pointer-events-none",
              separatorColor[d]
            )
          }
        ),
        /* @__PURE__ */ jsxRuntime.jsx(
          "button",
          {
            ref: arrowButtonRef,
            type: "button",
            tabIndex: -1,
            disabled: isDisabled,
            "aria-haspopup": arrowHasPopup,
            "aria-expanded": arrowExpanded,
            title: arrowTitle,
            onClick: handleArrowClick,
            onMouseDown: handleArrowMouseDown,
            onFocus: handleInnerFocusIn,
            className: cn(
              "inline-flex items-center justify-center cursor-pointer",
              sizeClasses3.arrow,
              "shrink-0",
              "rounded-e-[inherit] rounded-s-none",
              "bg-transparent",
              textColorClass[d],
              "outline-none transition-colors overflow-visible relative",
              "disabled:pointer-events-none",
              arrowFocusRing,
              !effectiveActiveArrow && arrowBtnHoverClass[d],
              !effectiveActiveArrow && hoverRounding,
              effectiveActiveArrow && arrowBtnActiveClass[d],
              effectiveActiveArrow && arrowBtnActiveHoverClass[d],
              effectiveActiveArrow && activeRounding
            ),
            children: /* @__PURE__ */ jsxRuntime.jsx(SlimArrowDownIcon, { className: sizeClasses3.chevron })
          }
        ),
        /* @__PURE__ */ jsxRuntime.jsxs("span", { id: `${id}-invisibleText`, className: "sr-only", children: [
          t("SPLIT_BUTTON_KEYBOARD_HINT"),
          accessibleName ? ` ${accessibleName}` : ""
        ] }),
        /* @__PURE__ */ jsxRuntime.jsx("span", { id: `${id}-invisibleTextDefault`, className: "sr-only", children: text })
      ]
    }
  );
}
function FxRenameTitle({
  text,
  editMode: controlledEditMode,
  wrappingType = "Normal",
  onEditAccept,
  onEditCancel,
  onEditModeChange,
  className,
  ref
}) {
  const { t } = reactI18next.useTranslation("fx");
  const inputRef = React45.useRef(null);
  const isCommittingRef = React45.useRef(false);
  const [internalEditMode, setInternalEditMode] = React45.useState(false);
  const [editValue, setEditValue] = React45.useState(text);
  const [originalValue, setOriginalValue] = React45.useState(text);
  const editMode = controlledEditMode ?? internalEditMode;
  React45.useEffect(() => {
    if (!editMode) {
      setEditValue(text);
    }
  }, [text, editMode]);
  React45.useEffect(() => {
    if (editMode && inputRef.current) {
      setTimeout(() => {
        inputRef.current?.focus();
        inputRef.current?.select();
      }, 0);
    }
  }, [editMode]);
  const enterEditMode = React45.useCallback(() => {
    setOriginalValue(text);
    setEditValue(text);
    setInternalEditMode(true);
    onEditModeChange?.({ editMode: true });
  }, [text, onEditModeChange]);
  const exitEditMode = React45.useCallback(() => {
    setInternalEditMode(false);
    onEditModeChange?.({ editMode: false });
  }, [onEditModeChange]);
  const acceptEdit = React45.useCallback(() => {
    const newValue = editValue.trim();
    const previousValue = originalValue;
    exitEditMode();
    onEditAccept?.({ value: newValue, previousValue });
  }, [editValue, originalValue, exitEditMode, onEditAccept]);
  const cancelEdit = React45.useCallback(() => {
    const cancelledValue = editValue;
    exitEditMode();
    onEditCancel?.({ value: cancelledValue });
  }, [editValue, exitEditMode, onEditCancel]);
  const handleKeyDown = React45.useCallback(
    (e) => {
      if (e.key === "Enter" && editValue.trim().length > 0) {
        e.preventDefault();
        isCommittingRef.current = true;
        acceptEdit();
      } else if (e.key === "Escape") {
        e.preventDefault();
        isCommittingRef.current = true;
        cancelEdit();
      }
    },
    [editValue, acceptEdit, cancelEdit]
  );
  const handleBlur = React45.useCallback(() => {
    if (isCommittingRef.current) {
      isCommittingRef.current = false;
      return;
    }
    if (editValue.trim().length > 0) {
      acceptEdit();
    } else {
      cancelEdit();
    }
  }, [editValue, acceptEdit, cancelEdit]);
  const handleDoubleClick = React45.useCallback(() => {
    if (!editMode) {
      enterEditMode();
    }
  }, [editMode, enterEditMode]);
  React45.useImperativeHandle(ref, () => ({
    enterEditMode,
    exitEditMode,
    getEditValue: () => editValue
  }));
  return /* @__PURE__ */ jsxRuntime.jsxs(
    "div",
    {
      className: cn(
        "relative inline-flex items-center min-h-9",
        editMode && "w-full max-w-[600px]",
        className
      ),
      children: [
        !editMode && /* @__PURE__ */ jsxRuntime.jsx(
          "span",
          {
            className: cn(
              "relative z-10 font-semibold cursor-text flex items-center min-h-9",
              wrappingType === "None" ? "truncate" : "whitespace-pre-line"
            ),
            title: t("FX_DOUBLE_CLICK_TO_RENAME"),
            onDoubleClick: handleDoubleClick,
            children: text
          }
        ),
        editMode && /* @__PURE__ */ jsxRuntime.jsx("div", { className: "flex items-center w-full bg-sapphire-background-primary border-2 border-sapphire-border-accent rounded-md px-3.5 min-h-9", children: /* @__PURE__ */ jsxRuntime.jsx(
          "input",
          {
            ref: inputRef,
            type: "text",
            value: editValue,
            onChange: (e) => setEditValue(e.target.value),
            onKeyDown: handleKeyDown,
            onBlur: handleBlur,
            className: "flex-1 min-w-0 bg-transparent border-none outline-none text-base font-semibold text-sapphire-text-primary selection:bg-sapphire-border-accent selection:text-white"
          }
        ) })
      ]
    }
  );
}
var NavBackIcon = ({ className }) => /* @__PURE__ */ jsxRuntime.jsx("svg", { xmlns: "http://www.w3.org/2000/svg", width: "20", height: "20", viewBox: "0 0 20 20", fill: "none", className, children: /* @__PURE__ */ jsxRuntime.jsx("path", { d: "M13.4939 4.04418C13.1385 3.66784 12.5459 3.65098 12.1694 4.00634L6.54443 9.31517C6.357 9.4921 6.25047 9.7386 6.25024 9.99633C6.25018 10.2543 6.35696 10.5015 6.54443 10.6787L12.1694 15.9936C12.5458 16.3491 13.1383 16.3321 13.4939 15.9558C13.8493 15.5795 13.8323 14.9869 13.4561 14.6313L8.55371 9.99633L13.4561 5.36864C13.8324 5.01321 13.8492 4.42064 13.4939 4.04418Z", fill: "currentColor" }) });
var BUTTON_GAP = 8;
var BREADCRUMB_TITLE_GAP = 16;
var PRIORITY_ORDER = {
  alwaysOverflow: -1,
  low: 0,
  medium: 1,
  high: 2
};
var SMALL_SCREEN_BREAKPOINT = 600;
var RELATION_TYPE_LABELS = {
  space: "Related Spaces",
  job: "Related Jobs",
  conversation: "Related Conversations"
};
var RELATION_TYPE_ORDER = ["space", "job", "conversation"];
function FxPaneHeaderAction({
  icon,
  text,
  design = "Tertiary",
  showText = false,
  priority = "medium",
  centered = false,
  disabled = false,
  tooltip,
  onClick,
  className,
  ref
}) {
  const showTextLabel = showText && text;
  const buttonRef = React45.useRef(null);
  React45.useImperativeHandle(ref, () => buttonRef.current);
  const handleClick2 = (_detail) => {
    onClick?.({}, buttonRef.current?.nativeElement || null);
  };
  return /* @__PURE__ */ jsxRuntime.jsx(
    Button,
    {
      ref: buttonRef,
      design,
      disabled,
      tooltip: tooltip || text,
      icon,
      iconOnly: !showTextLabel,
      onClick: handleClick2,
      className: cn("shrink-0 fx-pane-header-action", className),
      "data-priority": priority,
      "data-centered": centered,
      "data-show-text": showText,
      "data-text": text,
      children: showTextLabel && text
    }
  );
}
FxPaneHeaderAction.displayName = "FxPaneHeaderAction";
function FxPaneHeaderSegmentedAction({
  priority = "medium",
  centered = false,
  selectedId,
  showText = true,
  // Can be controlled externally for text collapse
  children,
  onSelectionChange,
  className,
  ref
}) {
  const options = [];
  React45.Children.forEach(children, (child) => {
    if (React45.isValidElement(child) && child.type?.displayName === "FxPaneHeaderSegmentedOption") {
      options.push(child.props);
    }
  });
  return /* @__PURE__ */ jsxRuntime.jsx(
    SegmentedButton,
    {
      ref,
      selectedId,
      onSelectionChange: (detail) => onSelectionChange?.({ selectedId: detail.selectedId }),
      className: cn("shrink-0", className),
      "data-priority": priority,
      "data-centered": centered,
      "data-show-text": showText,
      children: options.map((option) => /* @__PURE__ */ jsxRuntime.jsx(
        SegmentedButtonItem,
        {
          id: option.id,
          icon: option.icon,
          text: showText ? option.text : void 0,
          selected: option.selected
        },
        option.id
      ))
    }
  );
}
FxPaneHeaderSegmentedAction.displayName = "FxPaneHeaderSegmentedAction";
var FxPaneHeaderSegmentedOption = () => null;
FxPaneHeaderSegmentedOption.displayName = "FxPaneHeaderSegmentedOption";
function FxPaneHeaderSplitAction({
  icon,
  text,
  showText = false,
  children,
  onClick,
  onOptionSelect,
  className,
  ref
}) {
  const splitRef = React45.useRef(null);
  const [dropdownOpen, setDropdownOpen] = React45.useState(false);
  const showTextLabel = showText && text;
  const options = [];
  React45.Children.forEach(children, (child) => {
    if (React45.isValidElement(child) && child.type?.displayName === "FxPaneHeaderSplitOption") {
      options.push(child.props);
    }
  });
  const mergedRef = React45.useCallback(
    (node) => {
      splitRef.current = node;
      if (typeof ref === "function") {
        ref(node);
      } else if (ref) {
        ref.current = node;
      }
    },
    [ref]
  );
  return /* @__PURE__ */ jsxRuntime.jsxs(jsxRuntime.Fragment, { children: [
    /* @__PURE__ */ jsxRuntime.jsx(
      SplitButton,
      {
        ref: mergedRef,
        icon,
        text: showTextLabel ? text : void 0,
        onClick: () => onClick?.(void 0),
        onArrowClick: () => setDropdownOpen((prev) => !prev),
        activeArrowButton: dropdownOpen,
        className: cn("shrink-0", className)
      }
    ),
    /* @__PURE__ */ jsxRuntime.jsx(
      Menu,
      {
        opener: splitRef.current?.nativeElement,
        open: dropdownOpen,
        onClose: () => setDropdownOpen(false),
        onItemClick: (detail) => {
          const optionId = detail.item.data;
          if (optionId) onOptionSelect?.({ optionId });
          setDropdownOpen(false);
        },
        children: options.map((option) => /* @__PURE__ */ jsxRuntime.jsx(
          MenuItem,
          {
            icon: option.icon,
            text: option.text,
            data: option.id
          },
          option.id
        ))
      }
    )
  ] });
}
FxPaneHeaderSplitAction.displayName = "FxPaneHeaderSplitAction";
var FxPaneHeaderSplitOption = () => null;
FxPaneHeaderSplitOption.displayName = "FxPaneHeaderSplitOption";
function FxPaneHeader({
  pane = "center",
  showBack = false,
  title = "",
  showTitleArrow = false,
  showAddButton = false,
  addButtonTooltip,
  related = [],
  titleEditable = false,
  titleEditMode: controlledEditMode,
  headingLevel,
  breadcrumbs,
  subheader,
  children,
  onBackClick,
  onTitleArrowClick,
  onRelationClick,
  onTitleEditAccept,
  onTitleEditCancel,
  onTitleEditModeChange,
  onAddClick,
  showBorder = false,
  className,
  style,
  ref
}) {
  const { t } = reactI18next.useTranslation("fx");
  const headerRef = React45.useRef(null);
  const Heading = headingLevel ?? (pane === "center" ? "h1" : "h2");
  const measureContainerRef = React45.useRef(null);
  const titleArrowRef = React45.useRef(null);
  const relationsButtonRef = React45.useRef(null);
  const overflowButtonRef = React45.useRef(null);
  const renameTitleRef = React45.useRef(null);
  const [internalEditMode, setInternalEditMode] = React45.useState(false);
  const [relationsMenuOpen, setRelationsMenuOpen] = React45.useState(false);
  const [overflowMenuOpen, setOverflowMenuOpen] = React45.useState(false);
  const [headerWidth, setHeaderWidth] = React45.useState(0);
  const [measurements, setMeasurements] = React45.useState(null);
  const editMode = controlledEditMode ?? internalEditMode;
  const layoutContext = useFxLayoutContext();
  const startPaneVisible = layoutContext?.startVisible ?? false;
  const endPaneVisible = layoutContext?.endVisible ?? false;
  const compactMode = layoutContext?.compactMode ?? false;
  const leftmostVisiblePane = layoutContext?.leftmostVisiblePane ?? "start";
  const maxPanes = layoutContext?.maxPanes ?? 3;
  const utilityEndPane = layoutContext?.utilityEndPane ?? false;
  const isSettingsMode = layoutContext?.isSettingsMode ?? false;
  const allowStart = layoutContext?.allowStart ?? true;
  const allowEnd = layoutContext?.allowEnd ?? true;
  const isCenterHeader = pane === "center";
  const isEndHeader = pane === "end";
  const handleToggleStart = layoutContext?.toggleStartPane;
  const handleToggleEnd = layoutContext?.toggleEndPane;
  const handleHamburger = layoutContext?.openNavigation;
  const handleClose = isEndHeader && (maxPanes === 1 || utilityEndPane) ? layoutContext?.toggleEndPane : void 0;
  const showStartToggle = isCenterHeader && allowStart && handleToggleStart && (maxPanes >= 2 || !showBack);
  const toggleStartUsesBackIcon = maxPanes === 1;
  const showEndToggle = isCenterHeader && allowEnd && !utilityEndPane && !isSettingsMode && handleToggleEnd;
  const showHamburger = compactMode && handleHamburger && pane === leftmostVisiblePane;
  const showBackButton = (isCenterHeader || isEndHeader) && showBack;
  const hasRelated = related.length > 0;
  const isSmallScreenEditMode = editMode && headerWidth > 0 && headerWidth < SMALL_SCREEN_BREAKPOINT;
  React45.useImperativeHandle(ref, () => ({
    focus: () => headerRef.current?.focus(),
    get nativeElement() {
      return headerRef.current;
    },
    getNativeElement: () => headerRef.current
  }));
  const actionItems = React45.useMemo(() => {
    const items = [];
    const flattenChildren = (nodes) => {
      const result = [];
      React45.Children.forEach(nodes, (child) => {
        if (!React45.isValidElement(child)) return;
        if (child.type === React45__namespace.default.Fragment) {
          result.push(...flattenChildren(child.props.children));
        } else {
          result.push(child);
        }
      });
      return result;
    };
    const flatChildren = flattenChildren(children);
    flatChildren.forEach((child) => {
      const props = child.props;
      const priorityProp = props.priority || "medium";
      const priorityValue = PRIORITY_ORDER[priorityProp] ?? 1;
      const displayName = child.type?.displayName;
      const isSegmented = displayName === "FxPaneHeaderSegmentedAction";
      items.push({
        element: child,
        priority: priorityValue,
        // For segmented actions, showText defaults to true (always has text initially)
        showText: isSegmented ? props.showText ?? true : props.showText ?? false,
        text: props.text ?? "",
        icon: props.icon,
        centered: props.centered ?? false,
        isSegmented
      });
    });
    return items;
  }, [children]);
  const breadcrumbItemData = React45.useMemo(() => {
    if (!breadcrumbs || !React45.isValidElement(breadcrumbs)) return [];
    const items = [];
    React45.Children.forEach(breadcrumbs.props.children, (child) => {
      if (React45.isValidElement(child) && child.type?.displayName === "BreadcrumbsItem") {
        const childProps = child.props;
        const text = typeof childProps.children === "string" ? childProps.children : "";
        items.push({ text });
      }
    });
    return items;
  }, [breadcrumbs]);
  const overflowState = React45.useMemo(() => {
    const nonOverflowIndices = actionItems.map((item, i) => ({ item, i })).filter(({ item }) => item.priority !== PRIORITY_ORDER.alwaysOverflow).map(({ i }) => i);
    const totalBreadcrumbItems = breadcrumbItemData.length;
    if (!measurements || headerWidth === 0) {
      return {
        visibleIndices: new Set(nonOverflowIndices),
        textVisibility: actionItems.map((item) => item.showText),
        relationsVisible: hasRelated,
        breadcrumbOverflowCount: 0
      };
    }
    const m = measurements;
    const availableWidth = headerWidth;
    let baseFixedWidth = m.startSection + BUTTON_GAP;
    if (showHamburger && m.hamburger > 0) {
      baseFixedWidth += m.hamburger + BUTTON_GAP;
    }
    if (showStartToggle && m.toggleStart > 0) {
      baseFixedWidth += m.toggleStart + BUTTON_GAP;
    }
    if (showBackButton && m.backButton > 0) {
      baseFixedWidth += m.backButton + BUTTON_GAP;
    }
    if (editMode) {
      baseFixedWidth += 200;
    }
    let builtInWidth = 0;
    if (showEndToggle && m.toggleEnd > 0) {
      builtInWidth += m.toggleEnd + BUTTON_GAP;
    }
    const calcBreadcrumbsWidth = (overflowCount) => {
      if (totalBreadcrumbItems === 0) return 0;
      if (overflowCount >= totalBreadcrumbItems) {
        return m.breadcrumbOverflowOnly + BREADCRUMB_TITLE_GAP;
      }
      if (overflowCount === 0) {
        const itemsWidth = m.breadcrumbItems.reduce((sum, w) => sum + w, 0);
        return itemsWidth + BREADCRUMB_TITLE_GAP;
      }
      const visibleItemsWidth = m.breadcrumbItems.slice(overflowCount).reduce((sum, w) => sum + w, 0);
      return m.breadcrumbOverflow + visibleItemsWidth + BREADCRUMB_TITLE_GAP;
    };
    const actionStates = actionItems.map((item, index) => ({
      index,
      showText: item.showText,
      visible: item.priority !== PRIORITY_ORDER.alwaysOverflow,
      priority: item.priority
    }));
    let relationsVisible = hasRelated;
    let breadcrumbOverflowCount = 0;
    const calcActionsWidth = () => {
      let width = 0;
      if (relationsVisible && m.relations > 0) {
        width += m.relations + BUTTON_GAP;
      }
      for (const state of actionStates) {
        if (state.visible && m.actions[state.index]) {
          const aw = m.actions[state.index];
          width += (state.showText ? aw.withText : aw.iconOnly) + BUTTON_GAP;
        }
      }
      const hasHidden = actionStates.some((s) => !s.visible);
      const relationsOverflowed = hasRelated && !relationsVisible;
      if ((hasHidden || relationsOverflowed) && m.overflow > 0) {
        width += m.overflow + BUTTON_GAP;
      }
      return width;
    };
    const fits = (bcOverflow) => {
      const breadcrumbsWidth2 = calcBreadcrumbsWidth(bcOverflow);
      const fixedWidth2 = baseFixedWidth + breadcrumbsWidth2;
      const spaceForButtons2 = availableWidth - fixedWidth2 - builtInWidth;
      return calcActionsWidth() <= spaceForButtons2;
    };
    while (!fits(breadcrumbOverflowCount) && breadcrumbOverflowCount < totalBreadcrumbItems) {
      breadcrumbOverflowCount++;
    }
    const breadcrumbsWidth = calcBreadcrumbsWidth(breadcrumbOverflowCount);
    const fixedWidth = baseFixedWidth + breadcrumbsWidth;
    const spaceForButtons = availableWidth - fixedWidth - builtInWidth;
    const fitsActions = () => calcActionsWidth() <= spaceForButtons;
    while (!fitsActions()) {
      let targetIdx = -1;
      let targetPriority = Infinity;
      let targetOriginalIndex = Infinity;
      for (let i = 0; i < actionStates.length; i++) {
        const state = actionStates[i];
        if (state.visible && state.showText) {
          if (state.priority < targetPriority || state.priority === targetPriority && state.index < targetOriginalIndex) {
            targetIdx = i;
            targetPriority = state.priority;
            targetOriginalIndex = state.index;
          }
        }
      }
      if (targetIdx === -1) break;
      actionStates[targetIdx].showText = false;
    }
    if (!fitsActions() && relationsVisible && !relationsMenuOpen) {
      relationsVisible = false;
    }
    while (!fitsActions()) {
      let targetIdx = -1;
      let targetPriority = Infinity;
      let targetOriginalIndex = Infinity;
      for (let i = 0; i < actionStates.length; i++) {
        const state = actionStates[i];
        if (state.visible) {
          if (state.priority < targetPriority || state.priority === targetPriority && state.index < targetOriginalIndex) {
            targetIdx = i;
            targetPriority = state.priority;
            targetOriginalIndex = state.index;
          }
        }
      }
      if (targetIdx === -1) break;
      actionStates[targetIdx].visible = false;
    }
    return {
      visibleIndices: new Set(actionStates.filter((s) => s.visible).map((s) => s.index)),
      textVisibility: actionStates.map((s) => s.showText),
      relationsVisible,
      breadcrumbOverflowCount
    };
  }, [
    measurements,
    headerWidth,
    actionItems,
    hasRelated,
    showHamburger,
    showStartToggle,
    showBackButton,
    showEndToggle,
    editMode,
    relationsMenuOpen,
    breadcrumbItemData
  ]);
  const centeredActionIndex = React45.useMemo(() => {
    for (let i = 0; i < actionItems.length; i++) {
      if (actionItems[i].centered && overflowState.visibleIndices.has(i)) {
        return i;
      }
    }
    return -1;
  }, [actionItems, overflowState.visibleIndices]);
  const centeringAvailableSpace = React45.useMemo(() => {
    if (!measurements || headerWidth === 0 || centeredActionIndex === -1) {
      return [-1, -1];
    }
    const m = measurements;
    const centeredMeasurement = m.actions[centeredActionIndex];
    if (!centeredMeasurement) {
      return [-1, -1];
    }
    const showText = overflowState.textVisibility[centeredActionIndex];
    const centeredWidth = showText ? centeredMeasurement.withText : centeredMeasurement.iconOnly;
    let leftContentWidth = m.startSection + BUTTON_GAP;
    if (showHamburger && m.hamburger > 0) {
      leftContentWidth += m.hamburger + BUTTON_GAP;
    }
    if (showStartToggle && m.toggleStart > 0) {
      leftContentWidth += m.toggleStart + BUTTON_GAP;
    }
    for (let i = 0; i < centeredActionIndex; i++) {
      if (overflowState.visibleIndices.has(i)) {
        const aw = m.actions[i];
        if (aw) {
          const hasText = overflowState.textVisibility[i];
          leftContentWidth += (hasText ? aw.withText : aw.iconOnly) + BUTTON_GAP;
        }
      }
    }
    let rightContentWidth = 0;
    for (let i = centeredActionIndex + 1; i < actionItems.length; i++) {
      if (overflowState.visibleIndices.has(i)) {
        const aw = m.actions[i];
        if (aw) {
          const hasText = overflowState.textVisibility[i];
          rightContentWidth += (hasText ? aw.withText : aw.iconOnly) + BUTTON_GAP;
        }
      }
    }
    const hasOverflowedActions = actionItems.some((_, i) => !overflowState.visibleIndices.has(i));
    const relationsOverflowed = hasRelated && !overflowState.relationsVisible;
    const needsOverflowButton = hasOverflowedActions || relationsOverflowed;
    if (needsOverflowButton && m.overflow > 0) {
      rightContentWidth += m.overflow + BUTTON_GAP;
    }
    if (overflowState.relationsVisible && hasRelated && m.relations > 0) {
      rightContentWidth += m.relations + BUTTON_GAP;
    }
    if (showEndToggle && m.toggleEnd > 0) {
      rightContentWidth += m.toggleEnd + BUTTON_GAP;
    }
    const halfTotal = headerWidth / 2;
    const halfCentered = centeredWidth / 2;
    const leftSpace = halfTotal - halfCentered - leftContentWidth;
    const rightSpace = halfTotal - halfCentered - rightContentWidth;
    return [Math.floor(leftSpace), Math.floor(rightSpace)];
  }, [
    measurements,
    headerWidth,
    centeredActionIndex,
    actionItems,
    overflowState,
    showHamburger,
    showStartToggle,
    showEndToggle,
    hasRelated
  ]);
  const centeringActive = centeringAvailableSpace[0] >= BUTTON_GAP && centeringAvailableSpace[1] >= BUTTON_GAP;
  const visibleActions = actionItems.filter((_, i) => overflowState.visibleIndices.has(i));
  const overflowedActions = actionItems.filter((_, i) => !overflowState.visibleIndices.has(i));
  const actionsBeforeCentered = centeringActive ? visibleActions.filter((_, i) => {
    const originalIndex = actionItems.indexOf(visibleActions[i]);
    return originalIndex < centeredActionIndex;
  }) : [];
  const actionsAfterCentered = centeringActive ? visibleActions.filter((_, i) => {
    const originalIndex = actionItems.indexOf(visibleActions[i]);
    return originalIndex > centeredActionIndex;
  }) : [];
  const centeredAction = centeringActive && centeredActionIndex !== -1 ? actionItems[centeredActionIndex] : null;
  const showOverflowButton = overflowedActions.length > 0 || hasRelated && !overflowState.relationsVisible;
  React45.useEffect(() => {
    const header = headerRef.current;
    if (!header) return;
    const observer = new ResizeObserver((entries) => {
      const newWidth = Math.floor(entries[0]?.contentRect.width || 0);
      if (newWidth !== headerWidth) {
        setHeaderWidth(newWidth);
      }
    });
    observer.observe(header);
    return () => observer.disconnect();
  }, [headerWidth]);
  const renderBreadcrumbs = React45.useCallback(() => {
    if (!breadcrumbs || !React45.isValidElement(breadcrumbs)) return breadcrumbs;
    return React45.cloneElement(breadcrumbs, {
      overflowCount: overflowState.breadcrumbOverflowCount,
      noPadding: true
    });
  }, [breadcrumbs, overflowState.breadcrumbOverflowCount]);
  React45.useEffect(() => {
    const measureContainer = measureContainerRef.current;
    if (!measureContainer) return;
    requestAnimationFrame(() => {
      const getWidth = (selector) => {
        const el = measureContainer.querySelector(selector);
        return el?.offsetWidth || 0;
      };
      const breadcrumbItems = [];
      for (let i = 0; i < breadcrumbItemData.length; i++) {
        breadcrumbItems.push(getWidth(`[data-measure="bc-item-${i}"]`));
      }
      const actions = [];
      for (let i = 0; i < actionItems.length; i++) {
        actions.push({
          iconOnly: getWidth(`[data-measure="${i}-icon"]`),
          withText: getWidth(`[data-measure="${i}-text"]`)
        });
      }
      setMeasurements({
        breadcrumbItems,
        breadcrumbOverflow: getWidth("[data-measure='bc-overflow']"),
        breadcrumbOverflowOnly: getWidth("[data-measure='bc-overflow-only']"),
        breadcrumbSeparator: getWidth("[data-measure='bc-separator']"),
        startSection: getWidth("[data-measure='start-section']"),
        hamburger: getWidth("[data-measure='hamburger']"),
        toggleStart: getWidth("[data-measure='toggle-start']"),
        backButton: getWidth("[data-measure='back-button']"),
        toggleEnd: getWidth("[data-measure='toggle-end']"),
        overflow: getWidth("[data-measure='overflow']"),
        relations: getWidth("[data-measure='relations']"),
        actions
      });
    });
  }, [actionItems, breadcrumbItemData]);
  const handleTitleEditAccept = React45.useCallback(
    (detail) => {
      setInternalEditMode(false);
      onTitleEditAccept?.(detail);
    },
    [onTitleEditAccept]
  );
  const handleTitleEditCancel = React45.useCallback(
    (detail) => {
      setInternalEditMode(false);
      onTitleEditCancel?.(detail);
    },
    [onTitleEditCancel]
  );
  const handleTitleEditModeChange = React45.useCallback(
    ({ editMode: newEditMode }) => {
      setInternalEditMode(newEditMode);
      onTitleEditModeChange?.({ editMode: newEditMode });
    },
    [onTitleEditModeChange]
  );
  const getRelationIcon = (type) => {
    switch (type) {
      case "space":
        return /* @__PURE__ */ jsxRuntime.jsx(SpacesIcon, { className: "h-4 w-4" });
      case "job":
        return /* @__PURE__ */ jsxRuntime.jsx(JobsIcon, { className: "h-4 w-4" });
      case "conversation":
        return /* @__PURE__ */ jsxRuntime.jsx(ConversationsIcon, { className: "h-4 w-4" });
      default:
        return /* @__PURE__ */ jsxRuntime.jsx(lucideReact.Share2, { className: "h-4 w-4" });
    }
  };
  const groupedRelations = React45.useMemo(() => {
    const groups = {
      space: [],
      job: [],
      conversation: []
    };
    related.forEach((item) => {
      if (groups[item.type]) {
        groups[item.type].push(item);
      }
    });
    return groups;
  }, [related]);
  const renderRelationMenuItems = React45.useCallback(
    (closeMenu) => {
      const items = [];
      let isFirstGroup = true;
      RELATION_TYPE_ORDER.forEach((type) => {
        const typeRelations = groupedRelations[type];
        if (typeRelations.length === 0) return;
        if (!isFirstGroup) {
          items.push(/* @__PURE__ */ jsxRuntime.jsx(MenuSeparator, {}, `sep-${type}`));
        }
        isFirstGroup = false;
        items.push(
          /* @__PURE__ */ jsxRuntime.jsx(MenuHeader, { text: RELATION_TYPE_LABELS[type] }, `header-${type}`)
        );
        typeRelations.forEach((item) => {
          items.push(
            /* @__PURE__ */ jsxRuntime.jsx(
              MenuItem,
              {
                icon: item.icon || getRelationIcon(item.type),
                text: item.name,
                onClick: () => {
                  onRelationClick?.(item);
                  closeMenu();
                }
              },
              item.id
            )
          );
        });
      });
      return items;
    },
    [groupedRelations, onRelationClick]
  );
  const renderAction = (item, index, showText) => {
    const child = item.element;
    const displayName = child.type?.displayName;
    const props = child.props;
    if (displayName === "FxPaneHeaderAction") {
      return /* @__PURE__ */ jsxRuntime.jsx(
        FxPaneHeaderAction,
        {
          ...props,
          showText: showText && props.showText
        },
        index
      );
    }
    if (displayName === "FxPaneHeaderSegmentedAction") {
      const segProps = child.props;
      return /* @__PURE__ */ jsxRuntime.jsx(
        FxPaneHeaderSegmentedAction,
        {
          ...segProps,
          showText
        },
        index
      );
    }
    if (displayName === "FxPaneHeaderSplitAction") {
      const splitProps = child.props;
      return /* @__PURE__ */ jsxRuntime.jsx(
        FxPaneHeaderSplitAction,
        {
          ...splitProps,
          showText: showText && splitProps.showText
        },
        index
      );
    }
    return React45__namespace.default.cloneElement(child, { key: index });
  };
  const renderOverflowedActions = React45.useCallback(
    (closeMenu, isFirstInMenu) => {
      const items = [];
      overflowedActions.forEach((item, index) => {
        const child = item.element;
        const displayName = child.type?.displayName;
        const isFirst = index === 0 && isFirstInMenu;
        const isLast = index === overflowedActions.length - 1;
        if (displayName === "FxPaneHeaderAction") {
          const props = child.props;
          items.push(
            /* @__PURE__ */ jsxRuntime.jsx(
              MenuItem,
              {
                icon: props.icon,
                text: props.text || "Action",
                onClick: () => {
                  props.onClick?.({}, overflowButtonRef.current?.nativeElement || null);
                  closeMenu();
                }
              },
              `action-${index}`
            )
          );
        } else if (displayName === "FxPaneHeaderSegmentedAction") {
          const props = child.props;
          const options = [];
          React45.Children.forEach(props.children, (optChild) => {
            if (React45.isValidElement(optChild) && optChild.type?.displayName === "FxPaneHeaderSegmentedOption") {
              options.push(optChild.props);
            }
          });
          if (!isFirst && items.length > 0) {
            items.push(/* @__PURE__ */ jsxRuntime.jsx(MenuSeparator, {}, `seg-sep-before-${index}`));
          }
          options.forEach((opt) => {
            const isSelected = opt.selected || opt.id === props.selectedId;
            items.push(
              /* @__PURE__ */ jsxRuntime.jsx(
                MenuItem,
                {
                  icon: opt.icon,
                  text: opt.text,
                  endContent: isSelected ? /* @__PURE__ */ jsxRuntime.jsx("span", { className: "text-primary", children: "\u2713" }) : void 0,
                  onClick: () => {
                    props.onSelectionChange?.({ selectedId: opt.id });
                    closeMenu();
                  }
                },
                `seg-${index}-${opt.id}`
              )
            );
          });
          if (!isLast) {
            items.push(/* @__PURE__ */ jsxRuntime.jsx(MenuSeparator, {}, `seg-sep-after-${index}`));
          }
        } else if (displayName === "FxPaneHeaderSplitAction") {
          const props = child.props;
          const options = [];
          React45.Children.forEach(props.children, (optChild) => {
            if (React45.isValidElement(optChild) && optChild.type?.displayName === "FxPaneHeaderSplitOption") {
              options.push(optChild.props);
            }
          });
          items.push(
            /* @__PURE__ */ jsxRuntime.jsx(
              MenuItem,
              {
                icon: props.icon,
                text: props.text || "Action",
                children: options.map((opt) => /* @__PURE__ */ jsxRuntime.jsx(
                  MenuItem,
                  {
                    icon: opt.icon,
                    text: opt.text,
                    onClick: () => {
                      props.onOptionSelect?.({ optionId: opt.id });
                      closeMenu();
                    }
                  },
                  `split-opt-${index}-${opt.id}`
                ))
              },
              `split-${index}`
            )
          );
        } else {
          const props = child.props;
          if (props.text || props.icon) {
            items.push(
              /* @__PURE__ */ jsxRuntime.jsx(
                MenuItem,
                {
                  icon: props.icon,
                  text: props.text || "Action",
                  onClick: () => {
                    props.onClick?.({});
                    closeMenu();
                  }
                },
                `unknown-${index}`
              )
            );
          }
        }
      });
      return items;
    },
    [overflowedActions]
  );
  const renderOverflowRelationsSubmenu = React45.useCallback(
    (closeMenu) => {
      const submenuItems = [];
      let isFirstGroup = true;
      RELATION_TYPE_ORDER.forEach((type) => {
        const typeRelations = groupedRelations[type];
        if (typeRelations.length === 0) return;
        if (!isFirstGroup) {
          submenuItems.push(/* @__PURE__ */ jsxRuntime.jsx(MenuSeparator, {}, `overflow-sep-${type}`));
        }
        isFirstGroup = false;
        submenuItems.push(
          /* @__PURE__ */ jsxRuntime.jsx(MenuHeader, { text: RELATION_TYPE_LABELS[type] }, `overflow-header-${type}`)
        );
        typeRelations.forEach((item) => {
          submenuItems.push(
            /* @__PURE__ */ jsxRuntime.jsx(
              MenuItem,
              {
                icon: item.icon || getRelationIcon(item.type),
                text: item.name,
                onClick: () => {
                  onRelationClick?.(item);
                  closeMenu();
                }
              },
              `overflow-rel-${item.id}`
            )
          );
        });
      });
      return /* @__PURE__ */ jsxRuntime.jsx(
        MenuItem,
        {
          icon: /* @__PURE__ */ jsxRuntime.jsx(lucideReact.Share2, { className: "h-4 w-4" }),
          text: t("FX_RELATED"),
          children: submenuItems
        },
        "overflow-relations"
      );
    },
    [groupedRelations, onRelationClick]
  );
  return /* @__PURE__ */ jsxRuntime.jsxs(
    "header",
    {
      ref: headerRef,
      className: cn(
        "fx-pane-header border-b transition-colors duration-200",
        showBorder ? "border-border" : "border-transparent",
        editMode && "fx-pane-header--title-edit-mode",
        className
      ),
      style: {
        containerType: "inline-size",
        marginInline: "-1.25rem",
        paddingInline: "1.25rem",
        paddingBottom: "1.5rem",
        ...style
      },
      children: [
        /* @__PURE__ */ jsxRuntime.jsxs(
          "div",
          {
            ref: measureContainerRef,
            "aria-hidden": "true",
            style: {
              position: "absolute",
              visibility: "hidden",
              pointerEvents: "none",
              width: "max-content",
              top: "-9999px",
              left: "-9999px"
            },
            children: [
              breadcrumbItemData.map((item, idx) => /* @__PURE__ */ jsxRuntime.jsxs("div", { "data-measure": `bc-item-${idx}`, className: "inline-flex items-center", children: [
                /* @__PURE__ */ jsxRuntime.jsx("span", { className: "inline-flex items-center gap-0.5 text-base font-semibold text-foreground px-0.5 py-0.5", children: item.text }),
                /* @__PURE__ */ jsxRuntime.jsx("span", { className: "mx-1.5 text-base font-semibold select-none", children: "/" })
              ] }, idx)),
              /* @__PURE__ */ jsxRuntime.jsxs("div", { "data-measure": "bc-overflow", className: "inline-flex items-center", children: [
                /* @__PURE__ */ jsxRuntime.jsxs("span", { className: "inline-flex items-center gap-0.5 text-base px-0.5 py-0.5", children: [
                  /* @__PURE__ */ jsxRuntime.jsx("span", { children: "\u2026" }),
                  /* @__PURE__ */ jsxRuntime.jsx(lucideReact.ChevronDown, { className: "h-3 w-3" })
                ] }),
                /* @__PURE__ */ jsxRuntime.jsx("span", { className: "mx-1.5 text-base font-semibold select-none", children: "/" })
              ] }),
              /* @__PURE__ */ jsxRuntime.jsx("div", { "data-measure": "bc-overflow-only", className: "inline-flex items-center", children: /* @__PURE__ */ jsxRuntime.jsxs("span", { className: "inline-flex items-center gap-0.5 text-base px-0.5 py-0.5", children: [
                /* @__PURE__ */ jsxRuntime.jsx("span", { children: "\u2026" }),
                /* @__PURE__ */ jsxRuntime.jsx(lucideReact.ChevronDown, { className: "h-3 w-3" })
              ] }) }),
              /* @__PURE__ */ jsxRuntime.jsx("div", { "data-measure": "bc-separator", className: "inline-flex items-center gap-2", children: /* @__PURE__ */ jsxRuntime.jsx("span", { className: "text-foreground font-semibold", children: "/" }) }),
              /* @__PURE__ */ jsxRuntime.jsxs("div", { "data-measure": "start-section", className: "inline-flex items-center gap-2", children: [
                /* @__PURE__ */ jsxRuntime.jsx("span", { className: "text-base font-semibold whitespace-pre-line", children: title }),
                showTitleArrow && /* @__PURE__ */ jsxRuntime.jsx(lucideReact.ChevronDown, { className: "h-4 w-4" })
              ] }),
              /* @__PURE__ */ jsxRuntime.jsx("div", { "data-measure": "hamburger", className: "inline-block", children: /* @__PURE__ */ jsxRuntime.jsx(Button, { design: "Tertiary", iconOnly: true, icon: /* @__PURE__ */ jsxRuntime.jsx(MenuIcon, { className: "h-4 w-4" }) }) }),
              /* @__PURE__ */ jsxRuntime.jsx("div", { "data-measure": "toggle-start", className: "inline-block", children: /* @__PURE__ */ jsxRuntime.jsx(ToggleButton, { design: "SecondaryNeutral", icon: /* @__PURE__ */ jsxRuntime.jsx(lucideReact.List, { className: "h-4 w-4" }) }) }),
              /* @__PURE__ */ jsxRuntime.jsx("div", { "data-measure": "back-button", className: "inline-block", children: /* @__PURE__ */ jsxRuntime.jsx(Button, { design: "SecondaryNeutral", size: "Large", iconOnly: true, icon: /* @__PURE__ */ jsxRuntime.jsx(NavBackIcon, {}) }) }),
              /* @__PURE__ */ jsxRuntime.jsx("div", { "data-measure": "toggle-end", className: "inline-block", children: /* @__PURE__ */ jsxRuntime.jsx(ToggleButton, { design: "SecondaryNeutral", icon: /* @__PURE__ */ jsxRuntime.jsx(NewConversationIcon, { className: "h-4 w-4" }) }) }),
              /* @__PURE__ */ jsxRuntime.jsx("div", { "data-measure": "overflow", className: "inline-block", children: /* @__PURE__ */ jsxRuntime.jsx(Button, { design: "SecondaryNeutral", iconOnly: true, icon: /* @__PURE__ */ jsxRuntime.jsx(MoreIcon, { className: "h-4 w-4" }) }) }),
              /* @__PURE__ */ jsxRuntime.jsx("div", { "data-measure": "relations", className: "inline-block", children: /* @__PURE__ */ jsxRuntime.jsx(Button, { design: "SecondaryNeutral", iconOnly: true, icon: /* @__PURE__ */ jsxRuntime.jsx(lucideReact.Share2, { className: "h-4 w-4" }) }) }),
              actionItems.map((item, index) => {
                const displayName = item.element.type?.displayName;
                const isSegmented = displayName === "FxPaneHeaderSegmentedAction";
                if (isSegmented) {
                  return /* @__PURE__ */ jsxRuntime.jsxs(React45__namespace.default.Fragment, { children: [
                    /* @__PURE__ */ jsxRuntime.jsx("div", { "data-measure": `${index}-icon`, className: "inline-block", children: renderAction(item, index, false) }),
                    /* @__PURE__ */ jsxRuntime.jsx("div", { "data-measure": `${index}-text`, className: "inline-block", children: renderAction(item, index, true) })
                  ] }, index);
                }
                return /* @__PURE__ */ jsxRuntime.jsxs(React45__namespace.default.Fragment, { children: [
                  /* @__PURE__ */ jsxRuntime.jsx("div", { "data-measure": `${index}-icon`, className: "inline-block", children: renderAction(item, index, false) }),
                  /* @__PURE__ */ jsxRuntime.jsx("div", { "data-measure": `${index}-text`, className: "inline-block", children: renderAction(item, index, true) })
                ] }, index);
              })
            ]
          }
        ),
        /* @__PURE__ */ jsxRuntime.jsxs("div", { className: "flex items-center gap-2 min-h-9", children: [
          !centeringActive && /* @__PURE__ */ jsxRuntime.jsxs(jsxRuntime.Fragment, { children: [
            /* @__PURE__ */ jsxRuntime.jsxs("div", { className: "flex items-center gap-2 min-w-0 flex-1", children: [
              showHamburger && !isSmallScreenEditMode && /* @__PURE__ */ jsxRuntime.jsx(
                Button,
                {
                  design: "Tertiary",
                  iconOnly: true,
                  icon: /* @__PURE__ */ jsxRuntime.jsx(MenuIcon, { className: "h-4 w-4" }),
                  onClick: () => handleHamburger(),
                  tooltip: t("FX_OPEN_NAVIGATION"),
                  className: "shrink-0 fx-pane-header-hamburger text-sapphire-text-primary"
                }
              ),
              showStartToggle && !isSmallScreenEditMode && (toggleStartUsesBackIcon ? /* @__PURE__ */ jsxRuntime.jsx(
                Button,
                {
                  design: "SecondaryNeutral",
                  size: "Large",
                  iconOnly: true,
                  icon: /* @__PURE__ */ jsxRuntime.jsx(NavBackIcon, {}),
                  onClick: () => handleToggleStart(),
                  tooltip: t("FX_BACK_TO_LIST"),
                  className: "shrink-0 fx-pane-header-toggle-start"
                }
              ) : /* @__PURE__ */ jsxRuntime.jsx(
                ToggleButton,
                {
                  design: "SecondaryNeutral",
                  icon: /* @__PURE__ */ jsxRuntime.jsx(lucideReact.List, { className: "h-4 w-4" }),
                  pressed: startPaneVisible,
                  onClick: () => handleToggleStart(),
                  tooltip: startPaneVisible ? "Hide list" : "Show list",
                  className: "fx-pane-header-toggle-start"
                }
              )),
              showBackButton && !isSmallScreenEditMode && /* @__PURE__ */ jsxRuntime.jsx(
                Button,
                {
                  design: "SecondaryNeutral",
                  size: "Large",
                  iconOnly: true,
                  icon: /* @__PURE__ */ jsxRuntime.jsx(NavBackIcon, {}),
                  onClick: () => onBackClick?.(),
                  tooltip: t("FX_BACK"),
                  className: "shrink-0 fx-pane-header-back"
                }
              ),
              /* @__PURE__ */ jsxRuntime.jsx("div", { className: "flex flex-col min-w-0 flex-1", children: /* @__PURE__ */ jsxRuntime.jsxs("div", { className: "flex items-center gap-2 min-w-0", children: [
                breadcrumbs && !editMode && /* @__PURE__ */ jsxRuntime.jsx("div", { className: "min-w-0", children: renderBreadcrumbs() }),
                breadcrumbs && !editMode && title && /* @__PURE__ */ jsxRuntime.jsx("span", { className: "text-foreground font-semibold text-base select-none shrink-0 -mx-1", children: "/" }),
                titleEditable ? /* @__PURE__ */ jsxRuntime.jsx(
                  FxRenameTitle,
                  {
                    ref: renameTitleRef,
                    text: title,
                    editMode,
                    wrappingType: "Normal",
                    onEditAccept: handleTitleEditAccept,
                    onEditCancel: handleTitleEditCancel,
                    onEditModeChange: handleTitleEditModeChange,
                    className: "min-w-0"
                  }
                ) : /* @__PURE__ */ jsxRuntime.jsx(Heading, { className: "text-base font-semibold whitespace-pre-line min-w-0 flex items-center min-h-9", children: title }),
                showTitleArrow && !editMode && /* @__PURE__ */ jsxRuntime.jsx(
                  "button",
                  {
                    ref: titleArrowRef,
                    type: "button",
                    onClick: () => onTitleArrowClick?.(titleArrowRef.current),
                    className: "shrink-0 p-0.5 hover:bg-muted rounded",
                    children: /* @__PURE__ */ jsxRuntime.jsx(lucideReact.ChevronDown, { className: "h-4 w-4" })
                  }
                )
              ] }) })
            ] }),
            !isSmallScreenEditMode && /* @__PURE__ */ jsxRuntime.jsxs("div", { className: "flex items-center gap-1 shrink-0 fx-pane-header-end-section", children: [
              visibleActions.map((item) => {
                const originalIndex = actionItems.indexOf(item);
                return renderAction(item, originalIndex, overflowState.textVisibility[originalIndex]);
              }),
              overflowState.relationsVisible && hasRelated && /* @__PURE__ */ jsxRuntime.jsxs(jsxRuntime.Fragment, { children: [
                /* @__PURE__ */ jsxRuntime.jsx(
                  Button,
                  {
                    ref: relationsButtonRef,
                    design: "SecondaryNeutral",
                    iconOnly: true,
                    icon: /* @__PURE__ */ jsxRuntime.jsx(lucideReact.Share2, { className: "h-4 w-4" }),
                    onClick: () => {
                      setOverflowMenuOpen(false);
                      setRelationsMenuOpen(true);
                    },
                    tooltip: t("FX_RELATED"),
                    className: "shrink-0 fx-pane-header-relations"
                  }
                ),
                relationsMenuOpen && /* @__PURE__ */ jsxRuntime.jsx(
                  Menu,
                  {
                    open: relationsMenuOpen,
                    opener: relationsButtonRef.current?.nativeElement ?? null,
                    onClose: () => setRelationsMenuOpen(false),
                    children: renderRelationMenuItems(() => setRelationsMenuOpen(false))
                  },
                  "relations-menu"
                )
              ] }),
              showOverflowButton && /* @__PURE__ */ jsxRuntime.jsxs(jsxRuntime.Fragment, { children: [
                /* @__PURE__ */ jsxRuntime.jsx(
                  Button,
                  {
                    ref: overflowButtonRef,
                    design: "SecondaryNeutral",
                    iconOnly: true,
                    icon: /* @__PURE__ */ jsxRuntime.jsx(MoreIcon, { className: "h-4 w-4" }),
                    onClick: () => {
                      setRelationsMenuOpen(false);
                      setOverflowMenuOpen(true);
                    },
                    tooltip: t("FX_MORE_ACTIONS"),
                    className: "shrink-0 fx-pane-header-overflow"
                  }
                ),
                overflowMenuOpen && /* @__PURE__ */ jsxRuntime.jsxs(
                  Menu,
                  {
                    open: overflowMenuOpen,
                    opener: overflowButtonRef.current?.nativeElement ?? null,
                    onClose: () => setOverflowMenuOpen(false),
                    children: [
                      !overflowState.relationsVisible && hasRelated && renderOverflowRelationsSubmenu(() => setOverflowMenuOpen(false)),
                      !overflowState.relationsVisible && hasRelated && overflowedActions.length > 0 && /* @__PURE__ */ jsxRuntime.jsx(MenuSeparator, {}),
                      renderOverflowedActions(
                        () => setOverflowMenuOpen(false),
                        overflowState.relationsVisible || !hasRelated
                      )
                    ]
                  },
                  "overflow-menu"
                )
              ] }),
              showEndToggle && /* @__PURE__ */ jsxRuntime.jsx(
                ToggleButton,
                {
                  design: "SecondaryNeutral",
                  icon: /* @__PURE__ */ jsxRuntime.jsx(NewConversationIcon, { className: "h-4 w-4" }),
                  pressed: endPaneVisible,
                  onClick: () => handleToggleEnd(),
                  tooltip: endPaneVisible ? "Hide chat" : "Show chat",
                  className: "fx-pane-header-toggle-end"
                }
              ),
              showAddButton && !editMode && /* @__PURE__ */ jsxRuntime.jsx(
                Button,
                {
                  design: "Secondary",
                  iconOnly: true,
                  icon: /* @__PURE__ */ jsxRuntime.jsx(lucideReact.Plus, { className: "h-4 w-4" }),
                  onClick: () => onAddClick?.(),
                  tooltip: addButtonTooltip || "Add",
                  className: "shrink-0 fx-pane-header-add"
                }
              ),
              handleClose && /* @__PURE__ */ jsxRuntime.jsx(
                Button,
                {
                  design: "Tertiary",
                  iconOnly: true,
                  icon: /* @__PURE__ */ jsxRuntime.jsx(lucideReact.X, { className: "h-4 w-4" }),
                  onClick: () => handleClose?.(),
                  tooltip: t("FX_CLOSE"),
                  className: "shrink-0 fx-pane-header-close text-sapphire-text-primary"
                }
              )
            ] })
          ] }),
          centeringActive && !isSmallScreenEditMode && /* @__PURE__ */ jsxRuntime.jsxs(jsxRuntime.Fragment, { children: [
            /* @__PURE__ */ jsxRuntime.jsxs("div", { className: "fx-pane-header-left-zone", children: [
              showHamburger && /* @__PURE__ */ jsxRuntime.jsx(
                Button,
                {
                  design: "Tertiary",
                  iconOnly: true,
                  icon: /* @__PURE__ */ jsxRuntime.jsx(MenuIcon, { className: "h-4 w-4" }),
                  onClick: () => handleHamburger(),
                  tooltip: t("FX_OPEN_NAVIGATION"),
                  className: "shrink-0 fx-pane-header-hamburger text-sapphire-text-primary"
                }
              ),
              showStartToggle && (toggleStartUsesBackIcon ? /* @__PURE__ */ jsxRuntime.jsx(
                Button,
                {
                  design: "SecondaryNeutral",
                  size: "Large",
                  iconOnly: true,
                  icon: /* @__PURE__ */ jsxRuntime.jsx(NavBackIcon, {}),
                  onClick: () => handleToggleStart(),
                  tooltip: t("FX_BACK_TO_LIST"),
                  className: "shrink-0 fx-pane-header-toggle-start"
                }
              ) : /* @__PURE__ */ jsxRuntime.jsx(
                ToggleButton,
                {
                  design: "SecondaryNeutral",
                  icon: /* @__PURE__ */ jsxRuntime.jsx(lucideReact.List, { className: "h-4 w-4" }),
                  pressed: startPaneVisible,
                  onClick: () => handleToggleStart(),
                  tooltip: startPaneVisible ? "Hide list" : "Show list",
                  className: "fx-pane-header-toggle-start"
                }
              )),
              showBackButton && /* @__PURE__ */ jsxRuntime.jsx(
                Button,
                {
                  design: "SecondaryNeutral",
                  size: "Large",
                  iconOnly: true,
                  icon: /* @__PURE__ */ jsxRuntime.jsx(NavBackIcon, {}),
                  onClick: () => onBackClick?.(),
                  tooltip: t("FX_BACK"),
                  className: "shrink-0 fx-pane-header-back"
                }
              ),
              /* @__PURE__ */ jsxRuntime.jsx("div", { className: "flex flex-col min-w-0 shrink-0", children: /* @__PURE__ */ jsxRuntime.jsxs("div", { className: "flex items-center gap-2 min-w-0", children: [
                breadcrumbs && !editMode && /* @__PURE__ */ jsxRuntime.jsx("div", { className: "min-w-0", children: renderBreadcrumbs() }),
                breadcrumbs && !editMode && title && /* @__PURE__ */ jsxRuntime.jsx("span", { className: "text-foreground font-semibold text-base select-none shrink-0 -mx-1", children: "/" }),
                titleEditable ? /* @__PURE__ */ jsxRuntime.jsx(
                  FxRenameTitle,
                  {
                    ref: renameTitleRef,
                    text: title,
                    editMode,
                    wrappingType: "Normal",
                    onEditAccept: handleTitleEditAccept,
                    onEditCancel: handleTitleEditCancel,
                    onEditModeChange: handleTitleEditModeChange,
                    className: "min-w-0"
                  }
                ) : /* @__PURE__ */ jsxRuntime.jsx(Heading, { className: "text-base font-semibold whitespace-pre-line min-w-0 flex items-center min-h-9", children: title }),
                showTitleArrow && !editMode && /* @__PURE__ */ jsxRuntime.jsx(
                  "button",
                  {
                    ref: titleArrowRef,
                    type: "button",
                    onClick: () => onTitleArrowClick?.(titleArrowRef.current),
                    className: "shrink-0 p-0.5 hover:bg-muted rounded",
                    children: /* @__PURE__ */ jsxRuntime.jsx(lucideReact.ChevronDown, { className: "h-4 w-4" })
                  }
                )
              ] }) }),
              /* @__PURE__ */ jsxRuntime.jsx("div", { className: "flex-1" }),
              actionsBeforeCentered.map((item) => {
                const originalIndex = actionItems.indexOf(item);
                return renderAction(item, originalIndex, overflowState.textVisibility[originalIndex]);
              })
            ] }),
            /* @__PURE__ */ jsxRuntime.jsx("div", { className: "fx-pane-header-center-zone", children: centeredAction && renderAction(
              centeredAction,
              centeredActionIndex,
              overflowState.textVisibility[centeredActionIndex]
            ) }),
            /* @__PURE__ */ jsxRuntime.jsxs("div", { className: "fx-pane-header-right-zone", children: [
              actionsAfterCentered.map((item) => {
                const originalIndex = actionItems.indexOf(item);
                return renderAction(item, originalIndex, overflowState.textVisibility[originalIndex]);
              }),
              overflowState.relationsVisible && hasRelated && /* @__PURE__ */ jsxRuntime.jsxs(jsxRuntime.Fragment, { children: [
                /* @__PURE__ */ jsxRuntime.jsx(
                  Button,
                  {
                    ref: relationsButtonRef,
                    design: "SecondaryNeutral",
                    iconOnly: true,
                    icon: /* @__PURE__ */ jsxRuntime.jsx(lucideReact.Share2, { className: "h-4 w-4" }),
                    onClick: () => {
                      setOverflowMenuOpen(false);
                      setRelationsMenuOpen(true);
                    },
                    tooltip: t("FX_RELATED"),
                    className: "shrink-0 fx-pane-header-relations"
                  }
                ),
                relationsMenuOpen && /* @__PURE__ */ jsxRuntime.jsx(
                  Menu,
                  {
                    open: relationsMenuOpen,
                    opener: relationsButtonRef.current?.nativeElement ?? null,
                    onClose: () => setRelationsMenuOpen(false),
                    children: renderRelationMenuItems(() => setRelationsMenuOpen(false))
                  },
                  "relations-menu"
                )
              ] }),
              showOverflowButton && /* @__PURE__ */ jsxRuntime.jsxs(jsxRuntime.Fragment, { children: [
                /* @__PURE__ */ jsxRuntime.jsx(
                  Button,
                  {
                    ref: overflowButtonRef,
                    design: "SecondaryNeutral",
                    iconOnly: true,
                    icon: /* @__PURE__ */ jsxRuntime.jsx(MoreIcon, { className: "h-4 w-4" }),
                    onClick: () => {
                      setRelationsMenuOpen(false);
                      setOverflowMenuOpen(true);
                    },
                    tooltip: t("FX_MORE_ACTIONS"),
                    className: "shrink-0 fx-pane-header-overflow"
                  }
                ),
                overflowMenuOpen && /* @__PURE__ */ jsxRuntime.jsxs(
                  Menu,
                  {
                    open: overflowMenuOpen,
                    opener: overflowButtonRef.current?.nativeElement ?? null,
                    onClose: () => setOverflowMenuOpen(false),
                    children: [
                      !overflowState.relationsVisible && hasRelated && renderOverflowRelationsSubmenu(() => setOverflowMenuOpen(false)),
                      !overflowState.relationsVisible && hasRelated && overflowedActions.length > 0 && /* @__PURE__ */ jsxRuntime.jsx(MenuSeparator, {}),
                      renderOverflowedActions(
                        () => setOverflowMenuOpen(false),
                        overflowState.relationsVisible || !hasRelated
                      )
                    ]
                  },
                  "overflow-menu"
                )
              ] }),
              showEndToggle && /* @__PURE__ */ jsxRuntime.jsx(
                ToggleButton,
                {
                  design: "SecondaryNeutral",
                  icon: /* @__PURE__ */ jsxRuntime.jsx(NewConversationIcon, { className: "h-4 w-4" }),
                  pressed: endPaneVisible,
                  onClick: () => handleToggleEnd(),
                  tooltip: endPaneVisible ? "Hide chat" : "Show chat",
                  className: "fx-pane-header-toggle-end"
                }
              ),
              showAddButton && !editMode && /* @__PURE__ */ jsxRuntime.jsx(
                Button,
                {
                  design: "SecondaryNeutral",
                  iconOnly: true,
                  icon: /* @__PURE__ */ jsxRuntime.jsx(lucideReact.Plus, { className: "h-4 w-4" }),
                  onClick: () => onAddClick?.(),
                  tooltip: addButtonTooltip || "Add",
                  className: "shrink-0 fx-pane-header-add"
                }
              ),
              handleClose && /* @__PURE__ */ jsxRuntime.jsx(
                Button,
                {
                  design: "Tertiary",
                  iconOnly: true,
                  icon: /* @__PURE__ */ jsxRuntime.jsx(lucideReact.X, { className: "h-4 w-4" }),
                  onClick: () => handleClose?.(),
                  tooltip: t("FX_CLOSE"),
                  className: "shrink-0 fx-pane-header-close text-sapphire-text-primary"
                }
              )
            ] })
          ] })
        ] }),
        subheader && /* @__PURE__ */ jsxRuntime.jsx("div", { className: "w-full mt-2", children: subheader })
      ]
    }
  );
}
var pathData8 = [
  "M455 32q10 0 17.5 7.5T480 58q0 6-2 9L323 464q-7 16-24 16-8 0-14.5-4t-9.5-12l-64-164-163-65q-16-6-16-24 0-17 16-24L445 34q6-2 10-2zM127 212l97 39 126-125zm259-50L260 287l39 98z"
];
var PaperPlaneIcon = Object.assign(
  React45__namespace.default.forwardRef(
    (props, ref) => /* @__PURE__ */ jsxRuntime.jsx(Icon, { ref, pathData: pathData8, ...props })
  ),
  { displayName: "PaperPlaneIcon", iconName: "paper-plane" }
);
var pathData9 = [
  "M256 320q-33 0-56.5-23.5T176 240V80q0-33 23.5-56.5T256 0t56.5 23.5T336 80v160q0 33-23.5 56.5T256 320zm-29-80q0 12 8.5 20.5T256 269t20.5-8.5T285 240V80q0-12-8.5-20.5T256 51t-20.5 8.5T227 80v160zm195-48q11 0 18.5 7.5T448 218q0 36-12.5 69t-35 60-53 44.5T282 414v72q0 11-7.5 18.5T256 512t-18.5-7.5T230 486v-72q-35-5-65.5-23t-53-44.5-35-59.5T64 218q0-11 7.5-18.5T90 192t18 7.5 7 18.5q0 28 11 55t30.5 47 45 32.5T256 365t54.5-12.5 45-32.5 30.5-47 11-55q0-11 7-18.5t18-7.5z"
];
var MicrophoneIcon = Object.assign(
  React45__namespace.default.forwardRef(
    (props, ref) => /* @__PURE__ */ jsxRuntime.jsx(Icon, { ref, pathData: pathData9, ...props })
  ),
  { displayName: "MicrophoneIcon", iconName: "microphone" }
);
var pathData10 = [
  "M256 0q53 0 100 20t81.5 54.5T492 156t20 100-20 100-54.5 81.5T356 492t-100 20-100-20-81.5-54.5T20 356 0 256t20-100 54.5-81.5T156 20 256 0zm150 183q10-9 10-23 0-13-9.5-22.5T384 128t-22 9L186 308l-68-63q-9-9-22-9t-22.5 9.5T64 268q0 15 10 24l91 83q9 9 21 9 13 0 23-9z"
];
var SysEnter2Icon = Object.assign(
  React45__namespace.default.forwardRef(
    (props, ref) => /* @__PURE__ */ jsxRuntime.jsx(Icon, { ref, pathData: pathData10, ...props })
  ),
  { displayName: "SysEnter2Icon", iconName: "sys-enter-2" }
);
var pathData11 = [
  "M256 0q53 0 99.5 20T437 75t55 81.5 20 99.5-20 99.5-55 81.5-81.5 55-99.5 20-99.5-20T75 437t-55-81.5T0 256t20-99.5T75 75t81.5-55T256 0zm45 256l74-73q9-11 9-23 0-13-9.5-22.5T352 128q-12 0-23 9l-73 74-73-74q-10-9-23-9t-22.5 9.5T128 160q0 12 9 23l74 73-74 73q-9 10-9 23t9.5 22.5T160 384t23-9l73-74 73 74q11 9 23 9 13 0 22.5-9.5T384 352t-9-23z"
];
var ErrorIcon = Object.assign(
  React45__namespace.default.forwardRef(
    (props, ref) => /* @__PURE__ */ jsxRuntime.jsx(Icon, { ref, pathData: pathData11, ...props })
  ),
  { displayName: "ErrorIcon", iconName: "error" }
);
var pathData12 = [
  "M505 399q7 13 7 27 0 21-15.5 37.5T456 480H56q-25 0-40.5-16.5T0 426q0-14 7-27L208 59q17-27 48-27 14 0 27 6.5T304 59zM288 176q0-14-9-23t-23-9-23 9-9 23v96q0 14 9 23t23 9 23-9 9-23v-96zm-32 240q14 0 23-9t9-23-9-23-23-9-23 9-9 23 9 23 23 9z"
];
var AlertIcon = Object.assign(
  React45__namespace.default.forwardRef(
    (props, ref) => /* @__PURE__ */ jsxRuntime.jsx(Icon, { ref, pathData: pathData12, ...props })
  ),
  { displayName: "AlertIcon", iconName: "alert" }
);
var pathData13 = [
  "M256 0q53 0 99.5 20T437 75t55 81.5 20 99.5-20 99.5-55 81.5-81.5 55-99.5 20-99.5-20T75 437t-55-81.5T0 256t20-99.5T75 75t81.5-55T256 0zm0 160q14 0 23-9t9-23-9-23-23-9-23 9-9 23 9 23 23 9zm32 64q0-14-9-23t-23-9-23 9-9 23v160q0 14 9 23t23 9 23-9 9-23V224z"
];
var InformationIcon = Object.assign(
  React45__namespace.default.forwardRef(
    (props, ref) => /* @__PURE__ */ jsxRuntime.jsx(Icon, { ref, pathData: pathData13, ...props })
  ),
  { displayName: "InformationIcon", iconName: "information" }
);
var pathData14 = [
  "M256 0q53 0 99.5 20T437 75t55 81.5 20 99.5-20 99.5-55 81.5-81.5 55-99.5 20-99.5-20T75 437t-55-81.5T0 256t20-99.5T75 75t81.5-55T256 0zm32 289q31-10 50.5-36.5T358 192q0-43-29.5-72.5T256 90q-21 0-39.5 8T184 118.5 162 148t-8 35q0 20 9 30.5t23 10.5q13 0 22.5-9t9.5-23q0-16 11-27t27-11 27 11 11 27-11 27-27 11q-14 0-23 9.5t-9 22.5v26q0 14 9 23t23 9q13 0 22-9t10-22zm-32 127q14 0 23-9t9-23-9-23-23-9-23 9-9 23 9 23 23 9z"
];
var SysHelp2Icon = Object.assign(
  React45__namespace.default.forwardRef(
    (props, ref) => /* @__PURE__ */ jsxRuntime.jsx(Icon, { ref, pathData: pathData14, ...props })
  ),
  { displayName: "SysHelp2Icon", iconName: "sys-help-2" }
);
var semanticDesignClass = {
  ["Neutral" /* Neutral */]: "bg-background text-sapphire-text-accent-2 border-sapphire-tag-border",
  ["Information" /* Information */]: "bg-sapphire-info-bg text-sapphire-info border-transparent",
  ["Positive" /* Positive */]: "bg-sapphire-positive-bg text-sapphire-positive border-transparent",
  ["Negative" /* Negative */]: "bg-sapphire-negative-bg text-sapphire-negative border-transparent",
  ["Critical" /* Critical */]: "bg-sapphire-warning-bg text-sapphire-warning border-transparent"
};
var stateIconMap = {
  ["Positive" /* Positive */]: SysEnter2Icon,
  ["Negative" /* Negative */]: ErrorIcon,
  ["Critical" /* Critical */]: AlertIcon,
  ["Information" /* Information */]: InformationIcon,
  ["Neutral" /* Neutral */]: SysHelp2Icon
};
var set1Classes = {
  ["1" /* CS1 */]: "bg-purple-600 text-sapphire-neutral-foreground-white border-transparent",
  ["2" /* CS2 */]: "bg-blue-600 text-sapphire-neutral-foreground-white border-transparent",
  ["3" /* CS3 */]: "bg-neutral-600 text-sapphire-neutral-foreground-white border-transparent",
  ["4" /* CS4 */]: "bg-purple-700 text-sapphire-neutral-foreground-white border-transparent",
  ["5" /* CS5 */]: "bg-blue-700 text-sapphire-neutral-foreground-white border-transparent",
  ["6" /* CS6 */]: "bg-neutral-700 text-sapphire-neutral-foreground-white border-transparent",
  ["7" /* CS7 */]: "bg-purple-800 text-sapphire-neutral-foreground-white border-transparent",
  ["8" /* CS8 */]: "bg-blue-800 text-sapphire-neutral-foreground-white border-transparent",
  ["9" /* CS9 */]: "bg-neutral-800 text-sapphire-neutral-foreground-white border-transparent",
  ["10" /* CS10 */]: "bg-purple-500 text-sapphire-neutral-foreground-white border-transparent"
};
var set2Classes = {
  ["1" /* CS1 */]: "bg-purple-100 text-purple-700 border-transparent",
  ["2" /* CS2 */]: "bg-blue-100 text-blue-700 border-transparent",
  ["3" /* CS3 */]: "bg-neutral-100 text-neutral-700 border-transparent",
  ["4" /* CS4 */]: "bg-purple-200 text-purple-800 border-transparent",
  ["5" /* CS5 */]: "bg-blue-200 text-blue-800 border-transparent",
  ["6" /* CS6 */]: "bg-neutral-200 text-neutral-800 border-transparent",
  ["7" /* CS7 */]: "bg-purple-50 text-purple-600 border-transparent",
  ["8" /* CS8 */]: "bg-blue-50 text-blue-600 border-transparent",
  ["9" /* CS9 */]: "bg-neutral-50 text-neutral-600 border-transparent",
  ["10" /* CS10 */]: "bg-purple-100 text-purple-600 border-transparent"
};
function getDesignClasses(design, colorScheme, accent) {
  if (design === "Set1" /* Set1 */) {
    return set1Classes[colorScheme] ?? set1Classes["1" /* CS1 */];
  }
  if (design === "Set2" /* Set2 */) {
    return set2Classes[colorScheme] ?? set2Classes["1" /* CS1 */];
  }
  if (design === "Neutral" /* Neutral */ && accent) {
    return "bg-sapphire-tag-accent-bg text-sapphire-text-accent-2 border-sapphire-tag-accent-bg";
  }
  return semanticDesignClass[design] ?? semanticDesignClass["Neutral" /* Neutral */];
}
function TagExploration({
  design = "Neutral" /* Neutral */,
  size = "S" /* S */,
  colorScheme = "1" /* CS1 */,
  children,
  icon,
  interactive = false,
  readOnly = false,
  accent = false,
  wrappingType = "None" /* None */,
  hideStateIcon = false,
  onClose,
  onClick,
  accessibleName,
  accessibleNameRef,
  className,
  style,
  id,
  "data-testid": dataTestId,
  ref
}) {
  const tagRef = React45.useRef(null);
  const d = design;
  const s = size;
  const cs = colorScheme;
  React45.useImperativeHandle(
    ref,
    () => ({
      focus() {
        tagRef.current?.focus();
      },
      blur() {
        tagRef.current?.blur();
      },
      isFocused() {
        return document.activeElement === tagRef.current;
      },
      get nativeElement() {
        return tagRef.current;
      }
    }),
    [tagRef]
  );
  const handleClick2 = React45.useCallback(
    (e) => {
      if (!interactive) return;
      onClick?.({ originalEvent: e, isKeyboard: false });
    },
    [interactive, onClick]
  );
  const handleKeyDown = React45.useCallback(
    (e) => {
      if (!interactive) return;
      if (e.key === "Enter" || e.key === " ") {
        e.preventDefault();
        onClick?.({ originalEvent: e, isKeyboard: true });
      }
    },
    [interactive, onClick]
  );
  const handleCloseClick = React45.useCallback(
    (e) => {
      e.stopPropagation();
      onClose?.({ originalEvent: e, isKeyboard: false });
    },
    [onClose]
  );
  const handleCloseKeyDown = React45.useCallback(
    (e) => {
      if (e.key === "Enter" || e.key === " ") {
        e.preventDefault();
        e.stopPropagation();
        onClose?.({ originalEvent: e, isKeyboard: true });
      }
    },
    [onClose]
  );
  const isSemanticDesign = d !== "Set1" /* Set1 */ && d !== "Set2" /* Set2 */;
  let displayIcon = null;
  if (icon) {
    displayIcon = icon;
  } else if (isSemanticDesign && !hideStateIcon) {
    const StateIcon = stateIconMap[d];
    if (StateIcon) {
      const iconSizeClass = s === "L" /* L */ ? "h-4 w-4" : "h-3 w-3";
      displayIcon = /* @__PURE__ */ jsxRuntime.jsx(StateIcon, { className: iconSizeClass });
    }
  }
  const sizeClass = s === "L" /* L */ ? "text-sm px-[9px] py-0.5 rounded-full gap-1" : "text-xs px-2 py-1 rounded-full gap-1";
  const closeBtnSizeClass = s === "L" /* L */ ? "h-4 w-4" : "h-3 w-3";
  const wrappingClasses = wrappingType === "None" /* None */ ? "max-w-full" : "";
  const textWrappingClasses = wrappingType === "None" /* None */ ? "overflow-hidden text-ellipsis whitespace-nowrap" : "whitespace-normal break-words";
  const focusPaddingClass = s === "L" /* L */ ? "focus-visible:px-[8px] focus-visible:py-px" : "focus-visible:px-[7px] focus-visible:py-[3px]";
  const interactiveClasses = interactive && !readOnly ? [
    "cursor-pointer",
    "hover:bg-sapphire-tag-hover-bg hover:border-transparent",
    "active:bg-sapphire-tag-active-bg active:border-transparent active:rounded-[20px]",
    `focus-visible:outline-none focus-visible:bg-sapphire-tag-hover-bg focus-visible:border-2 focus-visible:border-sapphire-tag-focus-border ${focusPaddingClass}`
  ].join(" ") : "";
  return /* @__PURE__ */ jsxRuntime.jsxs(
    "span",
    {
      ref: tagRef,
      id,
      role: interactive && !readOnly ? "button" : void 0,
      tabIndex: interactive && !readOnly ? 0 : void 0,
      "aria-label": accessibleName,
      "aria-labelledby": accessibleNameRef,
      onClick: handleClick2,
      onKeyDown: handleKeyDown,
      className: cn(
        "inline-flex items-center border transition-colors font-normal leading-5 tracking-[0.25px]",
        sizeClass,
        getDesignClasses(d, cs, accent),
        wrappingClasses,
        interactiveClasses,
        readOnly && "opacity-40 pointer-events-none",
        className
      ),
      style,
      "data-testid": dataTestId,
      "data-part": "root",
      children: [
        displayIcon && /* @__PURE__ */ jsxRuntime.jsx("span", { className: "flex-shrink-0", "data-part": "icon", children: displayIcon }),
        children && /* @__PURE__ */ jsxRuntime.jsx("span", { "data-part": "text", className: textWrappingClasses, children }),
        onClose && !readOnly && /* @__PURE__ */ jsxRuntime.jsx(
          "button",
          {
            type: "button",
            onClick: handleCloseClick,
            onKeyDown: handleCloseKeyDown,
            "aria-label": "Remove",
            "data-part": "close",
            className: cn(
              "flex-shrink-0 inline-flex items-center justify-center rounded-full",
              "hover:opacity-70 focus:outline-none focus:ring-1 focus:ring-ring",
              "cursor-pointer"
            ),
            tabIndex: 0,
            children: /* @__PURE__ */ jsxRuntime.jsx(DeclineIcon, { className: closeBtnSizeClass })
          }
        )
      ]
    }
  );
}
function FxPromptInput({
  value: controlledValue,
  placeholder = "Type a message...",
  disabled = false,
  statusMessage,
  disclaimerMessage,
  showDictate = true,
  showVoice = true,
  mode: explicitMode,
  typeaheadEnabled = true,
  maxSuggestions = 5,
  debounceMs = 150,
  actions,
  contextItems,
  toolbarButtons,
  onSubmit,
  onLiveChange,
  onPlusPress,
  onDictatePress,
  onVoicePress,
  onTypeaheadRequest,
  onActionSelect: _onActionSelect,
  onContextItemRemove,
  className,
  style,
  ref
}) {
  const { t } = reactI18next.useTranslation("fx");
  const inputRef = React45.useRef(null);
  const textareaRef = React45.useRef(null);
  const containerRef = React45.useRef(null);
  const plusButtonRef = React45.useRef(null);
  const [internalValue, setInternalValue] = React45.useState("");
  const [actionsMenuOpen, setActionsMenuOpen] = React45.useState(false);
  const [suggestions, setSuggestions] = React45.useState([]);
  const [suggestionsVisible, setSuggestionsVisible] = React45.useState(false);
  const [selectedIndex, setSelectedIndex] = React45.useState(0);
  const [suggestionsDismissed, setSuggestionsDismissed] = React45.useState(false);
  const [requestIdCounter, setRequestIdCounter] = React45.useState(0);
  const layoutContext = useFxLayoutContext();
  const contextMode = layoutContext?.inputMode;
  const effectiveMode = explicitMode ?? contextMode ?? "oneline";
  const isMultiline = effectiveMode === "multiline";
  const value = controlledValue ?? internalValue;
  const hasValue = value.trim().length > 0;
  const visibleSuggestions = React45.useMemo(
    () => suggestions.slice(0, maxSuggestions),
    [suggestions, maxSuggestions]
  );
  const currentCompletion = React45.useMemo(() => {
    if (!suggestionsVisible || visibleSuggestions.length === 0) return "";
    const selected = visibleSuggestions[selectedIndex];
    return selected?.text ?? "";
  }, [suggestionsVisible, visibleSuggestions, selectedIndex]);
  React45.useImperativeHandle(ref, () => ({
    focus: () => {
      if (isMultiline) {
        textareaRef.current?.focus();
      } else {
        inputRef.current?.focus();
      }
    },
    clear: () => {
      setInternalValue("");
      setSuggestions([]);
      setSuggestionsVisible(false);
      onLiveChange?.({ value: "" });
    },
    getValue: () => value,
    setValue: (v) => {
      setInternalValue(v);
      onLiveChange?.({ value: v });
    },
    get nativeElement() {
      return containerRef.current;
    },
    getNativeElement: () => containerRef.current,
    setSuggestions: (newSuggestions, requestId) => {
      if (requestId < requestIdCounter - 1) return;
      setSuggestions(newSuggestions);
      setSuggestionsVisible(newSuggestions.length > 0 && !suggestionsDismissed);
      setSelectedIndex(0);
    }
  }));
  const handleChange = React45.useCallback(
    (newValue) => {
      setInternalValue(newValue);
      setSuggestionsDismissed(false);
      onLiveChange?.({ value: newValue });
    },
    [onLiveChange]
  );
  const handleSubmit = React45.useCallback(() => {
    if (hasValue && !disabled) {
      onSubmit?.({ value: value.trim() });
      setInternalValue("");
      setSuggestions([]);
      setSuggestionsVisible(false);
    }
  }, [hasValue, value, disabled, onSubmit]);
  const acceptSuggestion = React45.useCallback(() => {
    if (!suggestionsVisible || visibleSuggestions.length === 0) return false;
    const selected = visibleSuggestions[selectedIndex];
    if (!selected) return false;
    const newValue = value + selected.text;
    setInternalValue(newValue);
    onLiveChange?.({ value: newValue });
    setSuggestionsVisible(false);
    setSuggestions([]);
    return true;
  }, [suggestionsVisible, visibleSuggestions, selectedIndex, value, onLiveChange]);
  const handleKeyDown = React45.useCallback(
    (e) => {
      if (e.key === "Tab" && suggestionsVisible && visibleSuggestions.length > 0) {
        e.preventDefault();
        acceptSuggestion();
        return;
      }
      if (suggestionsVisible && visibleSuggestions.length > 0) {
        if (e.key === "ArrowUp") {
          e.preventDefault();
          setSelectedIndex(
            (prev) => prev <= 0 ? visibleSuggestions.length - 1 : prev - 1
          );
          return;
        }
        if (e.key === "ArrowDown") {
          e.preventDefault();
          setSelectedIndex(
            (prev) => prev >= visibleSuggestions.length - 1 ? 0 : prev + 1
          );
          return;
        }
      }
      if (e.key === "Escape") {
        if (suggestionsVisible) {
          e.preventDefault();
          setSuggestionsVisible(false);
          setSuggestionsDismissed(true);
          return;
        }
      }
      if (e.key === "Enter" && (!isMultiline || !e.shiftKey)) {
        e.preventDefault();
        handleSubmit();
      }
    },
    [
      suggestionsVisible,
      visibleSuggestions,
      acceptSuggestion,
      isMultiline,
      handleSubmit
    ]
  );
  const handleSuggestionClick = React45.useCallback(
    (index) => {
      setSelectedIndex(index);
      const selected = visibleSuggestions[index];
      if (!selected) return;
      const newValue = value + selected.text;
      setInternalValue(newValue);
      onLiveChange?.({ value: newValue });
      setSuggestionsVisible(false);
      setSuggestions([]);
    },
    [visibleSuggestions, value, onLiveChange]
  );
  React45.useEffect(() => {
    if (!typeaheadEnabled || !value.trim() || !onTypeaheadRequest || suggestionsDismissed) {
      if (suggestions.length > 0) {
        setSuggestions([]);
      }
      if (suggestionsVisible) {
        setSuggestionsVisible(false);
      }
      return;
    }
    const controller = new AbortController();
    const currentRequestId = requestIdCounter + 1;
    setRequestIdCounter(currentRequestId);
    const timeoutId = setTimeout(async () => {
      try {
        const results = await onTypeaheadRequest({
          value,
          requestId: currentRequestId,
          signal: controller.signal
        });
        if (results && Array.isArray(results)) {
          setSuggestions(results);
          setSuggestionsVisible(results.length > 0);
          setSelectedIndex(0);
        }
      } catch {
      }
    }, debounceMs);
    return () => {
      clearTimeout(timeoutId);
      controller.abort();
    };
  }, [value, typeaheadEnabled, onTypeaheadRequest, debounceMs, suggestionsDismissed]);
  const renderLeftButtons = () => {
    if (!actions && !onPlusPress) return null;
    return /* @__PURE__ */ jsxRuntime.jsxs("div", { className: "flex shrink-0", children: [
      /* @__PURE__ */ jsxRuntime.jsx(
        Button,
        {
          ref: plusButtonRef,
          design: "Neutral",
          iconOnly: true,
          icon: /* @__PURE__ */ jsxRuntime.jsx(AddIcon, { className: "h-4 w-4" }),
          onClick: () => {
            if (actions) {
              setActionsMenuOpen(true);
            }
            onPlusPress?.();
          },
          disabled,
          tooltip: t("FX_ADD"),
          className: "h-7 w-7 bg-muted hover:border-sapphire-prompt-accent text-sapphire-prompt-accent"
        }
      ),
      actions && /* @__PURE__ */ jsxRuntime.jsx(
        Menu,
        {
          open: actionsMenuOpen,
          opener: plusButtonRef.current?.nativeElement,
          onClose: () => setActionsMenuOpen(false),
          placement: "top-start",
          children: actions
        }
      )
    ] });
  };
  const renderRightButtons = () => {
    return /* @__PURE__ */ jsxRuntime.jsx("div", { className: "flex shrink-0 gap-1", children: hasValue ? /* @__PURE__ */ jsxRuntime.jsx(
      Button,
      {
        design: "Neutral",
        iconOnly: true,
        icon: /* @__PURE__ */ jsxRuntime.jsx(PaperPlaneIcon, { className: "h-4 w-4" }),
        onClick: handleSubmit,
        disabled,
        tooltip: t("FX_SEND"),
        className: "h-7 w-7 bg-muted hover:border-sapphire-prompt-accent text-sapphire-prompt-accent"
      }
    ) : /* @__PURE__ */ jsxRuntime.jsxs(jsxRuntime.Fragment, { children: [
      showDictate && /* @__PURE__ */ jsxRuntime.jsx(
        Button,
        {
          design: "Tertiary",
          iconOnly: true,
          icon: /* @__PURE__ */ jsxRuntime.jsx(MicrophoneIcon, { className: "h-4 w-4" }),
          onClick: onDictatePress,
          disabled,
          tooltip: t("FX_DICTATE"),
          className: "h-7 w-7 text-sapphire-prompt-accent"
        }
      ),
      showVoice && /* @__PURE__ */ jsxRuntime.jsx(
        Button,
        {
          design: "Tertiary",
          iconOnly: true,
          icon: /* @__PURE__ */ jsxRuntime.jsx(lucideReact.AudioWaveform, { className: "h-4 w-4" }),
          onClick: onVoicePress,
          disabled,
          tooltip: t("FX_VOICE"),
          className: "h-7 w-7 text-sapphire-prompt-accent"
        }
      )
    ] }) });
  };
  const renderContextItems = () => {
    if (!contextItems || contextItems.length === 0) return null;
    return /* @__PURE__ */ jsxRuntime.jsx("div", { className: "flex items-center gap-1.5 shrink-0", children: contextItems.map((item) => /* @__PURE__ */ jsxRuntime.jsx(
      TagExploration,
      {
        design: "Neutral",
        size: "S",
        hideStateIcon: true,
        className: "text-sapphire-prompt-accent",
        icon: item.icon ? /* @__PURE__ */ jsxRuntime.jsx("span", { className: "flex items-center justify-center h-3.5 w-3.5", children: item.icon }) : void 0,
        onClose: item.removable !== false && onContextItemRemove ? () => onContextItemRemove({ item }) : void 0,
        children: item.label
      },
      item.id
    )) });
  };
  const renderSuggestions = () => {
    if (!suggestionsVisible || visibleSuggestions.length === 0) return null;
    return /* @__PURE__ */ jsxRuntime.jsxs("div", { className: "relative flex items-end mb-2 min-h-10", children: [
      !isMultiline && /* @__PURE__ */ jsxRuntime.jsx(
        "div",
        {
          className: "invisible whitespace-pre text-sm",
          style: { paddingLeft: "3.5rem" },
          children: value
        }
      ),
      /* @__PURE__ */ jsxRuntime.jsx(
        "div",
        {
          className: "flex flex-col-reverse gap-1.5",
          style: { marginLeft: isMultiline ? 0 : "-1.2rem", maxWidth: "calc(100% - 4rem)" },
          children: visibleSuggestions.map((suggestion, index) => /* @__PURE__ */ jsxRuntime.jsx(
            "button",
            {
              type: "button",
              className: cn(
                "inline-flex items-center px-3 py-1.5 text-sm rounded-lg cursor-pointer transition-all",
                "whitespace-nowrap max-w-[200px] overflow-hidden text-ellipsis",
                "border bg-background"
              ),
              style: {
                borderColor: index === selectedIndex ? "var(--prompt-accent-muted)" : "color-mix(in srgb, var(--prompt-accent-muted) 50%, transparent)",
                boxShadow: index === selectedIndex ? "0 0 0 2px color-mix(in srgb, var(--prompt-accent-subtle) 50%, transparent)" : void 0
              },
              onMouseDown: (e) => {
                e.preventDefault();
                handleSuggestionClick(index);
              },
              onMouseEnter: () => setSelectedIndex(index),
              children: suggestion.label ?? suggestion.text
            },
            index
          ))
        }
      )
    ] });
  };
  return /* @__PURE__ */ jsxRuntime.jsxs(
    "div",
    {
      ref: containerRef,
      className: cn(
        "w-full relative",
        disabled && "opacity-60 pointer-events-none",
        className
      ),
      style,
      children: [
        statusMessage && /* @__PURE__ */ jsxRuntime.jsx(
          "div",
          {
            className: "px-4 pt-2 pb-5 text-sm rounded-t-lg relative z-[1] -mb-3 animate-in fade-in duration-500 bg-sapphire-info-bg text-sapphire-text-tertiary",
            children: statusMessage
          }
        ),
        renderSuggestions(),
        /* @__PURE__ */ jsxRuntime.jsxs(
          "div",
          {
            className: "fx-prompt-input relative rounded-lg bg-card",
            style: {
              // Shadow on left, right, bottom - most pronounced at bottom
              boxShadow: `
              -3px 0 8px -3px color-mix(in srgb, var(--prompt-accent-subtle) 20%, transparent),
              3px 0 8px -3px color-mix(in srgb, var(--prompt-accent-subtle) 20%, transparent),
              0 4px 12px -2px color-mix(in srgb, var(--prompt-accent-muted) 30%, transparent)
            `
            },
            children: [
              /* @__PURE__ */ jsxRuntime.jsx(
                "div",
                {
                  className: "absolute inset-0 rounded-lg pointer-events-none",
                  style: {
                    padding: "1.5px",
                    background: "linear-gradient(to right, color-mix(in srgb, var(--color-brand-blue) 60%, transparent), color-mix(in srgb, var(--color-brand-purple) 60%, transparent))",
                    WebkitMask: "linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0)",
                    WebkitMaskComposite: "xor",
                    maskComposite: "exclude"
                  }
                }
              ),
              isMultiline ? (
                // Multiline mode: textarea with toolbar below
                /* @__PURE__ */ jsxRuntime.jsxs("div", { className: "flex flex-col p-3", children: [
                  /* @__PURE__ */ jsxRuntime.jsx("div", { className: "flex-1 min-w-0 mb-2", children: /* @__PURE__ */ jsxRuntime.jsx(
                    "textarea",
                    {
                      ref: textareaRef,
                      value,
                      onChange: (e) => handleChange(e.target.value),
                      onKeyDown: handleKeyDown,
                      placeholder,
                      disabled,
                      rows: 1,
                      className: cn(
                        "w-full bg-transparent border-none outline-none resize-none text-base min-h-[2rem]",
                        "placeholder:text-sapphire-text-tertiary placeholder:italic placeholder:font-normal focus:ring-0 focus:outline-none"
                      ),
                      style: {
                        boxShadow: "none"
                      }
                    }
                  ) }),
                  /* @__PURE__ */ jsxRuntime.jsxs("div", { className: "flex items-center gap-1.5", children: [
                    renderLeftButtons(),
                    renderContextItems(),
                    toolbarButtons,
                    /* @__PURE__ */ jsxRuntime.jsx("div", { className: "flex-1" }),
                    " ",
                    renderRightButtons()
                  ] })
                ] })
              ) : (
                // Oneline mode: two rows - input on top, buttons below
                /* @__PURE__ */ jsxRuntime.jsxs("div", { className: "flex flex-col p-3", children: [
                  /* @__PURE__ */ jsxRuntime.jsxs("div", { className: "flex-1 min-w-0 relative mb-2", children: [
                    currentCompletion && /* @__PURE__ */ jsxRuntime.jsxs(
                      "div",
                      {
                        className: "absolute top-0 left-0 right-0 bottom-0 pointer-events-none flex items-center overflow-hidden text-sm",
                        children: [
                          /* @__PURE__ */ jsxRuntime.jsx("span", { className: "invisible whitespace-pre", children: value }),
                          /* @__PURE__ */ jsxRuntime.jsx("span", { className: "text-sapphire-text-tertiary whitespace-pre overflow-hidden text-ellipsis", children: currentCompletion })
                        ]
                      }
                    ),
                    /* @__PURE__ */ jsxRuntime.jsx(
                      "input",
                      {
                        ref: inputRef,
                        type: "text",
                        value,
                        onChange: (e) => handleChange(e.target.value),
                        onKeyDown: handleKeyDown,
                        placeholder,
                        disabled,
                        className: cn(
                          "w-full bg-transparent border-none outline-none text-base h-6",
                          "placeholder:text-sapphire-text-tertiary placeholder:italic placeholder:font-normal"
                        )
                      }
                    )
                  ] }),
                  /* @__PURE__ */ jsxRuntime.jsxs("div", { className: "flex items-center gap-1.5", children: [
                    renderLeftButtons(),
                    renderContextItems(),
                    toolbarButtons,
                    /* @__PURE__ */ jsxRuntime.jsx("div", { className: "flex-1" }),
                    " ",
                    renderRightButtons()
                  ] })
                ] })
              )
            ]
          }
        ),
        disclaimerMessage && /* @__PURE__ */ jsxRuntime.jsx(
          "div",
          {
            className: "text-center text-xs leading-4 mt-4 text-secondary-foreground",
            style: { letterSpacing: "0.4px" },
            children: disclaimerMessage
          }
        )
      ]
    }
  );
}
var pathData15 = [
  "M64 192q26 0 45 19t19 45-19 45-45 19-45-19-19-45 19-45 45-19zm192 0q26 0 45 19t19 45-19 45-45 19-45-19-19-45 19-45 45-19zm192 0q26 0 45 19t19 45-19 45-45 19-45-19-19-45 19-45 45-19z"
];
var OverflowIcon = Object.assign(
  React45__namespace.default.forwardRef(
    (props, ref) => /* @__PURE__ */ jsxRuntime.jsx(Icon, { ref, pathData: pathData15, ...props })
  ),
  { displayName: "OverflowIcon", iconName: "overflow" }
);
var listContainerVariants = cva(
  "relative w-full",
  {
    variants: {
      bordered: {
        true: "border border-sapphire-border-primary rounded-lg overflow-hidden",
        false: ""
      }
    },
    defaultVariants: {
      bordered: false
    }
  }
);
var listVariants = cva(
  "w-full m-0 p-0 list-none",
  {
    variants: {
      indent: {
        true: "pl-4",
        false: ""
      },
      separators: {
        true: "[&>li:not(:last-child)]:border-b [&>li:not(:last-child)]:border-sapphire-border-primary",
        false: ""
      }
    },
    defaultVariants: {
      indent: false,
      separators: true
    }
  }
);
var ListContext = React45__namespace.createContext(null);
function useOptionalListContext() {
  return React45.useContext(ListContext);
}
function useListSelection({
  mode,
  controlledKeys,
  defaultKeys = [],
  items,
  getItemKey,
  onSelectionChange
}) {
  const [internalKeys, setInternalKeys] = React45.useState(new Set(defaultKeys));
  const previousKeysRef = React45.useRef(new Set(defaultKeys));
  const isControlled = controlledKeys !== void 0;
  const selectedKeys = isControlled ? new Set(controlledKeys) : internalKeys;
  const getItemByKey = React45.useCallback((key) => {
    if (!items || !getItemKey) return void 0;
    return items.find((item, index) => getItemKey(item, index) === key);
  }, [items, getItemKey]);
  const getSelectedItems = React45.useCallback(() => {
    if (!items || !getItemKey) return [];
    return items.filter((item, index) => selectedKeys.has(getItemKey(item, index)));
  }, [items, getItemKey, selectedKeys]);
  const setSelected = React45.useCallback((key, selected) => {
    const modeStr = String(mode);
    if (modeStr === "None") return;
    const newKeys = new Set(selectedKeys);
    const previousKeys = new Set(selectedKeys);
    if (modeStr === "Single" || modeStr === "SingleStart" || modeStr === "SingleEnd") {
      if (selected) {
        newKeys.clear();
        newKeys.add(key);
      } else {
        newKeys.delete(key);
      }
    } else if (modeStr === "Multiple") {
      if (selected) {
        newKeys.add(key);
      } else {
        newKeys.delete(key);
      }
    }
    if (!isControlled) {
      setInternalKeys(newKeys);
    }
    const targetItem = getItemByKey(key);
    onSelectionChange?.({
      selectedItems: items?.filter((item, index) => getItemKey && newKeys.has(getItemKey(item, index))) ?? [],
      previouslySelectedItems: items?.filter((item, index) => getItemKey && previousKeys.has(getItemKey(item, index))) ?? [],
      selectionComponentPressed: false,
      targetItem,
      key,
      selectedKeys: Array.from(newKeys)
      // Always include the raw keys
    });
    previousKeysRef.current = newKeys;
  }, [mode, selectedKeys, isControlled, getItemByKey, items, getItemKey, onSelectionChange]);
  const toggleSelection = React45.useCallback((key) => {
    setSelected(key, !selectedKeys.has(key));
  }, [selectedKeys, setSelected]);
  const isSelected = React45.useCallback((key) => {
    return selectedKeys.has(key);
  }, [selectedKeys]);
  const selectAll = React45.useCallback(() => {
    if (!items || !getItemKey || String(mode) !== "Multiple") return;
    const allKeys = new Set(items.map(getItemKey));
    if (!isControlled) {
      setInternalKeys(allKeys);
    }
    onSelectionChange?.({
      selectedItems: items,
      previouslySelectedItems: getSelectedItems(),
      selectionComponentPressed: false,
      targetItem: items[0]
    });
  }, [items, getItemKey, mode, isControlled, onSelectionChange, getSelectedItems]);
  const deselectAll = React45.useCallback(() => {
    if (!isControlled) {
      setInternalKeys(/* @__PURE__ */ new Set());
    }
    onSelectionChange?.({
      selectedItems: [],
      previouslySelectedItems: getSelectedItems(),
      selectionComponentPressed: false,
      targetItem: void 0
    });
  }, [isControlled, onSelectionChange, getSelectedItems]);
  return {
    selectedKeys,
    setSelected,
    toggleSelection,
    isSelected,
    selectAll,
    deselectAll,
    getSelectedItems
  };
}
function useListNavigation({
  onFocusItem,
  onSelectItem,
  selectionMode,
  containerRef
}) {
  const [focusedIndex, setFocusedIndex] = React45.useState(-1);
  const getAllNavigable = React45.useCallback(() => {
    if (!containerRef.current) return [];
    const elements = Array.from(
      containerRef.current.querySelectorAll(
        'div[data-group-header="true"], li[role="listitem"], li[role="option"], li[role="menuitem"], li[role="treeitem"], button[data-growing-button]'
      )
    );
    return elements.filter((el) => {
      const groupContent = el.closest('ul[role="group"]');
      if (groupContent && groupContent.classList.contains("hidden")) return false;
      return true;
    });
  }, [containerRef]);
  const getAllNavigableExcludingGrowingButton = React45.useCallback(() => {
    if (!containerRef.current) return [];
    const elements = Array.from(
      containerRef.current.querySelectorAll(
        'div[data-group-header="true"], li[role="listitem"], li[role="option"], li[role="menuitem"], li[role="treeitem"]'
      )
    );
    return elements.filter((el) => {
      const groupContent = el.closest('ul[role="group"]');
      if (groupContent && groupContent.classList.contains("hidden")) return false;
      return true;
    });
  }, [containerRef]);
  const focusItem = React45.useCallback((index) => {
    const allNavigable = getAllNavigable();
    if (index < 0 || index >= allNavigable.length) return;
    const element = allNavigable[index];
    if (!element) return;
    const isGrowingButton = element.hasAttribute("data-growing-button");
    if (!isGrowingButton) {
      setFocusedIndex(index);
    }
    element.focus();
    onFocusItem?.(index);
  }, [getAllNavigable, onFocusItem]);
  const handleKeyDown = React45.useCallback((e) => {
    const key = e.key;
    if (key === "Tab") {
      return;
    }
    const target = e.target;
    const isInteractiveElement2 = target.tagName === "INPUT" || target.tagName === "TEXTAREA" || target.tagName === "SELECT" || target.tagName === "BUTTON" || target.getAttribute("role") === "button" || target.getAttribute("contenteditable") === "true" || target.closest('button, input, textarea, select, [role="button"], [contenteditable="true"]') !== null;
    if (isInteractiveElement2) {
      if (key === " " || key === "Enter" || key === "Delete") {
        return;
      }
    }
    const allNavigable = getAllNavigable();
    if (allNavigable.length === 0) return;
    const activeElement = document.activeElement;
    let currentIndex = allNavigable.indexOf(activeElement);
    if (currentIndex === -1) {
      currentIndex = focusedIndex >= 0 && focusedIndex < allNavigable.length ? focusedIndex : 0;
    }
    let newIndex = currentIndex;
    switch (key) {
      case "ArrowDown":
        e.preventDefault();
        newIndex = Math.min(currentIndex + 1, allNavigable.length - 1);
        break;
      case "ArrowUp":
        e.preventDefault();
        newIndex = Math.max(currentIndex - 1, 0);
        break;
      case "Home":
        e.preventDefault();
        const navigableExcludingButton = getAllNavigableExcludingGrowingButton();
        if (navigableExcludingButton.length > 0) {
          const firstElement = navigableExcludingButton[0];
          const indexInAll = allNavigable.indexOf(firstElement);
          if (indexInAll !== -1) {
            newIndex = indexInAll;
          }
        }
        break;
      case "End":
        e.preventDefault();
        const navigableExcludingButtonEnd = getAllNavigableExcludingGrowingButton();
        if (navigableExcludingButtonEnd.length > 0) {
          const lastElement = navigableExcludingButtonEnd[navigableExcludingButtonEnd.length - 1];
          const indexInAll = allNavigable.indexOf(lastElement);
          if (indexInAll !== -1) {
            newIndex = indexInAll;
          }
        }
        break;
      case "PageDown":
        e.preventDefault();
        const navigableExcludingButtonPageDown = getAllNavigableExcludingGrowingButton();
        const currentInFiltered = navigableExcludingButtonPageDown.indexOf(activeElement);
        if (currentInFiltered !== -1) {
          const newFilteredIndex = Math.min(currentInFiltered + 10, navigableExcludingButtonPageDown.length - 1);
          const targetElement = navigableExcludingButtonPageDown[newFilteredIndex];
          newIndex = allNavigable.indexOf(targetElement);
        } else {
          if (navigableExcludingButtonPageDown.length > 0) {
            const lastElement = navigableExcludingButtonPageDown[navigableExcludingButtonPageDown.length - 1];
            newIndex = allNavigable.indexOf(lastElement);
          }
        }
        break;
      case "PageUp":
        e.preventDefault();
        const navigableExcludingButtonPageUp = getAllNavigableExcludingGrowingButton();
        const currentInFilteredPageUp = navigableExcludingButtonPageUp.indexOf(activeElement);
        if (currentInFilteredPageUp !== -1) {
          const newFilteredIndex = Math.max(currentInFilteredPageUp - 10, 0);
          const targetElement = navigableExcludingButtonPageUp[newFilteredIndex];
          newIndex = allNavigable.indexOf(targetElement);
        } else {
          if (navigableExcludingButtonPageUp.length > 0) {
            const lastFilteredIndex = navigableExcludingButtonPageUp.length - 1;
            const targetIndex = Math.max(lastFilteredIndex - 10, 0);
            const targetElement = navigableExcludingButtonPageUp[targetIndex];
            newIndex = allNavigable.indexOf(targetElement);
          }
        }
        break;
      case " ":
        e.preventDefault();
        const spaceElement = allNavigable[currentIndex];
        if (spaceElement?.getAttribute("role") === "listitem") {
          if (String(selectionMode) !== "None" && String(selectionMode) !== "Delete") {
            onSelectItem?.(currentIndex);
          }
        }
        return;
      case "Delete":
        const deleteElement = allNavigable[currentIndex];
        if (deleteElement?.getAttribute("role") === "listitem" && String(selectionMode) === "Delete") {
          onSelectItem?.(currentIndex);
        }
        return;
      case "Enter":
        return;
      default:
        return;
    }
    if (newIndex !== currentIndex) {
      focusItem(newIndex);
    }
  }, [focusedIndex, getAllNavigable, getAllNavigableExcludingGrowingButton, selectionMode, onSelectItem, focusItem]);
  const registerItem = React45.useCallback((_index, _element) => {
  }, []);
  return {
    focusedIndex,
    setFocusedIndex,
    focusItem,
    handleKeyDown,
    registerItem
  };
}
function ListHeader({ text, children, sticky, className }) {
  if (!text && !children) return null;
  return /* @__PURE__ */ jsxRuntime.jsx(
    "div",
    {
      className: cn(
        "flex items-center gap-2.5 px-[0.75rem] py-[0.25rem] text-base font-normal text-sapphire-text-primary bg-background",
        sticky && "sticky top-0 z-10 bg-background",
        className
      ),
      role: "heading",
      "aria-level": 2,
      children: children || text
    }
  );
}
function ListLoading({ className }) {
  return /* @__PURE__ */ jsxRuntime.jsx(
    "div",
    {
      className: cn(
        "absolute inset-0 flex items-center justify-center bg-background/80 backdrop-blur-sm z-20",
        className
      ),
      role: "progressbar",
      "aria-label": "Loading",
      children: /* @__PURE__ */ jsxRuntime.jsx(lucideReact.Loader2, { className: "h-6 w-6 animate-spin text-sapphire-brand-foreground" })
    }
  );
}
function ListEmpty({ text, children, className }) {
  return /* @__PURE__ */ jsxRuntime.jsx(
    "div",
    {
      className: cn(
        "flex items-center justify-center px-4 py-12 text-sm text-sapphire-text-tertiary",
        className
      ),
      role: "status",
      children: children || text || "No data"
    }
  );
}
function ListGrowing({
  mode,
  buttonText = "More",
  onLoadMore,
  parentRef,
  loading,
  accessibilityAttributes,
  buttonRef
}) {
  const observerRef = React45.useRef(null);
  const triggerRef = React45.useRef(null);
  React45.useEffect(() => {
    if (String(mode) !== "Scroll" || !triggerRef.current) return;
    observerRef.current = new IntersectionObserver(
      (entries) => {
        const [entry] = entries;
        if (entry.isIntersecting && !loading) {
          onLoadMore?.();
        }
      },
      {
        root: parentRef?.current,
        rootMargin: "100px",
        threshold: 0
      }
    );
    observerRef.current.observe(triggerRef.current);
    return () => {
      observerRef.current?.disconnect();
    };
  }, [mode, onLoadMore, parentRef, loading]);
  if (String(mode) === "Button") {
    return /* @__PURE__ */ jsxRuntime.jsx("div", { className: "flex items-center justify-center py-3 border-t border-sapphire-border-primary", children: /* @__PURE__ */ jsxRuntime.jsxs(
      Button,
      {
        ref: buttonRef,
        design: "Tertiary" /* Tertiary */,
        onClick: () => onLoadMore?.(),
        disabled: loading,
        accessibleName: accessibilityAttributes?.name,
        "data-growing-button": "true",
        tabIndex: 0,
        className: "w-full mx-4",
        children: [
          loading ? /* @__PURE__ */ jsxRuntime.jsx(lucideReact.Loader2, { className: "h-4 w-4 animate-spin mr-2" }) : null,
          buttonText
        ]
      }
    ) });
  }
  if (String(mode) === "Scroll") {
    return /* @__PURE__ */ jsxRuntime.jsx(
      "div",
      {
        ref: triggerRef,
        className: "h-1",
        "aria-hidden": "true",
        "data-growing-trigger": true
      }
    );
  }
  return null;
}
function ListInner(props) {
  const {
    ref,
    // Header & Footer
    headerText,
    header,
    // Selection
    selectionMode = "None" /* None */,
    selectedKeys: controlledSelectedKeys,
    defaultSelectedKeys = [],
    onSelectionChange,
    // Data
    items,
    renderItem,
    getItemKey = (item, index) => String(item.id ?? item.key ?? index),
    children,
    // Empty & Loading
    noDataText = "No data available",
    noDataSlot,
    loading = false,
    loadingDelay = 1e3,
    // Growing
    growing = "None" /* None */,
    growingButtonText,
    hasMore = false,
    onLoadMore,
    // Visual
    separators = "All" /* All */,
    indent = false,
    stickyHeader = false,
    stickyFooter = false,
    // Events
    onItemClick,
    onItemDelete,
    onItemFocused,
    // Drag and Drop
    onMoveOver,
    onMove,
    // Accessibility
    accessibleName,
    accessibleNameRef,
    accessibleDescriptionRef,
    accessibleRole = "List" /* List */,
    accessibilityAttributes,
    // Standard
    className,
    style,
    id: providedId,
    "data-testid": dataTestId,
    "data-ai-context": dataAiContext
  } = props;
  const generatedId = React45.useId();
  const listId = providedId ?? generatedId;
  const containerRef = React45.useRef(null);
  const listRef = React45.useRef(null);
  const growingButtonRef = React45.useRef(null);
  const beforeSentinelRef = React45.useRef(null);
  const afterSentinelRef = React45.useRef(null);
  const [showLoading, setShowLoading] = React45.useState(false);
  const [mediaRange, setMediaRange] = React45.useState("M");
  const itemRegistry = React45.useRef(/* @__PURE__ */ new Map());
  const [previouslyFocusedItemKey, setPreviouslyFocusedItemKey] = React45.useState(null);
  const [draggedKey, setDraggedKey] = React45.useState(null);
  const [dropTargetKey, setDropTargetKey] = React45.useState(null);
  const [dropPlacement, setDropPlacement] = React45.useState(null);
  const selectionState = useListSelection({
    mode: selectionMode,
    controlledKeys: controlledSelectedKeys,
    defaultKeys: defaultSelectedKeys,
    items,
    getItemKey,
    onSelectionChange
  });
  const itemCount = React45.useMemo(() => {
    if (items) return items.length;
    return React45__namespace.Children.count(children);
  }, [items, children]);
  const navigationState = useListNavigation({
    onFocusItem: (index) => {
      const allNavigable = Array.from(
        containerRef.current?.querySelectorAll(
          'div[data-group-header="true"], li[role="listitem"]'
        ) || []
      );
      const element = allNavigable[index];
      if (element?.getAttribute("role") === "listitem" && items) {
        const itemKey = element.getAttribute("data-item-key");
        if (itemKey) {
          const itemIndex = items.findIndex((item, idx) => getItemKey(item, idx) === itemKey);
          if (itemIndex >= 0) {
            onItemFocused?.({ item: items[itemIndex], index: itemIndex });
          }
        }
      }
    },
    onSelectItem: (index) => {
      const allNavigable = Array.from(
        containerRef.current?.querySelectorAll(
          'div[data-group-header="true"], li[role="listitem"]'
        ) || []
      );
      const element = allNavigable[index];
      if (!element || element.getAttribute("role") !== "listitem") return;
      if (!items || !getItemKey) return;
      const itemKey = element.getAttribute("data-item-key");
      if (!itemKey) return;
      const itemIndex = items.findIndex((item2, idx) => getItemKey(item2, idx) === itemKey);
      if (itemIndex < 0) return;
      const item = items[itemIndex];
      const key = getItemKey(item, itemIndex);
      if (String(selectionMode) === "Delete") {
        onItemDelete?.({ item });
      } else {
        selectionState.toggleSelection(key);
      }
    },
    selectionMode,
    containerRef
  });
  React45.useEffect(() => {
    if (loading) {
      const timer = setTimeout(() => setShowLoading(true), loadingDelay);
      return () => clearTimeout(timer);
    } else {
      setShowLoading(false);
    }
  }, [loading, loadingDelay]);
  React45.useEffect(() => {
    const updateMediaRange = () => {
      const width = window.innerWidth;
      if (width < 600) setMediaRange("S");
      else if (width < 1024) setMediaRange("M");
      else if (width < 1440) setMediaRange("L");
      else setMediaRange("XL");
    };
    updateMediaRange();
    window.addEventListener("resize", updateMediaRange);
    return () => window.removeEventListener("resize", updateMediaRange);
  }, []);
  const registerItem = React45.useCallback((key, index) => {
    itemRegistry.current.set(key, index);
  }, []);
  const unregisterItem = React45.useCallback((key) => {
    itemRegistry.current.delete(key);
  }, []);
  const handleDragStart = React45.useCallback((key, _e) => {
    setDraggedKey(key);
  }, []);
  const handleDragEnd = React45.useCallback((_key, _e) => {
    setDraggedKey(null);
    setDropTargetKey(null);
    setDropPlacement(null);
  }, []);
  const handleDragOver = React45.useCallback((key, e) => {
    if (draggedKey && key === draggedKey) return;
    setDropTargetKey(key);
    const rect = e.currentTarget.getBoundingClientRect();
    const y = e.clientY - rect.top;
    const height = rect.height;
    if (y < height * 0.33) {
      setDropPlacement("Before");
    } else if (y > height * 0.67) {
      setDropPlacement("After");
    } else {
      setDropPlacement("On");
    }
  }, [draggedKey]);
  const handleDrop = React45.useCallback((targetKey, e) => {
    const targetIndex = itemRegistry.current.get(targetKey);
    if (targetIndex === void 0) return;
    if (!items || !getItemKey) return;
    const targetItem = items[targetIndex];
    if (!targetItem) return;
    const droppedKey = draggedKey || e.dataTransfer.getData("text/plain");
    if (!droppedKey || targetKey === droppedKey) return;
    const placement = dropPlacement || "After";
    const sourceIndex = itemRegistry.current.get(droppedKey);
    const sourceItem = sourceIndex !== void 0 ? items[sourceIndex] : void 0;
    const isMultiple = String(selectionMode) === "Multiple";
    const draggedIsSelected = droppedKey ? selectionState.isSelected(droppedKey) : false;
    const selectedItemsList = isMultiple && draggedIsSelected ? selectionState.getSelectedItems() : void 0;
    const moveDetail = {
      source: {
        element: sourceItem,
        // May be undefined for cross-list drops
        elements: selectedItemsList,
        index: sourceIndex ?? -1
        // -1 indicates cross-list drop
      },
      destination: {
        element: targetItem,
        placement,
        index: targetIndex
      },
      originalEvent: e.nativeEvent
    };
    onMoveOver?.(moveDetail);
    onMove?.(moveDetail);
    setDraggedKey(null);
    setDropTargetKey(null);
    setDropPlacement(null);
  }, [draggedKey, dropPlacement, items, getItemKey, selectionMode, selectionState, onMoveOver, onMove]);
  const handleForwardTabAfter = React45.useCallback((key, event) => {
    setPreviouslyFocusedItemKey(key);
    if (containerRef.current) {
      const allNavigable = Array.from(
        containerRef.current.querySelectorAll(
          'div[data-group-header="true"], li[role="listitem"], li[role="option"], button[data-growing-button]'
        )
      );
      const itemElement = allNavigable.find(
        (el) => el.getAttribute("data-item-key") === key || el.id === key
      );
      if (itemElement) {
        const itemIndex = allNavigable.indexOf(itemElement);
        navigationState.setFocusedIndex(itemIndex);
      }
    }
    if (String(growing) !== "None" && hasMore && growingButtonRef.current) {
      growingButtonRef.current.focus?.();
      return;
    }
    if (listRef.current) {
      const allTabbable = Array.from(
        document.querySelectorAll(
          'a[href], area[href], input:not([disabled]), select:not([disabled]), textarea:not([disabled]), button:not([disabled]), iframe, object, embed, [tabindex]:not([tabindex="-1"]), [contenteditable]'
        )
      );
      const target = event.target;
      const currentIndex = allTabbable.indexOf(target);
      if (currentIndex >= 0) {
        for (let i = currentIndex + 1; i < allTabbable.length; i++) {
          if (!listRef.current.contains(allTabbable[i])) {
            allTabbable[i].focus();
            return;
          }
        }
      } else {
        if (afterSentinelRef.current) {
          afterSentinelRef.current.focus();
        }
      }
    }
  }, [growing, hasMore, navigationState]);
  const handleForwardTabBefore = React45.useCallback((key, event) => {
    setPreviouslyFocusedItemKey(key);
    let itemElement = null;
    if (containerRef.current) {
      const allNavigable = Array.from(
        containerRef.current.querySelectorAll(
          'div[data-group-header="true"], li[role="listitem"], li[role="option"], button[data-growing-button]'
        )
      );
      itemElement = allNavigable.find(
        (el) => el.getAttribute("data-item-key") === key || el.id === key
      ) || null;
      if (itemElement) {
        const itemIndex = allNavigable.indexOf(itemElement);
        navigationState.setFocusedIndex(itemIndex);
      }
    }
    if (itemElement && event.target !== itemElement) {
      itemElement.focus();
      return;
    }
    if (listRef.current) {
      const allTabbable = Array.from(
        document.querySelectorAll(
          'a[href], area[href], input:not([disabled]), select:not([disabled]), textarea:not([disabled]), button:not([disabled]), iframe, object, embed, [tabindex]:not([tabindex="-1"]), [contenteditable]'
        )
      );
      const target = event.target;
      const currentIndex = allTabbable.indexOf(target);
      if (currentIndex >= 0) {
        for (let i = currentIndex - 1; i >= 0; i--) {
          if (!listRef.current.contains(allTabbable[i])) {
            allTabbable[i].focus();
            return;
          }
        }
      } else {
        if (beforeSentinelRef.current) {
          beforeSentinelRef.current.focus();
        }
      }
    }
  }, [navigationState]);
  const handleBeforeSentinelFocus = React45.useCallback((e) => {
    const relatedTarget = e.relatedTarget;
    const isFromAfterSentinel = relatedTarget === afterSentinelRef.current;
    const isFromListItem = relatedTarget?.closest('li[role="listitem"], li[role="option"]');
    const isFromGrowingButton = relatedTarget?.hasAttribute("data-growing-button");
    if (isFromAfterSentinel || isFromListItem || isFromGrowingButton) {
      const allTabbable = Array.from(
        document.querySelectorAll(
          'a[href], area[href], input:not([disabled]), select:not([disabled]), textarea:not([disabled]), button:not([disabled]), iframe, object, embed, [tabindex]:not([tabindex="-1"]), [contenteditable]'
        )
      );
      const sentinelIndex = allTabbable.indexOf(beforeSentinelRef.current);
      if (sentinelIndex > 0) {
        allTabbable[sentinelIndex - 1].focus();
      }
    } else {
      e.preventDefault();
      if (containerRef.current) {
        const allNavigable = Array.from(
          containerRef.current.querySelectorAll(
            'div[data-group-header="true"], li[role="listitem"], li[role="option"], button[data-growing-button]'
          )
        );
        if (allNavigable.length === 0) {
          if (afterSentinelRef.current) {
            afterSentinelRef.current.focus();
          }
          return;
        }
        if (previouslyFocusedItemKey) {
          const targetElement = allNavigable.find(
            (el) => el.getAttribute("data-item-key") === previouslyFocusedItemKey || el.id === previouslyFocusedItemKey
          );
          if (targetElement) {
            targetElement.focus();
            return;
          }
        }
        if (allNavigable[0]) {
          allNavigable[0].focus();
        }
      }
    }
  }, [previouslyFocusedItemKey]);
  const getAllTabbableElements = React45.useCallback(() => {
    return Array.from(
      document.querySelectorAll(
        'a[href], area[href], input:not([disabled]), select:not([disabled]), textarea:not([disabled]), button:not([disabled]), iframe, object, embed, [tabindex]:not([tabindex="-1"]), [contenteditable]'
      )
    );
  }, []);
  const focusNextAfterList = React45.useCallback(() => {
    if (!listRef.current || !afterSentinelRef.current) return false;
    const allTabbable = getAllTabbableElements();
    const sentinelIndex = allTabbable.indexOf(afterSentinelRef.current);
    if (sentinelIndex >= 0) {
      for (let i = sentinelIndex + 1; i < allTabbable.length; i++) {
        if (!containerRef.current?.contains(allTabbable[i])) {
          allTabbable[i].focus();
          return true;
        }
      }
    }
    return false;
  }, [getAllTabbableElements]);
  const enterListAtFocusedItem = React45.useCallback(() => {
    if (!containerRef.current) return false;
    const allNavigable = Array.from(
      containerRef.current.querySelectorAll(
        'div[data-group-header="true"], li[role="listitem"], li[role="option"]'
      )
    );
    if (allNavigable.length === 0) {
      beforeSentinelRef.current?.focus();
      return true;
    }
    const focusedIndex = navigationState.focusedIndex;
    const targetIndex = focusedIndex >= 0 && focusedIndex < allNavigable.length ? focusedIndex : 0;
    if (allNavigable[targetIndex]) {
      allNavigable[targetIndex].focus();
      return true;
    }
    return false;
  }, [navigationState.focusedIndex]);
  const handleAfterSentinelFocus = React45.useCallback((e) => {
    const relatedTarget = e.relatedTarget;
    const isFromGrowingButton = relatedTarget?.hasAttribute("data-growing-button");
    const isFromOutside = !relatedTarget || !containerRef.current?.contains(relatedTarget);
    if (isFromGrowingButton || isFromOutside) {
      enterListAtFocusedItem();
    } else {
      focusNextAfterList();
    }
  }, [focusNextAfterList, enterListAtFocusedItem]);
  const roleSupportsAriaSelected = React45.useMemo(() => {
    const roleStr = String(accessibleRole);
    return roleStr === "ListBox" /* ListBox */ || roleStr === "listbox" || roleStr === "Tree" /* Tree */ || roleStr === "tree";
  }, [accessibleRole]);
  const contextValue = React45.useMemo(() => ({
    selectionMode,
    selectedKeys: selectionState.selectedKeys,
    onSelect: selectionState.setSelected,
    onToggleSelect: selectionState.toggleSelection,
    isSelected: selectionState.isSelected,
    focusedIndex: navigationState.focusedIndex,
    onFocusItem: navigationState.setFocusedIndex,
    registerItem,
    unregisterItem,
    registerNavigationItem: navigationState.registerItem,
    // Tab navigation
    onForwardTabAfter: handleForwardTabAfter,
    onForwardTabBefore: handleForwardTabBefore,
    separators,
    indent,
    onItemClick: (_key, event) => {
      onItemClick?.({ item: event.currentTarget, originalEvent: event });
    },
    onItemDelete: (key) => {
      const index = itemRegistry.current.get(key);
      if (index !== void 0 && items) {
        onItemDelete?.({ item: items[index] });
        return;
      }
      if (items && getItemKey) {
        const item = items.find((item2, idx) => getItemKey(item2, idx) === key);
        if (item) {
          onItemDelete?.({ item });
        }
      }
    },
    // Drag and drop
    onDragStart: handleDragStart,
    onDragEnd: handleDragEnd,
    onDragOver: handleDragOver,
    onDrop: handleDrop,
    draggedKey,
    dropTargetKey,
    dropPlacement,
    mediaRange,
    totalItemCount: itemCount,
    getItemIndex: (key) => {
      return itemRegistry.current.get(key) ?? -1;
    },
    // Accessibility
    accessibleRole,
    supportsAriaSelected: roleSupportsAriaSelected
  }), [
    selectionMode,
    selectionState,
    navigationState,
    registerItem,
    unregisterItem,
    handleForwardTabAfter,
    handleForwardTabBefore,
    separators,
    indent,
    onItemClick,
    onItemDelete,
    items,
    getItemKey,
    mediaRange,
    itemCount,
    handleDragStart,
    handleDragEnd,
    handleDragOver,
    handleDrop,
    draggedKey,
    dropTargetKey,
    dropPlacement,
    accessibleRole,
    roleSupportsAriaSelected
  ]);
  React45.useImperativeHandle(ref, () => ({
    // Focus
    focus: () => listRef.current?.focus(),
    focusItem: (index) => navigationState.focusItem(index),
    getFocusedItemIndex: () => navigationState.focusedIndex,
    // Selection
    getSelectedKeys: () => Array.from(selectionState.selectedKeys),
    getSelectedItems: () => selectionState.getSelectedItems(),
    isSelected: selectionState.isSelected,
    select: (keys) => {
      const keyArray = Array.isArray(keys) ? keys : [keys];
      keyArray.forEach((key) => selectionState.setSelected(key, true));
    },
    deselect: (keys) => {
      const keyArray = Array.isArray(keys) ? keys : [keys];
      keyArray.forEach((key) => selectionState.setSelected(key, false));
    },
    toggleSelection: selectionState.toggleSelection,
    selectAll: selectionState.selectAll,
    deselectAll: selectionState.deselectAll,
    // Data
    getItems: () => items ?? [],
    getItemCount: () => itemCount,
    getItemByKey: (key) => items?.find((item, index) => getItemKey(item, index) === key),
    // Scrolling
    scrollToItem: (index, align = "start") => {
      const element = listRef.current?.children[index];
      element?.scrollIntoView({ behavior: "smooth", block: align });
    },
    scrollToTop: () => {
      containerRef.current?.scrollTo({ top: 0, behavior: "smooth" });
    },
    scrollToBottom: () => {
      containerRef.current?.scrollTo({
        top: containerRef.current.scrollHeight,
        behavior: "smooth"
      });
    },
    // Elements
    get listElement() {
      return listRef.current;
    },
    get scrollContainer() {
      return containerRef.current;
    }
  }), [selectionState, navigationState, items, itemCount, getItemKey]);
  const handleLoadMore = React45.useCallback(() => {
    onLoadMore?.({ currentCount: itemCount });
  }, [onLoadMore, itemCount]);
  const getRole = () => {
    const roleStr = String(accessibleRole);
    switch (roleStr) {
      case "Menu":
        return "menu";
      case "Tree":
        return "tree";
      case "ListBox":
        return "listbox";
      default:
        return "list";
    }
  };
  const isEmpty = itemCount === 0 && !loading;
  const renderedItems = React45.useMemo(() => {
    itemRegistry.current.clear();
    if (items && renderItem) {
      return items.map((item, index) => {
        const key = getItemKey(item, index);
        itemRegistry.current.set(key, index);
        return /* @__PURE__ */ jsxRuntime.jsx(React45__namespace.Fragment, { children: renderItem(item, index) }, key);
      });
    }
    if (children) {
      let index = 0;
      React45__namespace.Children.forEach(children, (child) => {
        if (React45__namespace.isValidElement(child) && child.props && typeof child.props.itemKey === "string") {
          itemRegistry.current.set(child.props.itemKey, index);
          index++;
        }
      });
    }
    return children;
  }, [items, renderItem, getItemKey, children]);
  return /* @__PURE__ */ jsxRuntime.jsx(ListContext.Provider, { value: contextValue, children: /* @__PURE__ */ jsxRuntime.jsxs(
    "div",
    {
      ref: containerRef,
      className: cn(
        listContainerVariants({ bordered: false }),
        // Make scrollable when growing mode or sticky header is used
        (String(growing) !== "None" || stickyHeader || stickyFooter) && "overflow-auto",
        // Always prevent horizontal scroll
        "overflow-x-hidden",
        className
      ),
      style,
      "data-testid": dataTestId,
      "data-ai-context": dataAiContext,
      onKeyDown: navigationState.handleKeyDown,
      children: [
        /* @__PURE__ */ jsxRuntime.jsx(
          ListHeader,
          {
            text: headerText,
            sticky: stickyHeader,
            children: header
          }
        ),
        /* @__PURE__ */ jsxRuntime.jsx(
          "span",
          {
            ref: beforeSentinelRef,
            tabIndex: 0,
            onFocus: handleBeforeSentinelFocus,
            style: {
              position: "absolute",
              width: "1px",
              height: "1px",
              padding: 0,
              margin: "-1px",
              overflow: "hidden",
              clip: "rect(0, 0, 0, 0)",
              whiteSpace: "nowrap",
              border: 0
            }
          }
        ),
        /* @__PURE__ */ jsxRuntime.jsx(
          "ul",
          {
            ref: listRef,
            id: listId,
            role: getRole(),
            "aria-label": accessibleName,
            "aria-labelledby": accessibleNameRef,
            "aria-describedby": accessibleDescriptionRef,
            "aria-busy": loading || void 0,
            "aria-multiselectable": roleSupportsAriaSelected && String(selectionMode) === "Multiple" ? true : void 0,
            className: cn(listVariants({ indent, separators: String(separators) !== "None" })),
            children: !isEmpty && renderedItems
          }
        ),
        /* @__PURE__ */ jsxRuntime.jsx(
          "span",
          {
            ref: afterSentinelRef,
            tabIndex: 0,
            onFocus: handleAfterSentinelFocus,
            style: {
              position: "absolute",
              width: "1px",
              height: "1px",
              padding: 0,
              margin: "-1px",
              overflow: "hidden",
              clip: "rect(0, 0, 0, 0)",
              whiteSpace: "nowrap",
              border: 0
            }
          }
        ),
        isEmpty && /* @__PURE__ */ jsxRuntime.jsx(ListEmpty, { text: noDataText, children: noDataSlot }),
        String(growing) !== "None" && hasMore && !loading && /* @__PURE__ */ jsxRuntime.jsx(
          ListGrowing,
          {
            mode: growing,
            buttonText: growingButtonText,
            onLoadMore: handleLoadMore,
            parentRef: containerRef,
            loading,
            accessibilityAttributes: accessibilityAttributes?.growingButton,
            buttonRef: growingButtonRef
          }
        ),
        showLoading && /* @__PURE__ */ jsxRuntime.jsx(ListLoading, {})
      ]
    }
  ) });
}
var List2 = React45__namespace.memo(ListInner);
List2.displayName = "List";

// src/components/list/utils/tabbable.ts
var TABBABLE_SELECTOR = [
  "a[href]",
  "area[href]",
  'input:not([disabled]):not([type="hidden"])',
  "select:not([disabled])",
  "textarea:not([disabled])",
  "button:not([disabled])",
  "iframe",
  "object",
  "embed",
  '[tabindex]:not([tabindex="-1"])',
  '[contenteditable]:not([contenteditable="false"])'
].join(",");
function getTabbableElements(container) {
  if (!container) return [];
  const elements = Array.from(
    container.querySelectorAll(TABBABLE_SELECTOR)
  );
  return elements.filter((el) => {
    const tabindex = el.getAttribute("tabindex");
    if (tabindex === "-1") return false;
    const style = window.getComputedStyle(el);
    if (style.display === "none" || style.visibility === "hidden") return false;
    let current = el;
    while (current && current !== container) {
      if (current.getAttribute("aria-hidden") === "true") return false;
      current = current.parentElement;
    }
    return true;
  });
}
function getActiveElement() {
  let activeElement = document.activeElement;
  while (activeElement?.shadowRoot?.activeElement) {
    activeElement = activeElement.shadowRoot.activeElement;
  }
  return activeElement;
}

// src/components/list/hooks/useListItemSelection.ts
var isInteractiveElement = (target, container) => {
  let el = target;
  while (el && el !== container) {
    const tag = el.tagName.toLowerCase();
    if (tag === "input" || tag === "textarea" || tag === "select" || tag === "button" || tag === "a" && el.hasAttribute("href") || el.getAttribute("role") === "button" || el.isContentEditable) {
      return true;
    }
    el = el.parentElement;
  }
  return false;
};
function useListItemSelection(options) {
  const { id, disabled, type, selectionMode, onClick, itemRef } = options;
  const listContext = useOptionalListContext();
  const isSelected = listContext ? listContext.isSelected(id) : false;
  const shouldForwardTabAfter = React45.useCallback(() => {
    if (!itemRef?.current) return true;
    const tabbables = getTabbableElements(itemRef.current);
    if (tabbables.length === 0) return true;
    const activeElement = getActiveElement();
    const lastTabbable = tabbables[tabbables.length - 1];
    return activeElement === lastTabbable;
  }, [itemRef]);
  const shouldForwardTabBefore = React45.useCallback((target) => {
    if (!itemRef?.current) return false;
    if (target === itemRef.current) return true;
    const tabbables = getTabbableElements(itemRef.current);
    if (tabbables.length === 0) return false;
    return target === tabbables[0];
  }, [itemRef]);
  const handleClick2 = React45.useCallback((e) => {
    if (disabled) return;
    const container = itemRef?.current ?? null;
    const clickTarget = e.target;
    if (clickTarget !== container && isInteractiveElement(clickTarget, container)) {
      return;
    }
    const typeStr = String(type);
    if (typeStr === "Inactive" || typeStr === "Detail") {
      return;
    }
    const modeStr = String(selectionMode);
    if (modeStr !== "None" && modeStr !== "Delete") {
      listContext?.onToggleSelect(id);
    }
    onClick?.(e);
    listContext?.onItemClick?.(id, e);
  }, [disabled, type, selectionMode, listContext, id, onClick, itemRef]);
  const handleKeyDown = React45.useCallback((e) => {
    if (disabled) return;
    const modeStr = String(selectionMode);
    if (["ArrowDown", "ArrowUp", "Home", "End", "PageDown", "PageUp"].includes(e.key)) {
      return;
    }
    if (e.key === "Tab") {
      const target = e.target;
      const tabbables = getTabbableElements(itemRef?.current || null);
      if (e.shiftKey) {
        if (shouldForwardTabBefore(target)) {
          e.preventDefault();
          listContext?.onForwardTabBefore?.(id, e);
        }
      } else {
        if (target === itemRef?.current && tabbables.length > 0) {
          e.preventDefault();
          tabbables[0].focus();
          return;
        }
        if (shouldForwardTabAfter()) {
          e.preventDefault();
          listContext?.onForwardTabAfter?.(id, e);
          return;
        }
      }
      return;
    }
    if (e.key === "Enter" || e.key === " ") {
      const keyTarget = e.target;
      const keyTag = keyTarget.tagName.toLowerCase();
      if (keyTag === "input" || keyTag === "textarea" || keyTag === "select" || keyTarget.isContentEditable || keyTarget.getAttribute("role") === "button" || keyTag === "a" && keyTarget.hasAttribute("href")) {
        return;
      }
      e.preventDefault();
      const typeStr = String(type);
      if (typeStr === "Inactive" || typeStr === "Detail") {
        return;
      }
      if (modeStr !== "None" && modeStr !== "Delete") {
        listContext?.onToggleSelect(id);
      }
      onClick?.(e);
      listContext?.onItemClick?.(id, e);
    }
  }, [disabled, type, selectionMode, listContext, id, onClick, shouldForwardTabAfter, shouldForwardTabBefore, itemRef]);
  const handleSelectionChange = React45.useCallback((checked) => {
    if (disabled) return;
    listContext?.onSelect(id, checked);
  }, [disabled, listContext, id]);
  return {
    isSelected,
    handleClick: handleClick2,
    handleKeyDown,
    handleSelectionChange
  };
}
function useListItemFocus(options) {
  const { itemRef, disabled, selectionMode, accessibleRole, controlledSelected } = options;
  const listContext = useOptionalListContext();
  const [isFocused, setIsFocused] = React45.useState(false);
  const [isFocusedWithin, setIsFocusedWithin] = React45.useState(false);
  const [isActive, setIsActive] = React45.useState(false);
  const getItemIndex = React45.useCallback(() => {
    let rootList = itemRef.current?.parentElement;
    while (rootList && rootList.getAttribute("role") !== "list" && rootList.getAttribute("role") !== "listbox") {
      rootList = rootList.parentElement;
    }
    if (rootList && itemRef.current) {
      const allNavigable = Array.from(
        rootList.querySelectorAll(
          'div[data-group-header="true"], li[role="listitem"], li[role="option"]'
        )
      );
      return allNavigable.indexOf(itemRef.current);
    }
    return -1;
  }, [itemRef]);
  const handleFocus = React45.useCallback((e) => {
    const isItemItself = e.target === itemRef.current;
    setIsFocused(isItemItself);
    setIsFocusedWithin(true);
    if (listContext) {
      const itemIndex2 = getItemIndex();
      if (itemIndex2 >= 0) {
        listContext.onFocusItem(itemIndex2);
      }
    }
  }, [listContext, getItemIndex, itemRef]);
  const handleBlur = React45.useCallback((e) => {
    const isLeavingItem = !itemRef.current?.contains(e.relatedTarget);
    if (isLeavingItem) {
      setIsFocused(false);
      setIsFocusedWithin(false);
    } else {
      setIsFocused(false);
    }
  }, [itemRef]);
  const handleMouseDown = React45.useCallback(() => {
    setIsActive(true);
  }, []);
  const handleMouseUp = React45.useCallback(() => {
    setIsActive(false);
  }, []);
  const handleMouseDownCapture = React45.useCallback(() => {
    setIsFocusedWithin(true);
  }, []);
  const focus = React45.useCallback(() => {
    itemRef.current?.focus();
  }, [itemRef]);
  const blur = React45.useCallback(() => {
    itemRef.current?.blur();
  }, [itemRef]);
  const modeStr = String(selectionMode);
  const isSelected = controlledSelected ?? (listContext?.isSelected(itemRef.current?.getAttribute("data-item-key") || "") || false);
  const roleStr = String(accessibleRole);
  const supportsAriaSelected = listContext?.supportsAriaSelected ?? false;
  const hasSelection = modeStr !== "None" && modeStr !== "Delete";
  const ariaSelected = roleStr === "Option" ? isSelected : supportsAriaSelected && hasSelection ? isSelected : void 0;
  const itemIndex = getItemIndex();
  const isFocusedItem = listContext?.focusedIndex === itemIndex;
  const isFirstItem = itemIndex === 0;
  const isTabbable = isFocusedItem || listContext?.focusedIndex === -1 && isFirstItem;
  const tabbableIndex = isTabbable ? 0 : -1;
  const tabIndex = disabled ? -1 : tabbableIndex;
  return {
    isFocused,
    isFocusedWithin,
    isActive,
    tabIndex,
    ariaSelected,
    handleFocus,
    handleBlur,
    handleMouseDown,
    handleMouseUp,
    handleMouseDownCapture,
    focus,
    blur,
    getItemIndex
  };
}
function useListItemDragDrop(options) {
  const { id, movable, disabled, onDragStart: onDragStartProp } = options;
  const listContext = useOptionalListContext();
  const isDragging = listContext?.draggedKey === id;
  const isDropTarget = listContext?.dropTargetKey === id;
  const dropPlacement = isDropTarget ? listContext?.dropPlacement : null;
  const handleDragStart = React45.useCallback((e) => {
    if (!movable || disabled) {
      e.preventDefault();
      return;
    }
    e.dataTransfer.effectAllowed = "move";
    e.dataTransfer.dropEffect = "move";
    e.dataTransfer.setData("text/plain", id);
    listContext?.onDragStart?.(id, e);
    onDragStartProp?.();
  }, [movable, disabled, id, listContext, onDragStartProp]);
  const handleDragEnd = React45.useCallback((e) => {
    listContext?.onDragEnd?.(id, e);
  }, [id, listContext]);
  const handleDragOver = React45.useCallback((e) => {
    e.preventDefault();
    e.dataTransfer.dropEffect = "move";
    listContext?.onDragOver?.(id, e);
  }, [id, listContext]);
  const handleDrop = React45.useCallback((e) => {
    e.preventDefault();
    listContext?.onDrop?.(id, e);
  }, [id, listContext]);
  const handleDragEnter = React45.useCallback((e) => {
    e.preventDefault();
  }, []);
  const handleDragLeave = React45.useCallback((_e) => {
  }, []);
  return {
    isDragging,
    isDropTarget,
    dropPlacement: dropPlacement || null,
    handleDragStart,
    handleDragEnd,
    handleDragOver,
    handleDragEnter,
    handleDragLeave,
    handleDrop
  };
}
function useListItemRegistration(options) {
  const { itemRef, itemKey } = options;
  const listContext = useOptionalListContext();
  const itemIndexRef = React45.useRef(-1);
  React45.useEffect(() => {
    if (listContext && itemKey) {
      let rootList = itemRef.current?.parentElement;
      while (rootList && rootList.getAttribute("role") !== "list" && rootList.getAttribute("role") !== "listbox") {
        rootList = rootList.parentElement;
      }
      if (rootList) {
        const allNavigable = Array.from(
          rootList.querySelectorAll(
            'div[data-group-header="true"], li[role="listitem"]'
          )
        );
        const index = allNavigable.indexOf(itemRef.current);
        if (index >= 0) {
          itemIndexRef.current = index;
          listContext.registerItem(itemKey, index);
          listContext.registerNavigationItem(index, itemRef.current);
        }
      }
    }
    return () => {
      if (listContext && itemKey && itemIndexRef.current >= 0) {
        listContext.unregisterItem(itemKey);
        listContext.registerNavigationItem(itemIndexRef.current, null);
        itemIndexRef.current = -1;
      }
    };
  }, [listContext, itemKey, itemRef]);
  return {};
}

// src/components/list/useListItem.ts
function useListItem(options = {}) {
  const {
    itemKey,
    disabled = false,
    movable = false,
    type = "Active",
    accessibleRole = "ListItem",
    selected: controlledSelected,
    onClick,
    onDragStart
  } = options;
  const itemRef = React45.useRef(null);
  const generatedId = React45.useId();
  const id = itemKey ?? generatedId;
  const listContext = useOptionalListContext();
  const selectionMode = listContext?.selectionMode ?? "None" /* None */;
  useListItemRegistration({ itemRef, itemKey });
  const selection = useListItemSelection({
    id,
    disabled,
    type,
    selectionMode,
    onClick,
    itemRef
  });
  const focus = useListItemFocus({
    itemRef,
    disabled,
    selectionMode,
    accessibleRole,
    controlledSelected
  });
  const handleMouseDown = React45.useCallback(() => {
    const typeStr = String(type);
    if (typeStr === "Active" || typeStr === "Navigation") {
      focus.handleMouseDown();
    }
  }, [type, focus]);
  const handleMouseUp = React45.useCallback(() => {
    focus.handleMouseUp();
  }, [focus]);
  const dragDrop = useListItemDragDrop({
    id,
    movable,
    disabled,
    onDragStart
  });
  return {
    // Refs
    itemRef,
    id,
    // State from sub-hooks (controlledSelected overrides context-based selection)
    isSelected: controlledSelected ?? selection.isSelected,
    isFocused: focus.isFocused,
    isFocusedWithin: focus.isFocusedWithin,
    isActive: focus.isActive,
    isDragging: dragDrop.isDragging,
    isDropTarget: dragDrop.isDropTarget,
    dropPlacement: dragDrop.dropPlacement,
    // Context values
    selectionMode,
    // Computed values
    tabIndex: focus.tabIndex,
    ariaSelected: focus.ariaSelected,
    // Event handlers (composed from sub-hooks)
    handleClick: selection.handleClick,
    handleKeyDown: selection.handleKeyDown,
    handleMouseDown,
    handleMouseUp,
    handleMouseDownCapture: focus.handleMouseDownCapture,
    handleFocus: focus.handleFocus,
    handleBlur: focus.handleBlur,
    handleDragStart: dragDrop.handleDragStart,
    handleDragEnd: dragDrop.handleDragEnd,
    handleDragOver: dragDrop.handleDragOver,
    handleDragEnter: dragDrop.handleDragEnter,
    handleDragLeave: dragDrop.handleDragLeave,
    handleDrop: dragDrop.handleDrop,
    // Selection handlers
    handleSelectionChange: selection.handleSelectionChange,
    // Helper functions
    focus: focus.focus,
    blur: focus.blur,
    getItemIndex: focus.getItemIndex
  };
}
var pathData16 = [
  "M438 224q11 0 18.5 7.5T464 250v140q0 38-26 64t-64 26H106q-38 0-64-26t-26-64V122q0-38 26-64t64-26h237q11 0 18 7.5t7 18.5-7 18-18 7H106q-16 0-27.5 11.5T67 122v268q0 16 11.5 27.5T106 429h268q16 0 27.5-11.5T413 390V250q0-11 7-18.5t18-7.5zm32-192q11 0 18.5 7.5T496 58q0 10-7 17L257 312q-6 8-18 8-10 0-18-8l-70-71q-7-7-7-18t7.5-18 18.5-7 18 7l51 53L452 40q8-8 18-8z"
];
var CompleteIcon = Object.assign(
  React45__namespace.default.forwardRef(
    (props, ref) => /* @__PURE__ */ jsxRuntime.jsx(Icon, { ref, pathData: pathData16, ...props })
  ),
  { displayName: "CompleteIcon", iconName: "complete" }
);
var pathData17 = [
  "M390 480H122q-38 0-64-26t-26-64V122q0-38 26-64t64-26h268q38 0 64 26t26 64v268q0 38-26 64t-64 26zM122 83q-17 0-28 11t-11 28v268q0 17 11 28t28 11h268q17 0 28-11t11-28V122q0-17-11-28t-28-11H122z"
];
var BorderIcon = Object.assign(
  React45__namespace.default.forwardRef(
    (props, ref) => /* @__PURE__ */ jsxRuntime.jsx(Icon, { ref, pathData: pathData17, ...props })
  ),
  { displayName: "BorderIcon", iconName: "border" }
);
var pathData18 = [
  "M390 32q38 0 64 26t26 64v268q0 38-26 64t-64 26H122q-38 0-64-26t-26-64V122q0-38 26-64t64-26h268zm39 90q0-17-11-28t-28-11H122q-17 0-28 11t-11 28v268q0 17 11 28t28 11h268q17 0 28-11t11-28V122zm-77 38v192H160V160h192z"
];
var TriStateIcon = Object.assign(
  React45__namespace.default.forwardRef(
    (props, ref) => /* @__PURE__ */ jsxRuntime.jsx(Icon, { ref, pathData: pathData18, ...props })
  ),
  { displayName: "TriStateIcon", iconName: "tri-state" }
);
function CheckBox({
  checked: controlledChecked,
  defaultChecked = false,
  indeterminate = false,
  disabled = false,
  readonly = false,
  displayOnly = false,
  required = false,
  size = "Large" /* Large */,
  text,
  children,
  wrappingType = "Normal" /* Normal */,
  valueState = "None",
  valueStateMessage,
  name,
  value = "on",
  accessibleName,
  accessibleNameRef,
  accessibleDescription,
  onChange,
  onFocus,
  onBlur,
  className,
  style,
  id: providedId,
  "data-testid": dataTestId,
  tabIndex,
  accessibilityAttributes,
  ref
}) {
  const inputRef = React45.useRef(null);
  const rootRef = React45.useRef(null);
  const generatedId = React45.useId();
  const id = providedId || generatedId;
  const descriptionId = `${id}-description`;
  const [internalChecked, setInternalChecked] = React45.useState(defaultChecked);
  const isControlled = controlledChecked !== void 0;
  const isChecked = isControlled ? controlledChecked : internalChecked;
  const isActive = isChecked || indeterminate;
  const doToggle = React45.useCallback(
    (originalEvent) => {
      if (disabled || readonly || displayOnly) return;
      const newChecked = !isChecked;
      if (!isControlled) {
        setInternalChecked(newChecked);
      }
      onChange?.({
        checked: newChecked,
        indeterminate: false,
        originalEvent
      });
    },
    [disabled, readonly, displayOnly, isChecked, isControlled, onChange]
  );
  React45.useImperativeHandle(
    ref,
    () => ({
      focus() {
        rootRef.current?.focus();
      },
      blur() {
        rootRef.current?.blur();
      },
      isFocused() {
        return document.activeElement === rootRef.current;
      },
      isChecked() {
        return isChecked;
      },
      toggle() {
        doToggle();
      },
      get nativeElement() {
        return inputRef.current;
      }
    }),
    [isChecked, doToggle]
  );
  const handleClick2 = React45.useCallback(
    (e) => {
      if (disabled || readonly || displayOnly) {
        e.preventDefault();
        return;
      }
      doToggle(e);
    },
    [disabled, readonly, displayOnly, doToggle]
  );
  const handleMouseDown = React45.useCallback(
    (e) => {
      if (disabled || displayOnly) {
        e.preventDefault();
      }
    },
    [disabled, displayOnly]
  );
  const handleKeyDown = React45.useCallback(
    (e) => {
      if (disabled || readonly || displayOnly) return;
      if (e.key === " " || e.key === "Enter") {
        e.preventDefault();
      }
    },
    [disabled, readonly, displayOnly]
  );
  const handleKeyUp = React45.useCallback(
    (e) => {
      if (disabled || readonly || displayOnly) return;
      if (e.key === " " || e.key === "Enter") {
        e.preventDefault();
        doToggle(e);
      }
    },
    [disabled, readonly, displayOnly, doToggle]
  );
  const labelContent = children || text;
  const isSmall = size === "Small" /* Small */;
  const isInteractive = !disabled && !readonly && !displayOnly;
  const hasWrappingLabel = wrappingType !== "None" /* None */ && !!labelContent;
  const getValueStateBorderClass2 = () => {
    if (isActive) return "";
    const borderMap = {
      Positive: "border-sapphire-positive",
      Negative: "border-sapphire-negative",
      Critical: "border-sapphire-warning",
      Information: "border-sapphire-info"
    };
    return borderMap[valueState] ?? "";
  };
  const getValueStateCheckedClass = () => {
    if (!isActive) return "";
    const checkedMap = {
      Positive: "bg-sapphire-positive border-sapphire-positive",
      Negative: "bg-sapphire-negative border-sapphire-negative",
      Critical: "bg-sapphire-warning border-sapphire-warning",
      Information: "bg-sapphire-info border-sapphire-info"
    };
    return checkedMap[valueState] ?? "bg-sapphire-canvas-primary border-sapphire-border-secondary";
  };
  const renderDisplayOnlyIcon = () => {
    const cls = "size-4 text-foreground";
    if (indeterminate) return /* @__PURE__ */ jsxRuntime.jsx(TriStateIcon, { className: cls });
    if (isChecked) return /* @__PURE__ */ jsxRuntime.jsx(CompleteIcon, { className: cls });
    return /* @__PURE__ */ jsxRuntime.jsx(BorderIcon, { className: cls });
  };
  return /* @__PURE__ */ jsxRuntime.jsxs("div", { className: "relative inline-block max-w-full align-top", children: [
    /* @__PURE__ */ jsxRuntime.jsx(
      "input",
      {
        ref: inputRef,
        type: "checkbox",
        name,
        value,
        checked: isChecked,
        disabled: disabled || displayOnly,
        readOnly: readonly,
        required,
        tabIndex: -1,
        "aria-hidden": "true",
        onChange: () => {
        },
        className: "absolute size-0 opacity-0 pointer-events-none"
      }
    ),
    /* @__PURE__ */ jsxRuntime.jsxs(
      "div",
      {
        ref: rootRef,
        id,
        role: "checkbox",
        "aria-checked": indeterminate ? "mixed" : isChecked,
        "aria-label": accessibleName,
        "aria-labelledby": !accessibleName ? accessibleNameRef || (labelContent ? `${id}-label` : void 0) : void 0,
        "aria-describedby": [
          valueState !== "None" && valueStateMessage ? descriptionId : null,
          accessibleDescription ? `${id}-desc-text` : null,
          accessibilityAttributes?.describedBy
        ].filter(Boolean).join(" ") || void 0,
        "aria-disabled": disabled || displayOnly || void 0,
        "aria-readonly": readonly || void 0,
        "aria-invalid": valueState === "Negative" || void 0,
        "aria-required": required || void 0,
        tabIndex: tabIndex !== void 0 ? tabIndex : disabled || displayOnly ? -1 : 0,
        onMouseDown: handleMouseDown,
        onClick: handleClick2,
        onKeyDown: handleKeyDown,
        onKeyUp: handleKeyUp,
        onFocus,
        onBlur,
        className: cn(
          "group relative inline-flex select-none max-w-full",
          "outline-none",
          labelContent ? "items-center" : "items-center justify-center",
          // Touch area: Cozy = 44px, Compact = 32px
          isSmall ? labelContent ? "min-h-8 py-1.5 ps-1 pe-1 gap-2" : "size-8 p-2" : labelContent ? "min-h-[44px] py-[11px] ps-1 pe-2 gap-[11px]" : "size-[44px] p-[11px]",
          "text-sm",
          "rounded-lg",
          // Focus ring via pseudo-element for asymmetric insets
          "outline-none",
          "after:content-[''] after:absolute after:pointer-events-none after:border-2 after:border-transparent after:rounded-md",
          labelContent ? isSmall ? "after:inset-x-0 after:inset-y-0" : "after:left-0 after:right-[2px] after:inset-y-[5px]" : "after:inset-[5px] after:rounded-lg",
          "focus:after:border-[var(--color-ring)]",
          disabled && "opacity-40 cursor-not-allowed",
          readonly && "cursor-default",
          displayOnly && "cursor-default",
          isInteractive && "cursor-pointer",
          className
        ),
        style,
        "data-testid": dataTestId,
        children: [
          displayOnly ? renderDisplayOnlyIcon() : /* @__PURE__ */ jsxRuntime.jsx(
            "span",
            {
              className: cn(
                "relative inline-flex items-center justify-center",
                "shrink-0",
                hasWrappingLabel && "self-start",
                isSmall ? hasWrappingLabel ? "size-4 rounded p-[2px] mt-0.5" : "size-4 rounded p-[2px]" : hasWrappingLabel ? "size-[22px] rounded-md p-[3px] mt-0.5" : "size-[22px] rounded-md p-[3px]",
                "transition-colors",
                // Border style: dashed for readonly, solid otherwise
                readonly ? "border border-dashed" : "border border-solid",
                // --- Unchecked state ---
                !isActive && !readonly && "bg-sapphire-canvas-primary border-sapphire-border-secondary",
                // --- Checked / indeterminate state (not readonly) ---
                // None value state: blue icon on white bg; value states: white icon on colored bg
                isActive && !readonly && valueState === "None" && "text-sapphire-shell-button-fg-selected",
                isActive && !readonly && valueState !== "None" && "text-sapphire-neutral-foreground-white",
                !readonly && getValueStateCheckedClass(),
                !readonly && getValueStateBorderClass2(),
                // --- Hover (unchecked, not readonly/disabled, no value state) ---
                !isActive && isInteractive && valueState === "None" && "group-hover:border-primary group-hover:bg-sapphire-shell-button-bg-selected",
                // --- Hover (checked, not readonly/disabled, no value state) ---
                isActive && isInteractive && valueState === "None" && "group-hover:bg-sapphire-shell-button-bg-selected group-hover:border-primary",
                // --- Read-only state: gray bg + dashed secondary border ---
                readonly && "bg-sapphire-background-secondary border-sapphire-border-secondary",
                // --- Read-only icon color: secondary-foreground (#353C4A) ---
                readonly && isActive && "text-secondary-foreground"
              ),
              children: indeterminate ? (
                // Solid square for tristate per Figma: 12px large, 8px small
                /* @__PURE__ */ jsxRuntime.jsx("span", { className: cn(
                  "block bg-current",
                  isSmall ? "size-2" : "size-3"
                ) })
              ) : isChecked ? /* @__PURE__ */ jsxRuntime.jsx(lucideReact.Check, { className: isSmall ? "size-3" : "size-4", strokeWidth: 3 }) : null
            }
          ),
          labelContent && /* @__PURE__ */ jsxRuntime.jsx(
            "span",
            {
              id: `${id}-label`,
              className: cn(
                "text-sm min-w-0 flex-1 text-foreground",
                wrappingType === "None" /* None */ && "truncate",
                wrappingType === "Normal" /* Normal */ && "break-words",
                required && "after:content-['*'] after:ml-0.5 after:text-sapphire-negative"
              ),
              children: labelContent
            }
          ),
          valueState !== "None" && valueStateMessage && /* @__PURE__ */ jsxRuntime.jsx("span", { id: descriptionId, className: "sr-only", children: valueStateMessage }),
          accessibleDescription && /* @__PURE__ */ jsxRuntime.jsx("span", { id: `${id}-desc-text`, className: "sr-only", children: accessibleDescription })
        ]
      }
    )
  ] });
}
var radioGroups = /* @__PURE__ */ new Map();
function getOrderedGroupEntries(name) {
  const entries = radioGroups.get(name) ?? [];
  const connectedEntries = entries.filter((entry) => entry.getNode()?.isConnected);
  if (connectedEntries.length !== entries.length) {
    if (connectedEntries.length === 0) {
      radioGroups.delete(name);
    } else {
      radioGroups.set(name, connectedEntries);
    }
  }
  return [...connectedEntries].sort((entryA, entryB) => {
    const nodeA = entryA.getNode();
    const nodeB = entryB.getNode();
    if (!nodeA || !nodeB || nodeA === nodeB) {
      return 0;
    }
    const position = nodeA.compareDocumentPosition(nodeB);
    if (position & Node.DOCUMENT_POSITION_FOLLOWING) {
      return -1;
    }
    if (position & Node.DOCUMENT_POSITION_PRECEDING) {
      return 1;
    }
    return 0;
  });
}
function updateGroupTabIndex(name) {
  const entries = getOrderedGroupEntries(name);
  if (entries.length === 0) {
    return;
  }
  const focusableEntries = entries.filter((entry) => !entry.isDisabled());
  const activeEntry = focusableEntries.find((entry) => entry.isChecked()) ?? focusableEntries[0] ?? null;
  entries.forEach((entry) => {
    entry.setTabIndex(!entry.isDisabled() && entry === activeEntry ? 0 : -1);
  });
}
function registerGroupEntry(name, entry) {
  const existingEntries = radioGroups.get(name) ?? [];
  const nextEntries = existingEntries.filter((existingEntry) => existingEntry.id !== entry.id);
  nextEntries.push(entry);
  radioGroups.set(name, nextEntries);
  updateGroupTabIndex(name);
}
function unregisterGroupEntry(name, id) {
  const existingEntries = radioGroups.get(name);
  if (!existingEntries) {
    return;
  }
  const nextEntries = existingEntries.filter((entry) => entry.id !== id);
  if (nextEntries.length === 0) {
    radioGroups.delete(name);
    return;
  }
  radioGroups.set(name, nextEntries);
  updateGroupTabIndex(name);
}
function getAdjacentGroupEntry(name, currentId, direction) {
  const focusableEntries = getOrderedGroupEntries(name).filter((entry) => !entry.isDisabled());
  if (focusableEntries.length <= 1) {
    return null;
  }
  const currentIndex = focusableEntries.findIndex((entry) => entry.id === currentId);
  const baseIndex = currentIndex >= 0 ? currentIndex : 0;
  const nextIndex = (baseIndex + direction + focusableEntries.length) % focusableEntries.length;
  return focusableEntries[nextIndex] ?? null;
}
function moveSelectionInGroup(name, currentId, direction, originalEvent) {
  const nextEntry = getAdjacentGroupEntry(name, currentId, direction);
  if (!nextEntry) {
    return;
  }
  nextEntry.focus();
  if (!nextEntry.isReadonly()) {
    nextEntry.select(originalEvent);
    return;
  }
  updateGroupTabIndex(name);
}
function uncheckOthersInGroup(groupName, currentId) {
  getOrderedGroupEntries(groupName).forEach((entry) => {
    if (entry.id !== currentId && entry.isChecked()) {
      entry.setChecked(false);
    }
  });
}
function getKeyNavigationDirection(key) {
  if (key === " " || key === "Enter") return "select";
  const isRTL = document.documentElement.dir === "rtl";
  if (key === "ArrowDown" || key === "Down" || key === "ArrowRight" && !isRTL || key === "Right" && !isRTL || key === "ArrowLeft" && isRTL || key === "Left" && isRTL) {
    return 1;
  }
  if (key === "ArrowUp" || key === "Up" || key === "ArrowLeft" && !isRTL || key === "Left" && !isRTL || key === "ArrowRight" && isRTL || key === "Right" && isRTL) {
    return -1;
  }
  return null;
}
function getEffectiveTabIndex(disabled, tabIndex, groupName, groupTabIndex) {
  if (disabled) return -1;
  if (tabIndex !== void 0) return tabIndex;
  if (groupName) return groupTabIndex ?? 0;
  return 0;
}
function getValueStateBorderClass(valueState) {
  const borderMap = {
    ["Positive" /* Positive */]: "border-sapphire-positive",
    ["Negative" /* Negative */]: "border-sapphire-negative",
    ["Critical" /* Critical */]: "border-sapphire-warning",
    ["Information" /* Information */]: "border-sapphire-info"
  };
  return borderMap[valueState] ?? "";
}
function getValueStateCheckedBorderClass(valueState) {
  const checkedBorderMap = {
    ["Positive" /* Positive */]: "border-sapphire-positive",
    ["Negative" /* Negative */]: "border-sapphire-negative",
    ["Critical" /* Critical */]: "border-sapphire-warning",
    ["Information" /* Information */]: "border-sapphire-info"
  };
  return checkedBorderMap[valueState] ?? "border-sapphire-border-secondary";
}
function getInnerDotColorClass(valueState) {
  const dotMap = {
    ["Positive" /* Positive */]: "bg-sapphire-positive",
    ["Negative" /* Negative */]: "bg-sapphire-negative",
    ["Critical" /* Critical */]: "bg-sapphire-warning",
    ["Information" /* Information */]: "bg-sapphire-info"
  };
  return dotMap[valueState] ?? "bg-sapphire-text-accent";
}
function getSizeClasses(isSmall, hasLabel) {
  if (isSmall) {
    return hasLabel ? "min-h-8 py-1.5 ps-1 pe-1 gap-2" : "size-8 p-2";
  }
  return hasLabel ? "min-h-[44px] py-[11px] ps-1 pe-2 gap-[11px]" : "size-[44px] p-[11px]";
}
function getFocusRingClasses(hasLabel, isSmall) {
  if (!hasLabel) return "after:inset-[5px] after:rounded-lg";
  if (isSmall) return "after:inset-x-0 after:inset-y-0";
  return "after:left-0 after:right-[2px] after:inset-y-[5px]";
}
var valueStateI18nKeys = {
  ["Negative" /* Negative */]: "RADIOBUTTON_VALUE_STATE_ERROR",
  ["Critical" /* Critical */]: "RADIOBUTTON_VALUE_STATE_WARNING",
  ["Positive" /* Positive */]: "RADIOBUTTON_VALUE_STATE_SUCCESS",
  ["Information" /* Information */]: "RADIOBUTTON_VALUE_STATE_INFORMATION"
};
function RadioCircle({
  isReadonly,
  isSmall,
  isChecked,
  hasWrappingLabel,
  isInteractive,
  valueState,
  svgR
}) {
  if (isReadonly) {
    return /* @__PURE__ */ jsxRuntime.jsxs(
      "span",
      {
        className: cn(
          "relative inline-flex items-center justify-center shrink-0",
          isSmall ? "size-4" : "size-[22px]",
          hasWrappingLabel && "self-start",
          isSmall && hasWrappingLabel && "mt-0.5"
        ),
        children: [
          /* @__PURE__ */ jsxRuntime.jsx(
            "svg",
            {
              className: "absolute inset-0 w-full h-full overflow-visible",
              focusable: "false",
              "aria-hidden": "true",
              children: /* @__PURE__ */ jsxRuntime.jsx(
                "circle",
                {
                  cx: "50%",
                  cy: "50%",
                  r: svgR,
                  fill: "var(--background-secondary)",
                  stroke: "var(--border-secondary)",
                  strokeWidth: "1",
                  strokeDasharray: "4, 2"
                }
              )
            }
          ),
          isChecked && /* @__PURE__ */ jsxRuntime.jsx(
            "span",
            {
              className: cn(
                "relative rounded-full",
                isSmall ? "size-2" : "size-3",
                "bg-sapphire-text-secondary"
              )
            }
          )
        ]
      }
    );
  }
  return /* @__PURE__ */ jsxRuntime.jsx(
    "span",
    {
      className: cn(
        "relative inline-flex items-center justify-center shrink-0 rounded-full",
        isSmall ? "size-4" : "size-[22px]",
        hasWrappingLabel && "self-start",
        isSmall && hasWrappingLabel && "mt-0.5",
        "transition-colors",
        "border border-solid",
        !isChecked && "bg-sapphire-canvas-primary border-sapphire-border-secondary",
        isChecked && getValueStateCheckedBorderClass(valueState),
        isChecked && "bg-sapphire-canvas-primary",
        !isChecked && getValueStateBorderClass(valueState),
        isInteractive && valueState === "None" /* None */ && !isChecked && "group-hover:border-sapphire-chrome-button-fg-selected group-hover:bg-sapphire-chrome-button-bg-selected",
        isInteractive && valueState === "None" /* None */ && isChecked && "group-hover:border-sapphire-border-accent group-hover:bg-sapphire-chrome-button-bg-selected"
      ),
      children: isChecked && /* @__PURE__ */ jsxRuntime.jsx(
        "span",
        {
          className: cn(
            "rounded-full",
            isSmall ? "size-2" : "size-3",
            getInnerDotColorClass(valueState)
          )
        }
      )
    }
  );
}
function RadioButton({
  checked: controlledChecked,
  defaultChecked = false,
  disabled = false,
  readonly = false,
  required = false,
  size = "Large" /* Large */,
  text,
  children,
  valueState = "None" /* None */,
  valueStateMessage,
  name,
  value = "",
  wrappingType = "Normal" /* Normal */,
  accessibleName,
  accessibleNameRef,
  accessibleDescription,
  accessibleDescriptionRef,
  onChange,
  onFocus,
  onBlur,
  className,
  style,
  id: providedId,
  "data-testid": dataTestId,
  tabIndex,
  ref
}) {
  const inputRef = React45.useRef(null);
  const rootRef = React45.useRef(null);
  const generatedId = React45.useId();
  const id = providedId || generatedId;
  const descriptionId = `${id}-description`;
  const [internalChecked, setInternalChecked] = React45.useState(defaultChecked);
  const [groupTabIndex, setGroupTabIndex] = React45.useState(null);
  const isControlled = controlledChecked !== void 0;
  const isChecked = isControlled ? controlledChecked : internalChecked;
  const setChecked = React45.useCallback(
    (checked) => {
      if (!isControlled) {
        setInternalChecked(checked);
      }
    },
    [isControlled]
  );
  const doSelect = React45.useCallback(
    (originalEvent) => {
      if (disabled || readonly || isChecked) return;
      if (name) {
        uncheckOthersInGroup(name, id);
      }
      if (!isControlled) {
        setInternalChecked(true);
      }
      onChange?.({
        checked: true,
        value,
        originalEvent
      });
    },
    [disabled, readonly, isChecked, isControlled, name, id, onChange, value]
  );
  React45.useEffect(() => {
    if (!name) {
      setGroupTabIndex(null);
      return;
    }
    const entry = {
      id,
      getNode: () => rootRef.current,
      isDisabled: () => disabled,
      isReadonly: () => readonly,
      isChecked: () => isChecked,
      setChecked,
      focus: () => rootRef.current?.focus(),
      select: doSelect,
      setTabIndex: setGroupTabIndex
    };
    registerGroupEntry(name, entry);
    return () => {
      unregisterGroupEntry(name, id);
    };
  }, [name, id, disabled, readonly, isChecked, setChecked, doSelect]);
  React45.useEffect(() => {
    if (!name) {
      return;
    }
    updateGroupTabIndex(name);
  }, [name, isChecked, disabled]);
  React45.useImperativeHandle(
    ref,
    () => ({
      focus() {
        rootRef.current?.focus();
      },
      blur() {
        rootRef.current?.blur();
      },
      isFocused() {
        return document.activeElement === rootRef.current;
      },
      isChecked() {
        return isChecked;
      },
      get nativeElement() {
        return inputRef.current;
      }
    }),
    [isChecked]
  );
  const handleClick2 = React45.useCallback(
    (e) => {
      if (disabled || readonly) {
        e.preventDefault();
        return;
      }
      doSelect(e);
    },
    [disabled, readonly, doSelect]
  );
  const handleMouseDown = React45.useCallback(
    (e) => {
      if (disabled) {
        e.preventDefault();
      }
    },
    [disabled]
  );
  const handleKeyDown = React45.useCallback(
    (e) => {
      if (disabled || readonly) return;
      const direction = getKeyNavigationDirection(e.key);
      if (name && (direction === 1 || direction === -1)) {
        e.preventDefault();
        moveSelectionInGroup(name, id, direction, e);
        return;
      }
      if (direction === "select") {
        e.preventDefault();
      }
    },
    [disabled, readonly, name, id]
  );
  const handleKeyUp = React45.useCallback(
    (e) => {
      if (disabled || readonly) return;
      if (e.key === " " || e.key === "Enter") {
        e.preventDefault();
        doSelect(e);
      }
    },
    [disabled, readonly, doSelect]
  );
  const labelContent = children ?? text;
  const isSmall = size === "Small" /* Small */;
  const isInteractive = !disabled && !readonly;
  const hasWrappingLabel = wrappingType !== "None" /* None */ && !!labelContent;
  const effectiveTabIndex = getEffectiveTabIndex(disabled, tabIndex, name, groupTabIndex);
  const { t } = reactI18next.useTranslation("fx");
  const valueStateI18nKey = valueStateI18nKeys[valueState];
  const defaultValueStateText = valueStateI18nKey ? t(valueStateI18nKey) : void 0;
  const svgR = isSmall ? 7.5 : 10.5;
  return /* @__PURE__ */ jsxRuntime.jsxs("div", { className: "relative inline-block max-w-full align-top", children: [
    /* @__PURE__ */ jsxRuntime.jsx(
      "input",
      {
        ref: inputRef,
        type: "radio",
        id,
        name,
        value,
        checked: isChecked,
        disabled,
        readOnly: readonly,
        required,
        tabIndex: -1,
        "aria-hidden": "true",
        onChange: () => {
        },
        className: "absolute size-0 opacity-0 pointer-events-none"
      }
    ),
    /* @__PURE__ */ jsxRuntime.jsxs(
      "div",
      {
        ref: rootRef,
        role: "radio",
        "aria-checked": isChecked,
        "aria-label": accessibleName,
        "aria-labelledby": !accessibleName ? accessibleNameRef || (labelContent ? `${id}-label` : void 0) : void 0,
        "aria-describedby": [
          accessibleDescriptionRef || null,
          valueState !== "None" /* None */ ? descriptionId : null,
          accessibleDescription ? `${id}-desc-text` : null
        ].filter(Boolean).join(" ") || void 0,
        "aria-disabled": disabled || void 0,
        "aria-invalid": valueState === "Negative" /* Negative */ || void 0,
        "aria-required": required || void 0,
        tabIndex: effectiveTabIndex,
        onMouseDown: handleMouseDown,
        onClick: handleClick2,
        onKeyDown: handleKeyDown,
        onKeyUp: handleKeyUp,
        onFocus,
        onBlur,
        "data-radio-group": name,
        "data-radio-id": id,
        className: cn(
          "group relative inline-flex select-none max-w-full",
          "outline-none",
          labelContent ? "items-center" : "items-center justify-center",
          getSizeClasses(isSmall, !!labelContent),
          "text-sm",
          "rounded-lg",
          "after:content-[''] after:absolute after:pointer-events-none after:border-2 after:border-transparent after:rounded-md",
          getFocusRingClasses(!!labelContent, isSmall),
          "focus:after:border-[var(--color-ring)]",
          disabled && "opacity-40 cursor-not-allowed",
          readonly && "cursor-default",
          isInteractive && "cursor-pointer",
          className
        ),
        style,
        "data-testid": dataTestId,
        children: [
          /* @__PURE__ */ jsxRuntime.jsx(
            RadioCircle,
            {
              isReadonly: readonly,
              isSmall,
              isChecked,
              hasWrappingLabel,
              isInteractive,
              valueState,
              svgR
            }
          ),
          labelContent && /* @__PURE__ */ jsxRuntime.jsx(
            "span",
            {
              id: `${id}-label`,
              className: cn(
                "text-sm min-w-0 flex-1 text-foreground",
                wrappingType === "None" /* None */ && "truncate",
                wrappingType === "Normal" /* Normal */ && "break-words",
                required && "after:content-['*'] after:ml-0.5 after:text-sapphire-negative"
              ),
              children: labelContent
            }
          ),
          valueState !== "None" /* None */ && /* @__PURE__ */ jsxRuntime.jsx("span", { id: descriptionId, className: "sr-only", children: valueStateMessage || defaultValueStateText }),
          accessibleDescription && /* @__PURE__ */ jsxRuntime.jsx("span", { id: `${id}-desc-text`, className: "sr-only", children: accessibleDescription })
        ]
      }
    )
  ] });
}
var pathData19 = [
  "M454 109q11 0 18.5 7t7.5 18-7.5 18.5T454 160h-19v294q0 24-17 41t-41 17H135q-24 0-41-17t-17-41V160H58q-11 0-18.5-7.5T32 134t7.5-18 18.5-7h70V58q0-24 17-41t41-17h140q24 0 41 17t17 41v51h70zm-275 0h154V58q0-7-7-7H186q-7 0-7 7v51zm205 51H128v294q0 7 7 7h242q7 0 7-7V160zm-186 64q11 0 18.5 7.5T224 250v140q0 11-7.5 18.5T198 416t-18-7.5-7-18.5V250q0-11 7-18.5t18-7.5zm116 0q11 0 18 7.5t7 18.5v140q0 11-7 18.5t-18 7.5-18.5-7.5T288 390V250q0-11 7.5-18.5T314 224z"
];
var DeleteIcon = Object.assign(
  React45__namespace.default.forwardRef(
    (props, ref) => /* @__PURE__ */ jsxRuntime.jsx(Icon, { ref, pathData: pathData19, ...props })
  ),
  { displayName: "DeleteIcon", iconName: "delete" }
);
function ListSelectionControl({
  selectionMode,
  position,
  isSelected,
  disabled = false,
  itemText = "item",
  deleteButton,
  onSelectionChange,
  onDeleteClick,
  ref
}) {
  const modeStr = String(selectionMode);
  const showSelectionBefore = position === "before" && (modeStr === "Multiple" || modeStr === "SingleStart");
  const showSelectionAfter = position === "after" && (modeStr === "SingleEnd" || modeStr === "Delete");
  const showRadio = modeStr === "SingleStart" || modeStr === "SingleEnd" || modeStr === "Single";
  const showCheckbox = modeStr === "Multiple";
  const showDelete = modeStr === "Delete";
  if (!showSelectionBefore && !showSelectionAfter) {
    return null;
  }
  const handleDeleteClick = (detail) => {
    detail.originalEvent.stopPropagation();
    onDeleteClick?.();
  };
  return /* @__PURE__ */ jsxRuntime.jsxs(
    "span",
    {
      ref,
      className: "shrink-0 pl-3 pr-1 flex items-center",
      onClick: (e) => e.stopPropagation(),
      children: [
        showCheckbox && /* @__PURE__ */ jsxRuntime.jsx(
          CheckBox,
          {
            checked: isSelected,
            onChange: (e) => onSelectionChange(e.checked),
            disabled,
            accessibleName: `Select ${itemText}`,
            tabIndex: -1
          }
        ),
        showRadio && !showDelete && /* @__PURE__ */ jsxRuntime.jsx(
          RadioButton,
          {
            checked: isSelected,
            onChange: (e) => onSelectionChange(e.checked),
            disabled,
            accessibleName: `Select ${itemText}`,
            tabIndex: -1
          }
        ),
        showDelete && (deleteButton ? React45__namespace.cloneElement(deleteButton, {
          onClick: handleDeleteClick
        }) : /* @__PURE__ */ jsxRuntime.jsx(
          Button,
          {
            design: "Tertiary" /* Tertiary */,
            iconOnly: true,
            icon: /* @__PURE__ */ jsxRuntime.jsx(DeleteIcon, { className: "h-4 w-4" }),
            onClick: handleDeleteClick,
            accessibleName: "Delete item",
            tooltip: "Delete",
            tabIndex: -1
          }
        ))
      ]
    }
  );
}
function DropIndicator({ visible, placement, orientation = "vertical", className, ref }) {
  if (!visible) return null;
  const isVertical = orientation === "vertical";
  const isBefore = placement === "Before";
  const isOn = placement === "On";
  return /* @__PURE__ */ jsxRuntime.jsx(
    "div",
    {
      ref,
      className: cn(
        "absolute pointer-events-none z-20",
        isVertical ? cn(
          "left-0 right-0 h-0.5 bg-primary",
          isBefore ? "-top-px" : isOn ? "inset-0 h-full bg-primary/10 border-2 border-primary border-dashed" : "-bottom-px"
        ) : cn(
          "top-0 bottom-0 w-0.5 bg-primary",
          isBefore ? "-left-px" : isOn ? "inset-0 w-full bg-primary/10 border-2 border-primary border-dashed" : "-right-px"
        ),
        className
      ),
      "aria-hidden": "true",
      "data-drop-indicator": true,
      "data-placement": placement
    }
  );
}

// src/components/list/shared-styles.ts
var LIST_ITEM_BASE_CLASSES = [
  "relative",
  "flex",
  "items-center",
  "w-full",
  "min-h-[2.75rem]",
  "transition-colors",
  "outline-none"
];
var LIST_ITEM_FOCUS_CLASSES = "ring-2 ring-inset ring-sapphire-border-focus z-10";
function ListItemBase(props) {
  const {
    ref,
    itemKey,
    type = "Active" /* Active */,
    disabled = false,
    movable = false,
    accessibleRole = "ListItem" /* ListItem */,
    accessibleNameRef,
    selected: controlledSelected,
    focused: externalFocused,
    onClick,
    onDragStart,
    onDelete,
    deleteButton,
    children,
    className,
    style,
    "data-testid": dataTestId
  } = props;
  const listContext = useOptionalListContext();
  const effectiveAccessibleRole = React45.useMemo(() => {
    if (accessibleRole !== void 0 && accessibleRole !== "ListItem" /* ListItem */) {
      return accessibleRole;
    }
    const parentRole = String(listContext?.accessibleRole || "List");
    if (accessibleRole === "ListItem" /* ListItem */ && parentRole === "List") {
      return "ListItem" /* ListItem */;
    }
    switch (parentRole) {
      case "ListBox":
        return "Option" /* Option */;
      case "Menu":
        return "MenuItem" /* MenuItem */;
      case "Tree":
        return "TreeItem" /* TreeItem */;
      default:
        return "ListItem" /* ListItem */;
    }
  }, [accessibleRole, listContext?.accessibleRole]);
  const listItem = useListItem({
    itemKey,
    disabled,
    movable,
    type,
    accessibleRole: effectiveAccessibleRole,
    selected: controlledSelected,
    onClick,
    onDragStart
  });
  const handleDeleteClick = React45.useCallback(() => {
    if (itemKey) {
      listContext?.onItemDelete?.(itemKey);
    }
    onDelete?.();
  }, [itemKey, listContext, onDelete]);
  React45.useImperativeHandle(
    ref,
    () => ({
      focus() {
        listItem.focus();
      },
      blur() {
        listItem.blur();
      },
      isFocused() {
        return document.activeElement === listItem.itemRef.current;
      },
      get nativeElement() {
        return listItem.itemRef.current;
      }
    }),
    [listItem]
  );
  const renderState = {
    isSelected: listItem.isSelected,
    isFocused: listItem.isFocused,
    isFocusedWithin: listItem.isFocusedWithin,
    isActive: listItem.isActive,
    isDragging: listItem.isDragging,
    isDropTarget: listItem.isDropTarget,
    dropPlacement: listItem.dropPlacement,
    selectionMode: String(listItem.selectionMode)
  };
  const modeStr = String(listItem.selectionMode);
  const position = modeStr === "SingleEnd" || modeStr === "Delete" ? "after" : "before";
  const shouldShowControl = modeStr !== "None";
  const content = typeof children === "function" ? children(renderState) : children;
  const resolvedRole = (() => {
    const roleStr = String(effectiveAccessibleRole);
    switch (roleStr) {
      case "Option":
        return "option";
      case "MenuItem":
        return "menuitem";
      case "TreeItem":
        return "treeitem";
      case "Group":
        return "group";
      case "None":
        return void 0;
      default:
        return "listitem";
    }
  })();
  return /* @__PURE__ */ jsxRuntime.jsxs(
    "li",
    {
      ref: listItem.itemRef,
      id: listItem.id,
      role: resolvedRole,
      "aria-labelledby": accessibleNameRef,
      "aria-selected": listItem.ariaSelected,
      "aria-disabled": disabled || void 0,
      "aria-setsize": listContext?.totalItemCount,
      "aria-posinset": itemKey && listContext ? listContext.getItemIndex(itemKey) + 1 || void 0 : void 0,
      tabIndex: listItem.tabIndex,
      draggable: movable,
      onClick: listItem.handleClick,
      onKeyDown: listItem.handleKeyDown,
      onMouseDownCapture: listItem.handleMouseDownCapture,
      onMouseDown: listItem.handleMouseDown,
      onMouseUp: listItem.handleMouseUp,
      onMouseLeave: listItem.handleMouseUp,
      onFocus: listItem.handleFocus,
      onBlur: listItem.handleBlur,
      onDragStart: listItem.handleDragStart,
      onDragEnd: listItem.handleDragEnd,
      onDragOver: listItem.handleDragOver,
      onDragEnter: listItem.handleDragEnter,
      onDragLeave: listItem.handleDragLeave,
      onDrop: listItem.handleDrop,
      className: cn(
        // Base item styles - container layout (shared with group headers)
        LIST_ITEM_BASE_CLASSES,
        "text-sm text-sapphire-text-primary",
        // State classes
        listItem.isSelected && "!bg-sapphire-brand-selected-background",
        listItem.isSelected && !(externalFocused || listItem.isFocused) && "!border-sapphire-border-accent",
        (externalFocused || listItem.isFocused) && LIST_ITEM_FOCUS_CLASSES,
        listItem.isActive && "bg-muted/80",
        listItem.isDragging && "opacity-50",
        movable && "cursor-grab",
        listItem.isDragging && "cursor-grabbing",
        disabled && "opacity-50 pointer-events-none cursor-not-allowed",
        // Interactive states
        (type === "Active" /* Active */ || type === "Navigation" /* Navigation */) && "cursor-pointer hover:bg-[var(--table-row-hover)] active:bg-[var(--neutral-pressed-background-2)]",
        type === "Inactive" /* Inactive */ && "cursor-default",
        // User's custom classes
        className
      ),
      style,
      "data-testid": dataTestId,
      "data-notification-item": props["data-notification-item"] || void 0,
      "data-notification-group": props["data-notification-group"] || void 0,
      "data-item-key": itemKey,
      "data-dragging": listItem.isDragging || void 0,
      children: [
        listItem.dropPlacement && /* @__PURE__ */ jsxRuntime.jsx(DropIndicator, { visible: true, placement: listItem.dropPlacement }),
        shouldShowControl && position === "before" && /* @__PURE__ */ jsxRuntime.jsx(
          ListSelectionControl,
          {
            selectionMode: listItem.selectionMode,
            position: "before",
            isSelected: listItem.isSelected,
            disabled,
            deleteButton,
            onSelectionChange: listItem.handleSelectionChange,
            onDeleteClick: handleDeleteClick
          }
        ),
        content,
        shouldShowControl && position === "after" && /* @__PURE__ */ jsxRuntime.jsx(
          ListSelectionControl,
          {
            selectionMode: listItem.selectionMode,
            position: "after",
            isSelected: listItem.isSelected,
            disabled,
            deleteButton,
            onSelectionChange: listItem.handleSelectionChange,
            onDeleteClick: handleDeleteClick
          }
        )
      ]
    }
  );
}
ListItemBase.displayName = "ListItemBase";
function NavArrowIcon({ className }) {
  return /* @__PURE__ */ jsxRuntime.jsx("svg", { xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16", fill: "none", className, children: /* @__PURE__ */ jsxRuntime.jsx("path", { d: "M5.20493 3.23532C5.48927 2.93424 5.96333 2.92076 6.2645 3.20504L10.7645 7.45211C10.9144 7.59365 10.9997 7.79085 10.9998 7.99704C10.9999 8.20338 10.9145 8.40121 10.7645 8.54293L6.2645 12.7949C5.96343 13.0792 5.48937 13.0656 5.20493 12.7646C4.92058 12.4635 4.93419 11.9895 5.2352 11.705L9.15707 7.99704L5.2352 4.29489C4.93413 4.01054 4.92064 3.53648 5.20493 3.23532Z", fill: "currentColor" }) });
}
var highlightBarVariants = cva(
  "absolute left-0 top-0 bottom-0 w-[3px]",
  {
    variants: {
      highlight: {
        ["None" /* None */]: "bg-transparent",
        ["Positive" /* Positive */]: "bg-sapphire-positive",
        ["Critical" /* Critical */]: "bg-sapphire-warning",
        ["Information" /* Information */]: "bg-sapphire-info",
        ["Negative" /* Negative */]: "bg-sapphire-negative"
      }
    },
    defaultVariants: {
      highlight: "None" /* None */
    }
  }
);
var additionalTextVariants = cva(
  "text-xs shrink-0",
  {
    variants: {
      state: {
        ["None" /* None */]: "text-sapphire-text-tertiary",
        ["Positive" /* Positive */]: "text-sapphire-positive",
        ["Critical" /* Critical */]: "text-sapphire-warning",
        ["Information" /* Information */]: "text-sapphire-info",
        ["Negative" /* Negative */]: "text-sapphire-negative"
      }
    },
    defaultVariants: {
      state: "None" /* None */
    }
  }
);
function ListItem(props) {
  const {
    ref,
    // Content
    text,
    description,
    icon,
    iconEnd = false,
    additionalText,
    additionalTextState = "None" /* None */,
    image,
    action,
    children,
    // Behavior
    type = "Active" /* Active */,
    highlight = "None" /* None */,
    navigated = false,
    wrappingType = "None" /* None */,
    tooltip,
    // Events
    onDetailClick,
    onDelete: _onDelete,
    // Custom elements
    deleteButton,
    // Rest passed to ListItemBase
    ...baseProps
  } = props;
  const shouldWrap = String(wrappingType) === "Normal";
  const displayIcon = icon && !iconEnd;
  const displayIconEnd = icon && iconEnd;
  const handleDetailButtonClick = React45__namespace.useCallback((detail) => {
    onDetailClick?.(detail.originalEvent);
  }, [onDetailClick]);
  const showNavArrow = String(type) === "Navigation" || navigated;
  return /* @__PURE__ */ jsxRuntime.jsx(
    ListItemBase,
    {
      ref,
      type,
      deleteButton,
      ...baseProps,
      children: () => /* @__PURE__ */ jsxRuntime.jsxs(jsxRuntime.Fragment, { children: [
        String(highlight) !== "None" && /* @__PURE__ */ jsxRuntime.jsx("span", { className: highlightBarVariants({ highlight }) }),
        /* @__PURE__ */ jsxRuntime.jsxs("span", { className: "flex-1 flex items-center px-[0.75rem] py-[0.5rem] gap-[0.75rem] min-w-0", children: [
          image && /* @__PURE__ */ jsxRuntime.jsx("span", { className: "shrink-0 size-8 max-w-8 max-h-8 overflow-hidden flex items-center justify-center", children: image }),
          displayIcon && /* @__PURE__ */ jsxRuntime.jsx("span", { className: "shrink-0 text-sapphire-text-tertiary", children: icon }),
          /* @__PURE__ */ jsxRuntime.jsxs("span", { className: "flex-1 flex flex-col justify-center min-w-0 gap-0.5", children: [
            /* @__PURE__ */ jsxRuntime.jsx("span", { className: "flex items-center gap-2", children: /* @__PURE__ */ jsxRuntime.jsx(
              "span",
              {
                className: cn(
                  "text-sm font-semibold text-sapphire-text-primary",
                  !shouldWrap && "truncate",
                  shouldWrap && "break-words"
                ),
                title: tooltip,
                children: children || text
              }
            ) }),
            description && /* @__PURE__ */ jsxRuntime.jsx(
              "span",
              {
                className: cn(
                  "text-xs text-sapphire-text-tertiary",
                  !shouldWrap && "truncate",
                  shouldWrap && "break-words"
                ),
                children: description
              }
            )
          ] }),
          additionalText && /* @__PURE__ */ jsxRuntime.jsx("span", { className: cn(
            additionalTextVariants({ state: additionalTextState }),
            "shrink-0"
          ), children: additionalText }),
          displayIconEnd && /* @__PURE__ */ jsxRuntime.jsx("span", { className: "shrink-0 text-sapphire-text-tertiary", children: icon }),
          String(type) === "Detail" && /* @__PURE__ */ jsxRuntime.jsx("span", { className: "shrink-0", children: /* @__PURE__ */ jsxRuntime.jsx(
            Button,
            {
              design: "Tertiary" /* Tertiary */,
              iconOnly: true,
              icon: /* @__PURE__ */ jsxRuntime.jsx(OverflowIcon, { className: "h-4 w-4" }),
              onClick: handleDetailButtonClick,
              accessibleName: "Show details",
              tooltip: "Details"
            }
          ) }),
          (action || showNavArrow) && /* @__PURE__ */ jsxRuntime.jsxs("span", { className: "shrink-0 flex items-center gap-[0.5rem]", children: [
            action && /* @__PURE__ */ jsxRuntime.jsx(
              "span",
              {
                onClick: (e) => e.stopPropagation(),
                onKeyDown: (e) => e.stopPropagation(),
                children: action
              }
            ),
            showNavArrow && /* @__PURE__ */ jsxRuntime.jsx(NavArrowIcon, { className: "text-sapphire-text-tertiary" })
          ] })
        ] })
      ] })
    }
  );
}
ListItem.displayName = "ListItem";
var pathData20 = [
  "M256 0q53 0 99.5 20T437 75t55 81.5 20 99.5-20 99.5-55 81.5-81.5 55-99.5 20-99.5-20T75 437t-55-81.5T0 256t20-99.5T75 75t81.5-55T256 0zm0 461q42 0 79.5-16t65.5-44 44-65.5 16-79.5-16-79.5-44-65.5-65.5-44T256 51t-79.5 16-65.5 44-44 65.5T51 256t16 79.5 44 65.5 65.5 44 79.5 16zm70-301q11 0 18.5 7.5T352 186t-7 18l-53 52 53 52q7 7 7 18t-7.5 18.5T326 352q-10 0-18-8l-52-52-52 52q-8 8-18 8-11 0-18.5-7.5T160 326q0-10 8-18l52-52-52-52q-8-8-8-18 0-11 7.5-18.5T186 160t18 7l52 53 52-53q7-7 18-7z"
];
var MessageErrorIcon = Object.assign(
  React45__namespace.default.forwardRef(
    (props, ref) => /* @__PURE__ */ jsxRuntime.jsx(Icon, { ref, pathData: pathData20, ...props })
  ),
  { displayName: "MessageErrorIcon", iconName: "message-error" }
);
var pathData21 = [
  "M256 512q-53 0-99.5-20T75 437t-55-81.5T0 256t20-99.5T75 75t81.5-55T256 0t99.5 20T437 75t55 81.5 20 99.5-20 99.5-55 81.5-81.5 55-99.5 20zm0-461q-42 0-79.5 16T111 111t-44 65.5T51 256t16 79.5 44 65.5 65.5 44 79.5 16 79.5-16 65.5-44 44-65.5 16-79.5-16-79.5-44-65.5-65.5-44T256 51zm-43 301q-10 0-17-7l-71-63q-9-8-9-19t7.5-18.5T142 237q10 0 17 7l53 47 128-131q8-8 18-8 11 0 18.5 7.5T384 178q0 10-7 17L231 344q-8 8-18 8z"
];
var MessageSuccessIcon = Object.assign(
  React45__namespace.default.forwardRef(
    (props, ref) => /* @__PURE__ */ jsxRuntime.jsx(Icon, { ref, pathData: pathData21, ...props })
  ),
  { displayName: "MessageSuccessIcon", iconName: "message-success" }
);
var pathData22 = [
  "M436 480H76q-31 0-53.5-21T0 407q0-21 10-37L191 68q10-17 27.5-26.5T256 32t37.5 9.5T321 68l181 302q10 16 10 37 0 31-22.5 52T436 480zM256 83q-15 0-21 12L54 396q-3 6-3 11 0 9 7.5 15.5T76 429h360q10 0 17.5-6.5T461 407q0-5-3-11L277 95q-6-12-21-12zm0 213q-11 0-18.5-7.5T230 270v-85q0-11 7.5-18.5T256 159t18.5 7.5T282 185v85q0 11-7.5 18.5T256 296zm0 105q-13 0-22-9t-9-23q0-13 9-22t22-9 22 9 9 22q0 14-9 23t-22 9z"
];
var MessageWarningIcon = Object.assign(
  React45__namespace.default.forwardRef(
    (props, ref) => /* @__PURE__ */ jsxRuntime.jsx(Icon, { ref, pathData: pathData22, ...props })
  ),
  { displayName: "MessageWarningIcon", iconName: "message-warning" }
);
var pathData23 = [
  "M256 0q53 0 100 20t81.5 54.5T492 156t20 100-20 100-54.5 81.5T356 492t-100 20-100-20-81.5-54.5T20 356 0 256t20-100 54.5-81.5T156 20 256 0zm0 461q43 0 80.5-16t65-44 43.5-65 16-80-16-80.5-43.5-65-65-43.5T256 51t-80 16-65 43.5-44 65T51 256t16 80 44 65 65 44 80 16zm0-333q13 0 22.5 9.5T288 160t-9.5 22.5T256 192t-22.5-9.5T224 160t9.5-22.5T256 128zm0 115q11 0 18 7.5t7 18.5v102q0 11-7 18.5t-18 7.5-18.5-7.5T230 371V269q0-11 7.5-18.5T256 243z"
];
var MessageInformationIcon = Object.assign(
  React45__namespace.default.forwardRef(
    (props, ref) => /* @__PURE__ */ jsxRuntime.jsx(Icon, { ref, pathData: pathData23, ...props })
  ),
  { displayName: "MessageInformationIcon", iconName: "message-information" }
);
var pathData24 = [
  "M181 103q7-7 17-7 11 0 18.5 7.5T224 122q0 10-8 18l-94 90h332q11 0 18.5 7.5T480 256t-7.5 18.5T454 282H122l94 90q8 8 8 18 0 11-7.5 18.5T198 416q-10 0-17-7L40 274q-8-6-8-18 0-11 8-19z"
];
var ArrowLeftIcon = Object.assign(
  React45__namespace.default.forwardRef(
    (props, ref) => /* @__PURE__ */ jsxRuntime.jsx(Icon, { ref, pathData: pathData24, ...props })
  ),
  { displayName: "ArrowLeftIcon", iconName: "arrow-left" }
);

// src/lib/dialogStack.ts
var DialogStackManager = class {
  constructor() {
    __publicField(this, "stack", []);
    __publicField(this, "baseZIndex", 50);
  }
  createBackdrop(onBackdropClick) {
    const el = document.createElement("div");
    el.setAttribute("aria-hidden", "true");
    el.setAttribute("data-dialog-backdrop", "true");
    el.setAttribute("popover", "manual");
    el.style.cssText = "position:fixed;inset:0;pointer-events:all;background:transparent;width:100vw;height:100vh;";
    const stop = (e) => e.stopPropagation();
    el.addEventListener("pointerdown", stop);
    el.addEventListener("pointerup", stop);
    const handleClick2 = (e) => {
      e.stopPropagation();
      onBackdropClick?.();
    };
    el.addEventListener("click", handleClick2);
    document.body.appendChild(el);
    el.showPopover();
    const cleanup = () => {
      el.removeEventListener("pointerdown", stop);
      el.removeEventListener("pointerup", stop);
      el.removeEventListener("click", handleClick2);
    };
    return { el, cleanup };
  }
  destroyBackdrop(item) {
    item.cleanupBackdrop();
    try {
      item.backdropEl.hidePopover();
    } catch {
    }
    item.backdropEl.remove();
  }
  /**
   * Register a new dialog on the stack
   */
  push(id, onBackdropClick) {
    const { el: backdropEl, cleanup: cleanupBackdrop } = this.createBackdrop(onBackdropClick);
    this.stack.push({ id, backdropEl, cleanupBackdrop });
    return this.baseZIndex + this.stack.length;
  }
  /**
   * Remove a dialog from the stack
   */
  pop(id) {
    const index = this.stack.findIndex((item2) => item2.id === id);
    if (index === -1) return;
    const item = this.stack[index];
    this.stack.splice(index, 1);
    this.destroyBackdrop(item);
  }
  /**
   * Get z-index for a specific dialog
   */
  getZIndex(id) {
    const index = this.stack.findIndex((item) => item.id === id);
    if (index === -1) return this.baseZIndex;
    return this.baseZIndex + index + 1;
  }
  /**
   * Check if dialog is the topmost
   */
  isTop(id) {
    if (this.stack.length === 0) return false;
    return this.stack[this.stack.length - 1].id === id;
  }
  /**
   * Get the number of open dialogs
   */
  getCount() {
    return this.stack.length;
  }
};
var dialogStack = new DialogStackManager();

// src/lib/focusUtils.ts
function isElementHidden(element) {
  if (element.hidden) {
    return true;
  }
  const style = window.getComputedStyle(element);
  if (style.display === "none" || style.visibility === "hidden") {
    return true;
  }
  return false;
}
function isScrollable(element) {
  const style = window.getComputedStyle(element);
  const overflowX = style.overflowX;
  const overflowY = style.overflowY;
  return (overflowX === "scroll" || overflowX === "auto" || overflowY === "scroll" || overflowY === "auto") && (element.scrollHeight > element.clientHeight || element.scrollWidth > element.clientWidth);
}
function isElementClickable(element) {
  if (element.disabled) {
    return false;
  }
  const tabindex = element.getAttribute("tabindex");
  if (tabindex && parseInt(tabindex, 10) < 0) {
    return false;
  }
  const clickableTags = ["BUTTON", "A", "INPUT", "SELECT", "TEXTAREA"];
  if (clickableTags.includes(element.tagName)) {
    return true;
  }
  if (element.onclick || element.hasAttribute("onclick")) {
    return true;
  }
  if (tabindex && parseInt(tabindex, 10) >= 0) {
    return true;
  }
  return false;
}
function isElemFocusable(element) {
  if (isElementHidden(element)) {
    return false;
  }
  return true;
}
function getChildrenForTraversal(element) {
  const children = [];
  if (element.shadowRoot) {
    children.push(...Array.from(element.shadowRoot.children));
  }
  if (element instanceof HTMLSlotElement) {
    const assigned = element.assignedElements({ flatten: true });
    children.push(...assigned);
  } else {
    children.push(...Array.from(element.children));
  }
  return children.filter((child) => child.nodeType === 1);
}
function findFocusableElementRecursive(container, forward = true, startFromContainer = false) {
  if (startFromContainer && isElemFocusable(container) && isElementClickable(container)) {
    return container;
  }
  const children = getChildrenForTraversal(container);
  const traverseOrder = forward ? children : children.reverse();
  for (const child of traverseOrder) {
    if (isElemFocusable(child) && isElementClickable(child)) {
      const isSafari = /^((?!chrome|android).)*safari/i.test(navigator.userAgent);
      if (isSafari && isScrollable(child) && !isElementClickable(child)) {
        continue;
      }
      return child;
    }
    const found = findFocusableElementRecursive(child, forward, false);
    if (found) {
      return found;
    }
  }
  return null;
}
function findFirstFocusableElement(container, initialFocusId) {
  if (!container) return null;
  if (initialFocusId) {
    const targetElement = document.getElementById(initialFocusId);
    if (targetElement && container.contains(targetElement) && isElemFocusable(targetElement) && isElementClickable(targetElement)) {
      return targetElement;
    }
  }
  return findFocusableElementRecursive(container, true, false);
}
function useIsPhone() {
  const [phone, setPhone] = React45__namespace.useState(() => isPhone());
  React45__namespace.useEffect(() => {
    const handler = () => setPhone(isPhone());
    window.addEventListener("resize", handler);
    window.addEventListener("orientationchange", handler);
    return () => {
      window.removeEventListener("resize", handler);
      window.removeEventListener("orientationchange", handler);
    };
  }, []);
  return phone;
}
var dialogVariants = cva(
  [
    "relative flex flex-col",
    "bg-sapphire-card-bg-primary rounded-2xl",
    "shadow-[0_20px_25px_-5px_rgba(10,10,10,0.15),0_10px_15px_-5px_rgba(10,10,10,0.05)]",
    "min-w-[320px] max-w-[554px]",
    "max-h-[90vh]",
    "outline-none",
    "p-0 m-0",
    "border border-border"
  ],
  {
    variants: {
      state: {
        ["None" /* None */]: "",
        ["Error" /* Error */]: "",
        ["Warning" /* Warning */]: "",
        ["Success" /* Success */]: "",
        ["Information" /* Information */]: ""
      },
      stretch: {
        // Desktop stretch: ~90% viewport, no rounding. Phone full-screen handled via JS (isPhone()).
        true: "max-w-[95vw] h-[90vh] w-[90%]",
        false: ""
      },
      resizable: {
        true: "overflow-hidden max-w-[95vw] max-h-[95vh]",
        false: ""
      }
    },
    defaultVariants: {
      state: "None" /* None */,
      stretch: false,
      resizable: false
    }
  }
);
var headerVariants = cva(
  [
    "flex items-center justify-between",
    "px-6 py-5",
    "shrink-0",
    "focus:outline-none focus:rounded-t-2xl focus:[box-shadow:inset_0_0_0_4px_white,inset_0_0_0_6px_var(--ring)]"
  ],
  {
    variants: {
      state: {
        ["None" /* None */]: "border-sapphire-border-primary",
        ["Error" /* Error */]: "bg-sapphire-negative-bg border-sapphire-negative",
        ["Warning" /* Warning */]: "bg-sapphire-warning-bg border-sapphire-warning",
        ["Success" /* Success */]: "bg-sapphire-positive-bg border-sapphire-positive",
        ["Information" /* Information */]: "bg-sapphire-info-bg border-sapphire-info"
      },
      draggable: {
        true: "cursor-move select-none",
        false: ""
      }
    },
    defaultVariants: {
      state: "None" /* None */,
      draggable: false
    }
  }
);
function Dialog({
  className,
  open: controlledOpen,
  defaultOpen = false,
  headerText,
  subHeaderText,
  headerEndContent,
  showBackButton,
  onBackButtonClick,
  hideHeaderBorder,
  header,
  children,
  footer,
  state = "None" /* None */,
  stretch = false,
  draggable = false,
  resizable = false,
  preventInitialFocus = false,
  enableBackdropClick = false,
  noPadding = false,
  initialFocus,
  accessibleName,
  accessibleNameRef,
  accessibleDescribedBy,
  accessibleRole,
  ariaRoleDescription,
  onBeforeOpen,
  onAfterOpen,
  onBeforeClose,
  onAfterClose,
  onEscapePress,
  onOpenChange,
  id,
  ref
}) {
  const [internalOpen, setInternalOpen] = React45__namespace.useState(defaultOpen);
  const isOpen = controlledOpen ?? internalOpen;
  const onPhone = useIsPhone();
  const [isPopoverActuallyOpen, setIsPopoverActuallyOpen] = React45__namespace.useState(false);
  const dialogId = React45__namespace.useId();
  const stableId = id || dialogId;
  const dialogRef = React45__namespace.useRef(null);
  const headerRef = React45__namespace.useRef(null);
  const contentRef = React45__namespace.useRef(null);
  const previousActiveElementRef = React45__namespace.useRef(null);
  const returnValueRef = React45__namespace.useRef(void 0);
  const isClosingRef = React45__namespace.useRef(false);
  const didPopRef = React45__namespace.useRef(false);
  const [isDragging, setIsDragging] = React45__namespace.useState(false);
  const [position, setPosition] = React45__namespace.useState(null);
  const positionRef = React45__namespace.useRef(position);
  React45__namespace.useLayoutEffect(() => {
    positionRef.current = position;
  }, [position]);
  const dragStartRef = React45__namespace.useRef({ x: 0, y: 0, dialogX: 0, dialogY: 0 });
  const effectiveRole = React45__namespace.useMemo(() => {
    if (state === "Error" /* Error */ || state === "Warning" /* Warning */) {
      return "alertdialog";
    }
    return accessibleRole || "dialog";
  }, [state, accessibleRole]);
  const effectiveAriaDescribedBy = React45__namespace.useMemo(() => {
    const parts = [];
    if (accessibleDescribedBy) {
      parts.push(accessibleDescribedBy);
    }
    if (draggable || resizable) {
      parts.push(`${stableId}-movement-descr`);
    }
    return parts.length > 0 ? parts.join(" ") : void 0;
  }, [accessibleDescribedBy, draggable, resizable, stableId]);
  const getMovementDescription = () => {
    if (draggable && resizable) {
      return "This dialog can be moved and resized. Use Tab to focus the header, then arrow keys to move. Use Shift+Arrow keys to resize.";
    }
    if (draggable) {
      return "This dialog can be moved. Use Tab to focus the header, then arrow keys to reposition.";
    }
    if (resizable) {
      return "This dialog can be resized. Use Tab to focus the header, then Shift+Arrow keys to resize.";
    }
    return "";
  };
  const ensureDirectPositioning = React45__namespace.useCallback(() => {
    const dialog = dialogRef.current;
    if (!dialog || positionRef.current) return positionRef.current;
    const rect = dialog.getBoundingClientRect();
    dialog.style.width = `${rect.width}px`;
    const pos = { x: rect.left, y: rect.top };
    setPosition(pos);
    return pos;
  }, []);
  const clampPosition = React45__namespace.useCallback(
    (x, y) => {
      const dialog = dialogRef.current;
      if (!dialog) return { x, y };
      const w = dialog.offsetWidth;
      const h = dialog.offsetHeight;
      const vw = window.innerWidth;
      const vh = window.innerHeight;
      if (!w || !h || !vw || !vh) return { x, y };
      return {
        x: Math.max(0, Math.min(vw - w, x)),
        y: Math.max(0, Math.min(vh - h, y))
      };
    },
    []
  );
  const handleClose = React45__namespace.useCallback((escPressed) => {
    const dialog = dialogRef.current;
    if (!dialog) return;
    if (isClosingRef.current) return;
    isClosingRef.current = true;
    const beforeCloseDetail = {
      escPressed,
      returnValue: returnValueRef.current
    };
    const shouldClose = onBeforeClose?.(beforeCloseDetail);
    if (shouldClose === false) {
      isClosingRef.current = false;
      if (controlledOpen === void 0) {
        setInternalOpen(true);
      } else {
        onOpenChange?.(true);
      }
      return;
    }
    if (dialog.matches(":popover-open")) {
      dialog.hidePopover();
      if (!didPopRef.current) {
        didPopRef.current = true;
        dialogStack.pop(stableId);
      }
      setIsPopoverActuallyOpen(false);
    }
    setPosition(null);
    dialog.style.width = "";
    if (controlledOpen === void 0) {
      setInternalOpen(false);
    }
    onOpenChange?.(false);
    const afterCloseDetail = {
      returnValue: returnValueRef.current
    };
    setTimeout(() => {
      onAfterClose?.(afterCloseDetail);
      previousActiveElementRef.current?.focus();
      returnValueRef.current = void 0;
      isClosingRef.current = false;
      didPopRef.current = false;
    }, 200);
  }, [controlledOpen, onBeforeClose, onAfterClose, onOpenChange, stableId]);
  React45__namespace.useEffect(() => {
    if (!isPopoverActuallyOpen) return;
    const dialog = dialogRef.current;
    if (!dialog) return;
    const resizeObserver = new ResizeObserver(() => {
      if (!dialogRef.current) return;
      if (resizable || draggable) {
        const pos = positionRef.current ?? ensureDirectPositioning();
        if (!pos) return;
        const dialogRect = dialogRef.current.getBoundingClientRect();
        const vw = window.innerWidth;
        const vh = window.innerHeight;
        const newX = Math.max(0, Math.min(vw - dialogRect.width, pos.x));
        const newY = Math.max(0, Math.min(vh - dialogRect.height, pos.y));
        if (newX !== pos.x || newY !== pos.y) {
          setPosition({ x: newX, y: newY });
        }
      }
    });
    resizeObserver.observe(dialog);
    return () => {
      resizeObserver.disconnect();
    };
  }, [isPopoverActuallyOpen, draggable, resizable, ensureDirectPositioning]);
  React45__namespace.useEffect(() => {
    if (!isPopoverActuallyOpen) return;
    const handleResize = () => {
      const dialog = dialogRef.current;
      if (dialog) {
        dialog.style.width = "";
      }
      setPosition(null);
    };
    window.addEventListener("resize", handleResize);
    return () => window.removeEventListener("resize", handleResize);
  }, [isPopoverActuallyOpen]);
  const isPopoverActuallyOpenRef = React45__namespace.useRef(isPopoverActuallyOpen);
  React45__namespace.useEffect(() => {
    isPopoverActuallyOpenRef.current = isPopoverActuallyOpen;
  }, [isPopoverActuallyOpen]);
  const onAfterCloseRef = React45__namespace.useRef(onAfterClose);
  React45__namespace.useEffect(() => {
    onAfterCloseRef.current = onAfterClose;
  }, [onAfterClose]);
  const handleCloseRef = React45__namespace.useRef(handleClose);
  React45__namespace.useEffect(() => {
    handleCloseRef.current = handleClose;
  }, [handleClose]);
  React45__namespace.useEffect(() => {
    const dialog = dialogRef.current;
    const id2 = stableId;
    return () => {
      const isOpen2 = dialog?.matches(":popover-open");
      if (isOpen2) {
        try {
          dialog.hidePopover();
        } catch {
        }
      }
      if ((isOpen2 || isPopoverActuallyOpenRef.current) && !didPopRef.current) {
        didPopRef.current = true;
        dialogStack.pop(id2);
        onAfterCloseRef.current?.({ returnValue: returnValueRef.current });
      }
    };
  }, [stableId]);
  React45__namespace.useEffect(() => {
    const dialog = dialogRef.current;
    if (!dialog) return;
    const isPopoverOpen = dialog.matches(":popover-open");
    if (isOpen && !isPopoverOpen) {
      isClosingRef.current = false;
      didPopRef.current = false;
      previousActiveElementRef.current = document.activeElement;
      const beforeOpenDetail = {};
      const shouldOpen = onBeforeOpen?.(beforeOpenDetail);
      if (shouldOpen === false) {
        if (controlledOpen === void 0) {
          setInternalOpen(false);
        } else {
          onOpenChange?.(false);
        }
        return;
      }
      dialogStack.push(stableId, enableBackdropClick ? () => handleCloseRef.current(false) : void 0);
      dialog.showPopover();
      setIsPopoverActuallyOpen(true);
      requestAnimationFrame(() => {
        requestAnimationFrame(() => {
          if (!preventInitialFocus) {
            const firstFocusable = findFirstFocusableElement(dialog, initialFocus);
            if (firstFocusable) {
              firstFocusable.focus();
            } else {
              dialog.focus();
            }
          }
        });
      });
      const timer = setTimeout(() => {
        onAfterOpen?.();
      }, 200);
      return () => clearTimeout(timer);
    } else if (!isOpen && isPopoverOpen) {
      handleClose(false);
    }
    if (!isPopoverOpen && isPopoverActuallyOpen) {
      setIsPopoverActuallyOpen(false);
    }
  }, [
    isOpen,
    handleClose,
    initialFocus,
    preventInitialFocus,
    onBeforeOpen,
    onAfterOpen,
    isPopoverActuallyOpen,
    controlledOpen,
    onOpenChange,
    stableId,
    enableBackdropClick
  ]);
  React45__namespace.useEffect(() => {
    const dialog = dialogRef.current;
    if (!isPopoverActuallyOpen || !dialog) return;
    addOpenedPopup({
      element: dialog,
      close: (escPressed) => {
        if (escPressed) onEscapePress?.();
        handleClose(!!escPressed);
      },
      type: "dialog",
      isModal: true
    });
    return () => removeOpenedPopup(dialog);
  }, [isPopoverActuallyOpen, handleClose, onEscapePress]);
  React45__namespace.useEffect(() => {
    const dialog = dialogRef.current;
    if (!dialog || !isPopoverActuallyOpen) return;
    const handleKeyDown = (e) => {
      if (e.key === "Escape") return;
      if (e.key === " " || ["ArrowUp", "ArrowDown", "ArrowLeft", "ArrowRight"].includes(e.key)) {
        const activeElement = document.activeElement;
        if (activeElement === dialog || activeElement === document.body) {
          e.preventDefault();
        }
      }
      if (e.key === "Tab") {
        const focusableElements = dialog?.querySelectorAll(
          'button, [href], input, select, textarea, [tabindex]:not([tabindex="-1"])'
        );
        if (!focusableElements || focusableElements.length === 0) return;
        const firstElement = focusableElements[0];
        const lastElement = focusableElements[focusableElements.length - 1];
        if (e.shiftKey) {
          if (document.activeElement === firstElement) {
            e.preventDefault();
            lastElement.focus();
          }
        } else {
          if (document.activeElement === lastElement) {
            e.preventDefault();
            firstElement.focus();
          }
        }
      }
      if (["ArrowUp", "ArrowDown", "ArrowLeft", "ArrowRight"].includes(e.key)) {
        const activeElement = document.activeElement;
        const isInput = activeElement instanceof HTMLInputElement || activeElement instanceof HTMLTextAreaElement || activeElement?.getAttribute("contenteditable") === "true";
        if (isInput) return;
        const isHeaderFocused = headerRef.current?.contains(activeElement);
        const shouldResize = e.shiftKey && resizable && isHeaderFocused;
        const shouldDrag = !e.shiftKey && draggable && isHeaderFocused;
        if (!shouldDrag && !shouldResize) {
          return;
        }
        e.preventDefault();
        const step = 10;
        let deltaX = 0;
        let deltaY = 0;
        switch (e.key) {
          case "ArrowUp":
            deltaY = -step;
            break;
          case "ArrowDown":
            deltaY = step;
            break;
          case "ArrowLeft":
            deltaX = -step;
            break;
          case "ArrowRight":
            deltaX = step;
            break;
        }
        if (shouldDrag) {
          const base = positionRef.current ?? ensureDirectPositioning() ?? { x: 0, y: 0 };
          setPosition(clampPosition(base.x + deltaX, base.y + deltaY));
        } else if (shouldResize && dialog) {
          const currentWidth = dialog.offsetWidth;
          const currentHeight = dialog.offsetHeight;
          const isRTL = document.documentElement.dir === "rtl";
          const base = positionRef.current ?? ensureDirectPositioning() ?? { x: 0, y: 0 };
          const effectiveDeltaX = isRTL ? -deltaX : deltaX;
          const newWidth = Math.max(200, currentWidth + effectiveDeltaX * 2);
          const newHeight = Math.max(150, currentHeight + deltaY * 2);
          dialog.style.width = `${newWidth}px`;
          dialog.style.height = `${newHeight}px`;
          const widthDiff = newWidth - currentWidth;
          const newX = isRTL ? base.x - widthDiff : base.x;
          setPosition(clampPosition(newX, base.y));
        }
      }
    };
    dialog.addEventListener("keydown", handleKeyDown);
    return () => {
      dialog.removeEventListener("keydown", handleKeyDown);
    };
  }, [isPopoverActuallyOpen, draggable, resizable, clampPosition, ensureDirectPositioning]);
  React45__namespace.useEffect(() => {
    const dialog = dialogRef.current;
    if (!dialog || !isPopoverActuallyOpen) return;
    const handleFocusOut = (e) => {
      const newFocusTarget = e.relatedTarget;
      if (!newFocusTarget || !dialog.contains(newFocusTarget)) {
        requestAnimationFrame(() => {
          const currentFocus = document.activeElement;
          if (!dialog.contains(currentFocus)) {
            dialog.focus();
          }
        });
      }
    };
    dialog.addEventListener("focusout", handleFocusOut);
    return () => {
      dialog.removeEventListener("focusout", handleFocusOut);
    };
  }, [isPopoverActuallyOpen]);
  const handleMouseDown = React45__namespace.useCallback(
    (e) => {
      if (!draggable) return;
      if (stretch && onPhone) return;
      if (!headerRef.current?.contains(e.target)) return;
      const pos = position ?? ensureDirectPositioning();
      setIsDragging(true);
      dragStartRef.current = {
        x: e.clientX,
        y: e.clientY,
        dialogX: pos?.x ?? 0,
        dialogY: pos?.y ?? 0
      };
    },
    [draggable, stretch, onPhone, position, ensureDirectPositioning]
  );
  const handleMouseMove = React45__namespace.useCallback(
    (e) => {
      if (!isDragging) return;
      const deltaX = e.clientX - dragStartRef.current.x;
      const deltaY = e.clientY - dragStartRef.current.y;
      const rawX = dragStartRef.current.dialogX + deltaX;
      const rawY = dragStartRef.current.dialogY + deltaY;
      setPosition(clampPosition(rawX, rawY));
    },
    [isDragging, clampPosition]
  );
  const handleMouseUp = React45__namespace.useCallback(() => {
    setIsDragging(false);
  }, []);
  const resizeState = React45__namespace.useRef(null);
  const handleResizeMouseDown = React45__namespace.useCallback(
    (e) => {
      if (!resizable) return;
      if (stretch && onPhone) return;
      e.preventDefault();
      e.stopPropagation();
      const dialog = dialogRef.current;
      if (!dialog) return;
      const pos = positionRef.current ?? ensureDirectPositioning();
      const rect = dialog.getBoundingClientRect();
      const rtl = document.documentElement.dir === "rtl";
      resizeState.current = {
        startX: e.clientX,
        startY: e.clientY,
        startWidth: rect.width,
        startHeight: rect.height,
        startLeft: pos?.x ?? rect.left,
        isRTL: rtl
      };
      const onMove = (ev) => {
        if (!resizeState.current || !dialogRef.current) return;
        const { startX, startY, startWidth, startHeight, startLeft, isRTL } = resizeState.current;
        const dx = ev.clientX - startX;
        const dy = ev.clientY - startY;
        let newWidth;
        let newLeft;
        if (isRTL) {
          newWidth = Math.max(200, Math.min(startWidth - dx, window.innerWidth * 0.95));
          newLeft = startLeft + dx;
          if (newLeft < 0) {
            newWidth = startWidth + startLeft;
            newLeft = 0;
          }
        } else {
          newWidth = Math.max(200, Math.min(startWidth + dx, window.innerWidth * 0.95));
        }
        const newHeight = Math.max(150, Math.min(startHeight + dy, window.innerHeight * 0.95));
        dialogRef.current.style.width = `${newWidth}px`;
        dialogRef.current.style.height = `${newHeight}px`;
        if (isRTL && newLeft !== void 0) {
          setPosition(clampPosition(newLeft, positionRef.current?.y ?? startLeft));
        }
      };
      const onUp = () => {
        resizeState.current = null;
        window.removeEventListener("mousemove", onMove);
        window.removeEventListener("mouseup", onUp);
      };
      window.addEventListener("mousemove", onMove);
      window.addEventListener("mouseup", onUp);
    },
    [resizable, stretch, onPhone, ensureDirectPositioning, clampPosition]
  );
  React45__namespace.useEffect(() => {
    if (isDragging) {
      document.addEventListener("mousemove", handleMouseMove);
      document.addEventListener("mouseup", handleMouseUp);
      return () => {
        document.removeEventListener("mousemove", handleMouseMove);
        document.removeEventListener("mouseup", handleMouseUp);
      };
    }
  }, [isDragging, handleMouseMove, handleMouseUp]);
  React45__namespace.useImperativeHandle(
    ref,
    () => ({
      show: () => {
        if (controlledOpen === void 0) {
          setInternalOpen(true);
        }
        onOpenChange?.(true);
      },
      showModal: () => {
        if (controlledOpen === void 0) {
          setInternalOpen(true);
        }
        onOpenChange?.(true);
      },
      close: (returnValue) => {
        returnValueRef.current = returnValue;
        handleClose(false);
      },
      focus: () => {
        dialogRef.current?.focus();
      }
    }),
    [controlledOpen, onOpenChange, handleClose]
  );
  return /* @__PURE__ */ jsxRuntime.jsxs(
    "dialog",
    {
      ref: dialogRef,
      id: stableId,
      popover: "manual",
      role: effectiveRole,
      "aria-labelledby": accessibleNameRef || (!accessibleName && headerText ? `${stableId}-title` : void 0),
      "aria-describedby": effectiveAriaDescribedBy,
      "aria-label": accessibleName || (!headerText && !accessibleNameRef ? "Dialog" : void 0),
      "aria-roledescription": ariaRoleDescription,
      tabIndex: -1,
      className: cn(
        dialogVariants({ state, stretch, resizable }),
        stretch && onPhone && "w-screen max-w-none h-[100dvh] max-h-none rounded-none min-w-0",
        className
      ),
      style: {
        ...stretch && onPhone ? {
          left: 0,
          top: 0,
          right: "auto",
          transform: "none"
        } : position ? {
          left: `${position.x}px`,
          top: `${position.y}px`,
          right: "auto",
          transform: "none"
        } : {
          left: "50%",
          top: "50%",
          right: "auto",
          transform: "translate(-50%, -50%)"
        }
      },
      onClick: (e) => e.stopPropagation(),
      onMouseDown: handleMouseDown,
      children: [
        (draggable || resizable) && /* @__PURE__ */ jsxRuntime.jsx(
          "span",
          {
            id: `${stableId}-movement-descr`,
            className: "absolute w-px h-px p-0 -m-px overflow-hidden whitespace-nowrap border-0",
            style: { clip: "rect(0, 0, 0, 0)" },
            children: getMovementDescription()
          }
        ),
        (header || headerText) && /* @__PURE__ */ jsxRuntime.jsx(
          "div",
          {
            ref: headerRef,
            className: cn(
              headerVariants({ state, draggable }),
              !hideHeaderBorder && state !== "Error" /* Error */ && state !== "Success" /* Success */ && state !== "Information" /* Information */ && state !== "Warning" /* Warning */ && "border-b"
            ),
            tabIndex: draggable || resizable ? 0 : void 0,
            "aria-roledescription": draggable && ariaRoleDescription ? ariaRoleDescription : void 0,
            children: header || /* @__PURE__ */ jsxRuntime.jsxs("div", { className: "flex items-center gap-3 w-full", children: [
              /* @__PURE__ */ jsxRuntime.jsxs("div", { className: "flex items-center gap-3 min-w-0", children: [
                state === "Error" && /* @__PURE__ */ jsxRuntime.jsx(MessageErrorIcon, { className: "h-5 w-5 text-sapphire-negative shrink-0" }),
                state === "Success" && /* @__PURE__ */ jsxRuntime.jsx(MessageSuccessIcon, { className: "h-5 w-5 text-sapphire-positive shrink-0" }),
                state === "Warning" && /* @__PURE__ */ jsxRuntime.jsx(MessageWarningIcon, { className: "h-5 w-5 text-sapphire-warning shrink-0" }),
                state === "Information" && /* @__PURE__ */ jsxRuntime.jsx(MessageInformationIcon, { className: "h-5 w-5 text-sapphire-info shrink-0" }),
                showBackButton && state === "None" && /* @__PURE__ */ jsxRuntime.jsx(
                  Button,
                  {
                    design: "SecondaryNeutral",
                    iconOnly: true,
                    icon: /* @__PURE__ */ jsxRuntime.jsx(ArrowLeftIcon, {}),
                    onClick: onBackButtonClick
                  }
                ),
                /* @__PURE__ */ jsxRuntime.jsxs("div", { className: "flex flex-col min-w-0", children: [
                  /* @__PURE__ */ jsxRuntime.jsx(
                    Title,
                    {
                      level: "H1",
                      id: `${stableId}-title`,
                      className: "text-xl font-semibold text-foreground truncate",
                      children: headerText
                    }
                  ),
                  subHeaderText && /* @__PURE__ */ jsxRuntime.jsx("span", { className: "text-sm font-normal text-sapphire-text-tertiary truncate", children: subHeaderText })
                ] })
              ] }),
              headerEndContent && /* @__PURE__ */ jsxRuntime.jsx("div", { className: "flex items-center gap-2 shrink-0 max-w-[50%] ml-auto", children: headerEndContent })
            ] })
          }
        ),
        /* @__PURE__ */ jsxRuntime.jsx(
          "div",
          {
            ref: contentRef,
            className: cn("flex-auto min-h-0 overflow-y-auto", !noPadding && "px-6 py-4"),
            tabIndex: -1,
            children
          }
        ),
        footer && /* @__PURE__ */ jsxRuntime.jsx("div", { className: "flex items-center justify-end gap-2 px-6 py-5 shrink-0", children: footer }),
        resizable && !onPhone && /* @__PURE__ */ jsxRuntime.jsx(
          "div",
          {
            className: "dialog-resize-handle absolute bottom-0 w-6 h-6 rounded-full flex items-center justify-center z-[1] end-0",
            onMouseDown: handleResizeMouseDown,
            "data-resize-handle": "true",
            children: /* @__PURE__ */ jsxRuntime.jsx(
              ResizeCornerIcon,
              {
                className: "h-4 w-4 text-sapphire-text-tertiary rtl:rotate-90"
              }
            )
          }
        )
      ]
    }
  );
}
function useIsPhone2() {
  const [phone] = React45.useState(() => isPhone());
  return phone;
}
function ResponsivePopover({
  headerText,
  placement,
  horizontalAlign,
  verticalAlign,
  hideArrow,
  offset,
  allowTargetOverlap,
  opener,
  open = false,
  initialFocus,
  preventFocusRestore,
  preventInitialFocus,
  accessibleName,
  accessibleNameRef,
  accessibleRole,
  accessibleDescription,
  header,
  footer,
  children,
  onBeforeOpen,
  onOpen,
  onBeforeClose,
  onClose,
  contentOnlyOnDesktop = false,
  noPadding = false,
  showCloseButton = true,
  className,
  style,
  id,
  ref
}) {
  const isPhone2 = useIsPhone2();
  const popoverRef = React45.useRef(null);
  const dialogRef = React45.useRef(null);
  const handleMobileClose = React45.useCallback(() => {
    onBeforeClose?.({ escPressed: false });
    onClose?.();
  }, [onBeforeClose, onClose]);
  React45.useImperativeHandle(
    ref,
    () => ({
      open() {
        if (isPhone2) {
          dialogRef.current?.show();
        } else {
          popoverRef.current?.open();
        }
      },
      close() {
        if (isPhone2) {
          dialogRef.current?.close();
        } else {
          popoverRef.current?.close();
        }
      },
      async applyFocus() {
        if (isPhone2) {
          dialogRef.current?.focus();
        } else {
          await popoverRef.current?.applyFocus();
        }
      },
      isOpen() {
        if (isPhone2) {
          return open;
        }
        return popoverRef.current?.isOpen() ?? false;
      },
      get nativeElement() {
        if (isPhone2) {
          return null;
        }
        return popoverRef.current?.nativeElement ?? null;
      }
    }),
    [isPhone2, open]
  );
  if (isPhone2) {
    const titleId = `${id || "responsive-popover"}-title`;
    const mobileHeader = header || headerText || showCloseButton ? /* @__PURE__ */ jsxRuntime.jsxs("div", { className: "flex items-center justify-between w-full", children: [
      header || headerText && /* @__PURE__ */ jsxRuntime.jsx(Title, { level: "H1", id: titleId, className: "text-base font-semibold", children: headerText }),
      showCloseButton && /* @__PURE__ */ jsxRuntime.jsx(
        Button,
        {
          design: "Tertiary",
          iconOnly: true,
          accessibleName: "Close",
          onClick: handleMobileClose,
          className: "ml-auto",
          icon: /* @__PURE__ */ jsxRuntime.jsxs(
            "svg",
            {
              xmlns: "http://www.w3.org/2000/svg",
              width: "16",
              height: "16",
              viewBox: "0 0 24 24",
              fill: "none",
              stroke: "currentColor",
              strokeWidth: "2",
              strokeLinecap: "round",
              strokeLinejoin: "round",
              children: [
                /* @__PURE__ */ jsxRuntime.jsx("line", { x1: "18", y1: "6", x2: "6", y2: "18" }),
                /* @__PURE__ */ jsxRuntime.jsx("line", { x1: "6", y1: "6", x2: "18", y2: "18" })
              ]
            }
          )
        }
      )
    ] }) : void 0;
    return /* @__PURE__ */ jsxRuntime.jsx(
      Dialog,
      {
        ref: dialogRef,
        open,
        onOpenChange: (isOpen) => {
          if (!isOpen) onClose?.();
        },
        headerText: !header && !showCloseButton ? headerText : void 0,
        header: mobileHeader,
        footer,
        stretch: true,
        noPadding,
        initialFocus,
        preventInitialFocus,
        accessibleName,
        accessibleNameRef: accessibleNameRef || (!accessibleName && headerText && !header && showCloseButton ? titleId : void 0),
        accessibleRole: accessibleRole === "AlertDialog" ? "alertdialog" : accessibleRole === "None" ? void 0 : "dialog",
        onBeforeOpen: () => onBeforeOpen?.(),
        onAfterOpen: () => onOpen?.(),
        onBeforeClose: (detail) => onBeforeClose?.({ escPressed: !!detail.escPressed }),
        onAfterClose: () => onClose?.(),
        className,
        id,
        children
      }
    );
  }
  return /* @__PURE__ */ jsxRuntime.jsx(
    Popover,
    {
      ref: popoverRef,
      open,
      opener,
      headerText: contentOnlyOnDesktop ? void 0 : headerText,
      header: contentOnlyOnDesktop ? void 0 : header,
      footer: contentOnlyOnDesktop ? void 0 : footer,
      placement,
      horizontalAlign,
      verticalAlign,
      hideArrow,
      offset,
      allowTargetOverlap,
      noPadding,
      initialFocus,
      preventFocusRestore,
      preventInitialFocus,
      accessibleName,
      accessibleNameRef,
      accessibleRole,
      accessibleDescription,
      onBeforeOpen,
      onOpen,
      onBeforeClose,
      onClose,
      className,
      style,
      id,
      children
    }
  );
}
function Label({
  id,
  for: forProp,
  htmlFor,
  showColon = false,
  required = false,
  wrappingType = "Normal" /* Normal */,
  children,
  text,
  className,
  style,
  "data-testid": dataTestId,
  ref
}) {
  const labelFor = forProp ?? htmlFor;
  const labelContent = children ?? text;
  return /* @__PURE__ */ jsxRuntime.jsxs(
    "label",
    {
      ref,
      id,
      htmlFor: labelFor,
      className: cn(
        "text-sm font-normal text-sapphire-text-tertiary",
        wrappingType === "None" /* None */ && "truncate",
        wrappingType === "Normal" /* Normal */ && "break-words",
        className
      ),
      style,
      "data-testid": dataTestId,
      children: [
        labelContent,
        showColon && ":",
        required && /* @__PURE__ */ jsxRuntime.jsx("span", { className: "text-destructive ml-0.5", "aria-hidden": "true", children: "*" }),
        required && /* @__PURE__ */ jsxRuntime.jsx("span", { className: "sr-only", children: "(required)" })
      ]
    }
  );
}
var highlightBarVariants2 = cva(
  "absolute left-0 top-0 bottom-0 w-[3px]",
  {
    variants: {
      highlight: {
        ["None" /* None */]: "bg-transparent",
        ["Positive" /* Positive */]: "bg-sapphire-positive",
        ["Critical" /* Critical */]: "bg-sapphire-warning",
        ["Information" /* Information */]: "bg-sapphire-info",
        ["Negative" /* Negative */]: "bg-sapphire-negative"
      }
    },
    defaultVariants: {
      highlight: "None" /* None */
    }
  }
);
function ListItemCustom(props) {
  const {
    ref,
    // Content
    children,
    // Behavior
    type = "Active" /* Active */,
    highlight = "None" /* None */,
    // Rest passed to ListItemBase
    ...baseProps
  } = props;
  return /* @__PURE__ */ jsxRuntime.jsx(
    ListItemBase,
    {
      ref,
      type,
      ...baseProps,
      children: () => /* @__PURE__ */ jsxRuntime.jsxs(jsxRuntime.Fragment, { children: [
        String(highlight) !== "None" && /* @__PURE__ */ jsxRuntime.jsx("span", { className: highlightBarVariants2({ highlight }) }),
        /* @__PURE__ */ jsxRuntime.jsx("span", { className: "flex-1 px-3 py-2 min-w-0", children })
      ] })
    }
  );
}
ListItemCustom.displayName = "ListItemCustom";
var pathData25 = [
  "M256 224q-12 0-18-8L103 75q-7-7-7-17 0-11 7.5-18.5T122 32q10 0 18 8l116 121L372 40q8-8 18-8 11 0 18.5 7.5T416 58q0 10-7 17L275 216q-8 8-19 8zm134 256q-10 0-18-8L256 351 140 472q-8 8-18 8-11 0-18.5-7.5T96 454q0-10 7-17l135-141q6-8 18-8 11 0 19 8l134 141q7 7 7 17 0 11-7.5 18.5T390 480z"
];
var CollapseIcon = Object.assign(
  React45__namespace.default.forwardRef(
    (props, ref) => /* @__PURE__ */ jsxRuntime.jsx(Icon, { ref, pathData: pathData25, ...props })
  ),
  { displayName: "CollapseIcon", iconName: "collapse" }
);
var pathData26 = [
  "M256 32q11 0 19 8l134 141q7 7 7 17 0 11-7.5 18.5T390 224q-10 0-18-8L256 95 140 216q-8 8-18 8-11 0-18.5-7.5T96 198q0-10 7-17L238 40q6-8 18-8zm134 256q11 0 18.5 7.5T416 314q0 10-7 17L275 472q-8 8-19 8-12 0-18-8L103 331q-7-7-7-17 0-11 7.5-18.5T122 288q10 0 18 8l116 121 116-121q8-8 18-8z"
];
var ExpandIcon = Object.assign(
  React45__namespace.default.forwardRef(
    (props, ref) => /* @__PURE__ */ jsxRuntime.jsx(Icon, { ref, pathData: pathData26, ...props })
  ),
  { displayName: "ExpandIcon", iconName: "expand" }
);
var pathData27 = [
  "M186 416q-11 0-18.5-7.5T160 390q0-10 8-18l121-116-121-116q-8-8-8-18 0-11 7.5-18.5T186 96q10 0 17 7l141 134q8 8 8 19 0 12-8 18L203 409q-7 7-17 7z"
];
var NavigationRightArrowIcon = Object.assign(
  React45__namespace.default.forwardRef(
    (props, ref) => /* @__PURE__ */ jsxRuntime.jsx(Icon, { ref, pathData: pathData27, ...props })
  ),
  { displayName: "NavigationRightArrowIcon", iconName: "navigation-right-arrow" }
);
var roleMap = {
  ["Complementary" /* Complementary */]: "complementary",
  ["Form" /* Form */]: "form",
  ["Region" /* Region */]: "region"
};
var levelToTag = {
  ["H1" /* H1 */]: "h1",
  ["H2" /* H2 */]: "h2",
  ["H3" /* H3 */]: "h3",
  ["H4" /* H4 */]: "h4",
  ["H5" /* H5 */]: "h5",
  ["H6" /* H6 */]: "h6"
};
var rootDesignClasses = {
  ["Primary" /* Primary */]: "bg-sapphire-card-bg-primary border border-sapphire-border-primary",
  ["Secondary" /* Secondary */]: "bg-sapphire-background-secondary border border-sapphire-border-primary",
  ["Child" /* Child */]: ""
};
var headerDesignClasses = {
  ["Primary" /* Primary */]: "pl-3 pr-4 py-3 gap-3",
  ["Secondary" /* Secondary */]: "pl-3 pr-4 py-3 gap-3",
  ["Child" /* Child */]: "pl-1 pr-2 py-1 gap-1"
};
var headerTitleClasses = {
  ["Primary" /* Primary */]: "text-base font-normal",
  ["Secondary" /* Secondary */]: "text-base font-normal",
  ["Child" /* Child */]: "text-sm font-normal"
};
var contentDesignClasses = {
  ["Primary" /* Primary */]: "p-4",
  ["Secondary" /* Secondary */]: "p-4",
  ["Child" /* Child */]: "p-3"
};
var headerBgClasses = {
  ["Primary" /* Primary */]: "bg-sapphire-card-bg-primary",
  ["Secondary" /* Secondary */]: "bg-sapphire-background-secondary",
  ["Child" /* Child */]: "bg-background"
};
var footerDesignClasses = {
  ["Primary" /* Primary */]: "px-4 py-4",
  ["Secondary" /* Secondary */]: "px-4 py-4 bg-sapphire-card-bg-secondary",
  ["Child" /* Child */]: ""
};
function Panel(props) {
  const {
    // Visual
    design = "Primary" /* Primary */,
    // Content
    children,
    headerText,
    header,
    subtitle,
    endSlot,
    footer,
    // Behavior
    fixed = false,
    collapsed: controlledCollapsed,
    defaultCollapsed = false,
    noAnimation = false,
    stickyHeader = false,
    noPadding = false,
    // Accessibility
    accessibleRole = "Form" /* Form */,
    headerLevel = "H2" /* H2 */,
    accessibleName,
    // Events
    onToggle,
    // Standard
    className,
    style,
    id: providedId,
    "data-testid": dataTestId,
    ref
  } = props;
  const panelRef = React45.useRef(null);
  const headerRef = React45.useRef(null);
  const contentWrapperRef = React45.useRef(null);
  const contentRef = React45.useRef(null);
  const generatedId = React45.useId();
  const panelId = providedId || generatedId;
  const headerId = `${panelId}-header`;
  const contentId = `${panelId}-content`;
  const [internalCollapsed, setInternalCollapsed] = React45.useState(defaultCollapsed);
  const isControlled = controlledCollapsed !== void 0;
  const isCollapsed = fixed ? false : isControlled ? controlledCollapsed : internalCollapsed;
  const animationRef = React45.useRef(null);
  const prevCollapsedRef = React45.useRef(isCollapsed);
  const [contentExpanded, setContentExpanded] = React45.useState(!isCollapsed);
  React45.useImperativeHandle(ref, () => ({
    focus() {
      headerRef.current?.focus();
    },
    blur() {
      headerRef.current?.blur();
    },
    isFocused() {
      return document.activeElement === headerRef.current;
    },
    get nativeElement() {
      return panelRef.current;
    }
  }), []);
  const handleToggle = React45.useCallback(() => {
    if (fixed) return;
    const newCollapsed = !isCollapsed;
    if (!isControlled) {
      setInternalCollapsed(newCollapsed);
    }
    onToggle?.({ collapsed: newCollapsed });
  }, [fixed, isCollapsed, isControlled, onToggle]);
  const pendingToggleRef = React45.useRef(false);
  const handleKeyDown = React45.useCallback((e) => {
    if (fixed) return;
    if (e.key === "Enter") {
      e.preventDefault();
      handleToggle();
    }
    if (e.key === " ") {
      e.preventDefault();
      pendingToggleRef.current = true;
    }
    if (e.key === "Escape" && pendingToggleRef.current) {
      e.preventDefault();
      pendingToggleRef.current = false;
    }
  }, [fixed, handleToggle]);
  const handleKeyUp = React45.useCallback((e) => {
    if (fixed) return;
    if (e.key === " ") {
      if (pendingToggleRef.current) {
        handleToggle();
      }
      pendingToggleRef.current = false;
    }
  }, [fixed, handleToggle]);
  React45.useEffect(() => {
    const wrapper = contentWrapperRef.current;
    if (!wrapper) {
      prevCollapsedRef.current = isCollapsed;
      return;
    }
    const wasCollapsed = prevCollapsedRef.current;
    prevCollapsedRef.current = isCollapsed;
    if (wasCollapsed === isCollapsed) return;
    if (noAnimation || fixed) {
      if (isCollapsed) {
        wrapper.style.display = "none";
        setContentExpanded(false);
      } else {
        wrapper.style.display = "";
        setContentExpanded(true);
      }
      return;
    }
    if (animationRef.current !== null) {
      cancelAnimationFrame(animationRef.current);
      animationRef.current = null;
    }
    const DURATION = 400;
    if (isCollapsed) {
      const computed = getComputedStyle(wrapper);
      const startHeight = parseFloat(computed.height);
      const startPaddingTop = parseFloat(computed.paddingTop);
      const startPaddingBottom = parseFloat(computed.paddingBottom);
      wrapper.style.overflow = "hidden";
      let start = null;
      const step = (timestamp) => {
        if (start === null) start = timestamp;
        const elapsed = timestamp - start;
        const progress = Math.min(elapsed / DURATION, 1);
        wrapper.style.height = `${startHeight * (1 - progress)}px`;
        wrapper.style.paddingTop = `${startPaddingTop * (1 - progress)}px`;
        wrapper.style.paddingBottom = `${startPaddingBottom * (1 - progress)}px`;
        if (progress < 1) {
          animationRef.current = requestAnimationFrame(step);
        } else {
          wrapper.style.display = "none";
          wrapper.style.overflow = "";
          wrapper.style.height = "";
          wrapper.style.paddingTop = "";
          wrapper.style.paddingBottom = "";
          animationRef.current = null;
          setContentExpanded(false);
        }
      };
      animationRef.current = requestAnimationFrame(step);
    } else {
      wrapper.style.display = "block";
      setContentExpanded(true);
      const computed = getComputedStyle(wrapper);
      const targetHeight = parseFloat(computed.height);
      const targetPaddingTop = parseFloat(computed.paddingTop);
      const targetPaddingBottom = parseFloat(computed.paddingBottom);
      wrapper.style.overflow = "hidden";
      wrapper.style.height = "0px";
      wrapper.style.paddingTop = "0px";
      wrapper.style.paddingBottom = "0px";
      let start = null;
      const step = (timestamp) => {
        if (start === null) start = timestamp;
        const elapsed = timestamp - start;
        const progress = Math.min(elapsed / DURATION, 1);
        wrapper.style.display = "block";
        wrapper.style.height = `${targetHeight * progress}px`;
        wrapper.style.paddingTop = `${targetPaddingTop * progress}px`;
        wrapper.style.paddingBottom = `${targetPaddingBottom * progress}px`;
        if (progress < 1) {
          animationRef.current = requestAnimationFrame(step);
        } else {
          wrapper.style.overflow = "";
          wrapper.style.height = "";
          wrapper.style.paddingTop = "";
          wrapper.style.paddingBottom = "";
          animationRef.current = null;
        }
      };
      animationRef.current = requestAnimationFrame(step);
    }
    return () => {
      if (animationRef.current !== null) {
        cancelAnimationFrame(animationRef.current);
        animationRef.current = null;
      }
    };
  }, [isCollapsed, noAnimation, fixed]);
  const resolvedDesign = design in rootDesignClasses ? design : "Primary" /* Primary */;
  const ariaRole = roleMap[accessibleRole] || "form";
  const HeadingTag = levelToTag[headerLevel] || "h2";
  const hasHeader = headerText || header;
  const isInteractive = !fixed;
  const isChild = resolvedDesign === "Child" /* Child */;
  const toggleIcon = isChild ? /* @__PURE__ */ jsxRuntime.jsx(
    NavigationRightArrowIcon,
    {
      className: cn(
        "h-4 w-4 shrink-0 text-sapphire-text-tertiary",
        !noAnimation && "transition-transform duration-200",
        !isCollapsed && "rotate-90"
      ),
      "aria-hidden": "true"
    }
  ) : isCollapsed ? /* @__PURE__ */ jsxRuntime.jsx(ExpandIcon, { className: "h-4 w-4 shrink-0 text-sapphire-text-tertiary", "aria-hidden": "true" }) : /* @__PURE__ */ jsxRuntime.jsx(CollapseIcon, { className: "h-4 w-4 shrink-0 text-sapphire-text-tertiary", "aria-hidden": "true" });
  return /* @__PURE__ */ jsxRuntime.jsxs(
    "div",
    {
      ref: panelRef,
      id: panelId,
      role: ariaRole,
      "aria-label": accessibleName,
      "aria-labelledby": !accessibleName && hasHeader ? headerId : void 0,
      className: cn(
        "rounded-lg overflow-clip text-sapphire-text-primary",
        rootDesignClasses[resolvedDesign],
        className
      ),
      style,
      "data-testid": dataTestId,
      "data-part": "root",
      children: [
        (hasHeader || isInteractive) && /* @__PURE__ */ jsxRuntime.jsxs(
          "div",
          {
            ref: headerRef,
            id: headerId,
            role: isInteractive ? "button" : void 0,
            tabIndex: isInteractive ? 0 : void 0,
            "aria-expanded": isInteractive ? !isCollapsed : void 0,
            "aria-controls": isInteractive ? contentId : void 0,
            onClick: isInteractive ? handleToggle : void 0,
            onKeyDown: isInteractive ? handleKeyDown : void 0,
            onKeyUp: isInteractive ? handleKeyUp : void 0,
            className: cn(
              "flex items-center min-h-11",
              headerDesignClasses[resolvedDesign],
              !stickyHeader && headerBgClasses[resolvedDesign],
              stickyHeader && cn("sticky top-0 z-10", headerBgClasses[resolvedDesign]),
              isInteractive && "cursor-pointer hover:bg-accent transition-colors",
              isInteractive && "focus:outline-none focus:ring-2 focus:ring-ring focus:ring-inset",
              "select-none",
              isChild ? "rounded-lg" : "rounded-t-lg",
              isCollapsed && !isChild && "rounded-b-lg"
            ),
            "data-part": "header",
            children: [
              isInteractive && toggleIcon,
              header ? /* @__PURE__ */ jsxRuntime.jsx("div", { className: "flex-1 min-w-0", "data-part": "header-content", children: header }) : /* @__PURE__ */ jsxRuntime.jsxs(jsxRuntime.Fragment, { children: [
                (headerText || subtitle) && /* @__PURE__ */ jsxRuntime.jsxs("div", { className: "flex-1 min-w-0", children: [
                  headerText && /* @__PURE__ */ jsxRuntime.jsx(
                    HeadingTag,
                    {
                      className: cn(
                        "text-sapphire-text-primary truncate m-0",
                        headerTitleClasses[resolvedDesign]
                      ),
                      "data-part": "header-text",
                      children: headerText
                    }
                  ),
                  subtitle && /* @__PURE__ */ jsxRuntime.jsx(
                    "p",
                    {
                      className: "text-sm text-sapphire-text-tertiary truncate m-0",
                      "data-part": "header-subtitle",
                      children: subtitle
                    }
                  )
                ] }),
                endSlot && /* @__PURE__ */ jsxRuntime.jsx(
                  "div",
                  {
                    className: "flex-shrink-0",
                    "data-part": "header-end-slot",
                    onClick: (e) => e.stopPropagation(),
                    onKeyDown: (e) => e.stopPropagation(),
                    onKeyUp: (e) => e.stopPropagation(),
                    children: endSlot
                  }
                )
              ] })
            ]
          }
        ),
        /* @__PURE__ */ jsxRuntime.jsxs(
          "div",
          {
            ref: contentWrapperRef,
            id: contentId,
            role: "region",
            "aria-labelledby": hasHeader ? headerId : void 0,
            style: !contentExpanded ? { display: "none" } : void 0,
            "data-part": "content",
            children: [
              /* @__PURE__ */ jsxRuntime.jsx(
                "div",
                {
                  ref: contentRef,
                  className: cn(
                    noPadding ? "" : contentDesignClasses[resolvedDesign]
                  ),
                  children
                }
              ),
              footer && !isChild && /* @__PURE__ */ jsxRuntime.jsx(
                "div",
                {
                  className: cn(
                    "flex items-center gap-2",
                    footerDesignClasses[resolvedDesign]
                  ),
                  "data-part": "footer",
                  children: footer
                }
              )
            ]
          }
        )
      ]
    }
  );
}
var pathData28 = [
  "M505 94q7 7 7 18t-6 17L130 505q-7 7-18 7H26q-11 0-18.5-7.5T0 486v-86q1-10 6-16L382 7q7-7 18-7t18 7zm-55 18l-50-50-50 50 50 50zm-86 86l-50-50L62 400l50 50z"
];
var EditIcon = Object.assign(
  React45__namespace.default.forwardRef(
    (props, ref) => /* @__PURE__ */ jsxRuntime.jsx(Icon, { ref, pathData: pathData28, ...props })
  ),
  { displayName: "EditIcon", iconName: "edit" }
);
var pathData29 = [
  "M188.902 201.895l10.994 30.984q0 9.995-7.996 12.993l-14.992 7.996h-2q-.999 1-2.998 1-7.995 0-10.994-6.997l-14.992-25.987q-4.997 1-8.995 1.5t-8.996.5-14.992-2L96.95 249.87q-2.999 5.997-10.995 5.997-1.999 0-2.998-.5t-2.999-.5l-14.992-7.996q-6.996-3.997-6.996-10.994 0-3.998 1-4.997l9.994-26.986Q55.971 191.9 44.977 176.908l-26.986 3.998q-4.998 1-8.496-2t-5.497-7.995L0 153.92v-2q0-4.997 1.999-8.495t6.996-4.497l22.988-7.996v-2.999q0-9.994 2-18.99t4.997-17.99L21.989 74.96q-3.998-2.998-3.998-8.995 0-4.998 2.998-8.996l9.995-12.993q4.997-4.997 10.994-4.997 2 0 5.997 1.999l18.99 12.993q7.996-6.996 16.991-11.494t18.99-7.496l2-22.988Q106.943 0 117.938 0h16.99q4.998 0 8.496 3.498t3.498 8.496l2 21.988q10.994 2 20.489 6.997t17.49 10.994l18.99-12.993q3.999-2 5.998-2 5.997 0 10.994 4.998l9.995 12.993q2.998 3.998 2.998 8.996t-3.998 8.995l-15.991 14.992q7.995 18.99 7.995 39.98v.999l22.988 6.996q8.996 2.999 8.996 12.993v2l-3.998 16.99q-2 9.995-11.994 9.995h-1.999l-25.986-3.998q-9.995 17.991-22.988 27.986zM82.957 127.933q0 18.99 12.493 31.984t32.483 12.993 32.483-12.993 12.494-31.984q0-19.99-12.494-32.483t-32.483-12.493T95.45 95.45t-12.493 32.483zm417.782 233.879q12.993 3.997 10.994 16.99v2l-4.997 20.989q-2.998 12.993-15.992 12.993h-1.999l-31.983-4.997q-8.995 18.99-28.985 34.981l12.993 33.983q0 5.997-.999 11.494t-6.996 9.495l-18.99 9.995q-5.997 1.998-6.997 1.998-8.995 0-13.993-7.995l-18.99-33.983q-4.997 1-10.494 1.5t-11.494.5q-4.998 0-9.495-.5t-9.495-1.5l-19.99 33.983q-4.997 7.995-12.993 7.995-4.998 0-6.997-1.998l-18.99-8.996q-8.995-3.998-8.995-13.993 0-3.997 1-5.996l12.993-33.983q-19.99-14.992-29.985-33.982l-33.982 4.997h-1.999q-12.993 0-15.992-12.993L191.9 383.8v-1.999q0-11.994 10.994-16.991l28.985-8.995v-3.998q0-24.987 8.996-45.976l-21.989-19.99q-2.998-2.998-4.997-10.994 0-6.997 3.998-10.994l12.993-16.992q4.997-5.996 11.994-5.996 5.996 0 8.995 2.998l23.987 15.992q19.99-16.991 44.977-23.988l1.999-28.985q1.999-14.992 15.992-14.992h20.989q15.991 0 15.991 14.992l2 27.986q24.986 5.997 47.974 21.988l22.988-15.991q4.998-2.999 8.996-2.999 7.995 0 11.993 5.997l12.994 16.991q3.997 3.998 3.997 10.994 0 5.997-4.997 10.995l-19.99 19.99q4.998 10.994 7.497 22.987t2.498 25.987v1zm-217.886-9.995q0 28.985 19.99 48.974t48.974 19.99 48.974-19.99 19.99-48.974-19.99-48.975-48.974-19.99-48.975 19.99-19.99 48.975z"
];
var SettingsIcon = Object.assign(
  React45__namespace.default.forwardRef(
    (props, ref) => /* @__PURE__ */ jsxRuntime.jsx(Icon, { ref, pathData: pathData29, ...props })
  ),
  { displayName: "SettingsIcon", iconName: "settings" }
);
var pathData30 = [
  "M70 397q11 0 18.5 7t7.5 18-7.5 18.5T70 448H26q-11 0-18.5-7.5T0 422v-32q0-55 29-99.5t77-67.5l-4-5q-19-18-28.5-41T64 128q0-27 10.5-50.5t28-41 40.5-27T192 0q27 0 50 10.5t40.5 28T310 79t10 49-9.5 49-27 40.5-41 28T192 256h-6q-28 0-52.5 10.5t-43 28.5-29 42.5T51 390v7h19zM192 51q-32 0-54.5 22.5T115 128t22.5 54.5T192 205q33 0 55-23t22-54q0-32-22.5-54.5T192 51zm313 161q7 7 7 18t-7 18l-25 26q-8 8-19 8-10 0-18-8l-50-51q-7-7-7-18t7-18l25-26q7-7 18-7 12 0 19 7zm-75 77q7 9 7 18t-7 18L254 504q-8 8-18 8h-50q-11 0-18.5-7.5T160 486v-51q0-9 7-18l176-179q8-8 18-8 12 0 18 8zm-55 18l-14-14-150 153v15h14z"
];
var UserEditIcon = Object.assign(
  React45__namespace.default.forwardRef(
    (props, ref) => /* @__PURE__ */ jsxRuntime.jsx(Icon, { ref, pathData: pathData30, ...props })
  ),
  { displayName: "UserEditIcon", iconName: "user-edit" }
);
function FxUserMenuContent({
  selectedAccount,
  otherAccounts,
  showManageAccount,
  showOtherAccounts,
  showEditAccounts,
  showEditButton,
  avatarInteractive = true,
  children,
  titleRef,
  onAvatarClick,
  onManageAccountClick,
  onEditAccountsClick,
  onAccountSwitch,
  ref
}) {
  const { t } = reactI18next.useTranslation("fx");
  return /* @__PURE__ */ jsxRuntime.jsxs("div", { ref, className: "px-2 pb-2", children: [
    selectedAccount && /* @__PURE__ */ jsxRuntime.jsxs(
      "div",
      {
        className: "flex flex-col items-center mb-2",
        "aria-label": t("FX_CURRENT_ACCOUNT"),
        children: [
          /* @__PURE__ */ jsxRuntime.jsx("div", { className: "relative mt-1 mb-2", children: /* @__PURE__ */ jsxRuntime.jsx(
            Avatar,
            {
              size: "L" /* L */,
              image: selectedAccount.avatarSrc,
              initials: selectedAccount.avatarInitials,
              colorScheme: selectedAccount.avatarColorScheme || "Accent6" /* Accent6 */,
              icon: /* @__PURE__ */ jsxRuntime.jsx(PersonPlaceholderIcon, { className: "h-6 w-6" }),
              interactive: avatarInteractive,
              onClick: avatarInteractive ? () => onAvatarClick?.() : void 0,
              accessibleName: selectedAccount.titleText,
              badge: showEditButton ? /* @__PURE__ */ jsxRuntime.jsx(
                "span",
                {
                  className: "flex items-center justify-center h-5 w-5 rounded-full",
                  style: { backgroundColor: "var(--brand-background)", color: "var(--neutral-foreground-white)" },
                  children: /* @__PURE__ */ jsxRuntime.jsx(EditIcon, { className: "h-3 w-3" })
                }
              ) : void 0
            }
          ) }),
          selectedAccount.titleText && /* @__PURE__ */ jsxRuntime.jsx(
            "span",
            {
              ref: titleRef,
              className: "text-center text-base font-semibold text-foreground mt-1",
              children: selectedAccount.titleText
            }
          ),
          selectedAccount.subtitleText && /* @__PURE__ */ jsxRuntime.jsx("span", { className: "text-center text-sm text-sapphire-text-tertiary mb-0.5", children: selectedAccount.subtitleText }),
          selectedAccount.description && /* @__PURE__ */ jsxRuntime.jsx("span", { className: "text-center text-sm text-sapphire-text-tertiary", children: selectedAccount.description }),
          selectedAccount.additionalInfo && /* @__PURE__ */ jsxRuntime.jsx("span", { className: "text-center text-sm text-sapphire-text-tertiary mt-1", children: selectedAccount.additionalInfo }),
          showManageAccount && /* @__PURE__ */ jsxRuntime.jsx(
            Button,
            {
              design: "Tertiary" /* Tertiary */,
              icon: /* @__PURE__ */ jsxRuntime.jsx(SettingsIcon, { className: "h-4 w-4" }),
              className: "mt-3 font-semibold",
              onClick: () => onManageAccountClick?.(),
              children: "Manage Account"
            }
          )
        ]
      }
    ),
    showOtherAccounts && otherAccounts.length > 0 && /* @__PURE__ */ jsxRuntime.jsx(
      Panel,
      {
        headerText: `Other Accounts (${otherAccounts.length})`,
        headerLevel: "H4" /* H4 */,
        defaultCollapsed: true,
        noPadding: true,
        className: "mb-2",
        header: /* @__PURE__ */ jsxRuntime.jsxs("div", { className: "flex flex-1 items-center justify-between min-w-0", children: [
          /* @__PURE__ */ jsxRuntime.jsxs("span", { className: "text-sm font-semibold truncate", children: [
            "Other Accounts (",
            otherAccounts.length,
            ")"
          ] }),
          showEditAccounts && /* @__PURE__ */ jsxRuntime.jsx(
            Button,
            {
              design: "Tertiary" /* Tertiary */,
              icon: /* @__PURE__ */ jsxRuntime.jsx(UserEditIcon, { className: "h-4 w-4" }),
              iconOnly: true,
              accessibleName: t("FX_EDIT_ACCOUNTS"),
              tooltip: t("FX_EDIT_ACCOUNTS"),
              onClick: (detail) => {
                detail.originalEvent.stopPropagation();
                onEditAccountsClick?.();
              },
              className: "ml-2"
            }
          )
        ] }),
        children: /* @__PURE__ */ jsxRuntime.jsx(
          List2,
          {
            accessibleName: `Other Accounts (${otherAccounts.length})`,
            separators: "All" /* All */,
            onItemClick: ({ item }) => {
              const accountId = item.getAttribute("data-item-key");
              const account = otherAccounts.find((a) => a.id === accountId);
              if (account) {
                onAccountSwitch(account);
              }
            },
            children: otherAccounts.map((account) => /* @__PURE__ */ jsxRuntime.jsx(
              ListItemCustom,
              {
                itemKey: account.id,
                accessibleName: `${account.titleText} ${account.subtitleText || ""} ${account.description || ""}`,
                children: /* @__PURE__ */ jsxRuntime.jsxs("div", { className: "flex items-center gap-3 w-full", children: [
                  /* @__PURE__ */ jsxRuntime.jsx(
                    Avatar,
                    {
                      size: "S" /* S */,
                      image: account.avatarSrc,
                      initials: account.avatarInitials,
                      colorScheme: account.avatarColorScheme || "Accent6" /* Accent6 */,
                      icon: /* @__PURE__ */ jsxRuntime.jsx(PersonPlaceholderIcon, { className: "h-4 w-4" }),
                      accessibleName: account.titleText
                    }
                  ),
                  /* @__PURE__ */ jsxRuntime.jsxs("div", { className: "flex flex-col flex-1 min-w-0 gap-0.5", children: [
                    account.titleText && /* @__PURE__ */ jsxRuntime.jsx("span", { className: "text-sm font-semibold text-foreground truncate", children: account.titleText }),
                    account.subtitleText && /* @__PURE__ */ jsxRuntime.jsx(Label, { className: "text-xs truncate", children: account.subtitleText }),
                    account.description && /* @__PURE__ */ jsxRuntime.jsx(Label, { className: "text-xs truncate", children: account.description })
                  ] }),
                  account.selected && /* @__PURE__ */ jsxRuntime.jsx(AcceptIcon, { className: "h-4 w-4 shrink-0 text-sapphire-text-tertiary" }),
                  account.loading && /* @__PURE__ */ jsxRuntime.jsx("div", { className: "h-4 w-4 shrink-0 animate-spin rounded-full border-2 border-sapphire-text-tertiary border-t-transparent" })
                ] })
              },
              account.id
            ))
          }
        )
      }
    ),
    React45__namespace.default.Children.count(children) > 0 && /* @__PURE__ */ jsxRuntime.jsx(
      List2,
      {
        accessibleName: "Actions",
        separators: "None" /* None */,
        children
      }
    )
  ] });
}
var NavBackIcon2 = () => /* @__PURE__ */ jsxRuntime.jsx("svg", { xmlns: "http://www.w3.org/2000/svg", width: "20", height: "20", viewBox: "0 0 20 20", fill: "none", children: /* @__PURE__ */ jsxRuntime.jsx("path", { d: "M13.4939 4.04418C13.1385 3.66784 12.5459 3.65098 12.1694 4.00634L6.54443 9.31517C6.357 9.4921 6.25047 9.7386 6.25024 9.99633C6.25018 10.2543 6.35696 10.5015 6.54443 10.6787L12.1694 15.9936C12.5458 16.3491 13.1383 16.3321 13.4939 15.9558C13.8493 15.5795 13.8323 14.9869 13.4561 14.6313L8.55371 9.99633L13.4561 5.36864C13.8324 5.01321 13.8492 4.42064 13.4939 4.04418Z", fill: "currentColor" }) });
var animationsInjected = false;
var injectAnimations = () => {
  if (animationsInjected) return;
  animationsInjected = true;
  const style = document.createElement("style");
  style.id = "fx-user-menu-panel-animations";
  style.textContent = `
    @keyframes fxUmFadeIn {
      from { opacity: 0; }
      to { opacity: 1; }
    }
    @keyframes fxUmFadeOut {
      from { opacity: 1; }
      to { opacity: 0; }
    }
    @keyframes fxUmSlideInFromLeft {
      from { transform: translateX(-100%); }
      to { transform: translateX(0); }
    }
    @keyframes fxUmSlideOutToLeft {
      from { transform: translateX(0); }
      to { transform: translateX(-100%); }
    }
  `;
  document.head.appendChild(style);
};
var ANIMATION_DURATION = 300;
function FxUserMenuPanel({
  open,
  children,
  footer,
  accessibleName,
  onClose,
  onBackClick,
  ref
}) {
  const [visible, setVisible] = React45.useState(false);
  const [closing, setClosing] = React45.useState(false);
  const backButtonRef = React45.useRef(null);
  const previousFocusRef = React45.useRef(null);
  const closeTimerRef = React45.useRef(null);
  const visibleRef = React45.useRef(false);
  React45.useEffect(() => {
    injectAnimations();
  }, []);
  React45.useEffect(() => {
    visibleRef.current = visible;
  }, [visible]);
  React45.useEffect(() => {
    if (closeTimerRef.current) {
      clearTimeout(closeTimerRef.current);
      closeTimerRef.current = null;
    }
    if (open) {
      previousFocusRef.current = document.activeElement;
      setVisible(true);
      setClosing(false);
    } else if (visibleRef.current) {
      setClosing(true);
      closeTimerRef.current = setTimeout(() => {
        setVisible(false);
        setClosing(false);
        closeTimerRef.current = null;
      }, ANIMATION_DURATION);
    }
    return () => {
      if (closeTimerRef.current) {
        clearTimeout(closeTimerRef.current);
        closeTimerRef.current = null;
      }
    };
  }, [open]);
  React45.useEffect(() => {
    if (visible && !closing) {
      requestAnimationFrame(() => {
        backButtonRef.current?.focus();
      });
    }
  }, [visible, closing]);
  React45.useEffect(() => {
    if (!visible && previousFocusRef.current) {
      previousFocusRef.current?.focus();
      previousFocusRef.current = null;
    }
  }, [visible]);
  const startClose = React45.useCallback(
    (callback) => {
      if (closing) return;
      setClosing(true);
      setTimeout(() => {
        setVisible(false);
        setClosing(false);
        onClose();
        callback?.();
      }, ANIMATION_DURATION);
    },
    [closing, onClose]
  );
  const handleBackdropClick = React45.useCallback(() => {
    startClose();
  }, [startClose]);
  const handleBackClick = React45.useCallback(() => {
    startClose(onBackClick);
  }, [startClose, onBackClick]);
  React45.useEffect(() => {
    if (!visible) return;
    const handleKeyDown = (e) => {
      if (e.key === "Escape") {
        e.stopPropagation();
        startClose();
      }
    };
    document.addEventListener("keydown", handleKeyDown);
    return () => document.removeEventListener("keydown", handleKeyDown);
  }, [visible, startClose]);
  React45.useEffect(() => {
    if (visible) {
      const prevOverflow = document.body.style.overflow;
      document.body.style.overflow = "hidden";
      return () => {
        document.body.style.overflow = prevOverflow;
      };
    }
  }, [visible]);
  if (!visible) return null;
  return reactDom.createPortal(
    /* @__PURE__ */ jsxRuntime.jsxs(
      "div",
      {
        ref,
        className: "fixed inset-0 z-50 flex",
        role: "dialog",
        "aria-modal": "true",
        "aria-label": accessibleName || "User Menu",
        children: [
          /* @__PURE__ */ jsxRuntime.jsx(
            "div",
            {
              className: "absolute inset-0 bg-black/50",
              style: {
                animation: closing ? `fxUmFadeOut ${ANIMATION_DURATION}ms cubic-bezier(0.4, 0, 0.2, 1) forwards` : `fxUmFadeIn ${ANIMATION_DURATION}ms cubic-bezier(0.4, 0, 0.2, 1)`
              },
              onClick: handleBackdropClick
            }
          ),
          /* @__PURE__ */ jsxRuntime.jsxs(
            "div",
            {
              className: "relative h-full flex flex-col shadow-xl rounded-r-2xl",
              style: {
                minWidth: "20rem",
                maxWidth: "calc(100vw - 2rem)",
                backgroundColor: "var(--card-bg-primary)",
                borderRight: "1px solid var(--border-primary)",
                animation: closing ? `fxUmSlideOutToLeft ${ANIMATION_DURATION}ms cubic-bezier(0.4, 0, 0.2, 1) forwards` : `fxUmSlideInFromLeft ${ANIMATION_DURATION}ms cubic-bezier(0.4, 0, 0.2, 1)`
              },
              onClick: (e) => e.stopPropagation(),
              children: [
                /* @__PURE__ */ jsxRuntime.jsxs(
                  "div",
                  {
                    className: "flex items-center h-12 px-2 shrink-0",
                    style: { borderBottom: "1px solid var(--border-primary)" },
                    children: [
                      /* @__PURE__ */ jsxRuntime.jsx(
                        Button,
                        {
                          ref: backButtonRef,
                          design: "SecondaryNeutral",
                          size: "Large",
                          iconOnly: true,
                          icon: /* @__PURE__ */ jsxRuntime.jsx(NavBackIcon2, {}),
                          accessibleName: "Back",
                          onClick: handleBackClick
                        }
                      ),
                      /* @__PURE__ */ jsxRuntime.jsx(Title, { level: "H4" /* H4 */, className: "flex-1 text-center pr-8", children: "Profile" })
                    ]
                  }
                ),
                /* @__PURE__ */ jsxRuntime.jsx(
                  "div",
                  {
                    className: "flex-1 overflow-y-auto overflow-x-hidden",
                    style: { overscrollBehavior: "contain" },
                    children
                  }
                ),
                /* @__PURE__ */ jsxRuntime.jsx(
                  "div",
                  {
                    className: "shrink-0 px-3 py-2",
                    style: { borderTop: "1px solid var(--border-primary)" },
                    children: footer
                  }
                )
              ]
            }
          )
        ]
      }
    ),
    document.body
  );
}
var FxUserMenuContext = React45.createContext({
  closeMenu: () => {
  }
});
var FxUserMenuItemGroupContext = React45.createContext(null);
function FxUserMenu({
  open = false,
  opener,
  useDialog = false,
  showManageAccount = false,
  showOtherAccounts = false,
  showEditAccounts = false,
  showEditButton = false,
  avatarInteractive = true,
  accounts = [],
  children,
  footer,
  onAvatarClick,
  onManageAccountClick,
  onEditAccountsClick,
  onSignOutClick,
  onOpen,
  onClose,
  onBackClick,
  onChangeAccount,
  onItemClick,
  placement = "Bottom",
  horizontalAlign = "End",
  verticalAlign,
  offset,
  className,
  accessibleName,
  ref
}) {
  const popoverRef = React45.useRef(null);
  const { t } = reactI18next.useTranslation("fx");
  const anchorRef = React45.useRef(null);
  const titleRef = React45.useRef(null);
  const [titleMovedToHeader, setTitleMovedToHeader] = React45.useState(false);
  const [isScrolled, setIsScrolled] = React45.useState(false);
  const [popoverReady, setPopoverReady] = React45.useState(false);
  const selectedAccount = accounts.find((a) => a.selected) || accounts[0];
  const otherAccounts = accounts.filter((a) => a !== selectedAccount);
  React45.useImperativeHandle(ref, () => ({
    close: () => onClose?.(),
    isOpen: () => open
  }));
  const closeMenu = React45.useCallback(() => {
    onClose?.();
  }, [onClose]);
  const handleOpen = React45.useCallback(() => {
    setPopoverReady(true);
    onOpen?.();
  }, [onOpen]);
  const handleClose = React45.useCallback(() => {
    setPopoverReady(false);
    setTitleMovedToHeader(false);
    setIsScrolled(false);
    onClose?.();
  }, [onClose]);
  React45.useEffect(() => {
    if (useDialog || !popoverReady) return;
    const scrollContainer = anchorRef.current?.parentElement;
    if (!scrollContainer || !titleRef.current) return;
    const handleScroll = () => {
      setIsScrolled(scrollContainer.scrollTop > 0);
    };
    scrollContainer.addEventListener("scroll", handleScroll);
    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.target === titleRef.current) {
            setTitleMovedToHeader(!entry.isIntersecting);
          }
        });
      },
      {
        root: scrollContainer,
        threshold: [0.15]
      }
    );
    observer.observe(titleRef.current);
    return () => {
      scrollContainer.removeEventListener("scroll", handleScroll);
      observer.disconnect();
    };
  }, [useDialog, popoverReady, selectedAccount]);
  const handleAccountSwitch = React45.useCallback(
    (account) => {
      if (!selectedAccount || account.id === selectedAccount.id) return;
      const result = onChangeAccount?.({
        prevSelectedAccount: selectedAccount,
        selectedAccount: account
      });
      if (result === false) return;
    },
    [selectedAccount, onChangeAccount]
  );
  const handleSignOut = React45.useCallback(() => {
    const result = onSignOutClick?.();
    if (result === false) return;
    closeMenu();
  }, [onSignOutClick, closeMenu]);
  const handleItemClick = React45.useCallback(
    (detail) => {
      const result = onItemClick?.(detail);
      if (result === false) return;
      closeMenu();
    },
    [onItemClick, closeMenu]
  );
  const hasCustomFooter = footer !== void 0 && footer !== null;
  const accessibleNameText = accessibleName || (selectedAccount ? `User Menu ${selectedAccount.titleText}` : "User Menu");
  const footerContent = hasCustomFooter ? /* @__PURE__ */ jsxRuntime.jsx("div", { className: "flex justify-end items-center w-full", children: footer }) : /* @__PURE__ */ jsxRuntime.jsx("div", { className: "flex justify-end items-center w-full", children: /* @__PURE__ */ jsxRuntime.jsx(
    Button,
    {
      design: "Tertiary" /* Tertiary */,
      icon: /* @__PURE__ */ jsxRuntime.jsx(lucideReact.LogOut, { className: "h-4 w-4" }),
      className: "font-semibold",
      onClick: handleSignOut,
      children: t("SIGN_OUT")
    }
  ) });
  const menuContent = /* @__PURE__ */ jsxRuntime.jsx(
    FxUserMenuContent,
    {
      selectedAccount,
      otherAccounts,
      showManageAccount,
      showOtherAccounts,
      showEditAccounts,
      showEditButton,
      avatarInteractive,
      titleRef,
      onAvatarClick,
      onManageAccountClick,
      onEditAccountsClick,
      onAccountSwitch: handleAccountSwitch,
      children
    }
  );
  if (useDialog) {
    return /* @__PURE__ */ jsxRuntime.jsx(FxUserMenuContext.Provider, { value: { closeMenu, onItemClick: handleItemClick }, children: /* @__PURE__ */ jsxRuntime.jsx(
      FxUserMenuPanel,
      {
        open,
        footer: footerContent,
        accessibleName: accessibleNameText,
        onClose: closeMenu,
        onBackClick,
        children: menuContent
      }
    ) });
  }
  const headerContent = /* @__PURE__ */ jsxRuntime.jsx(
    "div",
    {
      className: cn(
        "flex items-center px-3 min-h-[2.75rem]",
        isScrolled || titleMovedToHeader ? "border-b border-border shadow-sm" : ""
      ),
      children: /* @__PURE__ */ jsxRuntime.jsx("div", { className: "flex-1 min-w-0", children: titleMovedToHeader && selectedAccount && /* @__PURE__ */ jsxRuntime.jsx(Title, { level: "H4" /* H4 */, className: "truncate", children: selectedAccount.titleText }) })
    }
  );
  return /* @__PURE__ */ jsxRuntime.jsx(FxUserMenuContext.Provider, { value: { closeMenu, onItemClick: handleItemClick }, children: /* @__PURE__ */ jsxRuntime.jsxs(
    ResponsivePopover,
    {
      ref: popoverRef,
      opener,
      open,
      placement,
      horizontalAlign,
      verticalAlign,
      offset,
      noPadding: true,
      accessibleName: accessibleNameText,
      header: headerContent,
      footer: footerContent,
      onOpen: handleOpen,
      onClose: handleClose,
      showCloseButton: true,
      className: cn("w-80", className),
      children: [
        /* @__PURE__ */ jsxRuntime.jsx("div", { ref: anchorRef, className: "contents" }),
        menuContent
      ]
    }
  ) });
}
function FxUserMenuItem({
  text,
  icon,
  checked,
  disabled = false,
  children: _children,
  onClick,
  className,
  ref
}) {
  const { onItemClick } = React45.useContext(FxUserMenuContext);
  const groupContext = React45.useContext(FxUserMenuItemGroupContext);
  const itemId = React45.useRef(Math.random().toString(36).slice(2)).current;
  const isChecked = groupContext ? groupContext.checkedItems.has(itemId) : checked;
  const handleClick2 = React45.useCallback(
    (e) => {
      if (disabled) return;
      if (groupContext && groupContext.checkMode !== "None") {
        groupContext.onCheck(itemId);
      }
      if (onClick) {
        onClick(e);
      }
      const result = onItemClick?.({
        item: e.currentTarget,
        text: text || ""
      });
      if (result === false) return;
    },
    [disabled, onClick, onItemClick, text, groupContext, itemId]
  );
  return /* @__PURE__ */ jsxRuntime.jsx(
    ListItem,
    {
      ref,
      itemKey: itemId,
      text,
      icon,
      disabled,
      additionalText: isChecked ? "\u2713" : void 0,
      onClick: handleClick2,
      className: cn(
        "rounded-md",
        className
      )
    }
  );
}
function FxUserMenuItemGroup({
  checkMode = "None",
  children,
  className,
  ref
}) {
  const [checkedItems, setCheckedItems] = React45.useState(/* @__PURE__ */ new Set());
  const handleCheck = React45.useCallback(
    (itemId) => {
      setCheckedItems((prev) => {
        const next = new Set(prev);
        if (checkMode === "Single") {
          next.clear();
          next.add(itemId);
        } else if (checkMode === "Multiple") {
          if (next.has(itemId)) {
            next.delete(itemId);
          } else {
            next.add(itemId);
          }
        }
        return next;
      });
    },
    [checkMode]
  );
  return /* @__PURE__ */ jsxRuntime.jsx(
    FxUserMenuItemGroupContext.Provider,
    {
      value: {
        checkMode,
        checkedItems,
        onCheck: handleCheck
      },
      children: /* @__PURE__ */ jsxRuntime.jsx(
        "div",
        {
          ref,
          role: "group",
          className: cn("py-0.5", className),
          children
        }
      )
    }
  );
}

exports.BellIcon = BellIcon;
exports.CloseCommandFieldIcon = CloseCommandFieldIcon;
exports.CodeIcon = CodeIcon;
exports.ConversationsIcon = ConversationsIcon;
exports.CursorIcon = CursorIcon;
exports.DiscoverIcon = DiscoverIcon;
exports.FxLayout = FxLayout;
exports.FxLayoutContext = FxLayoutContext;
exports.FxPaneHeader = FxPaneHeader;
exports.FxPaneHeaderAction = FxPaneHeaderAction;
exports.FxPaneHeaderSegmentedAction = FxPaneHeaderSegmentedAction;
exports.FxPaneHeaderSegmentedOption = FxPaneHeaderSegmentedOption;
exports.FxPaneHeaderSplitAction = FxPaneHeaderSplitAction;
exports.FxPaneHeaderSplitOption = FxPaneHeaderSplitOption;
exports.FxPromptInput = FxPromptInput;
exports.FxUserMenu = FxUserMenu;
exports.FxUserMenuItem = FxUserMenuItem;
exports.FxUserMenuItemGroup = FxUserMenuItemGroup;
exports.JobsIcon = JobsIcon;
exports.JouleIcon = JouleIcon;
exports.MenuIcon = MenuIcon;
exports.OpenCommandFieldIcon = OpenCommandFieldIcon;
exports.PanelCloseIcon = PanelCloseIcon;
exports.PanelOpenIcon = PanelOpenIcon;
exports.SpacesIcon = SpacesIcon;
exports.VSCodeIcon = VSCodeIcon;
exports.WaveformIcon = WaveformIcon;
exports.useFxLayoutContext = useFxLayoutContext;
//# sourceMappingURL=index.cjs.map
//# sourceMappingURL=index.cjs.map