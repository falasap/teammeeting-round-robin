/**
 * Shared utilities for ComboBox and MultiComboBox components.
 *
 * Extracts common CVA variants, filter logic, value state rendering,
 * shared hooks, and action buttons to avoid code duplication.
 */
import React from "react";
import { ComboBoxFilter, ComboBoxSize, ValueState } from "../../types/combobox";
/**
 * Value-state-aware input container variants (shared by ComboBox and MultiComboBox).
 *
 * Each consumer passes its own base classes to customize padding/height,
 * then merges with the value state / disabled / readonly variants returned here.
 */
export declare const valueStateInputVariants: (props?: ({
    size?: boolean | ComboBoxSize | null | undefined;
    valueState?: boolean | ValueState | null | undefined;
    disabled?: boolean | "true" | "false" | null | undefined;
    readonly?: boolean | "true" | "false" | null | undefined;
} & {
    class?: import("../../lib/utils").ClassValue;
    className?: import("../../lib/utils").ClassValue;
}) | undefined) => string;
/**
 * Filter items based on filter mode.
 */
export declare function filterItems(text: string, filterValue: string, filterMode: ComboBoxFilter | `${ComboBoxFilter}`): boolean;
/**
 * Render the value state message — portaled into popover when open, inline when closed.
 */
export declare function ValueStateMessage({ isOpen, hasValueState, portalTarget, messageHeight, messageRef, messageId, normalizedValueState, valueStateMessage, }: {
    isOpen: boolean;
    hasValueState: boolean;
    portalTarget: HTMLDivElement | null;
    messageHeight: number;
    messageRef: React.RefObject<HTMLDivElement | null>;
    messageId: string;
    normalizedValueState: ValueState;
    valueStateMessage: React.ReactNode;
}): import("react/jsx-runtime").JSX.Element;
/**
 * Clear icon button (shared by ComboBox and MultiComboBox).
 */
export declare function ClearButton({ onClick, accessibleName, comboBoxSize }: {
    onClick: () => void;
    accessibleName?: string;
    comboBoxSize?: ComboBoxSize | `${ComboBoxSize}`;
}): import("react/jsx-runtime").JSX.Element;
/**
 * Dropdown arrow button (shared by ComboBox and MultiComboBox).
 */
export declare function DropdownArrowButton({ onClick, disabled, comboBoxSize }: {
    onClick: () => void;
    disabled?: boolean;
    comboBoxSize?: ComboBoxSize | `${ComboBoxSize}`;
}): import("react/jsx-runtime").JSX.Element;
/** A flattened item with its React element and optional group index. */
export interface FlattenedItem<P> {
    element: React.ReactElement<P>;
    groupIndex?: number;
}
/** A flattened item enriched with visibility and original index. */
export interface VisibleItem<P> extends FlattenedItem<P> {
    originalIndex: number;
    isVisible: boolean;
}
/**
 * Compute visible items from a flattened list, applying the filter.
 * Both ComboBox and MultiComboBox use the same shape.
 */
export declare function computeVisibleItems<P extends {
    disabled?: boolean;
}>(flattenedItems: FlattenedItem<P>[], filterValue: string, filter: ComboBoxFilter | `${ComboBoxFilter}`, getDisplayText: (element: React.ReactElement<P>) => string): VisibleItem<P>[];
/**
 * Filter navigable (visible + not disabled) items from a visible items list.
 */
export declare function computeNavigableItems<P extends {
    disabled?: boolean;
}>(visibleItems: VisibleItem<P>[]): VisibleItem<P>[];
/**
 * Hook for screen reader result-count announcements (debounced 300ms).
 * Used identically by ComboBox and MultiComboBox.
 */
export declare function useResultAnnouncement(filterValue: string, filteredCount: number): {
    resultAnnouncement: string;
    t: import("i18next").TFunction<"fx", undefined>;
};
/**
 * Hook for measuring the value-state message height (for portal placeholder).
 */
export declare function useMessageHeight(messageRef: React.RefObject<HTMLDivElement | null>, valueStateMessage: React.ReactNode): number;
/**
 * Hook for popover open/close/toggle state management.
 * Returns { openPopover, closePopover, handlePopoverClose, togglePopover }.
 */
export declare function usePopoverControls({ disabled, readonly, controlledOpen, isOpen, setInternalOpen, setFocusedIndex, onOpen, onClose, }: {
    disabled: boolean;
    readonly: boolean;
    controlledOpen: boolean | undefined;
    isOpen: boolean;
    setInternalOpen: React.Dispatch<React.SetStateAction<boolean>>;
    setFocusedIndex: React.Dispatch<React.SetStateAction<number>>;
    onOpen?: () => void;
    onClose?: () => void;
}): {
    openPopover: () => void;
    closePopover: () => void;
    handlePopoverClose: () => void;
    togglePopover: () => void;
};
//# sourceMappingURL=combobox-shared.d.ts.map