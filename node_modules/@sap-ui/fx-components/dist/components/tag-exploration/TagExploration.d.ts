import { TagExplorationProps } from "../../types/tag-exploration";
/**
 * Tag component
 *
 * A categorization label with optional close/remove functionality.
 * Supports semantic designs, color schemes, interactive mode, and a close button.
 *
 * @example
 * ```tsx
 * // Basic tag
 * <Tag design="Information">Draft</Tag>
 *
 * // With close button
 * <Tag design="Positive" onClose={(e) => handleRemove(e)}>Approved</Tag>
 *
 * // Interactive tag
 * <Tag interactive onClick={(e) => handleClick(e)}>Clickable</Tag>
 *
 * // Color scheme
 * <Tag design="Set2" colorScheme="1">Category</Tag>
 * ```
 */
export declare function TagExploration({ design, size, colorScheme, children, icon, interactive, readOnly, accent, wrappingType, hideStateIcon, onClose, onClick, accessibleName, accessibleNameRef, className, style, id, "data-testid": dataTestId, ref, }: TagExplorationProps): import("react/jsx-runtime").JSX.Element;
//# sourceMappingURL=TagExploration.d.ts.map