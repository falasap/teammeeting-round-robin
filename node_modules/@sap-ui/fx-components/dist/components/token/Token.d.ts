import type { TokenInternalProps } from "../../types/token";
/**
 * Token component
 *
 * A small interactive chip representing a selected value.
 * Typically used inside a Tokenizer or MultiInput.
 * Supports selection, deletion via close icon or keyboard, and read-only mode.
 *
 * @example
 * ```tsx
 * <Token text="React" />
 * <Token text="Selected" selected />
 * <Token text="Readonly" readonly />
 * <Token text="Closable" onDelete={(e) => handleDelete(e)} />
 * ```
 */
export declare function Token({ text, closeIcon, selected, readonly, singleToken, overflows, onSelect, onDelete, className, style, id, "data-testid": dataTestId, tabIndex, ref, }: TokenInternalProps): import("react/jsx-runtime").JSX.Element;
//# sourceMappingURL=Token.d.ts.map