import React from "react";
import { InputProps } from "../../types/input";
/**
 * Input component
 *
 * A native React implementation of the UI5 Input with full enterprise features:
 * - All standard input types (text, email, number, password, tel, url, search)
 * - Value states with semantic feedback
 * - Icons and clear button
 * - Full accessibility support
 * - Controlled and uncontrolled modes
 * - Form integration
 * - AI-ready imperative API
 *
 * @example
 * ```tsx
 * // Basic usage
 * <Input placeholder="Enter your name" />
 *
 * // With validation
 * <Input
 *   type="Email"
 *   valueState="Negative"
 *   valueStateMessage="Please enter a valid email"
 * />
 *
 * // Controlled
 * <Input value={email} onChange={(d) => setEmail(d.value)} />
 * ```
 */
declare function InputBase({ value: controlledValue, defaultValue, type, size, disabled, readonly, required, autocomplete, valueState, valueStateMessage, showValueStateIcon, maxLength, minLength, pattern, min, max, step, placeholder, showClearIcon, hideStepButtons, icon, iconPosition, accessibleName, accessibleNameRef, accessibleDescriptionRef, name, id: providedId, onInput, onChange, onFocus, onBlur, onKeyDown, onKeyUp, className, style, "data-testid": dataTestId, "data-ai-field": dataAiField, accessibilityAttributes, ref, }: InputProps): import("react/jsx-runtime").JSX.Element;
export declare const Input: React.MemoExoticComponent<typeof InputBase>;
export {};
//# sourceMappingURL=Input.d.ts.map