import { type Ref } from "react";
interface MonthPickerProps {
    /** Currently displayed year */
    currentYear: number;
    /** Currently focused month (0-11) */
    focusedMonth: number;
    /** Selected month (if any) */
    selectedMonth?: number;
    /** Callback when a month is selected */
    onSelectMonth: (month: number) => void;
    /** Callback when focus changes */
    onFocusChange: (month: number) => void;
    /** Callback to navigate to previous year (PageUp) */
    onPreviousYear?: () => void;
    /** Callback to navigate to next year (PageDown) */
    onNextYear?: () => void;
    /** Locale code (e.g., 'en-US', 'ja-JP') */
    locale?: string;
    /** Additional CSS classes */
    className?: string;
}
export declare function MonthPicker({ currentYear, focusedMonth, selectedMonth, onSelectMonth, onFocusChange, onPreviousYear, onNextYear, locale, className, ref, }: MonthPickerProps & {
    ref?: Ref<HTMLDivElement>;
}): import("react/jsx-runtime").JSX.Element;
export {};
//# sourceMappingURL=MonthPicker.d.ts.map