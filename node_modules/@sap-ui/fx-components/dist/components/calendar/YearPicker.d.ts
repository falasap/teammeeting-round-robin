import { type Ref } from "react";
interface YearPickerProps {
    /** Start year of the range */
    startYear: number;
    /** End year of the range */
    endYear: number;
    /** Currently focused year */
    focusedYear: number;
    /** Selected year (if any) */
    selectedYear?: number;
    /** Callback when a year is selected */
    onSelectYear: (year: number) => void;
    /** Callback when focus changes */
    onFocusChange: (year: number) => void;
    /** Callback to navigate to previous year range */
    onPreviousRange: () => void;
    /** Callback to navigate to next year range */
    onNextRange: () => void;
    /** Additional CSS classes */
    className?: string;
}
export declare function YearPicker({ startYear, endYear, focusedYear, selectedYear, onSelectYear, onFocusChange, onPreviousRange, onNextRange, className, ref, }: YearPickerProps & {
    ref?: Ref<HTMLDivElement>;
}): import("react/jsx-runtime").JSX.Element;
export {};
//# sourceMappingURL=YearPicker.d.ts.map