import React from "react";
import { MenuProps, MenuItemProps, MenuSeparatorProps, MenuHeaderProps, MenuItemGroupProps } from "../../types/menu";
/**
 * Menu component
 *
 * A dropdown menu that appears anchored to an opener element.
 * Uses Popover for positioning, Escape handling, click-outside, and focus management.
 * Supports nested submenus via MenuItem children.
 *
 * @example
 * ```tsx
 * const [open, setOpen] = useState(false);
 * const buttonRef = useRef<HTMLButtonElement>(null);
 *
 * <Button ref={buttonRef} onClick={() => setOpen(true)}>Open Menu</Button>
 * <Menu open={open} opener={buttonRef.current} onClose={() => setOpen(false)}>
 *   <MenuItem text="Edit" icon={<Edit />} onClick={() => console.log("edit")} />
 *   <MenuItem text="More Options" icon={<Settings />}>
 *     <MenuItem text="Option A" />
 *     <MenuItem text="Option B" />
 *   </MenuItem>
 *   <MenuSeparator />
 *   <MenuItem text="Settings" />
 * </Menu>
 * ```
 */
export declare function Menu({ open, opener, children, placement, onClose, onItemClick, onCheck, className, style, headerText, loading, loadingDelay, onBeforeOpen, onOpen, onBeforeClose, ref, }: MenuProps): import("react/jsx-runtime").JSX.Element;
/**
 * MenuItem component
 *
 * Supports submenus when children are provided. Children that are MenuItems
 * will render as a submenu that opens on hover.
 */
export declare function MenuItem({ text, icon, endContent, disabled, startsSection, children, onClick, className, data, checked, additionalText, loading: itemLoading, loadingDelay, tooltip, accessibleName, accessibilityAttributes, ref, }: MenuItemProps & {
    ref?: React.Ref<HTMLDivElement>;
}): import("react/jsx-runtime").JSX.Element;
export declare namespace MenuItem {
    var displayName: string;
}
/**
 * MenuItemGroup component
 *
 * Groups MenuItems with optional check mode (Single/Multiple).
 */
export declare function MenuItemGroup({ checkMode, children, accessibleName, className, ref }: MenuItemGroupProps & {
    ref?: React.Ref<HTMLDivElement>;
}): import("react/jsx-runtime").JSX.Element;
export declare namespace MenuItemGroup {
    var displayName: string;
}
/**
 * MenuSeparator component
 */
export declare function MenuSeparator({ className, ref }: MenuSeparatorProps & {
    ref?: React.Ref<HTMLDivElement>;
}): import("react/jsx-runtime").JSX.Element;
export declare namespace MenuSeparator {
    var displayName: string;
}
/**
 * MenuHeader component
 */
export declare function MenuHeader({ text, children, endContent, className, ref }: MenuHeaderProps & {
    ref?: React.Ref<HTMLDivElement>;
}): import("react/jsx-runtime").JSX.Element;
export declare namespace MenuHeader {
    var displayName: string;
}
//# sourceMappingURL=Menu.d.ts.map