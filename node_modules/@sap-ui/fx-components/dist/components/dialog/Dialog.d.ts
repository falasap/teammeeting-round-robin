import { type DialogProps } from "../../types/dialog";
import "./Dialog.css";
/**
 * Dialog component - Native <dialog> element with Popover API
 *
 * Uses native <dialog> with popover attribute for:
 * - Native dialog semantics and accessibility
 * - Popover API top-layer rendering
 * - ::backdrop pseudo-element
 * - Light dismiss behavior
 * - Better performance
 *
 * @example
 * ```tsx
 * <Dialog
 *   open={isOpen}
 *   onOpenChange={setIsOpen}
 *   headerText="Confirm Action"
 *   footer={
 *     <>
 *       <Button onClick={handleConfirm}>Confirm</Button>
 *       <Button variant="ghost" onClick={() => setIsOpen(false)}>Cancel</Button>
 *     </>
 *   }
 * >
 *   Are you sure you want to proceed?
 * </Dialog>
 * ```
 */
export declare function Dialog({ className, open: controlledOpen, defaultOpen, headerText, subHeaderText, headerEndContent, showBackButton, onBackButtonClick, hideHeaderBorder, header, children, footer, state, stretch, draggable, resizable, preventInitialFocus, enableBackdropClick, noPadding, initialFocus, accessibleName, accessibleNameRef, accessibleDescribedBy, accessibleRole, ariaRoleDescription, onBeforeOpen, onAfterOpen, onBeforeClose, onAfterClose, onEscapePress, onOpenChange, id, ref, }: DialogProps): import("react/jsx-runtime").JSX.Element;
//# sourceMappingURL=Dialog.d.ts.map