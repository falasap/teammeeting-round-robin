import { ToggleButtonProps } from "../../types/toggle-button";
import "../button/Button.css";
/**
 * ToggleButton component
 *
 * A button that toggles between pressed and unpressed states.
 *
 * @example
 * ```tsx
 * const [pressed, setPressed] = useState(false);
 *
 * <ToggleButton
 *   pressed={pressed}
 *   onChange={({ pressed }) => setPressed(pressed)}
 *   icon={<Star />}
 * >
 *   Favorite
 * </ToggleButton>
 * ```
 */
export declare function ToggleButton({ pressed: controlledPressed, design, size, icon, endIcon, children, disabled, iconOnly, tooltip, accessibleName, onChange, onClick, tabIndex, className, style, ref, }: ToggleButtonProps): import("react/jsx-runtime").JSX.Element;
//# sourceMappingURL=ToggleButton.d.ts.map