import type { ThemeContextValue, ThemeProviderProps } from "./types";
/**
 * Provider component for theme management.
 *
 * Manages theme state, applies CSS classes to `<html>`,
 * and detects system color scheme preference.
 * Theme selection is persisted to localStorage.
 *
 * @example
 * ```tsx
 * const THEMES = [
 *   { id: "light", name: "Light" },
 *   { id: "dark", name: "Dark" },
 *   { id: "ocean", name: "Ocean Deep" },
 * ];
 *
 * <ThemeProvider themes={THEMES}>
 *   <App />
 * </ThemeProvider>
 * ```
 */
export declare function ThemeProvider({ themes, defaultTheme, children, }: ThemeProviderProps): import("react/jsx-runtime").JSX.Element;
export declare namespace ThemeProvider {
    var displayName: string;
}
/**
 * Hook to access theme state and controls.
 *
 * @example
 * ```tsx
 * function ThemeToggle() {
 *   const { theme, resolvedTheme, setTheme } = useTheme();
 *   return (
 *     <button onClick={() => setTheme(resolvedTheme === "dark" ? "light" : "dark")}>
 *       Currently: {resolvedTheme}
 *     </button>
 *   );
 * }
 * ```
 */
export declare function useTheme(): ThemeContextValue;
//# sourceMappingURL=ThemeProvider.d.ts.map